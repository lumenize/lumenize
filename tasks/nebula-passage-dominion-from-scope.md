# Passage and dominion, computed from the scope

**Status:** Active child, **NEXT** — first of the three remaining in the passage/dominion sequence. ✅ [nebula-dominion-vocabulary-rename.md](archive/nebula-dominion-vocabulary-rename.md) **BUILT + archived 2026-08-11**, so its renames are in the tree; before [nebula-registry-route-guards.md](nebula-registry-route-guards.md) and [nebula-invite.md](nebula-invite.md). This is the **spine**: every file after it reads `access.authScope`. Not built, except the `isAdmin` → `scopeAdmin` rename, which landed 2026-08-07.

> ⏸️ **PAUSED MID-REVIEW — this file is NOT ready to build.** Stage 1 ran twice and is applied. Stage 2 ran; **M2 was REJECTED** (it read universal passage to the platform root as a hole — that is the model working, and `verify.ts:44` refuses a principal-less token before either verdict runs), and **M4, M6 and M3 are applied**. Two remain, both needing a call rather than a transcription:
> - **M1 — a moved verdict inside the verdict-preserving phase.** `worker-token.ts`'s `authScope: body.activeScope` conversion drops the **only parse** `body.activeScope` ever receives on the mint path, and at star tier `matchAccess` was **exact** where `isAtOrAbove` is not — so the conversion widens. ⇒ Phase 1 owes either the retained parse or an exactness note, pinned as a row in its table rather than left to the builder.
> - **M5 — a criterion that cannot be satisfied.** 🔒 *The mint's verdicts are unchanged* asserts `mint-narrower-token.test.ts` passes with only the claim renamed, but that test **imports `matchAccess`**, which Phase 1 deletes. ⚠️ The tempting repair — teaching `isAtOrAbove` the `.*` grammar — would revert this task inside the security predicate. ⇒ Restate the criterion over the four verdicts rather than over that file passing, and size the `.*` fixture sweep with a command.
>
> ✅ **The Registry-endpoint detour is COMPLETE** (2026-08-13). `docs/vision/auth.md` gained the R1–R7 route sequence, and the runner it described is **built and archived** — [nebula-route-pipeline.md](archive/nebula-route-pipeline.md), code at `packages/nebula-auth/src/route-pipeline.ts`. **B1 was re-derived against the rewritten doc and is applied below**, in § *Acceptance criteria*.

> 📐 **`/write-task` Pass 2 complete; the surviving prose has had both review stages.** Re-split 2026-08-11 and condensed 2026-08-12.

> ✅ **Not wipe-gated, and not invite-gated.** `authScopePattern` appears nowhere in `schemas.ts`, the KV refresh record stores `universeGalaxyStarId`, and the pattern is re-derived at every token issuance and persisted nowhere. The one wipe-gated column lives in [nebula-dominion-vocabulary-rename.md](archive/nebula-dominion-vocabulary-rename.md) § *Phase 2*.

## Objective

**Make the running code's `passage` and `dominion` be the ones [ADR-015](../docs/adr/015-passage-and-dominion.md) § *Decision* and `accepted` [`docs/vision/auth.md`](../docs/vision/auth.md) § *Coarse-grained access control* already specify, and make every site that needs either CALL them instead of re-deriving its own.** The model is not this file's to invent; this block is byte-identical in all three places, and must stay that way until this task file is archived:

```
isAtOrAbove(myScope, node)  — my scope covers the node: the same scope, or an ancestor of it.
                              The reserved platform scope is the ROOT of the tree, so it is
                              at or above every node.
isAtOrBelow(myScope, node)  — my scope sits at or beneath the node: the same scope, or a
                              descendant of it. Every scope is at or below the platform root.
                              Exactly isAtOrAbove with the arguments flipped:
                              isAtOrAbove(A, B) === isAtOrBelow(B, A).

dominion(access, node) = access.scopeAdmin ∧ isAtOrAbove(access.authScope, node)

passage(access, node)  = isAtOrBelow(access.authScope, node) ∨ dominion(access, node)
```

⚠️ **Both verdicts take exactly two inputs**: the caller's **`authScope`**, carried on the token — **never `activeScope`** — and the **node being called**.

Several goals, in the order they matter:

1. **Conformance.** Today the claim carries a wildcard *pattern* derived from the scope rather than the scope itself, and a non-admin reaches downward. Both are gaps `auth.md` names in its own `> **Today's code differs.**` blockquote, and closing them is the whole of the behavioural work.
2. **Reuse, so the wrong thing is hard to write by mistake.** ADR-015 clause 3 already requires exactly this, stating it as one-predicate-one-implementation over ADR-007: every site needing either verdict calls the shared predicate against the node it acts on, instead of re-inlining the conjunction. Today nine sites delegate to `hasDominionOver` while eight more call the containment check directly, and `passage` does not exist as a callable thing anywhere — `requirePassage` is the only place both arms appear together.
3. **Vocabulary — every identifier expressing this model spells it the way ADR-015 and `auth.md` do, and nothing expressing it is spelled any other way.** This has teeth in both directions. Today's names are normalized *onto* those terms: `matchAccess` and `buildAuthScopePattern` stop existing, and the claim becomes `access.authScope`. And no term is coined *beside* them, however reasonable a wrapper looks at the call site — a synonym is how a reader ends up holding a translation, which in this layer is how a security hole gets written.
4. **Detectability, so the rest survive later edits.** Afterwards a containment call outside `isAtOrAbove` / `isAtOrBelow` / `hasDominionOver` / `hasPassage` is non-conformant **by definition**, so the next one surfaces in a grep rather than in a review.

⚠️ **This file makes no claim on the current code's absence or presence of security risk, but it will make it much easier to assure that it is secure going forward.**

🔀 **`router.ts`'s Registry route GATE is not this file's** — [nebula-registry-route-guards.md](nebula-registry-route-guards.md) lands next and owns its shape and the argument for it. ⚠️ **Its containment EXPRESSION still converts here**, verdict-for-verdict, because Phase 1 deletes `matchAccess` and every call site must compile — the same verdict-preserving seam as the mint (§ *Constraints*).

## Context and current state

**Today**, verified against disk 2026-08-12 — post-rename, so these are the symbols you will actually find (`enforceScopeReach`, `requireAdmin`, `hasAdminOverScope` and `accessAdmin` are all gone from the tree):

- **A derived second string.** `buildAuthScopePattern(instanceName)` turns a member's scope into a pattern at **token** issuance — login and every refresh — and the result goes in the JWT as `access.authScopePattern`. Star tier passes through unchanged; universe and galaxy get `.*` appended; the reserved `nebula-platform` becomes `*`.
  - ⚠️ **It is also doing a SECOND job nobody named, and deleting it drops that job silently.** In `requirePassage` it is called for its side effect as much as its value: it **throws on an unparseable callee name** (>3 segments, illegal slug) and sits *before* the reach clause deliberately, so a malformed name fails closed instead of falling through to a comparison. The replacement predicates take a plain string and would happily compare a malformed one, so **an explicit `parseId(name)` fail-closed check must stay at that position**, with its comment — otherwise the only validation of the callee name vanishes inside a diff that reads as a pure rename.
- **A glob matcher.** `matchAccess(pattern, target)`: `*` matches everything, `prefix.*` matches the prefix and anything beneath, otherwise exact equality.
- **The dominion predicate.** `hasDominionOver(access, scope)` = `access.scopeAdmin && matchAccess(access.authScopePattern, scope)`, delegated to by `requireDominionHere`, `requirePermission`'s bypass, and `requirePassage`.
- **A SECOND enforcement surface, on the registry's HTTP routes.** `verifyInstanceJwt` ([router.ts](../packages/nebula-auth/src/router.ts)) is the only scope check every instance-scoped registry route passes through — `Missing` item 3.
- **Two directions, expressed identically.** `requirePassage` grants passage on either *the dominion arm* (`scopeAdmin` plus the caller's pattern covers this node) or *the tenant boundary* (a pattern built from **this node's own name** covers the caller's `aud`). The second is a child reaching its parent, and confers no dominion (ADR-015 § *Terminology*).

**Missing:**

1. **One fact, two strings** — the member's scope lives in `Memberships.universeGalaxyStarId`, and the JWT carries a *different* string derived from it that exists nowhere else, so a reader holds the derivation in their head to reconcile them. And **the two directions look like one mechanism**: both are glob matches against `buildAuthScopePattern` output, so nothing in the shape of the code says one grants dominion and the other deliberately does not.
2. **Non-admin downward movement exists at the MESH boundary and has no consumer.** ✅ **No separate work** — only `requirePassage`'s tenant arm reads `aud`, so computing passage from the member's scope instead refuses `{u}.{g}.{s}` for a non-admin at `{u}` by construction. Today such a caller has passage to every Star in the Universe and dominion over none, with no use case behind it. ⚠️ **ADR-008 is SILENT here, not permissive** — its Decision is scoped to *"every member of that Star"*, and a universe-tier non-admin is not a member; its § *Non-goals* disclaims cross-boundary visibility outright. Three sibling files read it as a **bound**, so do not cite it as licence. State the exposure plainly: a non-member of a tenant Star can reach it and read whatever those methods gate on Star-reachability alone. It is going away by construction.
3. 🔀 **The Registry's HTTP routes borrow their dominion from their consumers** — real, and owned entirely by [nebula-registry-route-guards.md](nebula-registry-route-guards.md) § *There is no authorization hole here*, which also says why the obvious fix belongs nowhere near this file. ⚠️ **That is about the gate's SHAPE; its containment expression is converted by § *Phase 1*** — `matchAccess` stops existing, so it has to be.

### The instruments that find every site

⚠️ **This file names INSTRUMENTS, not an inventory.** A hand-listed site table is a snapshot that rots when an edit lands 40 lines from a call, and it is a lower bound besides: run against disk 2026-08-12, the greps below surfaced decision sites the previous inventory did not name (`worker-token.ts:339`, `worker-token.ts:417`). **Run them, then classify what they return** — do not work from a list.

**1 — Completeness, as the UNION of two greps.** Neither alone is complete; run both.

```sh
# 1a — claim reads
grep -rn '\bauthScopePattern\b\|\bscopeAdmin\b' packages/*/src apps/nebula/src --include='*.ts'
# 1b — containment/predicate calls
grep -rn 'hasDominionOver(\|matchAccess(\|isPlatformInstance(\|buildAuthScopePattern(' packages/*/src apps/nebula/src --include='*.ts'
```

⚠️ **1a is complete over FILES, not over LINES — and the lines it misses are this task's most load-bearing conversions.** Measured 2026-08-12: 1a returns 150 hits across 20 files and 1b returns 46, and `comm`-ing the two **file** sets leaves only `testing.ts`, a re-export barrel with no decision in it. That file-level result does **not** license a line-level claim, because a site can decide containment with neither claim token on the line:

- **`requirePassage`'s tenant arm** (`nebula-do.ts:144`, `matchAccess(pattern, aud)`) decides from the callee **name** and `claims.aud`. 1a reaches `nebula-do.ts` only through unrelated lines at `:60`/`:69`.
- **`worker-token.ts:554`** decides from a pattern built off a registry row.
- **`nebula-client-gateway.ts`** is named in the classification table below and is returned by **neither** grep.

⇒ Work the union, and treat any site named in this file but absent from both greps as a known blind spot rather than as done. *Partial mitigation:* Phase 1's atomic deletion turns most missed sites into compile errors — but not `nebula-client-gateway.ts`, and never the classification judgment.

**2 — Triage.** Filter the wide grep down to conditional reads:

```sh
grep -rn '\bauthScopePattern\b\|\bscopeAdmin\b' packages/*/src apps/nebula/src --include='*.ts' \
  | grep -E 'if *\(|&&|\|\||\? |return .*(authScopePattern|scopeAdmin)'
```

150 → **30** on 2026-08-12, of which ~6 are comments and SQL. ⚠️ **This filter's precision DEPENDS on the naming criterion this same task installs** — it catches an aliased read only while the alias keeps the claim's name, so renaming the six `pattern` locals would blind it wherever the alias is the only hit. That is why instrument 1's union is what bounds the search and this one is only triage.

**3 — Conformance, after the change.** The one the first two cannot replace, because it asks about *shape* rather than location: **`isAtOrAbove` / `isAtOrBelow` may appear only inside `hasDominionOver`, `hasPassage`, or the named structural allow-list.** The conformance shape is the criterion 🔒 *Nothing re-derives either verdict*; run it there. ⚠️ **Do NOT read this as covering `isPlatformInstance` or bare `scopeAdmin` tests** — § *Known sites* pins survivors of both by design (the name reservation, `myScopeTree`'s and Profile's read-shape arms, `star.ts`'s identity latch), and converting them breaks criteria this file requires to pass. Today the equivalent shape returns **8** comment-filtered `matchAccess` call sites outside `parse-id.ts` (14 raw hits, 6 of them comments — the 6 are renames, not conversions).

⚠️ **BSD BRE trap:** do not narrow instrument 1 to `access\??\.scopeAdmin` — the `?` makes `s` optional and is matched literally, silently dropping `verifiedAccess.scopeAdmin`, the exact read that matters.

⚠️ **Audit by reading the CONSUMERS, not just the enclosing function** — a conjunction split across a file boundary is invisible to an enclosing-function read, and `router.ts:359` is exactly that shape.

### Known sites

The instruments say **where**; the class is judgment. It is obvious at most sites — a `scopeAdmin` conjunction means dominion, a check with no principal in it is structural — so what this file pins is only what a sweeper gets **wrong**. Five classes: **dominion** · **passage** · **structural fact** · **exact identity** · **not-a-decision**. Ownership is a separate axis, noted per row.

| Site | Class | Why a sweeper gets it wrong |
|---|---|---|
| `verify.ts` internal-consistency · `access-claims.ts` construction invariant · the refresh `activeScope` confine | **structural** | ⚠️ each calls **`isAtOrAbove`**, which carries the platform branch itself, and **none** gains a `scopeAdmin` conjunction — hand-rolling the comparison instead breaks a superuser, differently each time (below) |
| `star.ts`'s root-admin seed | **exact identity** | ⚠️ it asks *"is this Star my own scope?"*; making it hierarchical hands a Star's root grant to whichever covering admin arrives first, **permanently** — the seed latch is one-shot with no re-seed. Rename only; the pinned lift in [`on-hold/nebula-dataplane-root-admin.md`](on-hold/nebula-dataplane-root-admin.md) is the same class |
| `myScopeTree`'s bare `scopeAdmin` + `LIKE prefix.%` · Profile's scoped-admin branch | **dominion**, hand-rolled | ⚠️ the two sites that re-derive the conjunction by hand and become callers — the largest class, and the one a sweeper is most likely to convert *incorrectly* by adding a second `scopeAdmin` test |
| `nebula-do.ts`'s platform-name reject · `NebulaClientGateway.onBeforeCallToClient` | **exact identity** | a name reservation against a reserved constant, and an `aud` equality check — both look convertible and are not (§ *Constraints*) |
| `requirePassage`'s tenant arm | **passage** | correct today, but on the wrong INPUT — it reads `aud` (item 2). Today **nothing** calls passage as a thing; that is the finding |
| 🔀 `router.ts` `verifyInstanceJwt` + `worker-token.ts`'s two bare `scopeAdmin` reads | **dominion**, split across a FILE boundary | the conjunction's halves sit in different files (item 3). ⚠️ **Shape is the sibling's; the containment expression at `router.ts:359` still converts in § *Phase 1*** |

⚠️ **Structural is not a lesser class**; wrapping a structural fact in a verdict puts a `scopeAdmin` conjunction where no principal exists, and breaks token verification for every superuser.

⚠️ **Most hits are NOT decisions, and the residual rule is a rename — not a conversion.** 1a's 150 hits span `types.ts`, `schemas.ts`, `nebula-client.ts`, `dev-studio.ts`, `dev-container.ts` and `nebula-container.ts`: claim-shape declarations, storage columns, UI reads and comments wanting no verdict at all. **Three-way, not two:** a hit that decides **nothing** is a rename; a hit that decides **containment** becomes a predicate call; a hit that decides **identity** stays an equality and is renamed only. ⚠️ The third is why "gates behaviour ⇒ convert" is wrong — `star.ts`'s seed latch, the name reservation and the Gateway's `aud` check all gate behaviour and all MUST stay equalities. Of the ones that do gate: nine already delegate to `hasDominionOver` and change not at all, while `myScopeTree`'s bare `scopeAdmin` + `LIKE prefix.%` and the Profile scoped-admin branch's 37-line spread re-derive it by hand and become callers.

#### The superuser is the site class that breaks silently

**A super-admin is an ordinary membership at the reserved scope `nebula-platform`** ([nebula-auth-registry.ts:717](../packages/nebula-auth/src/nebula-auth-registry.ts) mints it only for a configured bootstrap email at that exact scope), so `authScope` carries **`nebula-platform` verbatim**. The `'*'` sentinel was the *derived* value and dies with the derivation — not a wildcard, and emphatically not an empty string, which is falsy and would grant everything on a mis-default.

🚨 **Hand-rolling a containment comparison anywhere breaks a superuser, silently and differently each time** — which is what the platform branch inside `isAtOrAbove` exists to prevent:

| Site | If it hand-rolls the comparison |
|---|---|
| `verify.ts` | their token **fails to verify at all** |
| the refresh `activeScope` confine | they can refresh to **no** scope |
| `access-claims.ts` | their token is **unmintable**, throwing at every mint site including `createNebulaTestToken` |
| `worker-token.ts:554` | a platform subject can no longer be impersonated |

⚠️ **No test reds on any of it.** The platform tests that would catch it live in the 19 `'*'` fixtures being rewritten in the same commit, so their redness is no signal.

⚠️ **Size the fixture sweep with the command, and note that NEITHER backstop covers it.** `grep -rn "authScopePattern: *'\*'" packages apps --include='*.ts'` returned **19 hits across 12 files** on 2026-08-10, including `harness/scenarios/superadmin-dominion.ts` and `harness/lib/prod-drive.ts` alongside eight test files. Instrument 1 is `src`-only, and the no-`authScopePattern`-survives criterion greps the claim **name** — so a fixture rewritten to `authScope: '*'` passes both while asserting over a value production can never mint. Each moves to `authScope: 'nebula-platform'`.

⚠️ **The root position makes the platform scope reachable from everywhere** — `isAtOrBelow(anyScope, 'nebula-platform')` is true, which is today's behaviour exactly (`matchAccess('*', aud)` likewise) [nebula-do.ts:124](../apps/nebula/src/nebula-do.ts) refuses those calls as a **name reservation**; it stays for this build, and § *Acceptance criteria* says why it is temporary.

## Design intent, constraints, and future state

### The target

§ *Objective* states the model. This section pins **which symbols carry it and how each is named** — nothing here restates a predicate.

⚠️ **Both predicates take `(myScope, node)` in that order, always — never flipped.** § *Objective*'s block states the identity that makes this dangerous: a transposed call is not a type error and not a test failure, it silently inverts the security model. That identity is also why `isAtOrBelow` exists as its own symbol; without it the upward arm would be spelled `isAtOrAbove(node, myScope)`, putting the same function on adjacent lines with flipped arguments. ⚠️ **Implement one as the other with the arguments swapped**, so the identity — and the platform-root branch with it — is structural rather than a property two functions must both remember.

⚠️ **`passage` is the UNION, not the upward arm alone.** An admin at `{u}` calling `{u}.{g}.{s}` has passage *because* they hold dominion there; writing passage as upward-only refuses the whole downward rule. It is also why `hasPassage` at the Registry gate would be an escalation (see [nebula-registry-route-guards.md](nebula-registry-route-guards.md) § *There is no authorization hole here*) — the same union misread in the opposite direction.

⚠️ **A caller with no principal never reaches either verdict — a structural fact, not a guard.** `verify.ts:44` refuses a token whose `access` carries no scope (`authScopePattern` today, `authScope` after Phase 1), so both verdicts run against a verified principal by construction. ⚠️ **Passage to the platform root is the model working, not a leak that wants closing** — every scope is at-or-below the root, so every authenticated caller has passage there, and § *Acceptance criteria*'s name-reservation criterion carries what that does and does not permit. ⚠️ **Whatever absent-`access` handling the verdicts carry is a stated contract, and it never moves onto `isAtOrAbove`** — that is a structural fact about two strings, unconditional by design, and its platform-root branch returns `true` without reading its second argument at all.

⚠️ **Open, and it is a `calibration.md` §4 re-derivation rather than a port:** `hasDominionOver`'s JSDoc argues its truthiness guard from `authScopePattern`, which Phase 1 deletes, and `requirePassage`'s tenant arm loses its `if (!aud) throw` when it stops reading `aud`. Decide whether either guard outlives its justification — `verify.ts:44` may already make both unreachable — instead of carrying them across on the strength of the old comments.

⚠️ **`dominion` and `passage` are RESERVED — [ADR-015](../docs/adr/015-passage-and-dominion.md) § *Terminology* is the definition home — and "reach" MUST NOT be used as a noun for either.** Three terms, one meaning each: `isAtOrBelow` the upward *structural* relation, `dominion` the downward verdict, `passage` the boundary verdict.

**The symbols THIS file pins, so no phase of it invents one.** Three tiers, and the tier decides the naming style: a **structural fact** gets a literal name, a **verdict** a reserved word, **data** its field name.

| Today | Becomes | Tier | Note |
|---|---|---|---|
| `matchAccess(pattern, target)` | `isAtOrAbove(myScope, node)` + `isAtOrBelow(myScope, node)` | structural | `Access` was vestigial — it takes two strings and never sees an `AccessEntry` |
| `hasDominionOver(access, node)` | its containment half becomes `isAtOrAbove` | verdict | the rename already landed in [nebula-dominion-vocabulary-rename.md](archive/nebula-dominion-vocabulary-rename.md), which withheld this arm because `matchAccess('*', …)` made it dead code |
| *(does not exist)* | `hasPassage(access, node)` | verdict | its route-guard consumers (`passageGuard`, `dominionOverScopeGuard`) are pinned in [nebula-registry-route-guards.md](nebula-registry-route-guards.md). ⚠️ **`*Guard` RETURNS a `Response`; `require*` THROWS** — a contract, not a style |
| `isPlatformInstance(id)` · `PLATFORM_INSTANCE_NAME` | `isPlatformScope(id)` · `PLATFORM_SCOPE` | structural | the value is a **scope** at every site — `registry:723` INSERTs it into the `Scopes` table, and `:445`/`:717`/`:1166` compare it against a slug, a `universeGalaxyStarId` and a delete target. Of the two predicate call sites, `parse-id.ts:116` is inside `buildAuthScopePattern` and dies with it; `nebula-do.ts:124` takes a node name, which `auth.md` § *Scopes* defines as the instanceName half of an address — a scope. ⚠️ **Still needed after the rename**, for three reasons only one of which is containment: the root branch **inside `isAtOrAbove`**; the **name reservation** at `nebula-do.ts:124`, which asks identity against a reserved constant; and two **work-avoidance short-circuits** — `myScopeTree`'s unbounded-query arm and Profile's no-registry-read arm — which cannot route through the predicate, since it needs the very data they exist to avoid fetching |
| `access` / `authScope` / `activeScope` / `scopeAdmin` | unchanged | data | always a possessed field, never a bare noun |
| `const pattern = …` (6 locals) | name it for what it holds | data | otherwise these read `const pattern = claims.access.authScope` — this task's own two-strings-for-one-fact defect rebuilt at function scope |

⚠️ **Naming tiers: `.claude/rules/coding-style.md` § *Security-verdict identifiers*, which governs locals and guards as well as exports** — that is why the criteria below carry a grep-shaped one, since no behavioural criterion can see a correct predicate called by a guard named for the bit over a local named for the pattern.

`buildAuthScopePattern` is deleted and the claim becomes **`access.authScope`** — the member's scope verbatim. The rename drops `Pattern` and nothing else; § *Decisions* carries why not `scope`.

⚠️ **`isAtOrAbove` compares WHOLE dot-separated segments — contract, not implementation detail.** The obvious `b === a || b.startsWith(a)` type-checks, passes every behavioural criterion below, and silently makes `u.g.s1` cover `u.g.s10` and `acme` cover `acme-2`, both legal slugs. The boundary is closed explicitly in two independent places today (`parse-id.ts:148` `startsWith(prefix + '.')`, `nebula-auth-registry.ts:689`'s `LIKE ${prefix + '.%'}`); the replacement owes the same guarantee.

✅ **`isAdmin` → `scopeAdmin` landed 2026-08-07** (66 files, at rest and in flight). ⚠️ `packages/auth` and `packages/mesh` keep their own `isAdmin` — a *different* bit (auth's `Subjects` column, and a flat top-level claim mesh mints for auth's gate) — and the Data-plane's `admin` **grant on an orgTree node** keeps its name, being one of a symmetric triple (`CHECK(permission IN ('admin','write','read'))`). A repo-wide replace would break all three, the mesh one silently.

### What this buys, and what it costs

- **Two places, one string.** The `Memberships` row and the JWT hold the same value — nothing to reconcile, and ADR-015's clauses stop needing to be memorized (same predicate, opposite arguments). ⚠️ **The URL is not a third place** — it names the **node being called**, which is the predicates' *other* argument; conflating the two is the mistake this layer exists to prevent.
- **`discover` becomes complete.** It returns the scopes a person holds memberships in and cannot see DAG grants (those live in each Star's DagTree, unreachable from the registry). Today a teammate with one high-tier non-admin membership shows up as one entry while the truth about where they can work is scattered across N DagTrees with no index. Afterwards membership *is* the answer, and a scope picker can show it.
- **A residual disappears.** [nebula-collaborator-tiers.md](on-hold/nebula-collaborator-tiers.md) used to file the minimum-privilege collaborator reaching down into tenant Stars as a disclosure question to decide; with no downward **passage** without `scopeAdmin`, the question stops existing rather than getting answered.

**Cost, accepted: a person who works in N scopes needs N memberships and N sessions**, where a high-tier non-admin membership would have covered them with one. Grants are unaffected — DAG grants are per-node under both models — so this costs sessions, not permissions. Accepted because the surfaces are usually already separate (Studio on the Galaxy, preview on the `.dev` Star); each membership carries its own acceptance, a real per-scope consent moment that reads well to an enterprise buyer; and it is what makes `discover` honest. ⚠️ **The one friction is N invite emails**, and it cannot be waived because the address is verified elsewhere: `Memberships.acceptedAt` is set only by consuming a link delivered to the mailbox, and per-membership acceptance is ADR-012's manufacture defense. If it ever bites, the fix is a multi-invite-in-one-email flow — years away (Larry, 2026-08-05).

### Constraints

- **[ADR-015](../docs/adr/015-passage-and-dominion.md)** — this **amends** it, but only where the ADR describes the *mechanism*. **The commitment is untouched**: downward total, upward nil, passage ≠ dominion. Its predicates and prose already read in scope vocabulary, so the remaining edits are small and Phase 4 owns them. ⚠️ **Ratification is OUT of this file and every sibling, and MUST NOT be re-added (decided with Larry 2026-08-11)** — he ratifies ad-hoc, gated on nothing. ADR-015's Status line is the authority.
  - ⚠️ **ADR-015's rejected-alternative row forbids this exact change, so retiring it must be said out loud.** It rejects *"tighten `requirePassage`'s tenant branch"* because *"narrowing it breaks legitimate non-admin upward reads"* — a harm that cannot occur under the new rule, since `docs/vision/auth.md` § *Coarse-grained access control* (**`accepted`**, where ADR-015 is only `Proposed`) preserves that read as its own table row while refusing only the descendant case this file deletes. Cite those rows rather than re-arguing it; per `docs/adr/README.md`, a conflict while `Proposed` is case (a), *change the decision*.
  - ⚠️ **`calibration.md` §4 is NOT amended, and MUST NOT be.** It cites the same prohibition to make a different point — *do not delete the branch because the comment defending it was wrong* — and the branch does survive here, with only its **input** changing from the client-chosen `aud` to the server-trusted `authScope`. Read the two prohibitions as one and the likely "fix" is deleting a branch that lesson correctly defends.
- 🚨 **The seam with the mint is VERDICT-PRESERVING vs VERDICT-CHANGING — not file-vs-file.** This file **does** rewrite the mint's containment expressions, necessarily: Phase 1 deletes `buildAuthScopePattern` and `matchAccess`, and every call site must compile. What it MUST NOT do is change a single mint verdict. [nebula-registry-route-guards.md](nebula-registry-route-guards.md) owns **which checks exist** at the mint — deleting, adding, reordering. ⚠️ **Do not restate this boundary as "nothing here touches the mint"**; that phrasing fights the compiler, and a boundary the compiler contradicts gets violated silently every time. The conversions are pinned in § *Phase 1*.
- 🔀 **`security.md`'s mint-side rule (2) and `canMintFor`** → [nebula-registry-route-guards.md](nebula-registry-route-guards.md). ⚠️ **That rule is ALREADY NARROWED** — four invariants collapsed to one predicate plus an identity check — and it says restoring the deleted ones is a **regression, not hardening**. It must NOT be "translated to scope terms".
- **[`docs/vision/auth.md`](../docs/vision/auth.md) — `status: accepted`**, and its § *Coarse-grained access control* already states this file's target model in present tense. Per `docs/vision/_review-lens.md` § *Status convention* contradicting an accepted doc is a **blocker**, so that section is the statement to conform to; the deliverable is deleting its `> **Today's code differs.**` blockquote, not rewriting its prose.
- **[ADR-008](../docs/adr/008-full-org-tree-visibility.md)** — unchanged, but its blast radius shrinks: fewer callers are Star-reachable.
- ⚠️ **`aud` stays on the token and on the wire — this work touches the passage decision ONLY.** `NebulaClientGateway.onBeforeCallToClient` refuses a mismatch between the originating call's `aud` and the receiving connection's, so a call out to a client reaches only connections in that scope. `authScope` cannot replace it: two tabs of one Galaxy admin on sibling Stars share `authScope` and `scopeAdmin`, so comparing those would deliver one tenant's subscription updates to the other.
- **`verify.ts`'s internal-consistency check is KEPT and re-worded** — it stops guarding passage (which no longer reads `aud`) and guards only the token-internal invariant the Gateway's outbound fence assumes. Its comment currently claims a reach purpose and must not survive that way.
- **Pre-alpha** — no users and not wipe-gated, so the claim rename is free now and a compatibility problem later.

## Decisions

| Decision | Rejected alternative — why |
|---|---|
| **The claim carries the member's authScope; both verdicts are computed from it (and the node called)** | The derived wildcard pattern — two strings for one fact, the second existing only inside a token, which is what makes the model hard to hold. |
| **Dominion stays a conjunction** — `scopeAdmin` ∧ scope-at-or-above | *"`scopeAdmin` is the only reach-relevant bit"* — true as a summary, fatal as an implementation; the ADR-015 violation that has shipped twice. |
| **Non-admin downward passage is dropped** | Keeping it — no consumer, and it is exactly the tenant-Star passage the collaborator design was about to accept as a residual. |
| **The reserved scope is the ROOT of the scope tree**, carried by a named `isPlatformInstance` branch inside `isAtOrAbove` | Two rejected. **(a)** A wildcard covering it incidentally (`matchAccess('*', …)`) — coverage falling out of a grammar rather than stated in a function. **(b)** Treating it as sitting *outside* the hierarchy, with a standing `isPlatformInstance ∨ …` disjunct in `dominion` — which `auth.md` argued for until 2026-08-12. Once the root rule holds, that disjunct can never be false: dead code inside a security predicate. It also leaves a node at the platform scope unreachable from every scope — foreclosing, on a containment technicality, whatever we might later decide belongs there. |
| **The claim is renamed off `authScopePattern`, dropping `Pattern` and nothing else** | Keeping the name — it asserts a pattern that no longer exists, and a name needing explanation is the rename signal (calibration §5). Also rejected: `→ scope`, which overstates the change (the value stopped being a *pattern*, not an auth scope), collides with `aud` (also a scope), and forces every reader of `auth.md` — which says `authScope` throughout — to hold a translation. |
| **`isAdmin` → `scopeAdmin`**, one spelling at rest and in flight | Keeping `isAdmin` in storage while the token says `admin` — one concept, two spellings. Also rejected: renaming the token's `access.admin` to `isAdmin` (fights the prefix-free precedent `emailVerified` / `acceptedAt`), and `registryAdmin` (names where the bit is stored, not what it is dominion over — and wrong anyway, since the bit reaches the Data-plane through the bypass). |
| **A derived token is exactly what the subject's own login would mint — `act` is the only addition.** One rule, not a per-claim table: `sub`, `access.authScope`, `access.scopeAdmin` and `profileId` are all the subject's, and `activeScope` is the caller's choice **bounded to what the subject could have chosen for themselves** (which is what `worker-token.ts:554` enforces, and why § *Phase 1* keeps it). `auth.md` § *Impersonation* states the same rule: every identity claim names the subject. ⚠️ **`activeScope` is an INPUT, and two bounds make it safe — both holding exactly as if the subject had minted for themselves.** **(1) Caller → subject:** the minted `authScope` is the subject's scope, and it must sit **at or below the caller's own `authScope`**, with `scopeAdmin` — i.e. the caller holds dominion over the subject (`worker-token.ts:543`, already delegating to `hasDominionOver`, so it inherits the platform branch). **(2) Subject → `activeScope`:** the requested `activeScope` must sit **at or below the subject's scope** (`:554`, 🔒 retained — § *Phase 1*). Together: a derived token can never carry more than the subject could have obtained alone, and only an admin who administers that subject can obtain one. 🔀 **This file records the decision and does NOT implement it** — today's mint derives the claim from `activeScope`, and Phase 1 preserves that verdict-for-verdict. [nebula-registry-route-guards.md](nebula-registry-route-guards.md) owns the change. | Two rejected alternatives, both the same mistake — deciding a claim field-by-field instead of from the rule. **(a)** Deriving `authScope` from the caller's `activeScope` (today's behaviour) — least-privilege that protects nothing, since the admin already holds total downward dominion as themselves, and it costs simulation fidelity: `myScopeTree` would show the impersonator a different scope picker than the subject sees. **(b)** Computing `scopeAdmin` as `caller.scopeAdmin ∧ subject.scopeAdmin` — an operand that can never be false (impersonating already requires dominion over the subject): dead code inside a security predicate, invisible to mutation testing, re-derived by every next reader. |
| **The upward rule reads `authScope`; `activeScope` leaves the security model entirely** | Deciding passage on `activeScope` — not independent (the refresh mint already confines it inside `authScope`), so it is a security decision on a derived value, sited inside a token endpoint where no authz reviewer would look. Checked against every row of `auth.md`'s table: all verdicts reproduce, and the two rows differing only in `activeScope` collapse. |
| **Land it before [nebula-invite.md](nebula-invite.md)'s review** | After the Galaxy collapse — every week adds surface describing the old model, and the collapse adds more. Five files already describe it. |

## Acceptance criteria

**Behaviour**

- **A non-admin reaches its own scope and nothing beneath it.** Passage at `{u}`; refused at `{u}.{g}` and `{u}.{g}.{s}`. *Reds against retaining downward passage for non-admins.*
- **An admin reaches downward, totally** — passage with dominion at `{u}`, `{u}.{g}` and `{u}.{g}.{s}`, including scopes created after the token was minted. *Reds against dropping the downward rule.*
- **Upward is nil for dominion.** An admin at `{u}.{g}` is refused **as admin** at `{u}`. *Reds against a symmetric predicate.*
- 🔒 **The scope is still the bound.** An admin at `{u1}` is refused everywhere in `{u2}`. *Reds against a predicate reading `scopeAdmin` without the scope — the "bare bit is dominion" bug, and the one this change makes easiest to write.*
- **The tenant direction survives, and confers no dominion.** Stated over **named methods**, because "denied" is also what a boundary refusal produces — so a criterion whose only observable is a denial cannot red against the collapse it exists to catch. A Star member whose `authScope` is that Star reaches its parent Galaxy and **`Galaxy.getLatestOntologyVersion` RETURNS** ([galaxy.ts:176](../apps/nebula/src/galaxy.ts), bare `@mesh()`), while **`Galaxy.setGalaxyConfig` refuses** ([galaxy.ts:130](../apps/nebula/src/galaxy.ts), `@mesh(requireDominionHere)`). *Reds against collapsing both directions into one rule — and against "fixing" upward passage into a denial.* 🚨 **Do NOT assert a blanket denial here, and do not let a reviewer talk you into one.** ADR-015 § *Terminology*: lacking dominion *"is an override, not a gate … lack it and the node's own guards decide — freely, and often in the caller's favour."* A peer-level invite is exactly that shape and **must be granted**, so a criterion demanding refusal would red against correct behaviour. Several reviewers have reached for the denial reading; it is wrong every time.
- **`aud` survives on the token and on the wire.** A call out to a client is still refused on an `aud` mismatch. *Reds against over-applying "passage no longer reads `aud`" into removing the claim — which would cross-deliver one tenant's updates to another.*
- **Platform means everywhere.** A `nebula-platform` admin reaches every scope, including the platform instance itself. *Reds against a hierarchy predicate with no case for a non-hierarchical name.*
- **A mesh call to a node NAMED `nebula-platform` is still refused**, for that same superuser. *Reds against removing the platform-name reject once its `'*'`-collapse justification is re-derived away.* ⚠️ **Its reason is NAME RESERVATION, not masquerade** — nothing intentional is deployed at that name yet. 🔀 **It is TEMPORARY and reversible**, and whatever is eventually deployed at that name (§ *Non-goals*) is what retires it. ⚠️ **The rule for whoever builds that: the name goes from REJECTED to BOUND — never to open.** Under the root model `nebula-platform` is the one instance name **every** authenticated caller has passage to, by construction; every other name needs the caller's scope at-or-below it or under their dominion. And `lmz.call` takes the binding and the instance name **separately**, so the callee's class is the caller's choice. Deleting this branch on its own would therefore let anyone instantiate an arbitrary DO class at the most-reachable name in the system and call its ungated `@mesh()` methods. Delete it in the same change that binds the name to a registered occupant, not before.
- 🔒 **Segment boundaries hold under colliding names.** `{u}.{g}.{s}1` is refused at `{u}.{g}.{s}10`; an admin at `{u}` is refused everywhere in `{u}-2`. *Reds against a prefix-match `isAtOrAbove` — the defect every other criterion is blind to, since no other scope names collide on a prefix.*
- 🔒 **A superuser is unchanged everywhere.** Their token **verifies**; authorized at every scope; may refresh to any `activeScope`; `myScopeTree` returns every scope; passes the Profile owner/admin gate with **zero registry reads**. *Reds against any consumer of `access` that lost the platform branch when `'*'` stopped being the value.* ⚠️ The verification limb fails CLOSED and TOTAL, so a miss there is a superuser who cannot log in, not one who quietly loses admin. ⚠️ Its **registry-route** and **mint-`aud`** limbs belong to [nebula-registry-route-guards.md](nebula-registry-route-guards.md) — do not look for them here, and do not re-add them.
- **Enumeration is unchanged for an admin.** `myScopeTree` returns the same set before and after, for the same identity. *Reds against a bound that narrows the query.*
- 🌐 **The same, driven as a `/live` scenario.** Real logins at three tiers, real sockets: a universe admin acts in a Star beneath, a star-scoped member is refused at the Galaxy, and a **superuser** logs in and reaches. ⚠️ **Fidelity, not capability** — pool-workers can assert the predicate, but only a real login proves the claim the *server actually minted* carries what the predicate expects.

**Shape — the half no behavioural criterion can see**

- 🔒 **Nothing re-derives either verdict** (instrument 3). The **tripwire** form — comment lines filtered out, because prose legitimately quotes the predicate:

  ```sh
  grep -rn 'isAtOrAbove(\|isAtOrBelow(' packages/*/src apps/nebula/src --include='*.ts' \
    | grep -vE ':[0-9]+: *(\*|//|/\*)'
  ```

  Every surviving hit must be inside `hasDominionOver`, `hasPassage`, or a **named structural allow-list** — never beside a hand-assembled `scopeAdmin` conjunction. *Reds against a fix applied one site at a time.* 🚨 **Second limb, and the one the grep above is structurally blind to: a site can re-derive containment BY VALUE, naming neither predicate.** `b === a || b.startsWith(a + '.')` in TypeScript and `LIKE ${prefix + '.%'}` in SQL both compute `isAtOrAbove` without spelling it, so a name-shaped instrument reports them as absent. `nebula-auth-registry.ts:1194` is one such site and is named in no instrument in this file; `:689` is a second, named only as evidence for the segment-boundary contract. Sweep `grep -rnE "startsWith\\(.*'\\.'|\\. *%'" packages/*/src apps/nebula/src --include='*.ts'` and give each hit a verdict — converted, or on the allow-list with its reason. *Reds against a conversion that leaves a value-shaped containment check standing while every name-shaped criterion greens.* ⚠️ **Without the comment filter this criterion is already broken**: [nebula-do.ts:102](../apps/nebula/src/nebula-do.ts:102) quotes the predicate in a comment, which is *correct* and would red the unfiltered grep. ⚠️ **The allow-list is the deliverable, not an inventory** — a list of every site rots on an unrelated edit, while a short list of exceptions fails loudly the moment a new site appears. Today the equivalent shape returns **8** comment-filtered `matchAccess` call sites outside `parse-id.ts` (14 raw, 6 of them comments), and that is the number this must bring down to the allow-list's length.
- 🔒 **The mint's verdicts are unchanged.** Four cases keep exactly today's outcome: an `activeScope` the caller cannot reach **refuses**; an un-administered subject **refuses**; an `activeScope` outside the subject's own scope **refuses**; and a `nebula-platform` subject impersonated by a platform caller **succeeds**. `mint-narrower-token.test.ts` passes with only the claim's name changed. *Reds against a conversion that moves a mint verdict while this file claims to be verdict-preserving — and specifically against dropping the platform arm at `:554`, which `matchAccess('*', …)` supplied for free and `isAtOrAbove` now supplies deliberately.* ⚠️ The fourth case is the one with no other backstop: the 🔒 *A superuser is unchanged everywhere* criterion explicitly disclaims the mint limb, and the platform fixtures are rewritten in this same commit.
- 🔒 **The instruments are capable of failing.** Run each § *The instruments that find every site* grep against the **pre-change** tree and confirm it returns the sites this file says it does; run instrument 1 against the post-change tree and confirm every hit is classified. *Reds against an instrument that goes green because it greps a name the change already deleted* — the same defect as a test that cannot fail, and the reason *No `authScopePattern` survives* cannot stand alone.
- 🔒 **No identifier still names the old model.** `grep -rnE '\b(buildAuthScopePattern|matchAccess)\b' packages/nebula-auth/src apps/nebula/src` and `grep -rnE 'const (pattern|subjectPattern|authScopePattern) *=' packages/nebula-auth/src apps/nebula/src` both return nothing; then **eyeball** `grep -rniE '[Pp]attern' packages/nebula-auth/src apps/nebula/src` for survivors holding a scope. *Reds against the shape this file most easily ships: correct predicates called over a local still named for the pattern.* ⚠️ Every behavioural criterion above passes with `hasDominionOver` called over a variable called `pattern`. ⚠️ Deliberately scoped to this file's symbols — the verdict names carry their own grep in [nebula-dominion-vocabulary-rename.md](archive/nebula-dominion-vocabulary-rename.md), so including them would make this green-by-inheritance.
- **No `authScopePattern` survives.** `grep -rn 'authScopePattern' packages apps` returns nothing outside archived task files. Eyeball the remainder.
- ✅ **No `isAdmin` survives — already satisfied (2026-08-07)**, kept as a regression check: `grep -rn 'isAdmin' packages/nebula-auth/src apps/nebula/src` returns **0**. ⚠️ Eyeball any future hit — the data-plane's `'admin'` tier and `packages/auth` / `packages/mesh`'s own `isAdmin` are different concepts keeping their names.

**Standing guidance and docs**

- **No standing-guidance statement forbids narrowing the tenant branch.** ⚠️ **Stated over the BRANCH, not the symbol** — `enforceScopeReach` is already `requirePassage` by the time this builds. ADR-015's amended text must carry clause 2 restated over `scopeAdmin` ∧ scope-at-or-above, plus the impersonation answer. ⚠️ `calibration.md` §4 stays untouched and correct — confirm its lesson still reads as "the branch was right", since the branch survives with a different input. *Reds against amending only ADR-015's Mechanism paragraph.*
- **No standing-guidance statement describes the pattern model.** ⚠️ State it structurally — *no rule or ADR describes dominion as pattern-coverage* — not as a list of files. Known sites: ADR-015's mechanism, **ADR-016's normative record contents**, `security.md`'s mint-side invariants, `workflow.md`'s ADR-015 one-liner. 🚨 **In ADR-016, change only `authScopePattern` in its record contents — NEVER the word `authority` in its trigger.** ADR-015's carve-out (2026-08-12) deliberately preserves that broader sense: an authority-*changing* action is broader than dominion, since a data-plane grant changes what a principal may do while touching neither the bit nor the scope. Narrowing that trigger to dominion would silently drop orgTree grant changes out of ADR-016, and by that ADR's own stakes the loss is unrecoverable history rather than a fixable bug. ⚠️ **"Standing guidance" is the CLAUDE.md list**, so it includes **`.claude/skills/*/SKILL.md`** and **agent memory** — the 2026-08-09 `scopeAdmin` sweep found a stale claim in each, neither reachable by a grep over `.claude/rules docs/adr tasks`, and **memory is the worst**: it loads every session unprompted and nothing type-checks it. Sweep both explicitly.
- 🔒 **No standing-guidance statement CITES A DELETED SYMBOL.** Scoped to **this file's own deletions**, and to standing guidance only: `grep -rn 'matchAccess\|buildAuthScopePattern' docs .claude` — giving every hit a verdict. ⚠️ **`tasks/` and `website/docs` are deliberately excluded**: frozen `tasks/archive/` MUST NOT be updated (`workflow.md`), and both are owned by their own criteria below. ⚠️ **A second axis, not a synonym for the criterion above** — that one sweeps the pattern *model*, this sweeps the fact that the *symbols* stop existing. **Guidance citing a deleted symbol is worse than guidance citing a deleted model**: the reader greps, finds nothing, and cannot tell whether the rule is stale or they are looking in the wrong place. ⚠️ **Scoped deliberately** — the verdict names are the archived sibling's already-swept deletions, so greping them here would pass on work this file did not do (the defect the *No identifier still names the old model* criterion says it avoids). ✅ **Measured 2026-08-12, so size it honestly: the genuine live target is ONE file** — `.claude/rules/testing.md:31`, which carries `matchAccess` *and* `authScopePattern` inside a worked example and is currently buried.
- **`website/docs` teaches the new model.** `grep -rniE 'authscopepattern|matchaccess|scopeadmin|isadmin' website/docs`, then `cd website && npm run check-examples`. ⚠️ **Not just the claim name** — the deleted grammar is taught in **prose and mermaid diagrams**, which no `@check-example` guards (`documentation.md`: *the checker guards fenced blocks, never prose*), and `auth-flows.md` alone diagrams the KV record and the discover response. ⚠️ `check-examples` is in neither CI, `npm test`, nor `test:doc`, so nothing else will run it — and this surface has already shipped one wrong instruction from this task's landed half.
- **`docs/vision/auth.md`'s one gap-blockquote for THIS file is REWRITTEN, not deleted.** ⚠️ **Stated STRUCTURALLY:** no `> **Today's code differs.**` blockquote describes behaviour this task builds — but the one in § *Coarse-grained access control* names **two** gaps and this task closes only the first two clauses of it. 🚨 **Deleting it would ship an `accepted` doc asserting parity the code lacks:** the predicate block says the reserved scope is the ROOT and *every* scope is at or below it, while `nebula-do.ts`'s **name reservation still refuses every call to a node named `nebula-platform`**, which this file retains deliberately (§ *Known sites*). ⇒ Rewrite it to name that one remaining divergence and point at whatever binds the name, per the name-reservation criterion below. ⚠️ **Verify the SECTION before editing, not the line:** `R1`–`R7` live in § *The layers a call passes*, **not** § *Coarse-grained access control* — a miscitation the sibling carries in two places and the archived route-pipeline file froze. This file's own citations are correct; confirm rather than inherit.
- 🔒 **The predicate block is byte-identical in all three files.** Extract the fenced block beginning `isAtOrAbove(myScope, node)` from § *Objective*, `docs/adr/015-passage-and-dominion.md` and `docs/vision/auth.md`; `sort -u | wc -l` over the three must be **1**. *Reds against a one-sided edit to the definition three files depend on* — which no other criterion can see, since every sweep greps tokens the new block does not contain.
- 🔒 **`coding-style.md` carries no ⏳ on symbols this file landed.** That rule marks `isAtOrAbove`, `isAtOrBelow` and `hasPassage` as not-yet-in-source, names this task file, and binds *delete the ⏳ when it does*. Afterwards `grep -n '⏳' .claude/rules/coding-style.md` returns only the route-guard sibling's entries. *Reds against landing symbols while the always-loaded rule still tells readers they cannot be grepped.*
- **`nebula-pre-alpha.md`'s Super-admin building block states the new claims shape.** Its row still gives `authScopePattern:'*'` and *"`matchAccess('*', …)` always true"* as the mechanism; both go. ⚠️ That file is the **living master**, read every session, so leaving it on the deleted model is the highest-traffic version of the unlearning cost this task exists to stop paying.
- **No active task file states dominion as pattern-coverage.** ⚠️ **Stated over the ASSERTION, not the token** — [nebula-invite.md](nebula-invite.md) and [nebula-registry-route-guards.md](nebula-registry-route-guards.md) legitimately *name* `authScopePattern` as the thing being replaced, so a criterion greping the token alone reds against correct files. Triage with `grep -rln 'authScopePattern\|pattern-covers\|matchAccess\|buildAuthScopePattern' tasks/*.md tasks/on-hold/*.md tasks/icebox/*.md` (frozen `tasks/archive/` is exempt) — ⚠️ **the narrower `authScopePattern`-only form misses siblings whose only hits are `matchAccess`**, including `nebula-profile-accepted-membership-gate.md` and `icebox/nebula-scoped-helper-naming.md`. Then read each hit: it passes if it describes the pattern model as the thing being deleted, and fails if it states in the present tense that dominion IS pattern-coverage. 🚨 **Second limb, and the one no pattern-grep can see: no active task file may treat the platform scope as sitting OUTSIDE the hierarchy, or assert that a containment site needs a platform disjunct.** [nebula-registry-route-guards.md](nebula-registry-route-guards.md) does both today — it says `isAtOrAbove('nebula-platform','{u}')` is *"false under any honest hierarchy comparison"*, which the root model makes true, and instructs a builder to add the disjunct § *Decisions* calls dead code. ⚠️ **The glob must cover `on-hold/` and `icebox/`** — `nebula-collaborator-tiers.md` moved to `on-hold/` on 2026-08-09, so a bare `tasks/*.md` passes *because the file left the glob*. ⚠️ **Run it before sizing the work** — the hit set is wider than the files this one names.

## Phases

**Four phases, ordered by dependency, not by risk.** ⚠️ **The Registry's borrowed-safety defect is NOT fixed here** — it belongs to [nebula-registry-route-guards.md](nebula-registry-route-guards.md), which lands after. Fixing it needs `hasPassage`, which needs the verdicts, which need the claim to carry the scope, so doing it earlier means writing it twice; and it is safe to wait: the sibling argues that gate is not an active risk, and invites are paused.

Each phase leaves the suite **no worse than the recorded baseline** — ⚠️ **not "green"**: the suite is RED today. Measure as a **delta** — same set passes, no NEW failure, no NEW skip. Baseline and the named tests live in [backlog.md](backlog.md) § *Testing & Quality*; do not restate the numbers here. A phase is done when its own criteria red against the pre-phase code and pass after. Every criterion is allocated except ✅ *No `isAdmin` survives*; 🔒 *A superuser is unchanged everywhere* spans Phases 1 and 3.

### Phase 1 — The claim carries the scope, and containment becomes a hierarchy predicate

The spine; every later phase reads `access.authScope`. **Atomic** — a half-renamed claim is a broken system, so one commit despite being the largest diff.

- The four symbol changes, per § *The target*'s table: `access.authScopePattern` → `access.authScope`; `buildAuthScopePattern` deleted (⚠️ keeping its `parseId` fail-closed job — § *Context and current state*); `matchAccess` → `isAtOrAbove` / `isAtOrBelow`, exported from `index.ts` and `testing.ts`; the reserved scope becomes `nebula-platform` verbatim.
- The three **structural** sites convert here, since they need a fact and not a verdict — each calling **`isAtOrAbove`**, none gaining a `scopeAdmin` conjunction. ⚠️ **Bare `isAtOrAbove` at any of them breaks a superuser, silently and differently each time — § *Known sites*.**
- Every fixture on the old sentinel moves to `authScope: 'nebula-platform'` — size it with the grep in § *Known sites*.
- 🚨 **The MINT's containment expressions convert here too — verdict-for-verdict.** Deleting `buildAuthScopePattern` forces them; there is no ordering in which they wait for a sibling. Pinned, so nobody re-derives them from a later file's deletion table:

  | Site | Becomes | Note |
  |---|---|---|
  | `worker-token.ts:338`+`:339` | `isAtOrAbove(record.universeGalaxyStarId, body.activeScope)` | the refresh confine; the local disappears with the pattern |
  | `:507` | `isAtOrAbove(payload.access.authScope, body.activeScope)` | the caller bound |
  | `mintNarrowerToken`'s subject bound — `const subjectPattern = buildAuthScopePattern(subjectIdentity.universeGalaxyStarId)` and the `matchAccess` on the next line (~`:553`/`:554`) | `isAtOrAbove(subjectIdentity.universeGalaxyStarId, body.activeScope)` | 🔒 **RETAINED — reworded IN PLACE, never deleted, not even briefly.** ⚠️ **Anchor on the quoted fragment, not the number** — this commit deletes lines above it, and this is the one row whose mis-resolution is dangerous |
  | `:575` | `authScope: body.activeScope` | verdict-identical — the pattern was only ever derived from this value. ⚠️ **NOT the subject's scope**, though § *Decisions* says a derived token should carry it: that is the sibling's change, and making it here would move a mint verdict inside a phase that promises not to |
  | `access-claims.ts:89` + its mint-only override option | renamed, **not removed** | the capability stays; only the claim's name changes |
  | `router.ts:359` | `isAtOrAbove(payload.access.authScope, instanceName)` | verdict-identical — containment only, **no `scopeAdmin` conjunction added**. The route GATE's shape is the sibling's; its containment EXPRESSION converts here because `matchAccess` stops existing |

  🚨 **`:554` is the one that must not be deleted, and the reason is not visible at the call site.** It bounds `activeScope` inside the **subject's** scope, which is what makes every intermediate state safe while `:575` still derives from `body.activeScope` — without it the mint issues an unbounded `authScope` from a caller-chosen value. ⚠️ **It is textually near-identical to `:507`, and [nebula-registry-route-guards.md](nebula-registry-route-guards.md) marks `:507` and `:569` DELETED while marking `:554` 🔒 RETAINED** — a file that builds *after* this one. Do not reach for that table during Phase 1; the row you need is above. (That sibling's own § *The mint asks one question* records an earlier draft getting exactly this backwards.)
- 🚨 **The MESH-boundary and Profile containment expressions convert here too, for the same forcing reason** — they call `matchAccess`, which stops existing. Each is verdict-identical, so none of them moves this phase off its promise:

  | Site | Becomes | Note |
  |---|---|---|
  | `nebula-do.ts:130` — `const pattern = buildAuthScopePattern(name)` | a parse-and-throw holding no pattern | its job **here** is the unparseable-tier refusal, which this phase's first bullet already promises to keep; the local goes with the pattern |
  | `nebula-do.ts:144` — `matchAccess(pattern, aud)`, `requirePassage`'s tenant arm | `isAtOrAbove(name, aud)` | verdict-identical — the same two inputs, spelled with the new symbol, since `matchAccess(buildAuthScopePattern(name), aud)` **is** *"aud is name or beneath it"*. 🚨 **The verdict moves in Phase 2**, when the second argument becomes `access.authScope`; that change is § *Context and current state*'s item 2 and pulling it forward here would break this phase's promise |
  | `profile.ts:309` — `scopes.some((s) => matchAccess(pattern, s))` | `scopes.some((s) => isAtOrAbove(claims.access.authScope, s))` | verdict-identical; the `pattern` local at `:308` is one of the six § *The target* renames |

- 🚨 **The two VALUE-coupled sites convert here too, and the compiler will not tell you** (M3). `myScopeTree`'s unbounded-query arm and `profile.ts`'s no-registry-read arm are coupled to the claim **by value** — `'*'` and `.endsWith('.*')` — not by symbol, so deleting `buildAuthScopePattern` leaves them compiling and wrong. Left to Phase 3 they would spend two phases broken: after Phase 1 a superuser enumerates **one** scope and loses the zero-registry-read Profile path. ⚠️ **Neither converts to a predicate call** — both are work-avoidance short-circuits that exist precisely to avoid fetching what the predicate needs (§ *The target*). They become `isPlatformScope` identity tests against the new claim.
- 🔀 **Mirror-edit owed to the sibling:** its per-site table is written in pattern vocabulary and against `:507`/`:554`/`:575` line shapes that stop existing after this phase. Flag it there rather than silently leaving it stale — covered by *No active task file states dominion as pattern-coverage*.

**Criteria:** 🔒 *Segment boundaries hold under colliding names* · *No `authScopePattern` survives* · 🔒 *The mint's verdicts are unchanged* · 🔒 *A superuser is unchanged everywhere* — **its `myScopeTree` and zero-registry-read Profile limbs now land here too**, with the verification and refresh ones, since the value-coupled sites moved into this phase.

### Phase 2 — The two verdicts exist, and the mesh boundary calls them

- `hasDominionOver`'s containment half becomes `isAtOrAbove`; `hasPassage` is new. Both shapes, argument order, and the fail-closed contract: § *The target*.
- `requirePassage` delegates to `hasPassage` instead of computing both arms inline; its tenant arm reads `authScope` instead of `aud`, which kills non-admin downward movement **by construction** (item 2 — no separate work).
- `nebula-do.ts`'s platform-name reject stays, its comment re-justified as a **name reservation** — never "masquerade prevention", which is not what it does (§ *Acceptance criteria*).

**Criteria:** *A non-admin reaches its own scope and nothing beneath it* · *An admin reaches downward, totally* · *Upward is nil for dominion* · 🔒 *The scope is still the bound* · *The tenant direction survives, and confers no dominion* · *Platform means everywhere* · *A mesh call to a node NAMED `nebula-platform` is still refused* · *`aud` survives on the token and on the wire*.

### Phase 3 — The remaining callers, and nothing re-derives

- Convert every remaining hit from instruments **1a and 1b**, classified per § *The instruments that find every site*. ⚠️ **The Profile scoped-admin branch and `myScopeTree`'s hand-rolled dominion convert HERE; their platform short-circuits do NOT** — those are value-coupled and moved to Phase 1, because the compiler cannot force them and a superuser is broken in between.
- ⚠️ **`star.ts` is RENAME-ONLY and MUST NOT become hierarchical.**
- 🌐 The `/live` scenario runs here, after all code lands — **including its superuser limb**, since the 19 `'*'` fixtures are being rewritten in the same work and are no backstop.

**Criteria:** 🔒 *Nothing re-derives either verdict* · 🔒 *The instruments are capable of failing* · 🔒 *No identifier still names the old model* · *Enumeration is unchanged for an admin* · the remaining limbs of 🔒 *A superuser is unchanged everywhere* (`myScopeTree`, and the Profile gate with **zero registry reads**) · 🌐 *The same, driven as a `/live` scenario*.

### Phase 4 — Standing guidance and published docs

Code is done; this is the unlearning-cost half. Every target, grep and trap is a criterion above — this phase is the sweep. What it owes beyond the greps:

- **ADR-015** — its normative sentences were already restated off pattern vocabulary, and the rejected-alternative row already carries its retirement note (annotated, not deleted). ✅ **The predicate block and the platform-root prose LANDED 2026-08-12** in both ADR-015 and `auth.md`, byte-identical to § *Objective*'s. ✅ **Measured remainder in ADR-015: exactly two edits.** (a) Delete its single `> **Today's code differs.**` blockquote — the only pattern-model reference left in the file — when the code lands; confirm with `grep -n 'authScopePattern\|wildcard' docs/adr/015-passage-and-dominion.md` rather than trusting this. (b) Clause 3 says *the live symbol is `hasDominionOver`* — singular, where `hasPassage` joins it once Phase 2 builds. ⚠️ **The predicate block is the definition three files depend on — never edit it in one and not the others.** ⚠️ Ratification is not part of this phase, or any phase.
- **`security.md` and ADR-015 must state at the site that the bare `scopeAdmin` bit is never dominion alone** — that sentence becomes *more* load-bearing after this change, since the bit becomes the only distinguishing claim.
- **Sibling task files**, each a named deliverable. ⚠️ **All are accurate against today's code and become wrong only when Phase 1 lands** — which is why they are swept here rather than earlier, unlike the always-loaded rules that today's ADR edits already falsified.
  - `nebula-registry-route-guards.md` — its per-site table is anchored on `worker-token.ts` line shapes that stop existing, **and** it asserts `isAtOrAbove('nebula-platform','{u}')` is false *"under any honest hierarchy comparison"*, which the root model makes **true** — instructing a builder to add the disjunct § *Decisions* calls dead code.
  - `nebula-profile-accepted-membership-gate.md` — its criterion is written over the deleted `'*'` literal and must be re-worded over **behaviour** (*a platform admin passes with zero registry reads*); it also nominalises `reach`, which goal 3 forbids.
  - `on-hold/nebula-collaborator-tiers.md` · `nebula-invite.md` — pattern vocabulary throughout.
  - `icebox/nebula-scoped-helper-naming.md` — a worked example arguing cross-tenant safety through `matchAccess('12345.*', …)`. ⚠️ **Its conclusion survives; only its mechanism dies** (`isAtOrAbove('12345','acme.app.tenant-a')` is false too), so this is a one-line rewrite, not a re-derivation. Icebox is parked, not frozen — `tasks/archive/` is the only exempt tree.
- ✅ **`security.md`'s rule (2) is ALREADY NARROWED — "restate it in scope terms" is the pre-collapse plan and MUST NOT be carried out** (§ *Constraints*).

**Criteria:** *No standing-guidance statement forbids narrowing the tenant branch* · *No standing-guidance statement describes the pattern model* · 🔒 *No standing-guidance statement CITES A DELETED SYMBOL* · 🔒 *The predicate block is byte-identical in all three files* · 🔒 *`coding-style.md` carries no ⏳ on symbols this file landed* · *`website/docs` teaches the new model* · *`docs/vision/auth.md`'s one gap-blockquote for THIS file is closed* · *`nebula-pre-alpha.md`'s Super-admin building block states the new claims shape* · *No active task file states dominion as pattern-coverage*.

## Non-goals

- 🔀 **The verdict RENAMES** (`requireAdmin`, `hasAdminOver*`, `enforceScopeReach`, the `accessAdmin` column) → [nebula-dominion-vocabulary-rename.md](archive/nebula-dominion-vocabulary-rename.md), **already built and archived**, so those symbols are gone from the tree before this starts.
- 🔀 **The Registry route guards, and the mint's authorization SHAPE** — which checks exist there, and in what order → [nebula-registry-route-guards.md](nebula-registry-route-guards.md). This file gives it `hasPassage` and the two containment predicates; it gives the routes a caller. ⚠️ **The mint's containment expressions are NOT a non-goal here** — Phase 1 rewrites them, verdict-for-verdict (§ *Constraints*).
- 🔀 **WHO may invite, and what bit an invite confers** → [nebula-invite.md](nebula-invite.md). This changes what a token derives from a membership, not how memberships are made.
- **Changing anything stored.** `Memberships` and the KV refresh record are already scope-shaped and stay exactly as they are.
- **Deciding what, if anything, lives AT the platform scope.** The root position makes it reachable from everywhere — that is a **passage** fact and nothing more, never a placement argument. Whether global standing guidance ends up in a DO, in edge KV, or as constants in code is undecided, and this file takes no position. Whatever eventually occupies the name is what retires `nebula-do.ts`'s reservation; until then that reject stays.
- **Putting `activeScope` in the browser URL.** [ADR-017](../docs/adr/017-the-url-is-the-view-state.md) wants it there and today's code falls short — a real gap, but a **UI-layer** one with no bearing on either verdict, since both read `authScope`. Do not pull it in here.
- **Narrowing the export surface.** The replacement predicates are exported from both `index.ts` and `testing.ts`, as `hasDominionOver` is today — the Node harness re-exports it precisely so a test never re-inlines the `scopeAdmin` ∧ scope-at-or-above conjunction by hand.
- **The collaborator grant bundle** → [nebula-collaborator-tiers.md](on-hold/nebula-collaborator-tiers.md).
- **A multi-invite-in-one-email flow** — the answer to the N-emails friction if it ever bites; years away.

## Relationships

- **Amends [ADR-015](../docs/adr/015-passage-and-dominion.md)** — commitment intact, mechanism replaced.
- **Soft-blocks** [nebula-invite.md](nebula-invite.md) — nothing in the invite mechanism depends on this, but that file's design intent and tier-coverage criterion are written in pattern vocabulary, so reviewing it first would anchor a panel on a model this deletes.
- **Already relied upon by** [nebula-collaborator-tiers.md](on-hold/nebula-collaborator-tiers.md) — its open question 1 was closed on this file's basis (2026-08-05) and its downward-passage section rewritten to depend on this landing, so shipping a different model reopens that file. It still carries the deleted vocabulary in at least three places, covered by the active-task-file criterion.
- **Touches** [nebula-profile-accepted-membership-gate.md](nebula-profile-accepted-membership-gate.md) at **two** sites in `#requireOwnerOrAdmin`, not one: `profile.ts:309`'s `scopes.some(s => matchAccess(pattern, s))` becomes the new predicate, and `profile.ts:274`'s `authScopePattern === '*'` super-admin short-circuit loses the literal it tests. The accepted-membership contract is unaffected, but that file's criterion is written over the deleted literal and must be re-worded over **behaviour** — *a platform admin passes with zero registry reads*.
- **Not related to the wipe.** Listed in [nebula-pre-alpha.md](nebula-pre-alpha.md) for sequencing only.
