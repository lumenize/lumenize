# Mesh — client `callAsync` (resilient Promise-returning client call + abort)

**Status**: **📋 DRAFT — not started; the next thing after [`mesh-continuation-only-calls.md`](mesh-continuation-only-calls.md)** (it completes that task's client-side model). Ready for `/review-task`. Working name `callAsync` is provisional — see D1.

**Packages**: `packages/mesh/` (`LumenizeClient`), then `apps/nebula/` (collapse the hand-rolled bridges onto it).
**Related**: [`mesh-continuation-only-calls.md`](mesh-continuation-only-calls.md) (built the client 4-arg in-heap leg, D16/D17, that this promotes to a primitive), [`on-hold/mesh-call-durable.md`](on-hold/mesh-call-durable.md) (the *guaranteed* tier; `callAsync` is the best-effort rung below it and its foundation — D9 client-side).

---

## Why now (the trigger)

The continuation-only migration deliberately kept the **client** model minimal — a bare in-heap handler keyed by `callId` (D16) — and suppressed any richer client abstraction until a real use case appeared. This mirrors why `callDurable` was moved to its own on-hold file: **wait for the use case, don't speculatively build the client's divergent model.**

`callDurable`'s use case never materialized. **`callAsync`'s did**, during Phase 2:
- `client.orgTree.*` mutators need a `Promise<T>` with **reject-on-failure** (doc-blessed via a `for-docs` test + `.rejects.toThrow()`-tested). To keep that contract on the continuation-only model, Phase 2 hand-rolled `#orgTreeMutate` + `#pendingMutations` + `handleOrgTreeResult` in `apps/nebula/src/nebula-client.ts`.
- That is the **second** copy of the same shape: `#readResource`/`handleReadResponse` (and the transaction path `handleTransactionResult`) already hand-roll a `requestId`-keyed Promise-settled-by-a-pushed-handler.

**Two copies is the signal** — and an undercount: a `grep -n '#pending' apps/nebula/src/nebula-client.ts` finds **four** bridges of this shape (the two one-shot pairs above, plus chat and subscribe — Phase 2 enumerates which migrate). Left un-factored, every new client SDK read/mutate re-hand-rolls a `requestId` map + a settler handler. Per CLAUDE.md ("Nebula fighting a package API — missing capability *or* awkward ergonomics — is product feedback for the package; surface it, don't quietly work around it"), the bridge belongs in Mesh.

Plus it closes a real gap the bridge left open (below).

---

## The gap this also closes (the hang)

The Phase-2 client bridge is resilient to the common failures that killed awaited `callRaw` — the in-heap settlers survive a tab **freeze** and a **WS reconnect**, and delivery **re-resolves to the current socket by `instanceName`** (D17). But two residual cases leave the Promise **hanging until reload**:
- a **freeze longer than the Gateway's zero-socket grace window** → the RESULT is dropped, nothing settles the Promise;
- any case where the RESULT is genuinely lost (callee acked-then-vanished, D6 row 4).

Both are heap-bounded (no server leak) and eventually recovered by **D8 reconcile on reload** — but the *live* UI just spins. The deferred mitigation was "an optional UI soft-timeout." **`AbortSignal` is that mitigation, done as a web standard** (see D3).

---

## Objective

Promote the ad-hoc client 4-arg→Promise bridge to a first-class **`LumenizeClient.callAsync<T>(...)` : Promise<T>** primitive:
- settled by the D16/D17 mechanism (in-heap, re-resolvable delivery) — resilient by construction;
- **`AbortSignal`-cancellable** so the wait can be bounded/cancelled deterministically instead of hanging to reload;
- **client-only** — the D16 asymmetry: a DO/Worker can't hold a heap Promise across hibernation, so it never offers "await a result" (it uses the traveling handler). `callAsync` exists ONLY on `LumenizeClient`.

Then collapse the Nebula hand-rolled bridges onto it.

---

## Design

### The primitive
```ts
// LumenizeClient
callAsync<T = any>(
  calleeBindingName: string,
  calleeInstanceNameOrId: string | undefined,
  remoteContinuation: Continuation<T>,
  options?: CallOptions & { timeoutMs?: number; signal?: AbortSignal },  // timeoutMs default 30_000; 0/Infinity disables
): Promise<T>;
```
- Generates a `callId`, stores `{ resolve, reject, signal, onAbort }` in a **new `#pendingAsyncCalls` map keyed by `callId`** (parallel to `#inHeapHandlers`; `callAsync` settles a Promise, it has no handler *chain*).
- Sends the CALL with `expectsResult: true` (reusing the D17 RESULT path).
- **`#handleCallResponse(message)` settles by `callId`**: success → `resolve(postprocess(result))`; `{$error}` → `reject(Error)`. (It already handles `#inHeapHandlers`; extend it to also check `#pendingAsyncCalls`.) The delete-on-delivery IS the dedup (a duplicate RESULT finds no entry — same as today).
- **No separate `requestId`, no user handler method** — this is why the Mesh version is cleaner than the Nebula hand-roll (which is built on the *public* 4-arg API, so it needs its own correlation id + a public `@mesh`-free handler).

Resilience is inherited, not re-implemented: in-heap survives freeze + reconnect; the Gateway re-resolves the RESULT to the current socket by `instanceName` (D17); reconnect mid-flight still settles.

### Abort (D3)
- `options.signal?: AbortSignal`. On abort **before** the RESULT: reject with `signal.reason` (a `DOMException`/`AbortError`), delete the `#pendingAsyncCalls` entry, remove the abort listener. A RESULT arriving after abort finds no entry → dropped (existing no-handler path).
- If `signal.aborted` is already true at the call site: reject immediately, don't dispatch (matches `fetch`).
- On normal settle: remove the abort listener too (no leak).
- **Abort cancels the WAIT, not the server OPERATION** — the call already left one-way, so the callee may still run. Retry-after-abort is safe ONLY for idempotent ops (client-supplied UUID / ADR-005 eTag), which the model already assumes for D8. Document loudly at the API.
- **Built-in default timeout (D4):** `timeoutMs` (default `30_000`; `0`/`Infinity` disables) composes with any caller `signal` via `AbortSignal.any([signal, AbortSignal.timeout(timeoutMs)])` — so the common path can't hang, and `signal` stays free for external cancel (unmount, user-cancel). Because the primitive is public (D2), the default is what makes the escape hatch safe-by-default.

---

## Decisions (to resolve in `/review-task`)

| # | Decision | Lean | Status |
|---|---|---|---|
| D1 | **Name** → **`callAsync`**. | **PINNED 2026-07-03 (Larry).** Rejected `callForResult` (under-distinguishes from 4-arg `call`, which *also* delivers a result) and `callDurableAsync` (overloads "Durable" for a best-effort thing; reserve `callDurable*` for the guaranteed tier). `-Async` = the Promise-returning variant, the widely-understood read. **Deciding factor: the name forces the Phase-3 ADR-003 amendment to spell out why the *client* environment makes an awaitable the right choice there** (heap-durable + re-resolvable fire-back, not socket-bound request/response — that callRaw contrast is the amendment's deliverable, owned there, not restated across this file). | **PINNED** |
| D2 | **Public or protected.** | **PINNED 2026-07-03 (Larry): PUBLIC on `client.lmz`** (the `LmzApiClient` surface, alongside `call`). Parity with the already-public `client.lmz.call` (App.vue uses it for wipe/teardown); MIT reuse (other client SDKs build on it); a legit app/UI escape hatch. Doc it "prefer higher-level SDK methods (`client.resources.*`) when they exist." Does NOT breach the bright line (client SDK surface, not raw `lmz.call`/platform/DO). | **PINNED** |
| D3 | **Abort** = fetch-style `AbortSignal`; abort rejects the wait (not the op). | As designed above. | PINNED (this task's reason to exist) |
| D4 | **Default timeout** (gated on D2=public — a public escape hatch with no default re-arms the exact hang `callAsync` exists to kill). | **PINNED 2026-07-03 (Larry): built-in default `timeoutMs: 30_000`, overridable; `0`/`Infinity` disables (rare long awaits); a caller `signal` is ADDITIVE (composed, not replacing) → internally `AbortSignal.any([signal, AbortSignal.timeout(timeoutMs)])`.** 30s matches the retired `CALL_RAW_QUEUE_TIMEOUT_MS` (and the still-live `CLIENT_CALL_TIMEOUT_MS`, `lumenize-client-gateway.ts:61`, which gates the mesh→client push — NOT retired). A public primitive must be safe-by-default: the built-in default is what makes the escape hatch un-hangable. SDK wrappers (`orgTree.*`, reads) inherit it for free (no per-wrapper timeout). | **PINNED** |
| D5 | **Migration scope + fire-back pattern.** `#orgTreeMutate` maps trivially (already the 4-arg framework fire-back / Pattern C). `#readResource`/`handleReadResponse` + the transaction path use **Pattern R** — the Star EXPLICITLY fires `handleReadResponse`/`handleTransactionResult(requestId, result)` — NOT the framework fire-back. Their surviving special logic (ontology-stale → `onShouldRefreshUI`; `mapTransactionResult`) moves onto `.then()/.catch()` of the returned Promise or a thin wrapper (the submit-gate is **retired**, not relocated — D7). | **PINNED 2026-07-03 (Larry): v1 = the three one-shot request→result bridges** (orgTree + read + transaction); chat + subscribe are out (enumerated at Phase 2). Do it while the model is loaded — a deferred "unify later" is a standing tax on human concentration (reviewer-attention-is-the-bottleneck) worse than the one-time risk. **⚠ Guardrail (non-negotiable):** read/transaction are the working, green, critical resource path — migrate under `/refactor-efficiently` `.only` discipline (one representative test green on `callAsync` first), keep their existing tests as the safety net, and **mutation-check that the surviving special-case logic still fires** (ontology-stale→`onShouldRefreshUI` on both read+transaction, `mapTransactionResult`). **Fire-back pattern → (a), PINNED 2026-07-03 (Larry):** unify onto Pattern C — `Star.read`/`Star.transaction` *return* the value and the framework fires it back via D17, dropping the `deliverReadResponse`/`deliverTransactionResult` seam. (b) — `callAsync` also supporting Pattern R (server-fires-named-handler-by-requestId) — is **rejected**: D17 is the mechanism THIS task built, so (a) *retires* the pre-D17 hand-roll rather than preserving it. **Targets = `Star.read`/`Star.transaction` AND `DevStudio.read`/`DevStudio.transaction`** (the `@mesh` Handler-1 fire-back methods — they share the `ResourceDataPlane`), **NOT `Resources.read`** (the SQL data-plane read, called server-side by `subscriptions.ts` — out of scope). **The read/transaction path is dual-homed (B1):** the client is ONE path routed by `#resourceHostBinding` (`nebula-client.ts:294`) and the `deliver*` seam is shared (`ResourceHostBridge`, implemented by *both* `star.ts:65-70` and `dev-studio.ts:158-161`), so migrating the client to `callAsync` forces **both** hosts to return-the-value — **DevStudio's resource Handler-1 + bridge migrate in LOCKSTEP** (small: its data-plane is identical to Star's; only DevStudio *codegen* stays deferred). **Two-exit gotcha (m3):** each Handler-1 has a version-gate that on stale currently self-fires `handleReadResponse`/`handleTransactionResult(OntologyStaleError)` and returns void — under (a) that branch instead **returns the `OntologyStaleError`** (the value the framework delivers) and `doRead`/`doTransaction` **return** their result up to Handler-1 (not `bridge.deliver*`). Read **rejects** on stale; transaction **resolves** with `{ontologyStale}` — the asymmetry is intentional, preserve it. The delivered Error must preserve `name`+props so `isOntologyStaleError` fires client-side. **Precondition:** DevStudio is a *second client-facing consumer* of these handlers (so it's in scope, not an out-of-scope caller); confirm no *other* caller relies on the explicit fire-back. | **PINNED** |
| D6 | **Relationship to `callDurable`.** | `callAsync` = best-effort client tier (heap-durable + re-resolvable + abort-to-unblock). The only property to preserve: `#pendingAsyncCalls` is a **swappable settlement registry**, so a future client-side `callDurable` can substitute a persisted store without reshaping `callAsync`. **Keep that seam open**; don't foreclose it. (The client-`callDurable` design itself — primary path = reconcile against a server-side durable record, IndexedDB only as a last resort — is owned by [`on-hold/mesh-call-durable.md`](on-hold/mesh-call-durable.md); don't re-derive it here.) | PINNED |
| D7 | **Transaction submit-gate → RETIRE (not relocate).** | **PINNED 2026-07-03 (Larry).** The client submit-gate (`#inFlightSubmit`/`#submitGate`/`#pumpSubmitGate`) serializes transactions to at-most-one-in-flight for **one** documented reason (`nebula-client.ts:333-338`, `:698-700`): the `handleTransactionResult` channel is **uncorrelated** (no `requestId`). `callAsync` correlates every call by `callId` → that reason dissolves. Same-resource **ordering** is owned by the **debounce per-key queue** (`debounce.ts` `openBatches`, client-side before submit); **no-double-commit under concurrency** is owned by **`resources.ts` Step 4.5a/6.5** (the authoritative replay re-check *inside* `transactionSync`, `:468`) + ADR-005 forward-only eTags — NOT the gate. So on migration **delete** the gate rather than porting it. ⚠ The invariant a concurrency test must not regress is **Step-6.5 replay-inside-`transactionSync`** (`resources.ts:460-468`): if a refactor ever moved replay detection out of `transactionSync`, gate removal would open a same-`newETag` double-commit window. (`#pendingReads` already proves the correlated, gate-free shape.) | **PINNED** |

---

## Non-goals
- **DO/Worker `callAsync`** — a DO's heap dies on hibernation, so it never awaits a result (it uses the traveling handler, D3 of the parent task). `callAsync` is `LumenizeClient`-only, by construction.
- **Guaranteed delivery** — that's `callDurable` ([on-hold](on-hold/mesh-call-durable.md)). `callAsync` is best-effort (reload → D8 reconcile).
- **Cancelling server-side work** — abort is wait-only (D3).

---

## Phases

### Phase 1 — Build `callAsync` + abort in Mesh
> ✅ **COMPLETE (2026-07-03).** `LumenizeClient.callAsync` on the `LmzApiClient` surface (D2); `#pendingAsyncCalls` map + `#handleCallResponse` extended to settle Promises by `callId` (checked before `#inHeapHandlers`); abort via `AbortSignal.any([signal, AbortSignal.timeout(timeoutMs)])` (D3/D4). **`AbortSignal.any`/`.timeout`/`.abort()` confirmed available under vitest-pool-workers** (m4 open question resolved — the tests exercise all three). **Two additions beyond the spec, both sensible:** (1) `disconnect()` now **rejects** in-flight `callAsync` Promises (they have an awaiting caller, unlike the fire-and-forget `#inHeapHandlers` it drops — otherwise a `using`-disposal awaiter hangs to timeout); (2) a `protected pendingAsyncCallCount()` test-only accessor — the delete-on-abort/delete-on-delivery no-leak properties are behaviorally invisible (a settled Promise no-ops a second settle), so the count is the transient surface the tests assert (mutation-verified: gutting delete-on-delivery reddens 3 tests). **Verified: mesh `main` 402/402, type-check clean, 0 new error artifacts** (the 13 workerd unhandled-rejection artifacts all pre-exist in alarms/call-target-validation/lumenize-do/lumenize-worker — none from the client).
- `LumenizeClient.callAsync` + `#pendingAsyncCalls`; extend `#handleCallResponse` to settle it by `callId`.
- `AbortSignal` support per D3 (pre-aborted, abort-before-result, cleanup-on-settle, drop-late-result).
- **Tests (capable-of-failing each — assert the TRANSIENT surface, not a self-healing end-state):**
  - settles on success RESULT; rejects on error RESULT (`$error` → Error).
  - **abort rejects + the `#pendingAsyncCalls` entry is gone + a subsequent RESULT is dropped** — assert via a `resolve`/`reject` spy called **exactly once** (m4: a naive "no double-settle" passes even if delete-on-abort is removed, since a 2nd settle silently no-ops on a settled Promise); **mutation-check** by removing the delete-on-abort (the test must go red). Pre-aborted signal rejects without dispatch.
  - **on normal settle the abort listener is removed** (spy `removeEventListener` — "no leak" has no other observable end-state).
  - **timeout rejects** — drive it **directly through `callAsync`** with a never-answered RESULT + a small **real** `timeoutMs` (fake timers may not patch the native `AbortSignal.timeout` under workerd; the in-repo pattern is a real small value + `vi.waitFor`, `lumenize-client-gateway.test.ts:517`), asserting the reject reason is the `AbortSignal.timeout` `DOMException`. **Confirm `AbortSignal.any`/`.timeout` are available under vitest-pool-workers before pinning them (D4).**
  - **client survives a WS reconnect (M1 — test the CLIENT-heap re-settle, not the Gateway):** drive it via the `TestClient` + `createMockWebSocketClass()` harness (`lumenize-client.test.ts:733-807`) — script `ws1` close → `client.connect()` → `ws2` delivers the RESULT → the `#pendingAsyncCalls` Promise settles. The parent's Flow-C harness (`lumenize-client-gateway.test.ts:441-478`) drives `gateway.__handleResponse` against raw sockets with **no client in the loop**, so it proves Gateway re-resolution, **NOT** the client-heap property — keep it only as the complementary Gateway-side proof.
  - duplicate RESULT dropped (dedup). Remove the now-doubly-dead client queue machinery notes if any remain.

### Phase 2 — Collapse the client's one-shot request→result bridges (orgTree + read + transaction)
> ✅ **COMPLETE (2026-07-03).** All three migrated onto `callAsync`; **submit-gate RETIRED (D7)** (`#submitGate`/`#inFlightSubmit`/`#pumpSubmitGate` + `handleTransactionResult`/`handleReadResponse` + `#pendingReads`/`#pendingMutations`/`PendingMutation`/`PendingRead` all deleted); **DevStudio resource Handler-1 + bridge migrated in LOCKSTEP (B1)**; `deliverReadResponse`/`deliverTransactionResult` seams dropped from the shared `ResourceHostBridge` + both hosts. `Star`/`DevStudio` `read`/`transaction` now **return** the value (framework fire-back, D17); the read-vs-transaction stale **asymmetry** is preserved (read THROWS `OntologyStaleError`→reject→`.catch` fires `onShouldRefreshUI`; transaction RETURNS it as a VALUE→resolve→`{ontologyStale}`). **`callAsync` return type widened to `Promise<Awaited<T>>`** (first async callee — the framework awaits the callee before firing back).
> **⚠ NEW DECISION — D8 (queue reconnect, Larry-approved "(A)"):** the debounce queue **no longer re-fires a fresh mesh call** on reconnect (`debounce.ts` `setConnectionState`). The in-flight `callAsync` re-resolves on its own (D16/D17); the queue keeps the SAME attempt (so the original submit's `.then`/`.catch` settle it) and just **re-arms the in-flight timeout** (`armTimeout`) as the lost-RESULT backstop. This removes the redundant fresh call + orphan risk that the old gate-clearing masked. (The alternative — dedup `#meshSubmit` by newETag — was rejected in favor of the smaller, honest model.)
> **Verified:** nebula **baseline 303/303** (302 + the new orgTree timeout test) on **both Star and DevStudio**; **frontend+unit 199/199** (8 queue-reconnect unit tests rewritten for D8/(A) + mutation-checked — reverting `armTimeout`-on-reconnect reddens the backstop test); mesh **main 402/402** (the `Awaited` widening didn't regress Phase 1); type-check clean (mesh + nebula). **One flag:** a single workerd "internal error; Uncaught (in promise)" surfaced during the 81s parallel baseline run — no test failed; likely a DO-teardown transient, worth a re-run to confirm.
> **Deferred (out of the pool-workers gate, need `wrangler dev`+chromium):** the two browser **bench** harnesses (`test/browser/harness-client.ts`, `throughput-harness-client.ts`) were migrated to `callAsync` + type-check clean, but the fanout/throughput/gateway-hop **benchmarks themselves need a re-run** to re-validate their timings (the throughput bench's manual resourceId-correlation collapsed into per-`callAsync` correlation — exactly what D7 retires; also fixed a pre-existing 2-arg `transaction()` call there).
**Inventory — enumerate by grep, not prose** (`grep -n '#pending' apps/nebula/src/nebula-client.ts`). Four `resolve/reject`-map-settled-by-a-pushed-handler bridges exist; this task migrates the three **one-shot request→result** ones and explicitly scopes out the other two:
- ✅ `#pendingMutations` (orgTree) · `#pendingReads` (read) · the transaction submit path (`#submitGate`/`#inFlightSubmit` → `handleTransactionResult`) — **in scope** (below).
- ⛔ `#pendingTurns` (DevStudio codegen chat — `trackTurn`/`onChatResult`) — **out**: DevStudio is deferred to the pre-alpha resume, and a minutes-long codegen turn is **subscribe-shaped** ([[nebula-prefer-subscribe-for-live-ui-data]]; the reactive-chat `Message` model owns it), not a one-shot awaitable.
- ⛔ `#pendingSubscribes` (subscribe) — **out**: a persistent-subscription lifecycle (re-resolvable, keyed by `SubscribeKey`), not a one-shot call→result bridge.

**⚠ Server-side, the read/transaction path is DUAL-HOMED (B1):** the client machinery above is ONE path routed by `#resourceHostBinding` (default `'STAR'`; the chat client sets `'DEV_STUDIO'`), and `deliverReadResponse`/`deliverTransactionResult` live on the shared `ResourceHostBridge` implemented by **both** `star.ts:65-70` **and** `dev-studio.ts:158-161` (the same `ResourceDataPlane.doRead`/`doTransaction`). So migrating the client to `callAsync` forces BOTH hosts to return-the-value — **`DevStudio.transaction`/`read` Handler-1 + its bridge wiring migrate in LOCKSTEP** (`dev-studio.ts:584/594` + `:158-163`). Its resource data-plane is identical to Star's, so this is small; only DevStudio **codegen** (chat turns, `#pendingTurns`) stays deferred, NOT its resource CRUD. **Add the DevStudio resource e2e suite to the green-suites gate.**

Migrate the three in-scope bridges onto `callAsync` **under `/refactor-efficiently` `.only` discipline** — get one representative test green on the primitive before rewriting the rest; the existing green tests are the safety net; the read/transaction paths are the **critical resource path**, so mutation-check the surviving special-case logic still fires.
- **orgTree** (trivial, Pattern C): `#orgTreeMutate` → `callAsync`; delete `#pendingMutations` + `handleOrgTreeResult`. `orgTree.*` inherits `callAsync`'s built-in default timeout (D4). Confirm `nebula-orgtree` baseline stays green (incl. `.rejects.toThrow()`) + a timed-out mutation rejects rather than hangs (new test). **orgTree is the clean place to prove the D4 default timeout** — it has NO engine-level timer, unlike the transaction path (whose pre-existing `conflict-outcome.ts:138` engine timeout would let a transaction "rejects on timeout" test pass even if `callAsync`'s timeout were broken).
- **read** (Pattern R → Pattern C per D5): `#readResource` → `callAsync`; **`Star.read` AND `DevStudio.read`** (both `@mesh` Handler-1 methods sharing the `ResourceDataPlane` — *not* `Resources.read`, the server-called SQL read) each **return** the `Snapshot|null|Error` (**both exits**: the ontology-stale branch **returns** the `OntologyStaleError` instead of self-firing `handleReadResponse`; `doRead` **returns** its result up to Handler-1 instead of `bridge.deliverReadResponse`), and the framework fires it back via D17 — dropping the `deliverReadResponse` seam **on both hosts**. **Relocate ontology-stale → `#dispatchOntologyStale`/`onShouldRefreshUI`** onto the `callAsync` `.catch` (read **rejects** on stale; the delivered Error must preserve `name`+props so `isOntologyStaleError` fires client-side). Delete `#pendingReads`/`handleReadResponse`. Concurrent-read correlation is now by `callId`.
- **transaction** (Pattern R, the gnarliest): `submitBatch` → `callAsync`; **`Star.transaction` AND `DevStudio.transaction`** each **return** the `TransactionResult`|Error (both exits funnel to the framework fire-back — the stale branch **returns** the `OntologyStaleError`; `doTransaction` **returns** its result up, not `bridge.deliverTransactionResult`). **Relocate `mapTransactionResult` + the ontology-stale→`{ontologyStale}` resolve** onto the wrapper — transaction **resolves** with `{ontologyStale}` (NOT reject; the read-vs-transaction asymmetry is intentional). **RETIRE the submit-gate (D7).** Test the retirement on the **transient surface (M2), not the healed end-state**: two `callAsync` transactions to different `resourceId`s are both in `#pendingAsyncCalls` at once (or Star logs the 2nd `transaction` entry before delivering the 1st — debug-sink marker) — FALSE under the old one-in-flight gate, TRUE after; **mutation-check by re-adding a serial gate** (the concurrency assertion must go red). For same-resource, assert the eTag/replay chain (Step-6.5), not merely "both committed" (which passes either way). Delete `handleTransactionResult`'s delivery role. Highest-risk — do it last, most carefully.
- Confirm no *other* caller relied on the dropped explicit fire-backs (D5 precondition; DevStudio is the known second consumer, migrated in lockstep above).
- **mesh.md carve-out lands NOW, not Phase 3 (m2):** the moment `callAsync` ships public+awaited (this phase), add the one-line greppable rule to `.claude/rules/mesh.md` — *the only awaitable on `client.lmz` is `callAsync`; `lmz.call` stays `void`* — so the always-loaded rule doesn't forbid the exact call the committed code makes for a whole phase. (The full ADR-003 amendment prose stays in Phase 3.)

### Phase 3 — ADR + docs
> 🟡 **PARTIAL (2026-07-03).** ✅ **`.claude/rules/mesh.md` done** — the greppable bright-line carve-out landed with Phase 2 (m2): "the only awaitable on `client.lmz` is `callAsync`; `lmz.call` stays `void`", plus a full `callAsync` rule entry (realizes ADR-003, client-only, D5-pattern-(a) callee returns the value). ✅ **Website docs done** — a `callAsync` section in `website/docs/mesh/lumenize-client.mdx` (§Awaiting a result — resilient awaitable, the abort-cancels-the-wait warning, client-only, prefer-subscribe) with a **validated `@check-example`** (a new `EditorClient.fetchContent` + `DocumentDO.readContent` in the `calls` for-docs app, exercised end-to-end through a real Gateway — the mini-app test also covers the pre-aborted-rejects case; `check-examples` green 230/230). Also **retired the stale `callRaw()` doc references** that were blocking `check-examples` (`mesh-api.mdx` → "Awaiting a result" pointer; `protocol.mdx` prose). **Remaining (fast-follow):** the **ADR-003/007 amendment folds into the PARENT task's Phase 3** (written after this lands — the parent already carries the pointer); the dedicated **m6 for-docs mini-app** that asserts *abort-then-the-op-still-committed-server-side* (the footgun's capable-of-failing runtime proof — the current for-docs covers the client-side reject, not the server-side commit); the parent's full `callRaw`-prose sweep across the remaining docs.
> **`/build-task` verifier fan-out (2026-07-03):** ran over Phase 1 + Phase 2. Confirmed **every structural criterion implemented correctly** on both. Caught **two real test-gaps** (mandatory capable-of-failing tests named in the success criteria but not written) — **both now fixed + mutation-verified:** (1) Phase-1 abort-listener-removed-on-normal-settle (spy on `signal.removeEventListener` with `timeoutMs:0`; reddens if the cleanup is gutted); (2) **Phase-2 M2 concurrency** (`nebula-client-read-transaction.test.ts` — two transactions to distinct resources both in `#pendingAsyncCalls` at once via `getPendingAsyncCallCount()`; reddens when a serial gate is re-added). Plus two minor stale-comment cleanups.
- **ADR-003 amendment — articulate the client carve-out (the point of the `callAsync` name, D1).** Fold into the parent task's Phase-3 ADR work (done after this lands, so one amendment covers the whole picture). The bright line ("nothing depends on request/response across hops") holds for **DO/Worker** — a held Promise dies on hibernation → continuations only. The **client** is the justified asymmetry: its heap survives freeze + reconnect and delivery re-resolves by `instanceName` (D16/D17), so `callAsync` is a **local Promise wrapper over one-way-fire + fire-back — NOT a socket-bound request/response session** (that's what `callRaw` was, and why it stranded on reconnect). So `callAsync` doesn't violate ADR-003; it *realizes* it in the one environment where an awaitable is both safe (heap-durable) and the natural ergonomic (browsers await). State the boundary explicitly: **client-only** — DOs/Workers never get `callAsync` (it would reintroduce the forbidden held-Promise-in-memory). (This is the full ADR prose; the one-line greppable `.claude/rules/mesh.md` carve-out — *the only awaitable on `client.lmz` is `callAsync`; `lmz.call` stays `void`* — already landed with Phase 2, m2.)
- **Docs** (`website/docs/mesh/{calls,continuations}.mdx`): `callAsync` as the client's resilient awaitable call; abort-cancels-the-wait-not-the-op; "prefer subscriptions for live UI data; `callAsync` for one-shot reads/mutations that need an ack/error."
- **for-docs `@check-example` for the abort footgun (m6 — the primitive's most dangerous contract):** a mini-app that aborts a `callAsync` mid-flight, then via a second `read`/`subscribe` shows the fired op **nonetheless committed server-side** — giving "abort cancels the wait, not the op; retry only idempotent ops" a capable-of-failing test. The "cancel this in-flight action" UI affordance (Notes) is a natural second scenario in the same mini-app.

### Success Criteria
- [ ] `LumenizeClient.callAsync` in Mesh, `AbortSignal`-cancellable + default `timeoutMs`, tested (settle/reject/abort/timeout/reconnect/dedup, each capable-of-failing — abort/dedup asserted on the **transient surface** via exactly-once spies, not a self-healing end-state; the **reconnect** test drives the client-heap re-settle via `lumenize-client.test.ts:733-807`, M1).
- [ ] **The three in-scope duplicates collapsed onto `callAsync`** (per the Phase-2 inventory): orgTree (`#orgTreeMutate`/`#pendingMutations`/`handleOrgTreeResult`), read (`#readResource`/`#pendingReads`/`handleReadResponse`), transaction (`submitBatch`/`handleTransactionResult`) — the hand-rolled `requestId`/`#pending*` maps + settler handlers deleted; a call whose result is lost now **rejects on timeout** (no hang, proven on the timer-free orgTree path). (`#pendingTurns` + `#pendingSubscribes` explicitly out — see the inventory.)
- [ ] **Fire-back pattern = (a) (D5), both hosts:** `Star.read`/`Star.transaction` **AND `DevStudio.read`/`DevStudio.transaction`** return the value (both Handler-1 exits) + framework fire-back via D17; the shared `deliverReadResponse`/`deliverTransactionResult` seams dropped — confirmed no *other* caller relied on the explicit fire-back.
- [ ] **DevStudio resource path migrated in LOCKSTEP (B1):** its `transaction`/`read` Handler-1 + bridge wiring on `callAsync`; the **DevStudio resource e2e suite is in the green-suites gate** (DevStudio *codegen* stays deferred).
- [ ] **Submit-gate RETIRED (D7):** `#inFlightSubmit`/`#submitGate`/`#pumpSubmitGate` deleted; **the concurrency test asserts the TRANSIENT surface** (two transactions in-flight at once, mutation-checked by re-adding a serial gate — M2), not a self-healing "both committed"; same-resource no-double-commit rests on `resources.ts` Step 4.5a/6.5 (the invariant not to regress).
- [ ] **Critical-path safety (D5 guardrail):** read/transaction migrated under `.only` discipline; the surviving special-case logic is **mutation-checked** to still fire — ontology-stale → `onShouldRefreshUI` (read **rejects**; transaction **resolves** `{ontologyStale}`), `mapTransactionResult`; concurrent reads stay correlated (now by `callId`).
- [ ] mesh + nebula-baseline suites green (incl. the full resource path **on both Star and DevStudio**); type-check clean.
- [ ] `callDurable`-for-clients seam left open (D6 — `#pendingAsyncCalls` swappable); the on-hold file cross-references `callAsync` as its foundation.

---

## Notes
- This is the client-side completion of the continuation-only model — `mesh-continuation-only-calls.md` is not truly "done" until this lands (its Phase-2 banner points here).
- The abort primitive also gives the UI a clean "cancel this in-flight action" affordance for free (not just timeouts) — e.g., navigating away from a slow mutation.
- Keep the parent task's bright line intact: raw `lmz.call` on the client stays fire-and-forget/4-arg-handler; `callAsync` is the **SDK-layer** awaitable built on top, not a reintroduction of socket-bound awaited `callRaw`.
