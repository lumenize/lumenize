# Move active scope choice until after authentication

**Status:** Active child — the login re-order and the data-use consent notice, one task. Design settled 2026-08-31 through a fresh `-alt` restart; the acceptance model re-settled always-modal at the Stage-1 gate (2026-09-01). Zero open questions; § *Decisions* holds the full set. Every pre-alpha user's first contact passes through surfaces this task rebuilds, which is what makes it invite-gated.

> 📐 **`/write-task` Pass 2 complete — design intent AND phases below.** Stage 1 ran twice (2026-08-31 framing; 2026-09-01 fresh pass post-gate) and is resolved; next: `/review-task` **Stage 2** (conformance) on this file. ⚠️ ADR-017 and ADR-018 are Proposed and exercised here — ratify between Stage 2 and `/build-task` (`docs/adr/README.md` § *Lifecycle*); ADR-016's ratification stays deferred per its own one-liner.

**Objective:** give users a way to select an active scope and be routed to it whenever they complete a login cycle — or whenever they pick "Home" from their avatar menu. Discovery stops answering anyone who has not proved they hold the address, and the data-use notice renders where a user commits.

## Relationships

- **Discharges** the merged ⚠️ GATE bullet in [nebula-pre-alpha.md](nebula-pre-alpha.md) § *Invite-gated* — the login re-order and the consent notice, which land before the first non-Larry invite.
- **Supersedes** the `discover(email)` enumeration-oracle row in [backlog.md](backlog.md) § *Nebula Auth* — that row filed this re-order as the proper fix; the row goes when this lands.
- **Answers a hand-off** from [backlog.md](backlog.md) § *Nebula Auth*'s self-narrowing row: working below one's membership is `activeScope` on the membership's session, never a dominion-justified session mint — see § *Decisions*.
- **Answers the Super-admin building-block gap** in [nebula-pre-alpha.md](nebula-pre-alpha.md) — the superuser arrives through the front door and their tree renders under the node budget; the impersonate half of that `/live` chain stays the master plan's.
- **Natural home for the create-app-flow coverage debt** ([backlog.md](backlog.md) § *Nebula*) — the notice's create-Galaxy render touches that exact flow, and the ui-smoke case is a Pass-2 candidate criterion.
- **Touches** [archive/nebula-invite.md](archive/nebula-invite.md) only at the consume helper: invites keep their scoped links and land on Home's consent modal; the consume joins mint-all, and the `.dev` co-mint rides along unchanged.
- **Documentation** — [docs/vision/auth.md](../docs/vision/auth.md) § *activeScope*'s Today-differs block describes this screen's absence and closes when it ships; the doc also gains § *Discovery*, and `website/docs/nebula/auth-flows.md` changes.
- **Re-grounds the login-flows-restoration row** (backlog § *Nebula Auth*): this task subsumes its SPA half and changes the flow its recipe pins (the scoped request goes scope-less; the landing becomes the Home 302), so the restoration sequences after this task and its row updates when this lands.
- **Folds in** backlog § *Nebula Auth*'s m6 row (pending-signup single-flight) — this task ships the double-clickable claim button the row names as its immediacy trigger; the `it.skip` stub gains its body, and the row retires when this lands.
- **Invalidates two more backlog rows, and touches a sibling:** § *Nebula Auth*'s redirect-targets row is premised on `NEBULA_AUTH_REDIRECT` (deleted here — the row goes at land), and its social-login row re-grounds on provider-proof-then-Home (its "discovery returns the scopes" premise retires). [nebula-registry-scope-in-url.md](nebula-registry-scope-in-url.md)'s "`my-scopes` does NOT move" lines are mooted by the absorption — no ordering constraint, but both files edit `router.ts`'s table: do not interleave. The self-narrowing row's per-choice-minting parenthetical updates when marked answered.
- **Backlog rows created with this design** (all in [backlog.md](backlog.md)): multi-email add-address + profile-merge; the Home-screen search box; the coming-soon durable-sink hand-off.

## Context and current state

**Built already** — verified against disk 2026-08-30/31; each entry states its fate:

- **`discover(email)`** returns one entry per membership for an address — unauthenticated, Turnstile-gated, and already marked TERMINAL in its own route-table comment, kept only because the login page cannot yet live without it. **Fate: retired** — the route goes; the harness `turnstile-canary`, its one non-production caller besides tests, re-points at another open endpoint — ⚠️ its gate-PASS probe needs a post-gate-failing request (a taken or invalid slug), since every surviving open row mints or sends mail on gate-pass.
- **The schema separates the two proofs.** `getAndVerifyIdentity` flips `Emails.emailVerified` — proof of the mailbox, per-address, set once — and separately sets `Memberships.acceptedAt`, per-membership. **Fate: carried — this pair is the design's spine.** Under mint-all, an invited membership's `acceptedAt` flips only at the modal's accept endpoint; a claim's flips at its own consume (§ *Decisions*).
- **`requestMagicLink`** inserts a scope-bound `MagicLinks` row and sends, minting no identity — except the bootstrap superuser arm, where an unauthenticated request naming the reserved scope with a configured bootstrap address writes that platform membership. **Fate: adapted** — it gains the scope-less form (the scoped request row retires with zero surviving consumers, its Turnstile entry with it, and emailed link URLs go scope-less), and the bootstrap mint moves to the consume, behind proof.
- **`consumeAndLogin`** hashes the token, generates the refresh token, calls the consume RPC, sets the cookie — two on the invite arm (`devSession`) — and 302s tier-split. Its JSDoc pins the scanner invariant: links are multi-use within TTL because corporate scanners fetch them, and the click first-touches no per-user DO. **Fate: adapted, not split** — one consume mints N cookies, one per membership of the address (**mint-all**, from here on; the dual-cookie arm generalized), and the 302 target comes from per-link-purpose code.
- **`refreshCookie`** — `Path={prefix}/{scope}`, `HttpOnly`, `Secure`, `SameSite=Strict`, fixed TTL. **Fate: carried unchanged.**
- **`handleRefreshToken`** exchanges the path-matched cookie for a JWT — a KV read, with the registry fallback on a miss that `security.md` § *Refresh tokens* documents. **Fate: carried unchanged — the destination surfaces' path and the Home page's bootstrap; refresh never touches acceptance.**
- **`my-scopes` → `myScopeTree`** — the authenticated scope-tree read: dominion-keyed so a member-less new galaxy still appears (its JSDoc defends exactly that), flat, unbounded on its platform arm; its two consumers are Studio's post-connect nudge and the manage panel, via `scopes.list()`. **Fate: absorbed** — the summary endpoint replaces it (its admin subtrees also read `Scopes`, preserving the member-less-galaxy property); `scopes.list()` re-backs onto the summary, and the route row + registry method retire.
- **The Studio login** lives inside `App.vue`: `discover` on the typed address, branch on the count, dead-end past one. **Fate: extracted** — the count branch dies and `App.vue` sheds its login code entirely; the login form moves to the auth SPA.
- **`claimUniverse`** writes the scope row and an unproved admin membership, reserves the slug, sends the link; the click proves and logs in, with three-way contested-slug resolution. **Fate: carried nearly intact** — it becomes the "Create a new workspace" affordance's engine, reached by user choice instead of by a pre-proof discovery result.
- **Scoped links** (invites, deep links) bypass discovery entirely. **Fate: carried.**

**Missing:**

1. **The Home screen does not exist.** A person with two memberships dead-ends in the UI.
2. **Discovery answers before anyone proves anything.** Any caller learns which scopes an address holds — and at Galaxy/Universe tiers membership usually is admin-ship (the shipped invite can mint non-admin peers), at the platform scope it is superuser-ship, and the scope name alone is sensitive regardless — so narrowing the response cannot close it.
3. **The newbie path is routed by the oracle.** The claim prompt appears only because `discover` answered zero — the exact pre-proof read this design deletes.
4. **The superuser cannot arrive through the front door.** Their membership is minted *by* the platform-link request, so first-login discovery returns nothing and the way in is a hand-typed URL.
5. **The flow forces a scope onto a scope-independent proof.** `emailVerified` belongs to the address, but the only path setting it demands a membership at a named scope.
6. **The data-use notice exists nowhere**, and its master-plan placement (the claim prompt) is a screen this task relocates.
7. **No auth SPA exists** — session-lifecycle UI lives inside Studio's product bundle.

The design sections below answer all seven: the flow and the screen for 1–5, the notice placements for 6, the auth SPA for 7.

## Design intent, constraints, and future state

### Getting to the Home screen

- System learns the email of a user that needs to sign in, by self-signup or invite. (Invite is the scoped arrival — its membership exists before the click, and its landing is Home with the consent modal front and center. Self-signup is the zero-membership arrival — § *Decisions*, the newbie flow.)
- System sends them an email with a magic link.
- User clicks the link. The response sets their cookies — one per membership of that address, each at its own authScope path — in a 302 to `/auth/{authScope}/home`, the segment preferring an accepted membership's scope. **The screen they are taken to is what this task builds.** No other identifier rides the URL: the page bootstraps a JWT at the segment scope's refresh endpoint, and the claims key everything after that.
- Alternatively, the user picks "Home" from their avatar menu; the app builds the URL's scope segment from its current claims.

### The Home screen

If the user's only authScope is a single **accepted** Star, the screen forwards to that Star with `activeScope == authScope`, showing a spinner while that round trip resolves. An arrival holding only unaccepted memberships renders the tree instead — its invited rows badged, their navigation gated by the consent modal (§ *Decisions*).

**Sections.** The **first section** shows the current email address — a dropdown once an address can be plural, with a small "use an unlisted email" link to the email-management screen (a coming-soon stub for now). The **main section** is the scope tree. **Bottom buttons:** "Logout all", with a quieter per-scope logout beside it.

**The main section.** The chosen email drives the tree; switching re-renders locally from data already fetched. The UI is scalable but friendly for folks with two Star scopes — it decides how to render from the quantity at each expanded level (say 20 or fewer, show them all). Jennifer, a Universe scopeAdmin with 5 Galaxies of 5–50 Stars each: her Universe row renders unclickable (no universe UI yet — eventually that is where she manages Galaxies), her 5 Galaxies beneath it unexpanded and clickable. Clicking a Galaxy takes her to its Studio; expanding one shows its Stars.

**The hand-off to the destination.** A clicked scope's surface must refresh at the *membership's* path (`/auth/acme/refresh-token`) while working at the clicked scope (`activeScope: acme.crm`) — and it cannot read cookies to find that path. The Home screen knows each subtree's owning membership, so it writes the per-workspace hint `NebulaClient` already reads (the shipped localStorage mechanism) before navigating. A destination that cannot mint — no hint, expired session — redirects to `/auth/login`, and the post-login landing is this screen again.

### Discovery reads a proven address, never a claimed one

Scope information is only ever returned to a caller holding a session, and every session is born from a click on mail delivered to the address. "Discovery is Turnstile-gated" and "we dropped the admin bit" are the decayed paraphrases — neither is the thing that must stay true. What replaces `discover` is the summary read behind the bootstrap JWT (§ *The data contract*).

### How the screen is served and bootstrapped

One assets bundle serves every page (`assets.directory` in `apps/nebula/wrangler.jsonc`). `/studio/*` is Workers-Assets-served by being unlisted in `run_worker_first`; `/auth/*` is worker-first, so the Home GET row in nebula-auth's table serves the built page in one line — `env.ASSETS.fetch` rewritten to the page's file — with caching/ETag headers passing through untouched, and the hashed `/assets/*` chunks bypassing the Worker entirely. The row is a **Worker-handled terminal, never a registry forward**: the singleton sits in one colo, and a page load never pays that RTT. The auth pages join the existing vite build as additional HTML entries, sharing one dist.

Bootstrap, in order: document GET (edge, static) → `POST /auth/{segment}/refresh-token` (path-matched cookie; a KV read with the registry fallback on a miss) → JWT held in memory → Bearer on the data calls, validated locally. A 401 at bootstrap degrades to the login form in the same SPA — a made-up scope produces a cookieless request, indistinguishable from a real scope with no session, so a typed URL learns nothing.

### The data contract

The summary returns the person's emails, each fully fleshed — memberships, and beneath admin memberships descendant levels breadth-first until a node budget (~50, configurable) is spent; a node past that frontier arrives as `childCount` only, and `expand(scope)` fetches one more level with its authz re-derived server-side. For Jennifer:

```jsonc
{
  "emails": [
    { "email": "jennifer@acme.com",   // marked current from the JWT's sub
      "current": true,
      "memberships": [
        { "scope": "acme", "tier": "universe", "scopeAdmin": true,
          "children": [               // first level under an admin root, eagerly
            { "scope": "acme.crm", "tier": "galaxy", "childCount": 12 },
            { "scope": "acme.hr",  "tier": "galaxy", "childCount": 5 }
            // …her other three galaxies, elided
          ] } ] },
    { "email": "jen@gmail.com",       // cross-email: no cookies in this browser, ever
      "memberships": [
        { "scope": "beta.tools.stars-r-us", "tier": "star", "scopeAdmin": false } ] }
  ]
}
```

Session state derives client-side from mint-all's shared clock: the bootstrap session being live means every same-address cookie in this browser is live too, so the current email's whole tree is "click goes straight in" and a non-session email's section carries one banner — "signing in here emails jen@gmail.com". Each membership row also carries its acceptance state and, when invite-minted, the `invitedByName` stamp — the invitation badge and the consent modal render from this response alone, with no further calls. The single-Star fast-forward decides itself from this response; the ≤20 thresholds live client-side. The summary serves the in-Studio flows too — `scopes.list()` re-backs onto it, and the manage panel rides the same budget + `expand` contract.

### Claims this rests on, stated so review can falsify them

- **Multiple `Set-Cookie` headers ride one 302** — the shipped invite `devSession` arm is this exact code path with N=2, so mint-all is a generalization, not a new capability.
- **Sending to an unknown address is already the standing behavior**, established by `requestMagicLink`'s no-mint invariant — the uniform response changes what a caller *learns*, not what we *send*.
- **Nothing here gates the wipe.** `MagicLinks` rows are ephemeral, the signup ticket is a cookie, mint-all writes existing row types, the acceptance flip updates an existing column — and the schema deltas — the `invitedBy*` stamp columns, plus `MagicLinks` admitting a scope-less, purpose-discriminated row — are all created free in the greenfield wipe everything already rides.
- **The invite mint runs where the inviter's Profile is reachable** — invites initiate mesh-side and reach the Registry through the facade (`auth.md` § *Grants in both planes*), but nothing on that path holds the inviter's display NAME today — so the name arrives CLIENT-SUPPLIED at the invite call, display-only: a vain name buys nothing a Profile rename doesn't, and `invitedBySub`/`invitedByProfileId` from verified claims keep the trail honest. This page needs no mesh access to display the stamp.
- **The shared `email-login.ts` helpers never call `discover`** — they go straight to the scoped request — so retiring the route breaks no login helper; `turnstile-canary` re-points to another open row.
- **The scanner invariant transfers.** Links stay multi-use within TTL and the click still first-touches no per-user DO; a scanner's extra fetches strand N orphan sessions per fetch instead of one, swept like any others.
- **`activeScope` confinement already supports working below a membership** — the refresh endpoint confines the requested `activeScope` to what the session reaches, and "authenticate at the universe, name the galaxy in `activeScope`" is how prod already works.
- **The per-workspace localStorage hint exists** — `NebulaClient` writes it on every token acquisition; the hand-off rides a shipped mechanism.

### Constraints

- **[ADR-009](../docs/adr/009-real-auth-path.md)** — real email login stays rung 1. The click now lands on the Home screen (or in the claimed universe), so `provisionAndLogin` / `loginViaEmail` and the vitest helpers change shape.
- **`security.md`** — no scope-less global refresh and no slide: cookies stay path-scoped per scope, the signup ticket refreshes nothing, and minting happens only at the click. The derived-session rule is untouched by "Logout all".
- **`router.ts`'s Turnstile set** — every unauthenticated registry endpoint stays listed there; the scope-less request form and the claim affordance's request both join it, and removing `discover` must leave the invariant true.
- **[ADR-012](../docs/adr/012-global-profile-visibility.md)** — the tree lists memberships regardless of `acceptedAt`, and the scoped-admin profile branch keeps gating on ACCEPTED. The always-modal acceptance decision preserves that manufacture defense — minting a cookie never flips acceptance, refresh never flips it, and an invited membership accepts only at the modal's own endpoint — and the ADR's "only writer is the login verify path" sentence updates at build to name the two explicit writers instead.
- **[ADR-013](../docs/adr/013-identity-profileid-resolution.md)** — the `invitedBy*` stamp rides the write-time-pinned-attribution case, amended 2026-09-01 to its general form (the handle, plus the resolved display value where the reading surface cannot resolve live). Still two licensed cases — the amendment generalized one rather than adding one — so `workflow.md`'s "exactly two" one-liner stays true untouched.
- **[ADR-017](../docs/adr/017-the-url-is-the-view-state.md)** — the URL carries the bootstrap scope and nothing else: no email, no credential; authz is re-evaluated on arrival, and a shared Home URL shows the recipient their own worlds.
- **[ADR-018](../docs/adr/018-singleton-is-the-scarce-resource.md)** — the summary and expand reads land on the registry singleton at human frequency (per visit, per click), the `my-scopes` cadence; the node budget is what keeps the superuser's response bounded.
- **`ui-theming.md`** — color through the daisyUI theme on every face; the theme decisions in the table are Larry's palette calls, placeholder-tier by the design-deferral stance.
- **Pre-alpha** — no users, no compatibility problem, no migration.

### Future state

- ⚠️ **Design consideration:** the email strip and plural data contract are the ready seam for multi-email profiles (add-address and merge are backlogged); nothing here forecloses them.
- ⚠️ **Design consideration:** Jennifer's unclickable Universe row is the placeholder for universe-level management UI — the row exists so universe management has a door when it earns one.
- ⚠️ **Design consideration:** the Home screen is the natural home for multi-session visibility later; sessions already coexist by cookie path, and this is the first surface that could show them.

## Decisions

| Decision | Rejected alternative — why |
|---|---|
| **Mint-all: the consume mints cookies for ALL the address's memberships, on one 302** — the invite dual-cookie arm generalized. The click stays the only minting moment: the screen is read-only, so the fixed 30-day clock runs from mailbox proof and nothing on the avatar-menu path extends it. One clock per address: one login renews the set; a dead mailbox takes every scope within 30 days of the last click. | **(a)** scoped links — resurrects the pre-proof oracle and the dead end. **(b)** one designated membership, others need another email — the original disease, milder. **(c)** lazy minting from the screen — a second minting site with a new authz rule for identical exposure, and exactly the capability that would let a monthly visit keep sessions alive without fresh proof. |
| **Working below a membership rides `activeScope` on that membership's session** — Jennifer enters `acme.crm` on her `acme` session; the refresh endpoint already confines `activeScope` to reach. Closes the hand-off in backlog § *Nebula Auth*'s self-narrowing row. | A dominion-justified refresh minted at a scope with no membership — a third minting site; sessions stay membership-anchored, and narrowing is `activeScope`'s job (token-level self-narrowing stays that backlog row's own question). |
| **Acceptance has two EXPLICIT writers, split by membership origin — and refresh flips nothing, ever.** A self-created membership (a claim) flips at its own consume, today's code path kept: the claimer named the slug and clicked their own link, and the only admin whose ADR-012 gate opens is themselves. An INVITED membership flips only at `POST /auth/{scope}/accept-membership` — the modal's Accept — authenticated by that membership's own path-scoped cookie, so the proof is self-carrying and no cross-membership rule exists. The bare and invite consumes flip nothing while minting their cookies. Scanner story: a prefetch can complete only a victimless self-claim; an invited membership cannot be accepted by any fetch, redirect-follow, or JS render, because acceptance is a form submit — the modal IS the interstitial POST-on-click `consumeAndLogin`'s JSDoc holds in reserve, so earlier drafts' detonation-sandbox residue is gone rather than accepted. The manufacture test pins per writer. | **(a)** consume-time flip for invites (today's code) — the victim's own mail gateway forges acceptance for any scanner-protected address. **(b)** flip at first refresh (the previous draft) — needed a no-flip sibling endpoint plus provenance rules deciding which endpoint each arrival may call, and the URL-derived bootstrap fallback (`App.vue:29`) was still a third door a shared link could walk through: two guards on a mechanism the modal's endpoint replaces outright (`calibration.md` §2). **(c)** flip everything the bare consume mints — a victim's unrelated login would accept an attacker's invite. |
| **Every invited membership's acceptance passes the informed-consent modal — there is never a quicker path (Larry, 2026-09-01).** The invite link's 302 lands on Home with the modal front and center (segment = the invited scope): *"You've been invited by {inviter} to collaborate in {scope}. Only accept if you know who this is and why."* — checkbox, Accept (the accept endpoint, then navigate into the workspace), Decline (stay on the tree, row badged, nothing written). The same modal gates tree navigation into any stamped-unaccepted row, so both arrival paths share ONE consent surface. The extra click on the invitee happy path is the deliberate price — seeing who invited you before standing in their scope is arguably the better UX. `invitedByName`, `invitedBySub`, and `invitedByProfileId` are stamped on the membership row at invite mint under ADR-013's write-time-pinned-attribution case (display-only, never keyed; columns free in the greenfield wipe), and the stamp doubles as the discriminator: stamped → invited → modal; unstamped → your own claim → no ceremony. The invite templates keep the warning sentence as belt — no longer load-bearing, since the modal stands on every path. | **Direct-landing invite 302s** (the previous draft) — teleports the invitee into the scope before any consent surface, making the email warning load-bearing and acceptance implicit. **Email-warning only** — powerless on the tree path, where the victim may never have seen the invite mail at all. **Modal without the stamp** — this page cannot reach Profiles (mesh-side; the surface is deliberately HTTP-only), so a render-time name lookup would drag NebulaClient back in. **Click = accept with no modal** — a curiosity-click on an unfamiliar badged row would hand a stranger private-profile access. |
| **This file owns the self-signup (claim) screen** — the zero-membership face of the same arrival. | A sibling task — it would take this file's consume contract as its central input mid-design. |
| **Newbie: a self-declared fast path plus a fallback slug screen.** "Create a new workspace" expands the login form to email + name; today's `claimUniverse` engine carries, and the click lands in the new universe — one form, one email, one click. The undeclared newbie's zero-membership consume lands on a post-click slug screen authorized by a **signup-ticket cookie** at a fixed non-scope path — spendable only at claim, short TTL, the proof credential's one surviving remnant — which also recovers a contested slug. | Forwarding the one-time token into the signup URL — a live credential in a browser-visible URL one hop past the email. Fallback-only — matches today's interaction count where the affordance beats it for nearly free. |
| **m6 folds in: pending claims converge at CONSUME — the clicked link wins, and its consume retires the siblings.** Both pending claims coexist at submit, so no unauthenticated deletion primitive exists; the first consumed link's transaction retires the address's sibling pending claims (predicate: `acceptedAt IS NULL` alone — requiring an unproven mailbox would make a scanner-prefetched claim un-replaceable) — scope rows, memberships, and any refresh records already minted for them, via the same revocation machinery `executeScopeDeletion` uses (`#invalidateRefreshTokensForSub`), so no session survives at a freed slug. A dead sibling link's consume lands on the ordinary invalid-token redirect. The deletion's ADR-016 record carries a SERVER-COMPOSED actor (the platform executing convergence), with the address and both slugs as record context — a client-supplied "the address itself" would name the person acted upon as the actor, ADR-016's own failure shape. The empty m6 `it.skip` in `identity-mint-point.test.ts` gains its body, and the backlog row retires when this lands. Trigger honesty: this task ships the double-clickable button that row names as its immediacy trigger. | **Converge-at-submit** (the previous draft) — `claim-universe` is unauthenticated, so anyone naming a victim's address could void their in-flight claim and free their reserved slug, contradicting accepted `auth.md` § *Founding a Star*'s "a pending claim cannot be taken over". **Refuse-with-resend** — dead-ends the honest changed-my-mind case. **Accepted residual** — ships a known dud button to F&F day 1 for one saved afternoon. |
| **`NEBULA_AUTH_REDIRECT` is deleted; destinations live in code** — per-link-purpose routing: bare → Home, zero-membership → slug screen, claim/invite → what the link names, tier-split star vs above. | Keeping the knob — a fossil of the `@lumenize/auth` fork; one deployment, never a second value, and `landingBase` already hardcodes the star arm. |
| **nebula-auth owns the feature; the page lives at `/auth/{authScope}/home`** — a GET row in nebula-auth's table serving the built page via the host's ASSETS binding; § *How the screen is served and bootstrapped* carries the mechanics. | **NebulaClient / the facade** — session lifecycle is HTTP (`auth.md`), and mesh entry needs the very bootstrap this page performs. **JWT embedded in the HTML** — per-user, uncacheable, a credential in the document. **An apex intercept row** — splits `/auth/*` across two tables. **Bare `/scopes?s=`** — loses the segment idiom and the namespace. **Passed over, not disproven: a router-shell `index.html`** with lazy route chunks — zero server code, but URL ownership moves into frontend router config; revisit if the page family grows. |
| **The auth screens are their own small SPA** — login, Home, signup fallback; source owned auth-side; `App.vue` sheds its login code. Folder mechanics are a phases decision. | Staying in the studio bundle — a session-lifecycle surface embedded in a product surface, today's mislocation preserved. |
| **Tree data: a summary with a node budget plus `expand`, built now** — § *The data contract*. | One-shot now, budget backlogged — the same shape minus counts saves ~a phase, and the row comes due exactly when the first big tenant arrives. An unbounded superuser response — every universe in one singleton read. |
| **`my-scopes`/`myScopeTree` is absorbed and retired.** The summary is a superset — its admin subtrees read the `Scopes` table, preserving the member-less-new-galaxy property the old method's JSDoc defends — and the in-Studio consumers (the post-connect nudge, the manage panel) hold Bearer JWTs, so `scopes.list()` re-backs mechanically and rides the same budget + `expand` contract. The unbounded platform arm dies with it. | Carried in parallel — two tree reads whose results must agree forever, the rejected unbounded platform arm surviving on one of them, in consumers this task already edits. |
| **All emails fully fleshed; the UI keeps the two-section layout.** Today `profileId`→email is 1:1, so the plural shape costs nothing, and dropdown switches re-render locally. | Emails as tree roots — one dead level for every single-email user, worst for the two-Star person we prioritized. Thin single-email data — the same bytes today, a refetch tomorrow. |
| **Session state per-SECTION, derived client-side** from mint-all's shared clock (§ *The data contract*). | Serving refresh records — they prove a session on SOME device, mis-badging every new laptop. |
| **The notice leaves the login page; it renders where a user commits** — the claim affordance, the fallback slug screen, and Studio's create-Galaxy flow. One component, three renders, all owned here; informational, no gate, no stored value; generic improve-the-product wording, never naming Studio or the model. Candidate string (approved as candidate, not final): *"What you build here may be reviewed by the Lumenize team to improve the product."* | Every login page — Universe/Galaxy owners own their tenants' data relationship; a per-tenant notice is the user-dev's call, and we only want data to improve Nebula. An accept-gate or stored flag — friction on the funnel, schema for a decoration. Splitting the create-Galaxy render out — a gate sliver with a deadline and no file. |
| **Logout: "Logout all" plus a quieter per-scope logout** — all revokes every refresh record for the address and expires all its cookies on one response, the mint-all's symmetric twin; both actions also land in avatar menus. | Per-scope only — not what "log out" means on a shared machine once cookies mint together. |
| **The invite consume joins mint-all** — same helper; an invitee's whole session set renews at the click; the `.dev` co-mint rides along. | A bespoke dual-cookie-only invite arm — per-path reasoning to maintain forever. |
| **Coming-soon component logs to `@lumenize/debug` now; durable later via the observability pipeline** — a distinctive event tag, and the hand-off is a backlog row, not a bespoke store. | A registry table — product telemetry in the identity domain, for a component that will render on non-auth surfaces. AE / R2 / a platform data plane — too much now, or does not exist. |
| **UI tech: Vue + Tailwind + daisyUI.** | — |
| **Theme: one shared neutral base — greyish beige — that Studio sub-classes with brand color.** A warm neutral receives the orange sub-class cleanly; the accent stays neutral in the base; user-dev apps extend colors and leave spacing/corners/shadows alone. Placeholder-tier: one OKLCH variable, revisited when design gets real attention. | Brown — heavy and dated as a full surface. Cool grey — warm/cool tension under Studio's accents. Per-surface bespoke themes — the restyle cost theming exists to avoid. |
| **User-facing vocabulary: the menu item, the screen, and the URL say "Home"; the tier nouns are Account (universe), App (galaxy), Tenant (star); "scope" appears in ZERO UI copy.** Rows and messages use concrete names ("Acme CRM") with small tier labels — the category noun is needed in exactly one slot, and "Home" fills it with no model knowledge required. Home is a ROUTER: it forwards when the best surface is unambiguous (today: the single-accepted-Star fast-forward) and renders the tree otherwise; the per-persona specializations are fast-follow (backlog § *Nebula*). `scope` stays the internal term — code, ADRs, user-dev docs. Supersedes this table's earlier "named Scopes" disposition (2026-09-01). ⚠️ Design consideration (copy, decided at build): the claim affordance's "Create a new workspace" — under Account, "create a new account" is natural signup language and frees "workspace" for Studio's `.dev` star. The manage-panel copy retrofits IN scope — `App.vue`'s "Manage my scopes" ×3 and "Loading your scopes…" die with the pivot (Larry, 2026-09-01: when we pivot, we fully pivot; half-done switches are where our biggest problems come from). | **"Scopes"** — honest to the model, but it demands the model of every user, and it is often a misnomer (one place, not a scope-plural). **"Workspaces" as the umbrella** — false for Account and App rows, and already spoken for by Studio's `.dev` workspace. |
| **Small dispositions:** the email-management stub gets its own auth-SPA route rendering coming-soon; `/auth/login` is the fresh-visitor entry once `App.vue` sheds its form. | — |

## Phases

Numbering equals executable order — one branch, sequential (`workflow.md`). Each phase cites the § *Acceptance criteria* entries it owns by their bold names; the inventory below stays the single source of each criterion's full statement. Mid-build, the bare-login 302 targets a page that lands in Phase 6 — acceptable under the pre-alpha one-wipe gate, and nothing before Phase 6 depends on the bare flow (helpers ride scoped links until Phase 9).

1. **Scope-less links exist, and the bootstrap mint moves behind proof.** `MagicLinks` gains purpose-discriminated, scope-less rows (greenfield schema); `requestMagicLink` gains the scope-less form with the uniform response; the platform-membership mint leaves the unauthenticated request arm and becomes an ensure-at-consume for bootstrap addresses. The scoped request row STAYS alive until Phase 9.
   - **Success criteria:** requests naming a member, a stranger, and a bootstrap address return identical bodies; a platform-scoped request writes no membership; a bootstrap address's scope-less consume ensures one. Owns the request half of 🔒 *An unproven address learns nothing* and the mint half of *A superuser arrives through the front door*.
   - **Mutation note:** restore the request-arm mint → the no-membership assertion reds; diverge the stranger response → the identical-bodies assertion reds.
2. **Mint-all, and destinations live in code.** `consumeAndLogin` mints one cookie per membership of the link's address on one 302; the 302 target comes from per-link-purpose code; `NEBULA_AUTH_REDIRECT` is deleted at every config site (`npm run types` after); the invite consume stops flipping `acceptedAt` while the claim consume keeps flipping only its own membership.
   - **Success criteria:** owns *One click, all sessions*; the invite-consume and bare-consume arms of *acceptance is always explicit*; the claim arm's flip; 🔒 *The refresh cookie stays path-scoped* re-asserted against the new consume; `grep -rn NEBULA_AUTH_REDIRECT` returns nothing.
   - **Mutation note:** mint only the link's membership → the two-cookie assertion reds; restore the invite-consume flip → the manufacture test's invite arm reds; widen a cookie path → the path-scope probe reds.
3. **m6: pending claims converge at consume.** The first consumed claim's transaction retires the address's sibling pending claims (`acceptedAt IS NULL`) — rows, memberships, and refresh records via `#invalidateRefreshTokensForSub` — recording a server-composed ADR-016 actor with the address and slugs as context. The m6 `it.skip` gains its body.
   - **Success criteria:** owns *Two claim submissions converge to ONE Universe, at consume* — including the revoked-session limb (a superseded claim's cookie no longer refreshes, probed per `security.md`'s same-scope shape).
   - **Mutation note:** skip the sibling retire → two Universes red the stub test; drop the revocation call → the stale-cookie probe reds; record the address as actor → the record assertion reds.
4. **The stamp, the summary, and the absorption.** `Memberships` gains `invitedBySub`/`invitedByName`/`invitedByProfileId`, stamped at invite mint with the client-supplied name threaded through the facade signature. The scope-less summary endpoint (Bearer, `subRateLimitGuard`) returns `emails[]` per § *The data contract* — budget, frontier `childCount`, acceptance state, stamps — and `expand(scope)` re-derives its authz server-side. `my-scopes`/`myScopeTree` retire; `scopes.list()` re-backs onto the summary; Studio's post-connect nudge and manage panel re-point.
   - **Success criteria:** the summary matches the § *The data contract* shape on a two-email fixture; a low test budget produces frontier counts; a non-admin's `expand` is refused; invited rows carry all three stamps; a member-less new galaxy appears under its admin subtree; the nudge and manage panel stay green on the new backing. Owns the data half of *The Jennifer tree renders by the rules*.
   - **Mutation note:** source subtrees from `Memberships` instead of `Scopes` → the member-less-galaxy assertion reds; ignore the budget → the frontier assertion reds; drop `expand`'s authz → the refusal reds; skip a stamp column → the stamp assertion reds.
5. **The two explicit writers, pinned.** `POST /auth/{scope}/accept-membership` flips its own membership off its own path-scoped cookie; "Logout all" revokes every record for the address and expires every cookie on one response; per-scope logout is untouched. The manufacture test re-pins all four arms: invite consume never, refresh never, accept does, claim-consume self only.
   - **Success criteria:** owns the remaining arms of *acceptance is always explicit* and all of *Logout all revokes everything for the address* (cookie-probed per `security.md`).
   - **Mutation note:** one mutation per test arm — add a flip to refresh, remove the accept flip, widen accept to a sibling membership, revoke only the called scope — each reds exactly its own arm.
6. **The auth SPA shell.** New vite HTML entries sharing the dist; the greyish-beige daisyUI base theme; nebula-auth GET rows serving `/auth/{authScope}/home`, `/auth/login`, and the email-management stub through the ASSETS binding as Worker-handled terminals; the login form with the "Create a new workspace" affordance (settle the account-vs-workspace copy here); the fallback slug screen with the signup-ticket cookie; the notice component rendered at the affordance and the fallback; the coming-soon component logging under its debug event tag.
   - **Success criteria:** owns *One email claims a Universe*, 🔒 *The signup ticket is spendable only at claim*, and the affordance + fallback renders of *The notice renders at all three placements* (gates nothing, stores nothing).
   - **Mutation note:** honor the ticket at the summary endpoint → the ticket criterion reds; drop a notice render → the placement check reds; add a has-seen write → the stores-nothing check reds.
7. **The Home screen.** Bootstrap via plain refresh at the segment scope; the email strip; the tree with the ≤20 rules, invitation badges, and the cross-email banner; the consent modal (Accept → the accept endpoint → navigate; Decline → stay); the single-accepted-Star fast-forward; the hand-off hint written before navigation; the invite 302 re-targeted to Home with the modal front and center.
   - **Success criteria:** owns the render half of *The Jennifer tree renders by the rules*, *Scoped links still work end to end* (invite → modal → workspace, `.dev` cookie, sessions renewed), and the modal arm of *acceptance is always explicit*.
   - **Mutation note:** navigate an invited row without the modal → the modal arm reds; break the hint write → the Jennifer galaxy-entry limb reds; fast-forward an unaccepted Star → the fast-forward assertion reds.
8. **Studio integration.** `App.vue` sheds its login block; the avatar menu gains Home, Logout all, per-scope logout, and the email-management stub; the create-Galaxy flow renders the notice (the third placement); the manage-panel copy retrofit (Account/App/Tenant/Home); the create-app ui-smoke case from the backlog row lands.
   - **Success criteria:** owns the create-Galaxy render of the notice criterion, *"scope" appears in no user-facing string*, and the *ui-smoke create-app* criterion; `App.vue` contains no login code (scoped grep).
   - **Mutation note:** reintroduce "Manage my scopes" → the copy grep reds; break `onCreateApp`'s flow → the ui-smoke case reds.
9. **Retirements and the parallel-survival sweep.** `discover` (route row, registry method, Turnstile entry), the scoped `email-magic-link` request row, and every other member of the deletion set go; `turnstile-canary` re-points to a post-gate-failing probe; the ADR-009 helpers (`email-login.ts`, `provisionAndLogin`) take their final shape against the scope-less flow.
   - **Success criteria:** owns 🔒 *An unproven address learns nothing* (consume half), *No unauthenticated route answers a question about an address* (structural over the router sets), and *Nothing replaced survives in parallel* — every retired symbol's scoped grep runs and returns nothing, pasted as evidence.
   - **Mutation note:** re-add any retired route row → the structural router-set assertion reds; the sweep greps red on any surviving symbol by construction.
10. **The `/live` scenarios.** Three container-free scenarios registered in `drive.ts`: a two-membership address enters both scopes off one email; a fresh address claims via the affordance on a single email; a superuser walks the front door to the platform root. Real mail, rung 1, per-limb mutation checks (`live.md`).
    - **Success criteria:** owns 🌐 *The same, driven as `/live` scenarios* — each limb isolated by its own mutation, with a positive control per `live.md`'s multi-limb rule.
    - **Mutation note:** per limb, named in the scenario files at write time — a scenario red only via limb 1 is the failure shape `live.md` § multi-limb documents.
11. **Documentation and cross-file lands.** `docs/vision/auth.md` gains § *Discovery*, narrows § *activeScope*'s Today-differs block, and rewrites the three consume-time-acceptance sentences (both greps pasted as evidence); ADR-012's writer sentence updates to the two explicit writers; `website/docs/nebula/auth-flows.md` follows; the backlog rows retire or re-ground per § *Relationships* (oracle, m6, restoration, redirect-targets, social-login, self-narrowing parenthetical); the master plan's GATE bullet flips ✅.
    - **Success criteria:** owns *`docs/vision/auth.md` narrows, gains, and stops asserting the old model*; every § *Relationships* obligation is executed and linked; prose gates pass on every governed file touched.
    - **Mutation note:** leave any of the three auth.md sentences unrewritten → the content grep reds; skip a backlog obligation → the Relationships walk in review reds it.

## Acceptance criteria — the inventory; every entry is owned by a named phase above

- 🔒 **An unproven address learns nothing.** Requests naming a member address and a stranger address produce identical responses, and no unauthenticated route returns scope names for an address. *Reds against keeping any pre-proof discovery route, including a narrowed one.*
- **One click, all sessions.** An address with two memberships clicks one link and receives both cookies on one 302; each scope on the Home screen opens without further email. *Reds against single-cookie minting.*
- **The Jennifer tree renders by the rules.** An admin membership shows its first level clickable; a galaxy click lands in that Studio via the authScope hand-off; expansion past the frontier fetches one level. *Reds against reach-less rendering and against a broken hand-off.*
- **One email claims a Universe** via the affordance, landing directly in the claimed universe with the mailbox recorded proven. *Reds against a second link.*
- **Two claim submissions for one address converge to ONE Universe, at consume.** Both pend; the first clicked link wins, and its consume retires the sibling pending claims with their sessions revoked. *Reds against a second Universe minting, and against a stale refresh record still minting at a freed slug — the m6 `it.skip` stub becomes this test's body.*
- 🔒 **The signup ticket is spendable only at claim.** It starts no session and reads no data; the fallback slug screen is its only consumer. *Reds against the ticket acting as a general credential.*
- 🔒 **The refresh cookie stays path-scoped per scope.** A session at one scope produces a cookie never sent to another scope's auth routes. *Reds against the global-refresh shortcut — the failure that would look like success everywhere else.*
- **A superuser arrives through the front door.** A configured bootstrap address that has never logged in types it, clicks, and sees the platform root in their tree. *Reds against leaving the mint on the unauthenticated request.*
- **Unaccepted memberships are offered, and acceptance is always explicit.** The tree shows an invited-but-unaccepted scope badged with its inviter's name; the bare and invite consumes mint cookies without flipping `acceptedAt`; refresh never flips it; an invited membership accepts only via the modal's accept endpoint, authenticated by its own cookie; a claim's consume accepts only itself. *Reds against an invite-consume flip, a refresh that flips, an acceptance without the modal's endpoint, and filtering the tree on acceptance — the manufacture test pins each writer.*
- **Scoped links still work end to end** — an invite click lands on Home's consent modal; Accept enters the workspace with the `.dev` cookie set and the invitee's other sessions renewed; Decline leaves `acceptedAt` NULL with the session set intact. *Reds against a direct-landing invite and against a modal-less acceptance.*
- **The notice renders at all three placements** — claim affordance, fallback slug screen, create-Galaxy — and gates nothing, stores nothing. *Reds against a missed render, an accept control, or a has-seen flag.*
- **Logout all revokes everything for the address** — every refresh record dead, every cookie expired, in one response; per-scope logout still works. *Reds against a logout that leaves a sibling session alive.*
- **"scope" appears in no user-facing string.** The auth SPA and every panel this task touches say Account, App, Tenant, and Home; a case-insensitive grep for "scope" over user-facing strings in those surfaces returns nothing. *Reds against `App.vue`'s "Manage my scopes" ×3 and "Loading your scopes…" surviving.*
- **Nothing replaced survives in parallel.** Every surface this design supersedes is deleted in the same build, never left beside its successor — the known set: `discover` (route row, registry method, Turnstile entry), `my-scopes`/`myScopeTree` and the old `scopes.list()` backing, the scoped `email-magic-link` request row and its Turnstile entry, `NEBULA_AUTH_REDIRECT` at every config site, `App.vue`'s login block, and the invite-consume `acceptedAt` flip. The set is a floor, not a cap: any build step that replaces a surface acquires the obligation to delete what it replaced. *Reds against any retired symbol surviving a scoped grep of the source tree after the build.*
- 🌐 **The same, driven as `/live` scenarios.** Real logins, real mail: a two-membership address enters both scopes off one email; a fresh address claims via the affordance; a superuser walks the front door. ⚠️ Fidelity, not capability — only a real run proves the mail carries a link the scope-less consume accepts.
- **No unauthenticated route answers a question about an address** — stated structurally over `router.ts`'s open and Turnstile sets, never as an endpoint list.
- **`docs/vision/auth.md` narrows, gains, and stops asserting the old model.** § *activeScope*'s Today-differs block rewrites down to what still differs after this ships; § *Discovery* is added; and no sentence in the doc still asserts unauthenticated discovery or consume-time acceptance — §§ *The Registry*, *Profiles*, and *Founding a Star* each carry one today. *Checkable beyond the Today-grep: `grep -nE 'mark(ed|s that membership) accepted|discovering where you are a member' docs/vision/auth.md` — exactly three lines today (336, 364, 466), all rewritten at build.*
- *(Candidate — Pass 2 decides the phase.)* **The ui-smoke create-app case** from the backlog's zero-coverage row lands here, driving the same screens the notice and login rebuild touch.

## Non-goals

- **Universe-level management UI** — Jennifer's Universe row renders unclickable; what sits behind it is future work with no task yet.
- **Multi-email add-address and profile-merge** → backlog § *Nebula Auth*; the email strip and plural contract are the ready seam.
- **The Home-screen search box** → backlog § *Nebula Auth*.
- **Email management** — a coming-soon stub only.
- **Turnstile policy** — unchanged; this changes which endpoints sit behind it, not how it works.
- **The invite mechanism** → [archive/nebula-invite.md](archive/nebula-invite.md); invites keep scoped links.
- **Anything about reach or the claims shape** → [archive/nebula-passage-dominion-from-scope.md](archive/nebula-passage-dominion-from-scope.md).
- **Consent machinery beyond the notice** — no records, no accept flow, no policy page, no per-user data controls.

## Open questions

None.
