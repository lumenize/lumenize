# Backlog

Small tasks and ideas for when I have time (evening coding, etc.)

> 🔴 **SECURITY-CRITICAL — Turnstile is OFF in prod (pre-alpha); turn it ON before public signup (alpha).**
> A deliberate, dated deferral (`TURNSTILE_SECRET_KEY` unset — confirmed 2026-07-06), acceptable **only** while access is F&F-invite-only / obscure-URL / cost-ceiling-watched. The automation **bypass** — the hard part — is ✅ built + canary-verified, so this is unblocked; **deferred to post-pre-alpha**, with the design + blocker catalog + phases now homed in **[tasks/on-hold/turnstile-on-in-prod.md](on-hold/turnstile-on-in-prod.md)**.

## Immediate work backlog

- [x] ~~**Switch local Nebula test email from Resend (~8s) to Cloudflare (~1s)**~~ — ❌ **REJECTED 2026-07-21, measured: there is no gap to win.** Median magic-link loop is **Cloudflare 1451 ms (n=9) vs Resend 1400 ms (n=10)** — indistinguishable, Resend marginally faster at the median, neither with a cold-start penalty. Results + method: [tasks/email-latency-cf-vs-resend.md](archive/decision-email-latency-cf-vs-resend.md). **Kept as a closed entry, not deleted, so the switch isn't re-proposed off the same false premise.**
  - **The `~8s` that justified this was never measured on Resend.** It traces to one uninstrumented, hand-wall-clocked figure (ADR-009, 2026-07-06) citing `apps/nebula/harness/prod.ts spin` — a command with no timer that, per the config in effect that day, sent via the **Cloudflare** binding. ADR-009's cost line is now corrected. **Do not re-cite ~8s.**
  - **Doing the switch would cost, not save:** the CF send binding **requires `remote: true`** (verified — without it miniflare simulates the send and nothing leaves the machine), which binds at pool load and needs CF creds. That is precisely the dependency the `LUMENIZE_NO_CF_REMOTE` opt-out exists to work around. Paying it for ~50 ms of noise is backwards. Also note the local `/live` harness lane is **already** on Cloudflare; only ui-smoke + browser/bench are on Resend, and they chose it deliberately to run creds-free — the lanes this item said to leave alone.
  - ✅ **There is no 200/day concern here — do not reintroduce it.** The beta cap applies only to the arbitrary-recipient **Email Sending** feature. Verified addresses/domains send via the **Routing** feature instead, so the cap never touches `*@lumenize.io` test email (double-confirmed with Cloudflare employees; `cf-email-sending-vs-routing` has said this since 2026-07-14). This has been misread repeatedly — including once in this very entry.
  - ⚠️ **Still open, now for the opposite reason:** ADR-009 rung 2 was justified on "~8s is too slow for every unit test". At **~0.9 s marginal** that rationale is gone entirely — not merely weakened. The ADR now flags rung 2 as open, standing only on flake/external-dependency grounds. **Larry's call whether it survives.**

- [ ] **Finish moving the test lanes onto real email login** (started 2026-07-21; the measurement that unblocked it is [tasks/email-latency-cf-vs-resend.md](archive/decision-email-latency-cf-vs-resend.md) — the loop is ~1.4s standalone / ~0.9s marginal, so **speed is not a reason to use a mint**). Landed already: one shared `@lumenize/email-test/client`, unique-recipient parallel lanes, and `loginViaEmail` (`apps/nebula/test/lib/email-login.ts`, verified against prod). Remaining:
  - ✅ **DONE 2026-07-21 — `connectDriver` logs in for REAL by default.** `provisionAndLogin` (`apps/nebula/test/lib/email-login.ts`) claims the universe — the one open, founder-minting entry, which mints with `isAdmin: true` *then* sends the link — consumes the real magic link, creates the galaxy/star beneath with that founder's token, and re-issues at the target scope. The founder's reach covers everything below, which is what lets a caller drive a star it never logged into directly (the shape prod uses). Verified on the running stack: `message-roundtrip` PASSED 19.2s on the real path; `superadmin-reach` PASSED on its explicit `mint: { reason }` justifications.
    - ⚠️ Logging in **directly at a fresh star** remains impossible — `createStar` writes a `Scopes` row with **no founder**, so `consumeMagicLink` → `getAndVerifyIdentity` → null → `302 /app?error=invalid_token`. That is open Star self-signup ([nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md)), already tracked by the `it.skip('claim-star: open self-signup mints an exact-star founder…')` in `packages/nebula-auth/test/nebula-auth-routes.test.ts`. **Don't add a second marker.** `provisionAndLogin` sidesteps it by claiming the universe above.
    - ✅ **De-forked 2026-07-21 (3c6641f):** `provisionAndLogin` and test-helpers' `foundAndLogin`/`bootstrapAdmin` no longer re-implement the same flow. The HTTP layer (`pointLinkAt`, `requestUniverseClaim` incl. the 409-already-claimed rule, `requestMagicLink`) lives in `test/lib/email-login.ts`; test-helpers delegates and keeps only what makes it a vitest helper (its `expect`s, cookie-jar refresh, 91 unchanged signatures). They stay separate surfaces on purpose — test-helpers is vitest-bound and jar-based, email-login is vitest-free for the Node harness.
    - **Ruled out — don't re-investigate:** `Browser` following redirects (it honors `redirect: 'manual'`; plain `fetch` fails identically), the issuer-origin rewrite (link + token verbatim correct), the `Secure`-cookie-over-http theory (no cookie is sent at all).
  - **Migrate the baseline test-app off `createNebulaTestToken`** — `apps/nebula/test/test-apps/baseline/{profile-do,profile-channel-collision,subscriber-list,profile-subscribe}.test.ts`. Use the same claim-a-universe recipe (`provisionAndLogin`) — NOT blocked on star-founder work. **Decided 2026-07-21: mixed target** — convert 1–2 representative tests to rung 1 (real login) so the lane genuinely exercises the full path, and move the rest to rung 2 (test-mode server issuance), so rung 3 goes to zero without making every authed baseline test depend on the network. That lane is the API-level default tier `testing.md` describes, and it is currently 100% synthetic; one of its own headers already says "the baseline login lane is expectedly red mid-turnover". Needs `EMAIL_PROVIDER` + a verified `AUTH_EMAIL_FROM` in its miniflare bindings, and a decision on whether `NEBULA_AUTH_TEST_MODE` can come off.
  - **Explain the ~7 s FIRST email send.** Controlled probe (2026-07-21, deployed prod): `discover` — same registry DO, **no send** — is **3030 ms cold / ~50 ms warm**, while `email-magic-link` is **6995 ms on an ALREADY-WARM DO**, recovering to ~1.2 s; reproducible across rounds ~15 min apart. So it is **not** DO cold start and **not** delivery (mail arrives before the POST returns) — it is the send itself: the `AUTH_EMAIL_SENDER` service-binding hop, or `env.EMAIL.send()`. Next step is server-side timing under the existing `DEBUG=nebula-auth`, not more black-box probing; a Resend cold-send comparison is the other obvious probe. **This is the unauthenticated signup endpoint — 7 s is a real first-impression cost.** Table: [tasks/email-latency-cf-vs-resend.md](archive/decision-email-latency-cf-vs-resend.md).
  - **Align the root wrangler with the packages' pin.** Root `node_modules/wrangler` is **4.86.0** while every package pins `^4.111.0`. `spawnWranglerDev` no longer cares (it now resolves package-local, `605dfc4`), but anything else invoking a bare `wrangler` from the repo root still gets a version that **silently ignores the `exports` DO-class registry** — which manifests as `ctx.storage.kv` throwing at runtime, not as a config error. Root has no `wrangler` in `devDependencies`; add one at `^4.111.0` or remove the stale hoisted copy.

- [ ] **Test-mode gating diverged between the two forked auth packages** (found 2026-07-21). `@lumenize/auth` requires **two** factors — `LUMENIZE_AUTH_TEST_MODE` **and** `?_test=true` per request ([lumenize-auth.ts:228,759](../packages/auth/src/lumenize-auth.ts)). `nebula-auth` gates on the **binding alone** ([nebula-auth-registry.ts:107](../packages/nebula-auth/src/nebula-auth-registry.ts)), and structurally can't do the same: the decision is made inside the registry DO, reached by RPC with no request URL to read. Consequence: a leaked `NEBULA_AUTH_TEST_MODE` on a deployed worker would hand magic links to **ordinary traffic**, where the same leak in `@lumenize/auth` would only affect requests that deliberately asked. Not currently exploitable — `audit-test-mode.sh` is what holds this line — but it is a real asymmetry to resolve when de-forking the token core ([auth-token-core-compose-not-fork](on-hold/auth-token-core-compose-not-fork.md)). If a per-request factor is wanted, the Worker would have to thread an explicit flag into the RPC rather than the DO sniffing a URL. (Nebula's `browserLogin` had been passing a no-op `?_test=true` copied from the auth pattern; removed, with the reason recorded in place.)

- [ ] **Manually re-read ADR-012 / ADR-013** now that they are committed (`1ea309b`, 2026-07-21). Both were written with `Status: Accepted` but had never been committed, so `CLAUDE.md`'s always-loaded one-liner index referenced files that did not exist in git — meaning every session loaded the *summary* of a decision whose full text was never reviewed in place. Nothing about them changed on commit; this is a read-through, not a re-decision. **ADR-014 needs no entry here** — it carries `Status: Proposed` and the index already says "pending Larry's read", which is the mechanism working as designed. If 012/013 turn out to need revision, flip their Status line rather than tracking it here.

- [ ] **Set up Substack** as a third cross-post channel (alongside Lumenize blog + Discord + Medium). Currently a gap in the post-2b/2c rollout for the parse-validator release — see `tasks/archive/parse-validate-release.md` "Revised staging". Once active, update `reference_content_distribution.md` and the parse-validate-release task file to fold Substack into the Day +3–5 broadcast window.

- [ ] Figure out how to give diagnostic channel power to my Agents and tests. For instance, maybe we could have used that for our email e2e test rather than standing up our own push mechanism. Maybe we should also upgrade debug to use this. See: https://developers.cloudflare.com/workers/runtime-apis/nodejs/diagnostics-channel/

- [ ] **Fully eliminate `@cloudflare/workers-types`** (started in `tasks/archive/do-exports-and-toolchain-upgrade.md`). The `packages/*` runtime/dev path is already off it — `wrangler types` output (`worker-configuration.d.ts`) provides the same globals, proven 12/12 type-check clean. Two threads remain: (1) the 6 `packages/*/tsconfig.build.json` still name it in `types` (resolved via the copy still hoisted by out-of-scope declarers) — but that `tsc -p tsconfig.build.json` **publish** path was *already broken pre-session* (`cloudflare:test` imported in `src`, cross-package `Env`), so making the publish build workers-types-free is its own careful pass, not a swap; (2) `apps/nebula`, `website`, `experiments/*`, `tooling` still declare it (keeping it hoisted). Not blocking anything — do when addressing the publish/build path or those apps.


## Lumenize Mesh

- [ ] **`callAsync` follow-ups (from `tasks/archive/mesh-client-callasync.md`, LANDED + ARCHIVED 2026-07-03/07-06).** Two threads left open when that task archived (neither a ship blocker):
  - **m6 for-docs proof — abort-then-the-op-still-commits.** A capable-of-failing runtime proof of `callAsync`'s most dangerous contract ("abort cancels the *wait*, not the server *op*; retry only idempotent ops"). The client-side *reject* IS tested (`packages/mesh/test/for-docs/calls/`); what's missing is a mini-app that aborts mid-flight then via a second `read`/`subscribe` shows the fired op **nonetheless committed server-side**. Explicitly a fast-follow.
  - **Revisit `#pendingSubscribes`** (`apps/nebula/src/nebula-client.ts`) — the one same-shape bridge `callAsync` deliberately scoped out (a persistent-subscription lifecycle keyed by `SubscribeKey`, NOT a one-shot call→result). "Needs more thought" on whether/how it unifies. *(The other scoped-out bridge, `#pendingTurns`/chat, is homed in `tasks/nebula-galaxy-collapse-and-chat.md` — it likely dissolves into `subscribe`.)*

- [ ] **`NebulaClient` resource-host binding is hardwired to `'STAR'` (Mesh/client product feedback from Nebula).** All ~17 data-plane call sites in `packages/nebula-frontend` `nebula-client.ts` (transaction/read/subscribe/unsubscribe/subscribeTree/subscribeReload + 10 dagTree ops) pin the binding to `'STAR'` + `#activeScope`, so the client cannot address any other Resource-hosting node. Surfaced 2026-06-29 by the `/review-task` panel on `tasks/archive/nebula-devstudio-data-plane.md` (Child 1 needs the client to reach a `DEV_STUDIO` Resource host). **That task makes the binding injectable (default `'STAR'`)** as its immediate fix; this item tracks the broader generalization — once ≥2 node types host Resources, the client should treat "which node hosts this Resource" as a first-class routing concern (not a per-call-site constant), and the mesh/client API should make that ergonomic rather than requiring a per-site swap.

- [ ] **`this.svc.ai` capability — wrap the hand-rolled Workers-AI REST call (Mesh product feedback from Nebula).** `apps/nebula/src/dev-studio.ts` `callModel` now hand-rolls a `fetch()` to Workers AI (token-authed REST, `unwrapWorkersAiRest`) because the `env.AI` binding can't authenticate in the secret-less hosted lane (`tasks/archive/nebula-in-ci.md` Phase 2). Per ADR-007, the durable form is a **per-node `this.svc.ai` capability** composed in like `this.svc.sql`/`this.svc.email`. The wrap should NOT merely relocate the REST call — it should **move the blocking AI `fetch()` off the DO's wall-clock** (two-one-way to a Worker), so it isn't mistaken for a pure refactor (`mesh.md` § two-one-way + `durable-objects.md` § wall-clock). Deferred to its first real consumer; until then Nebula keeps the in-`callModel` REST seam.

- [ ] **Broadcast-to-client could go fully async (defer the Star's wall-clock hold) — mesh follow-up from the continuation-only refactor.** Under `tasks/mesh-continuation-only-calls.md` pin svc.broadcast (b), a Star broadcasting to a **client** via the Gateway is the ONE deliberately-awaited, non-early-ack leg: the Gateway (non-mesh) awaits client delivery up to `CLIENT_CALL_TIMEOUT_MS` and returns `ClientDisconnectedError` synchronously, so the Star holds `#dispatchEnvelope` (and wall-clock) across the client round-trip. Accepted for the best-effort tier (broadcasts are fast + `onErrorOnly`). If that held window proves costly, route broadcast-to-client through a **Gateway fire-back** (early-ack + the D17 `__handleResponse` door) like every other client leg. Scope call deferred to Larry; tightly related to the `waitUntil` question below.

- [ ] **Fence-safe direct-broadcast primitive for `lmz-api` composers (Mesh product feedback from Nebula).** `svc.broadcast` is **`LumenizeDO`-only** ([lumenize-do.ts](../packages/mesh/src/lumenize-do.ts)) and its tier-worker path rewrites `metadata.caller`, so it can't serve a raw `lmz-api` composer that needs a **cross-scope** fanout gated on `metadata.caller.bindingName` — the Profile DO's `PROFILE`-fence ([nebula-profile-store.md](archive/nebula-profile-store.md) § Routing model). The Profile DO therefore **hand-rolls** a `lmz.call`-loop + a `@mesh() onProfileBroadcastResult` dead-subscriber cleanup mirroring `Star.onBroadcastResult` (it can't inherit it). Extract that into a reusable **direct-delivery broadcast** any `lmz-api` node (raw or `LumenizeDO`) can compose — no tier hop (so `metadata.caller` stays the origin node → the fence stays reliable at any N), `onErrorOnly` drop-on-failed-delivery built in. **Trigger: the 2nd consumer** (likely presence — its roster is also cross-scope-subscribable); until then the Profile hand-roll stands (< ~40 LOC, under the copy-paste-vs-extract bar). Related: the broadcast-to-client-async item above + the `NebulaClient` resource-host-binding generalization.

- [ ] Investigate use of waitUntil in lmz-api.ts. Do we actually need it? If we do, does it keep the DO in wall-clock billing mode? If we switched to making call always be two one-way calls, would we need it? *(2026-07-03: the mesh continuation-only Phase-1 spike — `tasks/mesh-continuation-only-calls.md` D15/m3 — now directly tests exactly this: whether `DurableObjectState.waitUntil` keeps a DO alive for a long post-ack fire-back, and its wall-clock/hibernation interaction. Fold this item's answer in from that spike's findings note.)*

- [x] ✅ **DONE 2026-07-14 — `ComposedMeshDO(Base, nodeTypeName)` mixin extracted (commit `c7889fd`; `index.ts` public-export follow-up).** De-dups the DO-mesh-composition glue that `LumenizeDO` + `LumenizeContainer` copied; the Profile DO ([nebula-profile-store.md](archive/nebula-profile-store.md)) will be the 3rd consumer. Landed in **`packages/mesh/src/lmz-api.ts`** + re-exported from `index.ts` (⚠️ the public **value**-export was **omitted in the first commit** — added 2026-07-14 as a follow-up, caught by the profile `/review-task` Stage-2; the mixin lives in `@lumenize/mesh`, NOT `apps/nebula`/`nebula-auth` — it factors code *out of* mesh classes; mesh must never import up-graph, [mesh.md](../.claude/rules/mesh.md) § Package dependency direction). `LumenizeDO` + `LumenizeContainer` now `extends ComposedMeshDO(DurableObject|Container, name)` + their à-la-carte capabilities; `LumenizeWorker` is out (different factory `createLmzApiForWorker`, no `ctx`). The mixin supplies `lmz`/`onBeforeCall`/`__executeOperation`/`__handleResponse` (with the `onValidationError` logging unified, namespaced by `nodeTypeName`).
  - **Two LumenizeDO-specific frictions the container-only spike didn't surface, both fixed cleanly:** (1) **`ctn()` stays per-class** — its `Continuation<this>` return can't cross the mixin boundary when a subclass concretizes an optional base method (`LumenizeDO` concretizes `DurableObject`'s optional `alarm`) → TS2417; the 5-line `ctn` stanza is left on each class. (2) **`super(ctx, env as Cloudflare.Env)`** in `LumenizeDO`'s ctor — the mixin erases `DurableObject`'s env generic, so the generic `Env` needs one cast. Plus the mixin body's 3 `this as unknown as {ctx,env}` casts (protected base members). No `@ts-ignore`, no gnarly generics.
  - **Validation:** `tsc` green on `packages/mesh` + `apps/nebula`. **Mesh suite 419 pass / 0 fail** (main + for-docs integration: getting-started/calls/alarms/security). **Nebula: zero new failures** — the baseline suite is currently **pre-existing-red** (surrogate-sub auth mid-flight → magic-link setup fails; `mixin-reverted` baseline fails identically, 244 vs my 243). One white-box container-prototype test updated (`hasOwn` → mixin-layer reachability). Re-run the auth-dependent nebula tests once surrogate-sub auth is wired.

- [ ] Split `getting-started/index.test.ts` so it's a clean 1:1 match with `getting-started.mdx`
  - Currently lines 81-151 add Bob multi-client collaboration, `createTestingClient` storage inspection, and spell check verification — none referenced by the `.mdx`
  - Move the multi-client/Bob scenario to a separate test file (e.g., `test/for-docs/getting-started/multi-client.test.ts` or a different for-docs folder)
  - The remaining test should mirror the `.mdx` Step 4 closely

- [ ] Improve continuation ergonomics (discovered during manual persistence test implementation)
  - **Reference**: `packages/mesh/test/for-docs/calls/document-do.ts` (`scheduleLocalTask`, `executePendingTask`) and `managing-context.mdx` Manual Persistence section
  - **Issue 1: Nested continuations execute immediately instead of being stored**
    - When passing a continuation as an argument (e.g., `persistTask(id, this.ctn().logMessage(msg))`), OCAN's `resolveNestedOperations` tries to execute it
    - Workaround: Create continuation inside the receiving method, not as an argument
    - Potential fix: Add `$defer` marker or make `getOperationChain()` work for remote types on client side
  - **Issue 2: Handler syntax easy to get wrong silently**
    - `this.ctn().handleResult` (no call) silently does nothing
    - Correct: `this.ctn().handleResult(this.ctn().$result)`
    - Potential fix: TypeScript lint for "continuation property accessed but never called", or proxy detection
  - **Issue 3: Void methods don't trigger local handlers (expected for remote, unclear for local)**
    - Remote: Expected (need response to confirm completion)
    - Local: Might be surprising when handler is never invoked
    - Consider: Document this clearly, or have void methods return `undefined` explicitly

- [ ] Add retry + skip logic for alarm handler failures
  - **Problem**: Currently, handler errors are caught and the alarm is deleted (one-time) or rescheduled (cron) — silent fire-and-forget with no retry
  - **Design**:
    1. Add `retryCount INTEGER DEFAULT 0` and `status TEXT DEFAULT 'pending' CHECK(status IN ('pending', 'skipped'))` columns to `__lmz_alarms` (migration needed)
    2. On handler failure, increment `retryCount` and set `time` to `now + 2^retryCount` seconds (exponential backoff)
    3. After 5 failures, set `status = 'skipped'` and log via `@lumenize/debug` (`lmz.alarms.Alarms` namespace)
    4. `triggerAlarms()` and `#scheduleNextAlarm()` filter with `WHERE status = 'pending'`
    5. Skipped alarms stay in the table as evidence — queryable via `getSchedules({ status: 'skipped' })`
  - **Why**: Prevents the 6-retry Cloudflare native alarm deletion (our `alarm()` never throws), gives transient failures a chance to recover, and makes persistent failures visible rather than silent
  - **Bonus**: This also closes the "dormant DO" durability hole — since we handle retries ourselves and `alarm()` never throws, Cloudflare never deletes the native alarm. Overdue alarms always get retried until they succeed or are explicitly skipped.
  - **Location**: `packages/mesh/src/alarms.ts` — modify `triggerAlarms()` error handling, add migration in constructor, update `#scheduleNextAlarm()` WHERE clause

- [ ] **`keepAlive` primitive for long-running DO operations (LLM calls, agent turns) — defeat idle-eviction during a single long await**
  - **The platform reality (analysis, 2026-06-22)**: per Cloudflare's [2026-06-19 changelog](https://developers.cloudflare.com/changelog/post/2026-06-19-outbound-connections-keep-dos-alive/) + [DO lifecycle docs](https://developers.cloudflare.com/durable-objects/concepts/durable-object-lifecycle/), an outbound **TCP / WebSocket** connection now keeps a DO alive for up to **15 min** — but **plain `fetch()`, Workers RPC, and bindings get nothing**. They're *subrequests*: while a handler awaits one, the DO counts as **idle** and is evicted after 70–140s of no *incoming* traffic, killing the in-flight call (streaming response bytes don't reset the clock). The shield is transport-specific (`connect()` TCP / outbound WS only, incl. a `fetch()` upgraded via `Upgrade: websocket`), NOT operation-duration. CF's own Agents docs confirm this shape is evictable and build `keepAlive`/`chatRecovery` for it.
  - **Motivating consumer**: `DevStudio.chat` ([apps/nebula/src/dev-studio.ts](apps/nebula/src/dev-studio.ts)) calls the model via the **Workers AI binding** (`env.AI.run`) — a subrequest, so **zero** connection shield. Today's single-SFC turn finishes inside the window, but the real agentic loop (multi-step, longer thinking) stacked on a cold container boot (`ensureUp`) can cross 70–140s with no incoming traffic → DevStudio evicted mid-generation. Wire the scoped heartbeat into `chat` when the agentic loop next gets a substantive pass (sequenced in [tasks/nebula-pre-alpha.md](tasks/nebula-pre-alpha.md); loop spec frozen at [tasks/archive/nebula-codegen-loop.md](archive/nebula-codegen-loop.md) — deliberately not wired here).
  - **Design**: a scoped `keepAlive` that **arms a self-rescheduling alarm** (≤~60s, under the 70s floor — go ~45–50s for jitter margin) at the start of a long op and **clears it at the end**. An alarm *is* an incoming event → resets the eviction clock; a self-`setTimeout` loop is internal JS (not clearly an "incoming event") and a single long one falls into the same idle-evict trap. Client-driven app-level pings are NOT the answer — a backgrounded / frozen / discarded tab is unreliable exactly when a long turn is running. Build it on **`svc.alarms`** (it already owns the single physical alarm slot) so it **multiplexes** with real app alarms instead of clobbering them — the slot collision is the actual integration cost. Scope it to the op (don't pin idle DOs alive forever — burns duration billing and defeats hibernation).
  - **Not a substitute for durability**: a heartbeat defeats *idle eviction* only; it does nothing for DO restarts/deploys (~1–2×/day) — anything you can't lose still needs checkpoint/resume. (The `cf-long-stream-limits` memory holds the platform-fact version of this, currently the older model.)
  - **Related**: the alarm retry/skip item above (same `packages/mesh/src/alarms.ts`); [tasks/icebox/lmz-call-eviction-safe-by-default.md](tasks/icebox/lmz-call-eviction-safe-by-default.md) (different mechanism — an RPC stub held across an await); [tasks/on-hold/mesh-cost-guidance-rebalance.md](tasks/on-hold/mesh-cost-guidance-rebalance.md) (the lifecycle-diagram billing data point — "waiting on a `fetch()`" bills; RPC rides the subrequest path).

- [ ] Investigate call chain depth and fanout limits
  - **Call chain depth**: We track `maxDepth: 50` in OCAN, but Cloudflare may enforce its own limit on RPC call depth
  - **Questions to answer**:
    1. Which limit is hit first — ours or Cloudflare's?
    2. Do sub-requests of sub-requests count toward Cloudflare's sub-request limit?
    3. What are the actual fanout limits for parallel calls?
  - **Potential workarounds for depth limits**:
    - Pass through an alarm (breaks the RPC chain)
    - Pass through LumenizeClient (WebSocket hop)
    - Establish WebSocket connection instead of Workers RPC
    - Cache/reuse RPC stubs (may only help with RpcTarget scenarios)
    - Return stub over RPC call (unclear if this helps)
  - **Experiment design**: Create test that chains N calls deep, measure where it fails
  - **Fanout experiment**: Create test that fans out to N parallel calls, measure limits
  - **Outcome**: Document findings, adjust `maxDepth` default if needed, document workarounds
- [ ] Consider adding explicit guidance that while async/await is discouraged, using Promise then/catch is fine. In that case, you are explicitly aknowledging that you know the input gates may open... or do they? We should answer that before deciding what to say.

- [ ] Consider adding these properties (.remote, .retryable, .overloaded, etc.) to lmz.call() errors: https://developers.cloudflare.com/durable-objects/best-practices/error-handling/. Maybe even consider using .idempotent, .safeToRetry or something to indicate that the error was thrown before any state change occured.

- [ ] Start using GitHub Releases

- [ ] Consider further decoupling LumenizeClient token refresh from `@lumenize/auth`
  - Current design: `refresh: string | () => Promise<string>` (docs updated 2025-01-14)
  - String form: POST to endpoint, expects `{ access_token }` response (default: `/auth/refresh-token`)
  - Function form: Custom refresh logic returning token string directly
  - Consider: Should we support additional response shapes for string form (configurable field name)?

- [ ] Consider expanding NADIS to LumenizeClient and/or LumenizeWorker. Right now, I can't think of a compelling reason for it. Alarms, sql, and fetch.proxy are all DO specific. However, debug is not.

- [ ] Build something that use the npm create functionality or Cloudflare's own deploy button or Cloudflare may have it's own create workers project plugin capability.

- [ ] **`createTestRefreshFunction` can't load under plain Node — make it Node-safe (2-line fix).** [`packages/mesh/src/create-test-refresh-function.ts`](../packages/mesh/src/create-test-refresh-function.ts) statically imports `signJwt`/`importPrivateKey`/`createJwtPayload` from the **`@lumenize/auth` barrel**, which transitively pulls `cloudflare:workers` → `ERR_UNSUPPORTED_ESM_URL_SCHEME` in Node. It is also **not exported from `packages/mesh/src/client-index.ts`** (the Node/browser-safe entry), so even once the import is fixed you can't `import { createTestRefreshFunction } from '@lumenize/mesh/client'`. Both gaps must close for a Node harness to use it.
  - **Nothing is broken today** — every current consumer runs inside vitest-pool-workers (Workers runtime), where the barrel import is fine. That's why it went unnoticed. It bites only from plain Node/tsx.
  - **Fix**: import from **`@lumenize/auth/client`** (whose header states those primitives live in `./jwt`, "no `cloudflare:workers` — safe to pull from Node test harnesses and CLIs") + add the export to `client-index.ts`. Near-zero risk: same three functions, same module, just the Node-safe subpath; Workers behavior unchanged. The `await import('cloudflare:test')` fallback is already dynamic and only fires when no `privateKey` is passed, so it stays fine. Add a regression test that imports it from Node.
  - **Why it matters**: it's public, documented API (`website/docs/mesh/testing.mdx`, `website/docs/auth/testing.mdx`) used across the mesh for-docs mini-apps (`getting-started`, `calls`, `calls/peer-guard`, `security`) plus its own unit test. And the need is proven: [`packages/nebula-auth/src/create-nebula-test-token.ts`](../packages/nebula-auth/src/create-nebula-test-token.ts) exists partly *because* of this gap — its header advertises "Node-safe by construction… runs under plain Node (tsx) against a real `wrangler dev`." The Node-safe version got solved once in nebula-auth instead of in mesh; closing this removes a reason for further divergence.
  - **Low urgency, low risk.** Surfaced 2026-07-22 while building a Node bench for `experiments/do-cold-start-bundle-ab` (had to inline the mint to work around it).


## Testing & Quality

- [ ] **`type-check` is not a CI gate — TS errors merge green.** `npm test` + CI (`ci.yml`) run vitest (esbuild/swc strip types without checking) + the test-mode audit; `npm run type-check` (`scripts/type-check.sh`, plain-tsc per package) is **separate + manual, never in CI**. So a type error (missing required field, wrong type) does not fail the build. Known blockers to promoting it to a gate: the plain-tsc loop **can't check the Vue SFC packages** (`nebula-studio-ui`), and the `tsconfig.build.json` publish path was already broken (see the do-exports/toolchain-upgrade follow-up). **Decide:** close those gaps and wire `type-check` into CI so "required field, no default → TS error" actually self-heals — or accept it's desk-only and lean on runtime guards. Surfaced 2026-07-18 designing the Galaxy write-debounce config (a required, no-default option whose self-healing was assumed to ride on a TS error).

- [ ] **ui-smoke's `--local` boot likely hangs locally (same detect the live harness hit).** The `ui-smoke`/`browser` `global-setup` boots via `hostedLocalBoot = !process.env.CLOUDFLARE_API_TOKEN → --local`. Building the `/live` harness (2026-07-05) proved `--local` + the apps/nebula DevContainer **HANGS after the image build** (workerd up, never `Ready on`, reproduces with a bare `wrangler dev`); the harness fixed it by booting **no-`--local`** (the `wrangler login` OAuth session covers the remote AI/email bindings). A login session is NOT a `CLOUDFLARE_API_TOKEN` env var, so the detect wrongly forces `--local` on a normal local run → ui-smoke likely hangs the same way locally (it may only ever run green in CI/hosted where the token IS set). **Verify:** run `npx vitest run --project ui-smoke` locally; if it hangs on boot, drop the `--local` auto-detect (or gate it on a real no-OAuth signal, not the token env var). See `apps/nebula/harness/FINDINGS.md` + the `reference-apps-nebula-local-boot-no-local-flag` memory.

- [ ] **Two divergent slug validators (single-source-of-truth smell).** `apps/nebula/src/dag-ops.ts:68` `SLUG_REGEX` (DAG node slugs) allows internal consecutive hyphens (rejects only leading/trailing); `packages/nebula-auth/src/parse-id.ts isValidSlug` (auth instance ids) is **stricter** — it ALSO rejects consecutive hyphens. So a string can be a valid DAG slug but an invalid auth instance (`test--u0` → auth 400 `invalid_instance`). Discovered 2026-06-24 building the local UI smoke (the review's `test--` marker had to become single-hyphen `test-`). Decide the canonical rule and converge (or document why they legitimately differ); a shared `isValidSlug` would prevent the next caller from tripping the gap.

- [ ] **A `wrangler dev`(+Docker) / prod e2e + smoke lane** *(LOCAL UI-smoke half ✅ BUILT + green 2026-06-24 — Phases 1–2 of Task ② `tasks/archive/nebula-local-smoke.md`, the `ui-smoke` vitest project; the `it.skip`-promotion half (A) was **reconciled by deletion — see (A) below**; the **prod** UI-smoke half rides the deploy task ③)* — share the same wrangler-dev-backed harness (the `ui-smoke`/`browser`/`chromium` projects auto-spawn `wrangler dev`; `global-setup.ts` supports a `BENCH_BASE_URL` override pointing at a *deployed* worker). Surfaced 2026-06-24 during `/review-task` of `tasks/nebula-release-process.md`.
  - **(A) The codegen/Container `it.skip` promotion — DISSOLVED 2026-06-24 (Task ② build; → zero `it.skip` in `apps/nebula`).** Those skips were **empty placeholders** written low-level (in-process `stub.__executeOperation` with synthetic claims) *only* because pool-workers can't construct a `Container`/`env.AI` — not because the worry is low-level. Now that the `ui-smoke` lane drives the real stack top-down, the worries are covered there or by existing deterministic suites, so the skips were **deleted, not ported** (no `__executeOperation` network-driving spike — that whole framing dissolved):
      - **Happy-path round-trip + non-blank render** (was the fetch/applyChanges/syncToDevContainer skips) → the `ui-smoke` prompt→preview test (asserts the regenerated app renders non-blank in the container-served preview).
      - **Self-correction + TurnRecord recording** (was the live-`chat()` skips) → already deterministic in `codegen-loop.test.ts` (error-tail-round-trips + `m4` TurnRecord); live confirmation = the smoke.
      - **Request-scope / command-port decoy** (security) → a focused Node-fetch test in the `ui-smoke` lane (`?activeScope=evil` ignored; `cf-container-target-port:9000` can't reach the command server).
      - **`json_schema` probe** → deleted (a one-off capability investigation, not a regression; typia post-validate ships + is covered).
    - **Residual hardening worries** (small + optional — a focused **unit** test only if one regresses, NOT a lane e2e):
      - **(i)** DevContainer **cold-boot** recovery + `ensureUp` boot-race retry.
      - **(ii)** `applyOntologyChange` **ordering** — container `setAppVersion` + source push BEFORE the Star install (a transient stale-version-flash guard).
      - **(iii)** dev-data **reset generation counter** — a `doTransaction` suspended at `facet.parseBatch` can resume AFTER `resetDevData`'s wipe and commit a Snapshot into the emptied tables, orphaning `Snapshot.nodeId → Nodes`. Bounded for the demo (single-admin; Studio quiesces its write queue before reset). Was `baseline/dev-star-data-lifecycle.test.ts`'s `it.skip`.
      - **(iv)** version-contract "ops succeed against the injected version" is **Wave-2 data-bound** (no backend ops until then; the reload-channel half is already covered in `reload-version-contract.test.ts`).
  - **(B) A small (≤5) TRUE UI-level smoke suite (Playwright drives the rendered Studio), runnable against BOTH `wrangler dev` AND prod. ✅ LOCAL half BUILT + green 2026-06-24** (Task ② — the `ui-smoke` project, 4 tests: shell / real-email login→connected / prompt→preview-non-blank / scope-decoy security; raw Playwright on the apps/nebula `wrangler dev`). **Remaining = the PROD run** (re-target the same body at a deployed URL — blocked on ③ serving the Studio from the worker, below). `test/browser/smoke.test.ts` is **API-level** (`NebulaClient` JS + fetch/cookie-jar `Browser`, no DOM); the `chromium` project is real-Chromium but only mounts **synthetic** mini-components (`document.body.innerHTML=…` + `Vue.createApp`) for reactivity tests. A UI smoke instead navigates the rendered Studio and **uses the UI**: minimal bar = **log in through the UI** (fill email, click send, complete the magic-link via the deployed `email-test` worker loop, land back) + **assert the key Studio UI elements render** (chat/app shell); richer = a ≤5 multi-step journey (set up state via the UI → check → manipulate → recheck). Use the `chromium` Playwright project + the same-origin proxy (`testing.md` § Real-browser tests).
    - **⚠️ PREREQUISITE (blocker):** the Studio UI shell must actually be **served from the deployed worker**. As of 2026-06-24 it is **NOT** — `apps/nebula/wrangler.jsonc` has no `assets` binding, `entrypoint.ts` 404s the root (its comment says "prod serve is Workers Assets" but that's unwired). The UI smoke can't exist until the Studio UI is served from a deploy (Workers Assets). See the open question in `tasks/nebula-release-process.md` / `tasks/nebula-pre-alpha.md`.
    - Also note `smoke.test.ts` itself can't target real prod (its `StarTest.applyOntologyForTest` is a test method absent from prod's `Star`) — an API-level prod smoke would need public-API-only flows through the real dev-apply path in a throwaway Universe with teardown; secondary to the UI smoke.
  - **Why not in the release task:** both are test-infra with their own design (env gating, Docker-in-test, AI flakiness; prod-smoke state setup/teardown + public-API rewrite). The first prod deploy already gets a minimal smoke (`/_version` compare + manual super-admin login). Pairs naturally with the eval/reward-function work now folded into (`tasks/nebula-studio-self-improvement.md`). **(B) is the high-value step right before sending pre-alpha invites.**

- [ ] **Smoke/e2e suites self-provision a random universe/galaxy/star per run** (≈1 week after Task ② `tasks/archive/nebula-local-smoke.md` landed — a planned upgrade). The Task-② UI smoke starts on a *fixed pre-provisioned* test scope; upgrade it to self-provision a **random** scope through the self-signup flow (`claimUniverse` / `createGalaxy`), torn down at the end with `ctx.deleteAll()` (DOs cost ~nothing at rest, so a stray orphan from a rare process-lockup is harmless; vitest teardown is reliable on ordinary failures). **Double duty:** exercises the **self-signup flows** AND makes test suites **parallel-safe** (no shared-scope collision — valuable as devs/CI grow). Prereq from Task ②: the harness is written **scope-parameterized** (scope as a fixture input, never hardcoded), so this is a small change.
  - **Teardown prereq (verified 2026-06-24):** provisioning is **registry-only** — the platform DOs self-provision lazily (no explicit `createStar`; `Star.onStart` seeds the founder admin on first touch); parents don't track children → the scope's DO set is **deterministic from the name**. Two halves:
    - **DO storage — a uniform, gated teardown (Larry's design):** the scope's DOs are `Universe`/`Galaxy`/`Star`/`DevStudio` (all `extends NebulaDO`) + `DevContainer` (`extends NebulaContainer extends LumenizeContainer` — NOT a NebulaDO) + the per-scope `NebulaAuth` (raw `DurableObject`). Add a **gated `teardown()`/`deleteAll()` mesh method to `NebulaDO`** (covers all four incl. the DevStudio `Workspace` source) **and to `NebulaContainer`/`LumenizeContainer`** (covers the container — which must also **stop the running container instance**, not just wipe `ctx.storage`). `Star.resetDevData()` becomes a thin `.dev`-guarded wrapper; `NebulaAuth` keeps its own teardown (subject-delete exists). It's a **per-node storage capability** (ADR-007) and **must be super-admin-gated** (destructive). Placement sub-question: the MIT base (`LumenizeDO`/`LumenizeContainer`, reusable, gate via `onBeforeCall`) vs the nebula base (`NebulaDO`/`NebulaContainer`, pre-gated) — decide at design time. (`NebulaClientGateway` is zero-storage; Resources/Subscriptions/DagTree are facets *inside* Star.)
    - **Registry rows:** `NebulaAuthRegistry.removeEmail()` + subject-delete (`DELETE /subject/:id`) already exist, but **NO `Instances`-row removal anywhere** (grepped: no `unclaim`/`removeInstance`/`deprovision`). Needs a new **super-admin-gated nebula-auth deprovision/instance-removal endpoint** (`DELETE` the scope's `Instances` + `Emails` rows) — the inverse of `claimUniverse`/`createGalaxy`.
    - **Do-now subset (Larry, 2026-06-24) + later:** **(1) super-admin as an ARRAY** — `NEBULA_AUTH_BOOTSTRAP_EMAIL` is a single env var today; upgrade to support multiple, so a dedicated **test super-admin** exists distinct from `larry@lumenize.com`. Name: **`automation.superadmin@lumenize.io`** (Larry, 2026-06-24 — covers test + CI; conveys both purpose and super-admin privilege). Same blast radius as `larry@` (shared CF Email Routing → same Gmail), so not *extra* attack surface — but still env-only (`.dev.vars` / `wrangler secret put`), and extend `audit-test-mode.sh` to the whole array. **(2) nebula-auth find-children** — a registry query for a scope's children (prefix match on `Instances.instanceName`: `{u}.*`, `{u}.{g}.*`); doesn't exist today. **(3) base-class `deleteAll()` super-admin-gated + CASCADING** — children-first (Universe → its Galaxies → their Stars → itself), driven by (2). Dep direction: platform DO → nebula-auth ✓, never reverse. **Test note:** the self-signup test self-provisions as a *random scope-admin* (exercises real signup); **teardown runs as the test super-admin**, so the run uses two identities. **Later:** self-scope-gating (a Universe/Galaxy/Star admin deletes their *own* scope with a UI "are you sure" — most Galaxies have no local admin, their Universe admin does it), and the nebula-auth gating audit (separate § Nebula Auth item).
  Surfaced 2026-06-24 (Larry's reframe of test cleanup).

- [ ] **Retire the fanout-agents benchmark → drop the `agents` dep from `apps/nebula`.** The Lumenize-broadcast-vs-Cloudflare-Agents fanout comparison has served its purpose; results are already captured in `apps/nebula/test/browser/RESULTS-fanout-agents-{local,deployed}.md` + `RESULTS-fanout-comparison-local.md` (KEEP those — blog 2e "Scaling Durable Object broadcast" + archived `fanout-scaling-benchmark.md` cite them). Delete the harness: `test/browser/fanout-agents.benchmark.ts`, `test/browser/worker/bench-agent.ts`, `test/browser/agents-harness-client.ts`, the `routeAgentRequest`/`Agent` imports in `test/browser/worker/index.ts`, the `bench:fanout:agents` script, and the `BenchAgent` binding in `test/browser/worker/wrangler.jsonc` — then drop `"agents": "^0.14.2"` from `apps/nebula/package.json`. Memory `fanout-bench-setup` / rule `mesh.md` already flag it as a "known straggler." Keep the `doc-test/.../testing-agent-with-agent-client` example (it legitimately documents `@lumenize/testing` + CF Agents, on its own `agents@0.2.13`). Surfaced during the 2026-06-22 tasks/ cleanup.

- [ ] Consider upgrading `@lumenize/testing`'s `websocket-shim` to use a real WebSocket client and integrate the `Browser` cookie jar
  - **Why**: Currently the shim is fetch-based (does the upgrade through `Browser.fetch`, which carries cookies). It works fine for our auth model where the access token rides in the `lmz.access-token.<jwt>` subprotocol — cookies aren't checked on the WS upgrade. But if a future auth scheme ever validates a session cookie at the upgrade step, the shim would already cover it. Conversely: today's nebula browser harness *probably* falls back to Node's native `WebSocket` for the round-trip path because the shim was built around the Cloudflare Workers `ws` server-side semantics, not a generic-network-WebSocket client. A real WebSocket client (e.g. `ws` package or Node 22's native, with cookies threaded through) would unify both layers.
  - **Discovered during**: Phase 2 of `tasks/archive/nebula-deployable-and-browser-harness.md` — investigating whether `browser.WebSocket` is suitable as the WS layer for the nebula round-trip test.
  - **Scope**: small but non-trivial — would need to either find a real WS client whose handshake we can override (so we can attach the cookie header from `Browser`), or roll our own thin upgrade wrapper. Not blocking anything today; revisit when reactivity tests need real WS auth flows.

- [ ] Consider promoting `waitForEmail` / `extractMagicLink` helpers from `packages/auth/test/e2e-email/email-test-helpers.ts` to a reusable location
  - **Why**: These helpers are auth-system-agnostic — they just talk to the deployed `email-test` Worker (`https://email-test.transformation.workers.dev`) over WebSocket. They aren't `@lumenize/auth`-specific or Nebula-specific. Currently the Nebula browser harness has to either copy them or import via relative path through another package's `test/` dir.
  - **Where they could live**: `@lumenize/email-test` (which already exists for the deployed Worker types) could export the client helpers as a subpath. Or a new `@lumenize/testing/email` subpath.
  - **Discovered during**: Phase 2 of `tasks/archive/nebula-deployable-and-browser-harness.md` — adding `auth-bootstrap.ts` to the nebula browser harness, which needs the same helpers.
  - **Don't do this now** — wait until the third consumer needs them.

- [ ] Audit all try/catch block to include cause chains. Make sure structured-clone supports arbitrarially deep cause chain reconstructions including custom errors.

- [ ] Audit tests for unawaited expected-rejection cases and remove `dangerouslyIgnoreUnhandledErrors` flag
  - **Why**: During the vitest 3→4 migration we added `dangerouslyIgnoreUnhandledErrors: true` to every `vitest.config.*` because vitest 4 now fails the run (exit 1) on unhandled rejections that vitest 3 silently swallowed. The flag restores vitest 3's behavior but masks real issues (see follow-up notes in that task).
  - **Pattern to find**: Tests that fire-and-forget a promise which is expected to reject. Example:
    ```ts
    someOp();  // kicks off an op that will reject
    expect(somethingElse).toBe(x);  // test completes
    // someOp's rejection arrives AFTER the test → unhandled
    ```
  - **Fix pattern**: Either `await` the promise and use `rejects.toThrow()`, OR attach `.catch()` to silence the expected rejection:
    ```ts
    someOp().catch(() => {});  // expected to reject
    ```
  - **Also common**: teardown code that disconnects a client (e.g., `client[Symbol.dispose]()`) which rejects pending in-flight promises. These need `.catch` on the in-flight promises, or the disconnect should settle them gracefully.
  - **Success criteria**: Remove `dangerouslyIgnoreUnhandledErrors: true` from every vitest config, full suite runs green with exit 0.
  - **Overlap**: Very related to the RPC-await audit above — both surface through "Unhandled Errors" counts. Probably worth doing together.

- [ ] Audit RPC call sites for proper `await` and turn off SonarQube `no-return-await` rule
  - **Why**: Cloudflare DO best practice says always `await` Workers RPC calls — unawaited calls become fire-and-forget, which (a) may rejection-leak (surfaces as vitest 4 "Unhandled Errors"), and (b) can end the request context before the RPC completes. SonarQube's `no-return-await` rule pushes you to remove `await` from `return await rpc()`, which changes try/catch semantics in a DO-hostile way: `return await foo()` catches errors *locally* in the current try/catch; `return foo()` passes them to the caller. The `no-return-await` rule was retracted by ESLint/typescript-eslint in 2023–2024 for this exact reason — SonarQube hasn't caught up.
  - **Audit approach**:
    1. Grep for RPC call patterns: `grep -rn '^\s*\(env\|this\.\w\+\|stub\)\.\w\+(' src/ packages/*/src/`
    2. For each hit, classify: awaited / returned / fire-and-forget
    3. Fire-and-forget cases must be intentional (`ctx.waitUntil` / `lmz.call` with explicit callback per the fire-and-forget-error-delivery pattern in CLAUDE.md) — anything else gets an `await` or `return`
    4. Inside try/catch that wants local error handling, prefer `return await` over `return` (SonarQube will complain — that's the point)
  - **Config change**: Disable SonarQube rule `typescript:S7785` (or whichever `no-return-await` equivalent applies to this project) in the project's SonarQube config
  - **Related**: the "Unhandled Errors" count in vitest 4 output is a proxy metric — if the audit drives it down, we can eventually remove the `dangerouslyIgnoreUnhandledErrors: true` flag from vitest configs. (Discovered during vitest 3→4 migration, 2026-04-20)

- [ ] Add vitest-workers-pool tests for `@lumenize/debug`
  - Current tests run in Node.js only (plain vitest)
  - Need Workers pool tests to verify `cloudflare:workers` env auto-detection works end-to-end
  - Test that `env.DEBUG` set in wrangler.jsonc/miniflare bindings is picked up without any manual configuration

- [ ] Add consistent debug logging across all Lumenize packages
  - Currently only added debug logging where we actively debugged issues
  - Should have comprehensive logging at key points in all packages
  - Use appropriate levels: debug (everything), info (milestones), warn (expected issues)
  - All Lumenize internal namespaces should use `lmz.*` prefix

- [ ] Refactor RequestSync/ResponseSync to not use real Request/Response objects internally
  - **Problem**: Currently uses real Request/Response objects under the covers, inheriting platform-specific quirks
  - Workers returns `undefined` for credentials/mode/referrer, browsers return `'about:client'` for referrer
  - Defeats the purpose of having a synchronous, platform-independent serialization API
  - **Solution**: Store all properties as plain data, only create real Request/Response in `toRequest()`/`toResponse()` methods
  - Would provide consistent behavior across all environments (Workers, Node, Browser)
  - Properties would return exactly what you set, no platform surprises

- [ ] Calibrate `/review-task` against real task files (carried over from archived [task-review-panel](archive/task-review-panel.md))
  - Run the panel against a recent task file AND a *completed* one where we know what the manual loops found — does it surface the same issues? Findings must be actionable, not generic advice.
  - **First data point (2026-06-10, coding-your-ui.md panel — [findings](archive/nebula-frontend-docs-panel-findings-2026-06-10.json))**: calibration probe re-reviewed the half Larry had hand-reviewed line-by-line and found 7 standing issues (3 source-verified majors), with **zero false positives across all 45 findings panel-wide**. Missed-major density: ~1.4/100 lines in the reviewed half vs ~1.8/100 unreviewed — human line-by-line review only modestly beat no review on *cross-artifact contract accuracy* (the misses all required tracing prose into source). Conclusion so far: trust the panel for factual/contract checking; human review's edge is intent/scope/audience altitude.
  - **Lens subsetting**: the skill says to drop irrelevant lenses (e.g. security on a pure refactor) — confirm manual subsetting is enough or whether it should auto-pick from task content.
  - **Product lens depth**: how much "vision coherence" is reviewable mechanically vs. needs a human? Sharpen the `product` lens prompt if it returns generic advice.
  - **Second data point (2026-06-15, §5.3.7 Nebula-frontend v1–v5 build retro — the largest single-batch build)**: the `/review-task` → `/build-task` cycle worked as designed — after review, the build ran at **~0.46 mid-build design decisions/phase, zero behavior churn, one structural reversal** (separate-package → subpath, caught at scaffolding). Verification earned its keep beyond rubber-stamping: a `/build-task` P9 verify panel surfaced an unguarded HTTP-403 operand that became a permanent rule (`testing.md` "mutation-check each operand of a compound condition"). **Friction was not in task-writing** — it was empirical test-infra discovery concentrated in the thin-spec **v4** phase (v4 carried 0 pinned decisions vs v3's 34; WS-disconnect tooling took 3 tries). **Fix shipped this retro**: the "vN phase = pinned OR explicitly exploratory" convention (`tasks/README.md`) + its enforcement in `/review-task` (spec-density flag) and `/build-task` (exploratory-phase verifier bar).
  - **Ultracode / ad-hoc-workflow value (same retro)**: of ~9 fan-outs across the build, **8 were skill-prescribed** (`/review-task` panel + `/build-task` one-verifier-per-phase); a standing multi-agent opt-in adds **nothing** on the parallel-code-writing axis (forbidden by the sequential-single-writer rule) and is largely redundant with `/build-task` on in-phase verification. It earned its keep on **exactly one** fan-out — the v5 docs-vs-shipped-source **drift audit** (3 agents), a SEAM neither skill covers (`/review-task` is pre-build over a task file; `/build-task` verifies code-vs-task-file, not shipped-doc drift) — which caught a "flush on unmount" overclaim propagated across two docs + an `authScope` auto-detect overclaim. **Takeaway**: reach for an ad-hoc workflow over the skills only in their seams (post-build doc/source reconciliation, completeness audits of finished artifacts, lens-widening beyond the default 4) — not for build/review work the skills already own. (Candidate: a small `doc-source-drift-audit` skill to formalize the one fan-out that paid off.)


## Documentation

- [ ] Review, edit, and convert `@skip-check` annotations in `website/docs/mesh/protocol.mdx` (13 pending)
  - File is explicitly labeled as a draft — decide which blocks should be `@skip-check-approved('conceptual')` vs converted to `@check-example`
  - Many are TypeScript interface definitions that could point at real source files

- [ ] Enforce `@skip-check` fails the build (only `@skip-check-approved` should pass)
  - **Current state**: `check-examples` silently skips both `@skip-check` and `@skip-check-approved` blocks
  - **Goal**: `@skip-check` (pending) should fail the build; only `@skip-check-approved` (human-reviewed) should pass
  - **Implementation**: In `tooling/check-examples/src/index.js`, add error collection for non-approved skips in verification mode
  - **Context**: `@skip-check` is meant to be temporary during Phase 1 drafting; we need enforcement to ensure examples are tested before publishing

- [ ] Review `@lumenize/auth` API Reference section for what should be public vs internal
  - JWT Utilities: `signJwt`, `verifyJwt`, `verifyJwtWithRotation`, `importPrivateKey`, `importPublicKey`, `parseJwtUnsafe` — which are truly needed by users?
  - WebSocket Utilities: `extractWebSocketToken`, `verifyWebSocketToken`, `getTokenTtl`, `WS_CLOSE_CODES` — internal implementation details?

- [ ] Add "Why continuations help with race conditions" explanation
  - The key insight: continuations make temporal gaps **explicit** rather than hidden in awaits
  - With async/await, you can accidentally read-await-write and create races
  - With continuations, the "this happens later" part is structurally visible
  - You can still have race conditions, but you're not accidentally creating them
  - Could be a section in continuations.mdx or a standalone concurrency guide
  - **Reframe per the do-throughput post**: race-prevention is a side-effect of the real value, which is making remote work *explicit* at points where pretending it's local would mislead you. The current framing somewhat over-emphasizes race-prevention; soften toward "explicit remote-work" as the headline benefit.

- [ ] Soften "favor sync over async" emphasis across docs and assistant guidance
  - **Why**: The DO concurrency model framing — "input gates make code passively correct so long as you don't await" — is elegant for simple workloads but insufficient at scale. Once a system has work crossing Workers RPC boundaries, sibling-DO coordination, or interleaved invocations to hit throughput, you need explicit mechanisms (eTags, two-phase commits, idempotency keys, version vectors). Current docs over-emphasize "no await, no race" as if it's the whole correctness story.
  - **Where to audit**:
    - `.claude/rules/durable-objects.md` ("Keep methods synchronous" + related sections)
    - `.claude/rules/mesh.md`
    - `packages/mesh/**/*.mdx` files that emphasize sync over async
    - `MEMORY.md` and feedback memories (esp. update with the evolution context — input-gate correctness was a useful early learning that doesn't *replace* explicit-mechanism thinking, just sets the floor)
  - **What softening looks like**: Don't drop the guidance — input gates ARE great for simple cases. Reframe as: "for simple workloads, input gates carry you a long way; for moderately complex distributed systems, you'll need explicit mechanisms (eTags, two-phase commits, version vectors) in addition." The Actor model inspiration still holds — it just has more in its toolbox than 'don't await.'
  - **Discovered during**: writing the do-throughput-misread blog post.

- [ ] Audit docs for over-emphasis on "wall-clock billing in the DO is the deciding factor"
  - **Context**: We've already softened one instance (see `feedback_short_settimeout_billing.md` — short setTimeouts in DOs aren't worth fretting about). Likely others remain.
  - **Where to audit**: `.claude/rules/durable-objects.md` ("Wall-clock billing" section), `.claude/rules/mesh.md`, mesh docs, `lumenize-do.mdx`, etc.
  - **What softening looks like**: Don't drop the guidance — wall-clock billing IS real. But avoid framing it as the SOLE deciding factor. Correctness, simplicity, throughput, and maintainability often outweigh wall-clock cost in moderately complex systems.
  - **Discovered during**: writing the do-throughput-misread blog post.

- [ ] Add optimistic concurrency example to calls.mdx or a new concurrency patterns doc
  - Show version/timestamp checking pattern
  - Read state + version, do work, check version hasn't changed before writing
  - Retry or fail strategies
  - More advanced topic — don't clutter basic docs

- [ ] Document Saga pattern for distributed transactions using `callContext.state`
  - **Use case**: Accumulate transaction steps across multiple hops (even across DOs), then commit atomically at the end
  - **Pattern**:
    1. **Accumulate** — Each hop adds its "intent" to `callContext.state.pendingOps` (writes, deletes, validations)
    2. **Validate** — At chain end, check optimistic concurrency (versions match, no conflicts)
    3. **Commit** — Apply all ops atomically within the final DO's synchronous execution
  - **Why `callContext.state` fits**:
    - State propagates through the entire call chain automatically
    - Each request gets isolated state via AsyncLocalStorage (no cross-request leakage)
    - Already established for "compute once, use everywhere"
  - **Tricky part**: Each DO has its own storage — no true distributed transaction across DOs. But you can:
    - Collect all ops in state
    - Have the "coordinator" DO apply its own ops
    - Make calls to other DOs to apply theirs (with version checks)
  - This is essentially the Saga pattern, where `callContext.state` becomes the saga's context
  - **Location**: New section in a concurrency/patterns doc, or expand `callContext.state` section in security.mdx

- [ ] Audit all docs and internal identifiers to favor `callContext` over `metadata` or `meta` where referring to the user-facing API
  - `envelope.metadata` is the transport layer field containing `{ callChain, callee, originAuth }`
  - `this.lmz.callContext` exposes this with convenience getters: `origin`, `caller`, `priorCaller`
  - Ensure docs consistently use "callContext" when describing what users access
  - Check `lmz-api.ts`, `CallEnvelope` interface, and any JSDoc comments

- [ ] Add this to the docs: https://discord.com/channels/595317990191398933/773219443911819284/1439941400778117292

- [ ] Migrate older packages from doc-testing/TypeDoc to hand-written docs with check-examples
  - Packages still using doc-testing generated files: rpc (quick-start, capn-web comparisons, operation-chaining), testing (agents, usage), mesh (services)
  - Packages still using TypeDoc: rpc, utils, testing, fetch, structured-clone (configured in `docusaurus.config.ts`)
  - For each: write hand-written `.mdx` with `@check-example` annotations, add API reference page (see `auth/api-reference.mdx` for the pattern), remove TypeDoc config and doc-testing sidebar entries
  - Can be done incrementally, one package at a time

- [ ] Add llms.txt support https://github.com/din0s/docusaurus-plugin-llms-txt

## Future bigger things

- [ ] **Working document-editor example** (browser ↔ Gateway ↔ DO). Complete, deployable system that developers can clone and run. UI framework decision documented. "Deploy to Cloudflare" button or clear deploy instructions. Linked from mesh docs. Originally Phase 4 of `mesh-post-release-part-2.md`; pulled into backlog when that file was archived during the demo-focus refactor.

- [ ] **Working agent example** showing Mesh + Cloudflare's Agent pattern. At least one working example with `@lumenize/testing` AgentClient that demonstrates a practical use case (not just echo). Linked from mesh docs. Originally Phase 5 of `mesh-post-release-part-2.md`.

- [ ] Publish our test-endpoints as part of @lumenize/testing. It's particularly useful now that it can be run in-process. Does it still need a token when used that way? Should we rename it httpbin to match? What's different about it compared to httpbin?

- [ ] Research adding `ctnBatch()` API to `this.lmz.call` for atomic multi-operation execution
  - Syntax: `this.ctnBatch([this.ctn().methodA(), this.ctn().methodB()])`
  - Would execute multiple operations atomically on remote DO without yielding between them
  - Use case: Multiple related operations that should execute together (e.g., transfer funds = debit + credit)
  - Implementation: Use microtask batching pattern to ensure no interleaving
  - Could batch the continuations into single RPC call, then use microtask pattern on receiver side
  - Reference: [Discord - microtask transaction pattern](https://discord.com/channels/595317990191398933/773219443911819284/1440314373473046529)

- [ ] Add an alternative to ctn (maybe ctnTransaction) to use a transaction for execution


## Infrastructure

- [ ] See `tasks/icebox/npm-publish-via-github-actions.md` for automation plans

## Website, Blog, etc.

- [ ] Publish blog 2e "Scaling Durable Object broadcast" — flip `draft: true` → published at `website/blog/2026-06-06-scaling-durable-object-broadcast/index.md` (only remaining item from archived [fanout-scaling-benchmark](archive/fanout-scaling-benchmark.md))

- [ ] Publish blog 2d "When time stops: benchmarking Cloudflare Durable Objects from outside" — **already drafted**; flip `draft: true` → published at `website/blog/2026-05-09-benchmarking-cloudflare-durable-objects-from-outside/index.md` (bench archived in [gateway-hop-benchmark](archive/gateway-hop-benchmark.md); also tracked in MEMORY as "Publish 2d").

- [ ] Cross post on Medium like this
        > If you are not a premium Medium member, read the full tutorial FREE here and consider joining medium to read more such guides.


## @lumenize/auth

- [ ] Make `@lumenize/auth` an MCP-compliant OAuth 2.1 Authorization Server (agentic access)
  - **Why**: MCP spec (2025-06) pins remote MCP server auth to OAuth 2.1. Claude Desktop, Cursor, ChatGPT connectors all speak this. Without it, a Lumenize-backed MCP server can't auto-connect to standard MCP clients — users have to manually bolt on custom glue. This is the #1 agentic gap, not social login.
  - **Leverages existing work**: Ed25519-signed JWTs, refresh rotation, and the RFC 8693 `act` claim (Delegation) are already in place. The `act` claim is exactly the semantic primitive Token Exchange needs — missing piece is the standards-compliant façade around it.
  - **Endpoints/discovery to add**:
    - `/.well-known/oauth-authorization-server` (RFC 8414) — AS metadata
    - `/.well-known/oauth-protected-resource` (RFC 9728) — PRM discovery so clients can find the AS from the resource server
    - `/authorize` with consent screen (OAuth 2.1 + PKCE mandatory)
    - `/token` supporting authorization_code grant, refresh_token grant, and `urn:ietf:params:oauth:grant-type:token-exchange` (RFC 8693) — token exchange wraps the existing `delegated-token` flow
    - `/register` — Dynamic Client Registration (RFC 7591) so MCP clients can self-register without pre-provisioned credentials
    - Resource Indicators (RFC 8707) honored in `/authorize` and `/token` so tokens are scoped to the specific MCP server
  - **Open design questions**: consent screen UX (reuse admin approval email pattern?), client storage (new DO, or extend auth DO?), scope model (start coarse: `mcp:read`, `mcp:write`?), token format (keep current JWT shape or add audience-bound variant?)
  - **References**: [MCP auth spec](https://modelcontextprotocol.io/specification/basic/authorization), [RFC 8693](https://datatracker.ietf.org/doc/html/rfc8693), [RFC 7591](https://datatracker.ietf.org/doc/html/rfc7591), [RFC 9728](https://datatracker.ietf.org/doc/html/rfc9728), [RFC 8707](https://datatracker.ietf.org/doc/html/rfc8707)
  - **Scope**: Big enough to promote to its own task file (`tasks/auth-oauth-provider.md`) when scheduled

- [ ] Add OpenID Connect provider endpoints so other apps can "Sign in with Lumenize"
  - **Why**: Federated identity provider capability — lets a Lumenize deployment serve as the IdP for third-party apps, SSO out to other tools, etc. Medium severity — matters if customers want to wire their Lumenize account into other SaaS, less critical than the MCP flow.
  - **What OIDC adds on top of OAuth 2.1**:
    - `/.well-known/openid-configuration` — OIDC discovery document (superset of `oauth-authorization-server`)
    - `openid` scope triggers issuance of an ID token alongside the access token
    - ID token is a JWT with identity claims (`sub`, `email`, `email_verified`, `name`, etc.) signed by the same Ed25519 key chain
    - `/userinfo` endpoint (optional but expected) for clients to fetch additional claims
    - JWKS endpoint (`/.well-known/jwks.json`) publishing the public signing keys — we already have key rotation, just need to expose it
  - **Dependency**: Much of this rides on the OAuth 2.1 AS work above (discovery, `/authorize`, `/token`, consent). Natural follow-on, not independent.
  - **References**: [OIDC Core 1.0](https://openid.net/specs/openid-connect-core-1_0.html), [OIDC Discovery 1.0](https://openid.net/specs/openid-connect-discovery-1_0.html)

## Nebula

### Ontology depth (post-demo)

Strengthen the case that Nebula's data layer is an *ontology* (not just a typed schema + DAG). Triggered by the 2026-05-03 Kumar article and successors — see [tasks/reference/ontology-research.md](reference/ontology-research.md). Items below in priority order; each is independently shippable.

- [ ] **Action types** — typed semantic verbs on top of `resources.transaction` (e.g. `approveOrder`, `releaseHold`), each binding {typia input schema + precondition predicate + patch shape + emitted provenance event}. This is the single primitive most cited as the schema-vs-ontology dividing line (Palantir's moat; Fabric IQ's "permitted actions" is a shallower version). Highest narrative payoff per LOC. Provenance / audit-trail story falls out for free once verbs emit events.

- [ ] **Ontology introspection endpoint** — runtime API returning types, properties, relationships, action types, and constraints. typia already has this metadata at compile time; the work is exposing it. Required for Studio's LLM to ground itself without re-shipping types, and the cheapest item that flips Nebula from "typed CRUD" to "agent-readable ontology." JSON-LD / OWL serialization is a v2 if we ever want the vendor-neutral story.

- [ ] **Typed relationships** — promote DAG edges (or add a parallel relationship graph) to carry `relationshipType` with its own schema, navigable bidirectionally with `@inverse` already sketched in [website/docs/nebula/ontology.md](../website/docs/nebula/ontology.md). Without this the relationship story reads as "we have a DAG" rather than "we have an ontology graph." Cheap on top of the 5.3-shipped DAG normalization (`{nodes, edges, permissions}`).

- [ ] **Derived / denormalized-field invariants** — a declarative way to specify a stored field as a function of others (e.g. an annotation `@derived count(items where status === 'open')`) that the **server computes, stores, enforces, and lands in snapshot history** for time-series analytics — provably consistent, no client drift. The real fix for *stored* aggregates: round-4 (#9, 2026-06-14) made `openCount` a **client-computed view-only** value precisely because this doesn't exist yet (see [tasks/archive/nebula-frontend.md](archive/nebula-frontend.md) Out-of-scope + the round-4 decision). Extends the typia/JSDoc annotation vocabulary (ADR-001); feeds the temporal/analytics substrate (ADR-004). Likely pairs with **Action types** (the verb that maintains the invariant) and the **introspection endpoint** (exposing the derivation).

### Other Nebula backlog

- [ ] **`validFrom` global monotonicity (high-water mark) — future-proof transaction ordering for higher per-DO rates.** `#calculateValidFrom` ([resources.ts:140-161](../apps/nebula/src/resources.ts)) advances `validFrom` past the **batch's own** current snapshots only → per-resource monotonic, but two same-millisecond transactions on **disjoint** resources can share a `validFrom`. **Safe now** (Resources transactions peak ~400/sec, well under the 1000/sec ms-granularity floor — collision risk negligible). **Exposure:** if Cloudflare raises the per-DO throughput ceiling (e.g. ~4000/sec), same-ms collisions become real and break any use of `validFrom` as a **global** order. **Fix — server (Resources logic):** keep a **global high-water mark** and advance `validFrom` past *it* (not just the batch's max) → a strict total order of transactions on the DO. Two impls: **(A)** an in-memory mark seeded from `MAX(validFrom)` on cold start, advanced in memory (loss-acceptable, re-derivable — **no per-txn write**, at the cost of a cold-start derivation / a small `validFrom` index); or **(B)** a persisted KV high-water mark, read+write per txn (**+1 write/txn**, no cold-start scan — Larry's lean, "probably correct regardless"). Tradeoff to settle: advancing the ms under sustained >1000/sec makes `validFrom` a **logical clock** drifting from wall time; a composite `(validFrom, seq)` tiebreaker preserves wall-clock fidelity instead. **Fix — client:** the deferred query-sub delta-catch-up baseline ([nebula-query-subscriptions.md](nebula-query-subscriptions.md)) must track a client-side **high-water mark of every observed `validFrom`** (NOT the max of its current set — removed members aren't in it). Server global monotonicity + that client HWM together enable **temporal-diff-since-`validFrom`** membership deltas with **no change-feed** (the snapshot history is the substrate). **Trigger:** whichever comes first — the per-DO rate ceiling rises materially, or we build the deferred query-sub delta optimization (this is its prerequisite). Surfaced 2026-06-30 (Larry).

- [ ] **Soft-delete (tombstone) + grace-period reaper + undelete — NOT a hard `deleteAll()`.** De-risks scope deletion and is the **ADR-004 model one level up** (non-destructive/temporal is already how Resources work). The user-facing teardown method does **not** hard-delete — it **marks the scope deleted** (a tombstone) so the DO stops responding (returns *gone*) and **enqueues it for later hard delete**. A **reaper** (cron/alarm) runs the actual cascading `ctx.deleteAll()` after a grace window: **`test-`-prefixed scopes → ~24 h**, everything else → **30 days**. **Undelete:** hitting a tombstoned scope offers a UI "undelete yourself" (clears the tombstone) within the window — accidental-delete recovery for **real users**, not just tests. **Reframes the teardown/random-scope work** (§ Testing & Quality): the super-admin-gated method is the *soft*-delete; the cascading hard `deleteAll()` becomes the **reaper's** action. Open (not now): where the tombstone + deletion-queue live (the registry? a reaper DO?); whether soft-delete cascades the tombstone to children immediately or the reaper does; reserving the `test-` instance-name prefix from real `claimUniverse` (it must be slug-valid for BOTH validators — `dag-ops.ts:68` SLUG_REGEX AND the stricter `nebula-auth parse-id.isValidSlug`, which rejects consecutive hyphens, so a SINGLE-hyphen `test-` — and it is the reaper's auto-reap marker, shared with the local-smoke test scope). Surfaced 2026-06-24 (Larry, to de-risk delete).

- [ ] **Self-service support sessions via user-consented delegation (distinct from admin god-mode reach).** Any user can start a support session with a support agent (likely an AI agent). When the agent needs to inspect or alter data, it requests a **delegation the user explicitly approves**; the agent then acts **as the user** (RFC 8693 `act.sub`), bounded by exactly the permissions the user already holds, over the **same endpoints the user already reaches** — low-level Resource inspect/edit and anything else in the user's grant. **Mechanism = the existing `delegated-token` / act-as core** (`POST .../delegated-token`, `packages/nebula-auth/src/nebula-auth.ts`): permissions key off `sub`, so the agent inherits the user's access and can never escalate above it. **Deliberately NOT the [onBeforeCall higher-admin reach](archive/nebula-onbeforecall-higher-admin-reach.md) model** — that's a privileged Lumenize support engineer wielding `*`/`{u}.*` authority (god-mode); this is **consent-gated, least-privilege, available to *any* user**, not a platform-admin capability. **Net-new work:** the consent/approval UX + session lifecycle (start → request scope → user approves → time-boxed/revocable → audited) and the `AuthorizedActors` wiring for the support-agent principal (a non-admin actor must be authorized per-principal, unlike the admin bypass at `nebula-auth.ts:988`). Out of scope for pre-alpha. **Related but different:** the `delegated-token` consent UX sketched in [nebula-pre-alpha.md](nebula-pre-alpha.md) (UX-exploratory) and [nebula-request-access.md](nebula-request-access.md) — the latter *requests a new grant* by climbing the org tree, vs. this *borrowing the user's existing access*.
- [ ] **Audit log of denied authorization attempts (request-access fishing detection).** [ADR-008](../docs/adr/008-full-org-tree-visibility.md) makes the full org tree — including **admin identity** — universally visible within a Star, and names this audit log as the load-bearing surface for detecting fishing-style probing of that visibility. It is **referenced by ADR-008 as not-yet-built**: until it lands, abuse of admin-identity visibility is *unobserved*, not undetectable-by-design. **Detection primitive: log EVERY denied attempt** — every `PermissionDeniedError` throw (`dag-tree.ts` `requirePermission`) and every `'permission-denied'` transaction outcome (`sub`, target node, tier, outcome, timestamp). **At `warn` level, NOT `error`** — a denial is *normal control flow* here (it's what drives the request-access climb), so error level would cry wolf; warn means "worth noticing in aggregate" without flagging an expected outcome as a failure. The warn stream is the immediate signal (via the debug/observability sink); the persisted audit log is its durable per-Star rollup so the **actual admin** (not just platform) can see abnormal request patterns. Additive hardening *within* the full-visibility model — ADR-008's reopen bar explicitly welcomes this, no reopen needed. Pairs with the request-access flow ([nebula-request-access.md](nebula-request-access.md)). Out of scope for pre-alpha. Surfaced 2026-06-30 (ADR-008 review; warn-level + log-every-denial per Larry).
- [ ] **Studio: "clear app source" / true fresh-start.** `resetDevData` ([star.ts:313](../apps/nebula/src/star.ts)) wipes the `.dev` Star's *data* (Resources) but NOT DevStudio's Workspace *source* (the shell `Workspace` + git over `ctx.storage.sql`). So a hand-built `App.vue` persists across sessions and the codegen loop reads it as `currentSource` (`runCodegenTurn`, [dev-studio.ts](../apps/nebula/src/dev-studio.ts)) — there's no way to blank the app for a clean test. Add a DevStudio source-reset (clear the Workspace + re-`git init`), human-gated like the Flow-1b data wipe; decide whether "fresh start" wipes source AND data or just source. Surfaced 2026-06-22 when a codegen turn picked up the *previous* session's leftover todo app (the tell: it read a black-bg app it never generated this session).
- [ ] **Studio: breaking-ontology-change → deterministic Reset-data/Revert prompt (+ relabel & relocate the manual Wipe).** When a codegen turn changes the ontology schema, existing `.dev` test data may no longer fit. **Detect deterministically — never via the LLM** (a missed breaking change silently corrupts the dev loop = a footgun). Design locked 2026-06-26 with Larry:
  1. **Trigger only when the ontology source file changes.** The git-backed Workspace (isomorphic-git) gives a before/after tree per `applyChanges`, so check whether that one file's blob changed each turn (path-scoped diff / content-hash); skip entirely on component-only turns. (Confirm the file name — assumed `ontology.d.ts`.)
  2. **Signal = validate the EXISTING `.dev` resources against the NEW ontology** (reuse the Star's runtime typia validation — confirm the seam), *not* a static add-required/delete/rename/type diff. Keys off consequence: catches new-required + type/format changes, correctly ignores optional-added + deleted-field (app just stops reading it), and no-ops when there's no data (no false nag). Rename needs no special detection — reads as delete+add-required, and the add-required half trips the data check (correct). **Validate ALL rows** (sandbox data is tiny + hand-made): short-circuit the yes/no decision on the first failure, full count for narration; **never sample** (missing the one bad row defeats the purpose).
  3. **Split detection (deterministic, platform) from narration (LLM)** — no prompt-tool, no trusting structured output for the *decision*; the model only narrates the why in friendly terms.
  4. **Prompt = a paragraph + two choices: (a) Revert the change** (roll the turn back — restore prior schema+code via git, keep data) **or (b) Wipe the test data** (keep the new schema). Hook at the apply boundary (`applyChanges` / DevStudio apply).
  5. **Fold in the manual Wipe UX:** relabel `resetDevData`'s "Wipe" → **"Reset data"** + a confirm, and move it from the chat header into "Manage my scopes" as a per-row action beside Delete (so reset-data vs delete-app contrast). Scope = `.dev` throwaway sandbox only; data *migration* is a real-tenant-Star concern for later. Pairs with the clear-app-source item above. Surfaced 2026-06-26 (Larry).
- [ ] **`nebula-auth` endpoint × response table in the README — the anti-drift artifact.** The README's § Endpoint Reference has three tables (`Auth flow endpoints` / `Authenticated scope endpoints` / `Registry endpoints`, mirroring `AUTH_FLOW_SUFFIXES` / `AUTHENTICATED_SUFFIXES` / `REGISTRY_ENDPOINTS`) but none enumerates **what an endpoint can answer**. Add request shape + **every possible response** per endpoint, including the Turnstile / JWT / rate-limit rows, not just the happy path. **Bind it to source both ways** or it rots: *every error `code` literal reachable in `src/*.ts` appears in ≥1 row, and every code in a row is reachable* — mechanically checkable against the `new RegistryError(status, 'code'` literals plus the first string arg of every `jsonError`/`json401`/`errorResponse` call. ⚠️ Not "exactly one row" — `forbidden` is emitted from ~8 sites across ≥5 endpoints. Larry asked for this specifically as the artifact that would have caught the error-shape drift; it stands on its own, independent of any shape change. Surfaced 2026-07-25.
- [ ] **One `nebula-auth` URL can answer in three different shapes (pre-existing, unfixed).** `/auth/{scope}/magic-link` returns: a JSON `{error, error_description}` 400 (missing token), a **bodyless 302** with `?error=invalid_token` (bad token — `redirectWithError`, the *primary* failure path of magic-link click and accept-invite), and a **plain-text** 405 (`new Response('Method Not Allowed')`). A cross-package **plain-text CORS 403** (`applyCorsPolicy` in `@lumenize/routing`) is reachable on the same URLs. ⚠️ **A redirect structurally cannot carry a body**, so full uniformity is unreachable — any fix is "JSON bodies are uniform + named carve-outs for redirect/plain-text", not "one shape everywhere". Not worth doing on its own; fold into the endpoint×response table above (document the carve-outs) or into the form-primitive work. Surfaced 2026-07-25 by the Stage-1 review that killed the error-contract task.
- [ ] **Synchronous digest to replace `await crypto.subtle.digest` — selection criteria (no consumer forcing it yet).** We have wanted a sync hash ~3 times; each time an `await` in the middle of otherwise-synchronous DO work forced a workaround (most recently the `transactionSync` ordering in [nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md) § Validation, resolved by hashing *before* the transaction rather than by a sync hash). **Scope the search to DIGESTS, not "sync crypto"**: of the 12 `crypto.subtle` uses, only `hashString` ([packages/auth/src/jwt.ts](../packages/auth/src/jwt.ts)) and `query-hash.ts` are digests — the rest are JWT **sign/verify** (HMAC/RSA), which have no practical sync replacement. **Candidates:** (a) **`node:crypto` `createHash`** — synchronous and [fully supported under `nodejs_compat`](https://developers.cloudflare.com/workers/runtime-apis/nodejs/crypto); ⚠️ **the blocker**: `hashString` lives in the **published MIT `@lumenize/auth`**, which has no `wrangler.jsonc` of its own, so a `node:crypto` import would **impose `nodejs_compat` on every downstream consumer** — acceptable only if we split the digest out or accept that constraint; (b) a **pure-JS SHA-256** (`@noble/hashes`, or copy-paste-with-attribution — SHA-256 is ~150 SLOC, inside the repo's <1000-SLOC copy-over-dependency rule). **Selection criteria:** synchronous · no build step · SHA-256 (+ HMAC if cheap) · **small bundle** (bundle size drives DO cold start — [experiments/do-cold-start-bundle-ab](../experiments/do-cold-start-bundle-ab/RESULTS.md)) · permissive license · **no new compat-flag burden on published-package consumers** · **byte-identical output to today's SHA-256+base64url** (persisted refresh/magic-link token hashes would otherwise all invalidate — softened by the pre-alpha wipe, but the binding constraint afterward). Surfaced 2026-07-25 (Larry).
- [ ] **Ontology-driven form-validation primitive (tenant forms) — typia Standard Schema + async-rule layer.** Tenant apps' ontology types are typia-based, so their **forms** should validate against those types: **collect-all, field-keyed, form-friendly errors, the same validator client (instant) + server (authoritative).** The hard part is **already built** — [`ts-runtime-parser-validator`](../packages/ts-runtime-parser-validator/src/typia-runtime-helpers.ts) emits **standalone** validators with an **AOT-inlined Standard Schema** interface (`INLINED_CREATE_STANDARD_SCHEMA` → `{ issues: [{ path, message }] }`, no runtime typia dependency). What's left: (a) a **message catalog** mapping typia's type-derived text (`expected string & Format<email>`) → user copy; (b) a **pluggable async-rule layer** for rules a type can't express (uniqueness, cross-record, existence — anything needing I/O), merged into the same collected list; (c) client wiring — Standard Schema → a Vue form lib (TanStack Form consumes Standard Schema directly), with **native HTML constraint validation** (`:user-invalid`, `type=email`, `pattern`) as the progressive-enhancement client layer. **Sequencing:** rides the container-move of the typia *generator* ([experiments/do-cold-start-bundle-ab](../experiments/do-cold-start-bundle-ab/RESULTS.md): the 9 MB `typia`+`@typia/transform` stays in the container; **generated validators are small and go anywhere**) — the DO cold-start work is the enabler. **First dogfood = a real Studio ontology resource-edit form**, NOT a trivial signup form. **Our own forms do NOT share this shape today** — and adopting it is a deliberate, separate swap. `claim-star`/`claimUniverse` validate **fail-fast** and emit the existing `{ error, error_description }` ([nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md) § Validation; a package-wide `issues[]` contract was drafted and **killed** 2026-07-25 — it bought uniformity nowhere and multi-error UX moved client-side). ⚠️ Separately, a real gap this primitive must close on its own terms: `INLINED_CREATE_STANDARD_SCHEMA` emits `{ message, path }` only — **no `code`** — and `path` is a `{key}` segment array. Machine-readable codes are what a caller branches on, so the primitive's `Issue` should carry a `code` (Standard Schema's `Issue` structurally admits the extra field, so the general form costs the same as the narrow one) — a **separate, deferred decision** (wants a frictionless generate-and-commit AOT path for static types before our build-free code takes it on). ADR-001; [standardschema.dev](https://standardschema.dev). Surfaced 2026-07-24 (Larry — spun off the star-founder §Validation discussion).
- [ ] **Studio dev-container recovery after a DEPLOY + silence the preview reconnect-storm (live prod repro 2026-06-29, [[cf-container-stuck-flag-cloud]]).** A **deploy** stales the warm container, and `ensureUp`'s `destroy()`/retry does **NOT** clear the stale `running` flag in the cloud (the SAME dead container id persisted across many attempts; only idle-evict clears it, ~min). So post-deploy the preview 500s "…not running" until evict, and the preview GET path (which never calls `ensureUp`) can't self-recover. Need a recovery that works post-deploy without waiting for idle-evict. **Plus:** the preview iframe **vite-client reconnect-storms** (WS "bad response" ×100s + ~1–2 GET/s) against the dead container — silence the reconnect under the prefix (HMR is config-disabled, so confirm the storm's source). **Lesson — don't repeat:** the waking page must stay **manual-reload, never auto** — a 2s auto-reload once made the stuck state UNRECOVERABLE (every proxy hit `renewActivityTimeout`, blocking idle-evict). Parked observe-first; pairs with the HMR-under-the-prefix DX item below. (Already shipped, for context: `ensureUp` force-restart for a *cold* stuck container `1e0fd67e`; event-driven preview auto-refresh `warmPreview→onPreviewReady` `d191d0e`.)
- [ ] **Studio loop DX — HMR under the path prefix.** Re-enable so the codegen loop is sub-second (no full iframe reload per turn). The WS path 101's cleanly, so it looks viable; needs `clientPort`/path tuning. Pairs with the `preview-path-prefix-vite-base` memory + the `nebula-studio-ui` README "No HMR under the prefix yet" note. (Relocated from the retired `nebula-agentic-development-engine.md` DX backlog, 2026-06-23.)
- [ ] **Studio loop DX — real-time thought streaming.** Upgrade the waiting→thought panel from after-completion to live token streaming (mesh chat-chunk channel + client handler). DX nicety, not on any critical path. (Relocated from the retired engine roadmap, 2026-06-23.)
- [x] ✅ **Wrangler + vitest-pool-workers bump — DONE 2026-07-16, and overshot the target** (`do-exports-and-toolchain-upgrade`, archived): every package now pins `wrangler ^4.111.0` / `@cloudflare/vitest-pool-workers ^0.18.5`, past this item's original 4.103 goal. ⚠️ **The ROOT is not aligned** — see the root-wrangler item above, which owns the remainder; this line stays only so the two aren't mistaken for the same open task. **The surviving durable fact:** the bump did **not** remove the `unplugin-swc` need, and won't — vite/Oxc still don't do TC39 decorators (mind the 2022-03↔2023-11 decorator-iteration gap if that's ever revisited). It did **not** broaden vitest-pool-workers to construct `extends Container` either — but that stopped mattering, since the container path is now raw `ctx.container` on a plain DO, which constructs fine ([[ctx-container-plain-do]]).
- [ ] **`client.logout()` (deferred from nebula-frontend v3 / P7 — NOW UNBLOCKED, both deps met 2026-06-15).** The client surface ([api-reference.md § client.logout](../website/docs/nebula/api-reference.md#clientlogout)) is small (POST the auth logout endpoint to revoke+clear the path-scoped refresh cookie → drop the in-memory access token → `disconnect()` so the factory mirrors `lmz.connection.state = 'disconnected'` → redirect). Both formerly-unmet deps are now satisfied: (1) ✅ the **mesh token-clear** landed in **P9** as `LumenizeClient.clearAccessToken()` (`packages/mesh/src/lumenize-client.ts`; nulls `#accessToken` + `#claims`, which `disconnect()` does NOT); (2) ✅ the **nebula-auth server logout endpoint ALREADY EXISTS + is tested** — the earlier "phantom endpoint" claim was STALE. `POST .../logout` → `NebulaAuth.#handleLogout` ([nebula-auth.ts:433](../packages/nebula-auth/src/nebula-auth.ts:433)) revokes the refresh token (`UPDATE RefreshTokens SET revoked = 1`, audit-logged) + clears the path-scoped cookie (`Set-Cookie: refresh-token=; Max-Age=0`), covered by `nebula-auth-routes.test.ts:316`/`:1092` + `nebula-auth.test.ts:401`/`:620` + the cross-Star `nebula-auth-integration.test.ts:127` flow. **Remaining work = the NebulaClient `client.logout()` client surface ONLY** (compose `POST ${baseUrl}/auth/${authScope}/logout` via the configured `fetch` + `credentials:'include'`, then `clearAccessToken()` + `disconnect()` + redirect), tested via §5.3.8 connection-lifecycle path 6. Small + fully testable now — a good early Studio-phase slice (the phased frontend build is complete through P11, so this is fresh work, not a remaining frontend phase).
- [ ] **`createNebulaClient` `authScope` URL auto-detect (deferred from nebula-frontend v3 / P7).** P7 ships `createNebulaClient(config)` with `baseUrl`/`activeScope`/`onShouldRefreshUI` auto-detecting but **`authScope` still required-in-practice** (omitting it throws a clear "auto-detect not yet implemented; pass authScope or wait for the Studio-hosting decision" error). Deferred because the URL→scope mapping is coupled to the still-open **Studio-UI-hosting / deployment-URL scheme** ([nebula-studio.md](archive/nebula-studio.md); see [nebula-frontend.md](archive/nebula-frontend.md) §5.3.7-v3 createNebulaClient item). **Larry's steer (2026-06-15):** all Studio users are **Galaxy admins by definition**, so the dev-Star case can still auto-detect `authScope` from the URL (a Galaxy admin's cookie is at `/auth/{galaxy}`, pattern `{galaxy}.*`; the dev Star's scope is derivable from the deployment URL). Design the derivation against that constraint once the hosting scheme lands. Until then, the api-reference flagship `createNebulaClient({ appVersion })` example is aspirational. **Trigger**: the Studio-hosting/deployment-URL decision is made, or a real browser deploy needs the zero-config form.
- [ ] **(low-pri, speculative) Mesh: classify a terminal auth failure at the WS *upgrade* (not just the refresh endpoint).** P9 + the nebula-frontend P10 slice made first-connect *refresh* 401/403 terminal (LoginRequiredError → `onLoginRequired` + `'disconnected'`). A separate edge: if a terminal auth failure surfaced at the WebSocket *upgrade* itself — an HTTP 403 before the WS opens, surfacing via `#connectInternal`'s WebSocket-construction/`onerror` path rather than a 4400/4403/4401 close code — it falls into the generic `else → #scheduleReconnect()`, not `onLoginRequired`. Largely theoretical today: the Gateway uses 4400/4403/4401 close codes for auth (which `#handleClose` already classifies), and a rejected WS upgrade typically surfaces as a generic connection error, not a readable HTTP 403. Flagged by the P10 verify panel; revisit only if a real upgrade-time-403 path appears. (`packages/mesh/src/lumenize-client.ts` `#connectInternal` / `#handleError`.)
- [ ] **Nebula frontend — deferred post-demo §5.3.8 probes** (extracted on archival of `tasks/archive/nebula-frontend.md`, 2026-06-15; full probe text + capable-of-failing notes are in that file's § Phase 5.3.8). Three for-docs probes were deferred when the Vue frontend merged — pick each up when its mechanism lands:
  - **Flash-class** — framework-agnostic default classes (`'committed'` → `lumenize-commit-success`; rollback resolutions → `lumenize-conflict-revert`; custom + `{committed:null, rolledBack:null}` disable forms) **and** the Vue per-element diff flash after a `'use-server'`. Couples to the v4 SFC-compile / DOM-flash mechanism.
  - **Resolver `context.bindings`** — the conflict resolver's third-arg context exposing elements bound to the conflicting resource's paths (today an empty-Map placeholder until the real mechanism lands).
  - **Per-field debounce from ontology** — validator bundle carries per-field `quietMs`/`maxWaitMs`; field-type defaults (boolean → eager) + explicit `@debounce(...)`/`@longform` annotations; resource-level "shortest active timer wins". Lands with the per-field config emission (v3 ships global constants).
- [ ] **Make `@lumenize/nebula`'s client-safe entries (`/client`, `/frontend`) truly *type*-safe for browser-only consumers.** Today they're browser-safe at **runtime** (DO value-imports erase / tree-shake), but a browser-only `tsconfig` (DOM lib, no `@cloudflare/workers-types`, no generated `Env`) **cannot type-check** a consumer that imports them: `/frontend` → `/client` re-exports constants/types (e.g. `END_OF_TIME`, `Snapshot`) from DO-bearing source files (`resources.ts`, and transitively `star.ts`/`galaxy.ts`/`subscriptions.ts`/`dag-tree.ts`), so the consumer's program pulls that source, which references `DurableObjectState`/`ctx`/`env`/the global `Env` and floods with errors. **Fix**: split the exported *types + constants* out of the DO-bearing modules into pure type/const files that `/client` re-exports, so no part of the client surface transitively imports a `DurableObject`. Also provide a **browser-safe way to type continuation calls** — `apps/nebula-studio-ui/src/App.vue` imports `import type { DevStudio, Star } from "@lumenize/nebula"` (the Worker root) purely to type `client.ctn<DevStudio>().chat(...)` / `ctn<Star>().resetDevData()`; there's no browser-safe entry exposing those DO method shapes. **Payoff**: `nebula-studio-ui` (the first external browser consumer of `@lumenize/nebula`) becomes type-checkable with `vue-tsc` — then un-skip it in `scripts/type-check.sh` (currently in `SKIP_PACKAGES`; plain `tsc` can't check a `.vue` app, and vue-tsc can't either until this lands). **Discovered during** the 2026-06-22 `npm run type-check` cleanup: wiring `vue-tsc` for `nebula-studio-ui` surfaced this; per CLAUDE.md (Nebula fighting a package API = surface it, don't work around in Nebula), the Vue app was excluded from the loop and this filed. Product feedback for `@lumenize/nebula`, not a Studio-UI bug.
- [ ] **FK referential integrity (deferred — see [ADR-006](../docs/adr/006-resources-reference-by-id.md)).** Validation only checks a relationship reference is a well-formed id `string`, not that the target exists — so dangling/nonexistent ids are accepted. Decide whether to enforce "the target exists, or is created in the same transaction," **scoped to intra-Star same-transaction FKs** (cross-Star refs are ids that can't be checked at write time and stay eventually-consistent). Lives in `apps/nebula/src/resources.ts` transaction validation + the relationship metadata from `extractTypeMetadata`. Low-pri; revisit when a concrete need arises.
- [ ] Refactor `dag-tree.ts` `requirePermission` to throw typed errors (`PermissionDeniedError`, `NodeNotFoundError`, `AuthenticationRequiredError`)
  - **Why**: Currently throws plain `Error` for three distinct conditions with different downstream handling needs. `Resources.transaction`'s permission-check refactor (Phase 5.3.3b) needed to distinguish "permission denied" (→ typed `TransactionError`) from "node not found" / "auth required" (→ propagate as Error). Fix is currently a fragile message-string match at [apps/nebula/src/resources.ts:368-377](apps/nebula/src/resources.ts:368) — `e.message.includes('permission required')`. Replace with `e instanceof PermissionDeniedError` checks.
  - **Pattern to follow**: `apps/nebula/src/errors.ts`'s `OntologyStaleError` + `isOntologyStaleError` (typed Error subclass with `name` override + type-guard for cross-boundary use). Same shape works for these — they stay server-side so `instanceof` works fine.
  - **Scope**: small. dag-tree.ts changes ~10 LOC; resources.ts catch block ~5 LOC; backlog because not urgent but worth doing before the next major change to either file.
  - **Discovered during**: Phase 5.3.3b retro (typed-error fragility in the permission refactor).

- [ ] **EU data residency for Stars (DO `jurisdiction`) — product feature, deferred**
  - **What**: `env.STAR.jurisdiction('eu').getByName(name)` hard-guarantees the DO runs and persists only in-jurisdiction — a compliance feature, distinct from best-effort `locationHint` placement ([mesh-origin-request.md](on-hold/mesh-origin-request.md) / [nebula-dataplane-root-admin.md](on-hold/nebula-dataplane-root-admin.md) Part 1b). `eu`, `fedramp`, `fedramp-high` exist in the API; only `eu` has a plausible customer near-term.
  - **The architectural cost (why deferred)**: a jurisdictioned namespace derives a DIFFERENT `DurableObjectId` for the same name — so every name→stub resolution site must know the instance's jurisdiction, forever: `getDOStub` (packages/routing), Gateway resolution ([lumenize-client-gateway.ts:586](../packages/mesh/src/lumenize-client-gateway.ts)), mesh `callRawImpl` ([lmz-api.ts:301](../packages/mesh/src/lmz-api.ts)), nebula-auth router `getByName` calls. A mismatch silently talks to a different (empty) DO.
  - **Design options to evaluate**: (a) encode jurisdiction in the instance-name grammar (e.g., a reserved universe segment) so it's deterministic from the name at every site — currently favored, since the name already IS the address everywhere; (b) Registry lookup + per-node cache.
  - **Constraints learned 2026-06-12**: jurisdiction IGNORES `locationHint` when both are given (no weur/eeur refinement inside `eu`; first-touch locality still applies within the jurisdiction). Must be an explicit signup choice, never auto-detected (compliance; at most pre-suggest via `request.cf.isEUCountry === '1'`). Existing Stars can't convert in place (different ID + DOs never relocate) — migration = history-substrate copy to a new DO, which ADR-004 makes well-defined. miniflare doesn't enforce jurisdictions → deployed-only verification ([tasks/archive/playwright-test-template.md](archive/playwright-test-template.md)).

- [ ] **Cross-DO data-consistency sweep (motivating case: dangling root-admin grants).** Subjects live in `nebula-auth`; DAG permission grants live in each Star's `Permissions` table, with no FK between them (different DOs, and nebula-auth must not depend on Nebula — dependency direction). So deleting a subject who holds a root-admin grant ([nebula-dataplane-root-admin.md](on-hold/nebula-dataplane-root-admin.md) Part 2 "known limitation") leaves a *dangling* grant: the last-admin invariant reads "held" but the grant points at a sub who can never authenticate. Accepted for now (recovery = scope-admin re-seeds via the `claims.access.admin` bypass; unrecoverable case already blocked by nebula-auth self-delete + bootstrap-admin guards). A periodic/lazy cross-DO sweep — reconcile grant `sub`s against live subjects, flag/strip orphans, surface adminless Stars — would be the broad janitor. **Don't build yet** (Larry, 2026-06-15: "if it ever comes up with customers, it'll be a support-ticket fix"); revisit when a real consistency need appears or this generalizes beyond the one case. The targeted, user-facing handling of the *same* dangling-admin case (filter dead admins at request-access climb termination, async-native there) is the higher-value first step and lives in [nebula-request-access.md](nebula-request-access.md); this sweep is the broader complement.

- [ ] **Reserve the `dev` 3rd-segment slug (and other platform-reserved slugs) at self-service Star claim — server-side.** The `.dev` Star is Studio's sandbox, and `resetDevData` (the data-wipe) ships on every `Star` hard-guarded to the `.dev` STAR-tier instance ([nebula-studio.md](archive/nebula-studio.md) § Dev-data reset, Decision 2 collapse). That guard's premise — *no tenant owns a real `{u}.{g}.dev` Star* — has no server-side enforcement today. ✅ **RESOLVED — scheduled as [nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md) § Slug + Phase 2.** ⚠️ **The 2026-06-21 layering call is REVERSED, deliberately:** this item used to say *"Do NOT add the reservation to `@lumenize/nebula-auth` — reserving an app-level slug in a lower-level package is a layering smell"*, deferring it to a Nebula-app-layer signup flow. But `claimStar` — the only path that can create a tenant `{u}.{g}.dev` — lives **in the registry**, and a reservation enforced anywhere else is not a control at all: validation has to sit where the write happens. (`nebula-auth` is also the *Nebula-specific* auth package, not the generic `@lumenize/auth`, so a Nebula-reserved slug is in-layer there.) The old note also described `claimStar` as an existing method that "accepts `dev`" — it does not exist yet. **Accepted risk until Phase 2 lands** is unchanged: a tenant self-claiming `{u}.{g}.dev` gets an admin-gated, **self-scoped** wipe of its own data only (no cross-tenant exposure). Surfaced by the Studio Stage-2 conformance review (M2), 2026-06-21; reversed 2026-07-25.
- [ ] **Container disk snapshots / `persistAcrossSessions` — re-evaluate the Studio dev-loop cold-start model once it ships.** Cloudflare announced (CF Discord #containers-beta, seen 2026-06-18, "rolling out in the coming weeks") **disk snapshots**: a container auto-snapshots its **full disk** (OS config, installed deps, modified files, data) on sleep and restores it on wake — `class X extends Sandbox { sleepAfter = "5m"; persistAcrossSessions = {type: "disk"} }` (optionally per-directory). **This dissolves the *ephemeral-disk premise* behind two just-made Phase 3.5 decisions** ([nebula-container-dev-loop.md](archive/nebula-container-dev-loop.md)): Q#1 (bake deps into the image to avoid cold-start `npm install`) and Q#2 (DevContainer-DO source store + re-hydrate-on-cold-boot). If disk persists across sleep, node_modules **and** source survive a sleep/wake with no reinstall and no re-hydrate — enabling a cleaner *"each user gets a persistent container that never resets"* (dev-box) model. **Open questions to evaluate when it lands (do NOT design on it now — still not shipped as of 2026-07-20; Containers itself went GA 2026-04-13):**
  - **`Sandbox` vs `Container` base class** — the example `extends Sandbox`, not `@cloudflare/containers` `Container`. Is `Sandbox` the same class, a superset, or CF's separate Sandbox-SDK abstraction? `LumenizeContainer extends Container` ([packages/mesh/src/lumenize-container.ts](../packages/mesh/src/lumenize-container.ts)), so adopting snapshots may interact with (or require changing) the node-type base — check before assuming a drop-in.
  - **What it does NOT cover** — snapshots persist across *sleep*, but likely not across instance **deletion/eviction**, **region move**, or an **image/approved-deps version bump**. So a durable source-of-truth backstop (the Q#2 DO store, or Galaxy #1b) is probably still needed for instance-loss + portability + upgrade — snapshots **reduce** the Q#2 need (the common sleep/wake path) but may not **eliminate** it. Confirm the invalidation/eviction semantics.
  - **Image-upgrade interaction** — when the baked approved-deps image bumps, does a user's snapshot (old node_modules) get invalidated/migrated? Ties into the "behind latest" version-drift notice (Phase 3.5, deferred post-demo) and whether bake-the-deps is still worth it for first-boot.
  - **Cost** — persistent per-user disk/snapshot storage is a new billing dimension vs ephemeral; evaluate cost-per-active-user against the stopped-container ≈ $0 model.
  - **Trigger**: container disk snapshots reach a usable/GA state → revisit Q#1 (bake) and Q#2 (DO-store) in nebula-container-dev-loop.md; the bake + DO-store decisions remain correct and shippable for the demo regardless (they don't depend on this feature).
  - **2026-07-07 (researched) — resolves open-Q #1; don't conflate the SDK with this feature.** CF's [Sandbox SDK](https://developers.cloudflare.com/sandbox/) is a **separate, higher-level** framework (Apache-2.0), *not* the native disk-snapshot above: `Sandbox extends Container` (a DO, extendable). Its **shipped** persistence is **manual** [`createBackup`/`restoreBackup`](https://developers.cloudflare.com/sandbox/guides/backup-restore/) (snapshot a dir → R2 squashfs, restore via FUSE copy-on-write overlay) — **not** write-through and **not** auto: the mount is lost on sleep/restart, so you **re-restore on every wake** (same orchestration we already own). The auto `persistAcrossSessions = {type:'disk'}` this item waits on is **not** in the SDK's [GA options](https://developers.cloudflare.com/sandbox/configuration/sandbox-options/) → the Sandbox SDK is **not** a shortcut to it. **Fit verdict:** don't adopt the SDK wholesale — it duplicates our own command-server/exec/preview-proxy/port-security (a 2nd coordination layer). At most **borrow the technique** (squashfs→R2 + FUSE-COW restore), and only if **non-regenerable** disk state ever appears (user-installed deps, a DB file, uploaded media) — today all container disk re-hydrates from Galaxy, so neither the SDK nor a manual snapshot buys much.
  - **2026-07-20 (re-researched at Larry's ask, backup/restore offered as a stopgap) — verdict UNCHANGED: don't adopt.** Four sharpenings, plus a live lead below.
    - **We don't pay the cost it removes.** CF's pitch benchmarks against a cold `npm install` ("boot + clone axios + npm install = 30 s; restore = 2 s") — but our deps are **baked** ([container/Dockerfile:45](../apps/nebula/container/Dockerfile#L45), 84 MB / 62 pkgs, **0 s** at boot) and the runtime repopulation is 1–3 source files, a few KB, ~30 ms warm `containerFetch` ([dev-studio.ts:322](../apps/nebula/src/dev-studio.ts#L322) → [dev-container.ts:467](../apps/nebula/src/dev-container.ts#L467)). Baked 0 s vs restore 2 s ⇒ adopting today is a **regression**, not a stopgap. (No measurement of our push exists anywhere because it's noise next to the ~2–4 s container boot.)
    - **No rsync/diffing — confirmed.** `createBackup()` builds a **full squashfs archive** and uploads the whole thing every time. But it's **asymmetric**: `restoreBackup()` FUSE-mounts that squashfs as a read-only lower layer under a writable upper (copy-on-write, lazy page-in, no copy) — the cheap half. Backup is the expensive half.
    - **Early adoption buys ~nothing.** CF states the native snapshot system "will use a similar API, so changing to this system will be simple once it is available" ⇒ the migration is cheap either way, which deflates the "gets us onto the snapshot model sooner" argument. Counterweight worth knowing: the GA post (2026-04-13) said native was "rolling out in the coming weeks" and it **still hasn't landed** — so "wait for native" has already cost a quarter.
    - **TTL makes it a cache, not storage** (new detail): default `ttl` 259200 s = **3 days**, checked **only at restore**, and it **never auto-deletes** (R2 lifecycle rules are on you). An inactive tenant's backup silently expires → falls back to the 20 s install.
    - **Base-class collision is worse than 07-07 knew.** Setup is `export { Sandbox } from '@cloudflare/sandbox'` — CF's own DO class, extending `Container`. Since 07-07 we moved *further* away: [experiments/plain-do-container](../experiments/plain-do-container) established we drive raw `ctx.container` on a plain DO precisely because `extends Container` **cannot be constructed under vitest-pool-workers**, and ADR-014 wants the companion DO to *be* the mesh hub. Inheriting a CF-authored container DO fights both.
    - **Trigger restated:** revisit on **user-supplied deps**, NOT on source repopulation. Likely shape when it comes: **bake the curated common set + restore/mount only the tenant's delta** — snapshotting the merged tree throws away the free image layer to get the delta.
  - **2026-07-20 — LEAD: R2 FUSE bucket mounts (Larry: "potential way forward if snapshots doesn't land before users need their own deps").** Shipped [2025-11-21](https://developers.cloudflare.com/changelog/post/2025-11-21-fuse-support-in-containers/), documented under **Containers**, not Sandbox: [containers/examples/r2-fuse-mount](https://developers.cloudflare.com/containers/examples/r2-fuse-mount/). It's an **image-level technique** — install a FUSE adapter (`tigrisfs`/`s3fs`/`gcsfuse`) in the Dockerfile, mount at startup — so **no Sandbox SDK and no base-class change**, which is exactly why it fits our raw-`ctx.container` path where backup/restore doesn't. Creds via `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` secrets + `R2_BUCKET_NAME`/`R2_ACCOUNT_ID` vars; `-o ro` for read-only; prefix (subdirectory) mounting supported. **Two things to settle if/when we build it (capture only — not gating):**
    - ⚠️ **Trust boundary.** Those are R2 API credentials sitting in the env of **the container where user-developer code runs**. Needs bucket-scoped (ideally prefix-scoped) tokens and/or a per-tenant bucket before it can hold tenant deps — secure-by-default, don't hand-wave this one.
    - ⚠️ **The documented perf profile is the opposite of `node_modules`.** CF: *"Object storage is not a POSIX-compatible filesystem, nor is it local storage"*, and explicitly disclaims high-performance I/O; node resolution is about the most stat-heavy small-file workload that exists, so a naive "mount `node_modules`" is likely bad. The documented use is **bootstrapping** ("datasets, models, or **dependencies**") — i.e. pull + extract to local disk. But note: if you're extracting a tarball anyway, a plain presigned GET does it **without FUSE**; FUSE only earns its keep for *lazy partial* reads of a large tree. **Measure before believing.**
    - Consequence: R2 FUSE is **not strictly better** than the squashfs+FUSE-COW technique 07-07 said to borrow — for `node_modules` specifically, one compressed archive beats N-thousand small GETs. It's a *different* tradeoff with a much lower adoption cost. Evaluate both against a real dep tree.
  - **2026-07-20 — MEASURED; verdict hardens, and the framing was wrong.** [experiments/container-dep-restore-bench](../experiments/container-dep-restore-bench/RESULTS.md) (Docker, `node:22-slim`, arm64, `--cpus=0.5` ≈ `standard-1`). Everything above was desk research; these are numbers.
    - **⇒ REFRAME (the most decision-relevant finding): user-supplied deps is an EGRESS/security question, not a speed question — and no snapshot mechanism routes around it.** Adding one big UI dep to the baked tree is **1–4 s** (`date-fns` 4.0, tiptap+starter-kit 3.3, `echarts` 3.2, `ag-grid-community` 1.9, `@tanstack/vue-table` 1.6, `chart.js` 1.0; minus a 0.7 s no-op floor ⇒ ~0.3–3.3 s real). Package **count** beats bytes (tiptap = 52 pkgs/25 MB costs what echarts' 13 pkgs/80 MB does). There is no latency problem to solve; what's missing is the decision to let a tenant container reach the npm registry (the Dockerfile calls runtime egress "deferred"; [nebula-outside-world.md](nebula-outside-world.md)'s `EgressBroker` is for the app-server facet, not this). **The deps must be installed somewhere before there is anything to snapshot** ⇒ egress is *upstream of, and unavoidable by*, this whole item. Corrected the contradicting claim at [nebula-galaxy-collapse-and-chat.md](nebula-galaxy-collapse-and-chat.md) ("gated on Container snapshots").
    - **The prize is ~1 s against a ~6 s build.** End-to-end fresh container → `node_modules` → `vite build`: baked **6.1 s** (0.0 s restore) · squashfs+COW overlay **6.5 s** (0.0 s) · tar.zst **7.1 s** (0.4 s) · tar.gz **7.3 s** (1.0 s) · `npm install` warm-cache **11.4 s** (5.0 s). Every viable mechanism ties baked within ~1 s. **And the backup half is expensive: `createBackup()`-equivalent squashfs creation = 50.7 s** vs **0.6 s for tar.zst** at the same ~20 MB. ⇒ if a restore mechanism is ever wanted, it's **tar.zst**, not the clever lazy-mount one.
    - **Two hard blockers for any mount-based shape** (both kill the naive "mount `node_modules` from R2"): (1) **a read-only `node_modules` cannot build at all** — `vite build` must *write* `.vite-temp` into it to load a TS config (this is presumably why CF's restore uses a COW overlay); (2) **kernel overlayfs on a FUSE lower breaks native-binary exec** — `esbuild` runs off squashfuse directly ✅ and off kernel-squashfs+overlay ✅ but off squashfuse+overlay ❌ `EINVAL`, and our tree has ≥3 native binaries the build must exec. ⚠️ Not a claim about CF: their "FUSE overlayfs" may be `fuse-overlayfs` (userspace), a different stack — but it's the hazard to verify first against **both** backup/restore and a writable R2-FUSE layer.
    - **Leg B (real R2 FUSE) deliberately NOT run** — it would measure a way to save ~1 s while inheriting both blockers above, a perf profile CF explicitly disclaims for high-IOPS work, and R2 credentials inside the tenant container. Revisit only if user deps arrive **and** egress is answered such that a pre-built per-tenant tree is the natural artifact.
    - ⚠️ **2026-07-20 (later, DEPLOYED) — the absolute numbers in the two bullets above are LOCAL Docker and proved 3–5× OPTIMISTIC. The relative conclusions survive; the budgets do not.** A deployed probe on real CF ([experiments/container-cold-start-probe](../experiments/container-cold-start-probe/RESULTS.md)) measured, for the same sequence: **baked path 8–10 s total** (build 6.8–9.3 s, stable everywhere), **user-dep path 11–33 s** (build 7.7–28 s). Local said 2.1 s / 7.9 s. **Use the CF numbers for any budgeting.** Three things that only showed up deployed: (1) **containers do NOT run where the caller enters** — entry was `IAD` on every run, containers landed in `MIA`/`EWR`/`CMH`/`YYZ`, so DO id/name selection cannot buy local placement; (2) **colo dominates** — `EWR` built in ~28 s at *both* standard-1 and standard-4 while `CMH`/`MIA` did 7.7–8.7 s at standard-4; (3) **instance sizing is worth ~2.7×** once colo is held constant, which is why [apps/nebula/wrangler.jsonc](../apps/nebula/wrangler.jsonc) sits at standard-2 — though that rests on 3 colo-matched pairs and is directional, not powered. **Net effect on THIS item: the verdict strengthens** — the build is 70–90 % of every scenario, and snapshots would remove only the ~1 s cold start and at most the ~4 s install, i.e. none of where the time actually goes.
    - **Two unrelated wins banked from the same run:** the baked image shipped **~179 MB of dead npm cache** (install layer 306 MB uncompressed) — `npm cache clean --force` added to [apps/nebula/container/Dockerfile](../apps/nebula/container/Dockerfile), measured **173 MB → 130 MB (−25%)** on the real image, and CF cold start is image-dependent. Also corrected: the older `container-dep-install-bench` RESULTS.md claims *"No native binaries"* — **wrong** (oxide + lightningcss + rollup are all native); and `tailwindcss` alone is a thin **1 MB / 1 pkg / 0.8 s** — the weight is `@tailwindcss/vite` (7.4 s / 47 MB), while the tree's real heavyweights are **`lucide-vue-next` 33 MB + `typescript` 23 MB = half of 111 MB**.
- [ ] **Container app has NO committed lockfile — baked dep versions float between image builds.** `apps/nebula/container/Dockerfile`'s `COPY app/package.json app/package-lock.json* ./` glob matches **nothing** (there is no `apps/nebula/container/app/package-lock.json`), so `npm install` re-resolves from the `^`-ranges at every image build. Evidence it's already drifted: the curated tree measured **84 MB / 62 pkgs** on 2026-06-18 and **111 MB / 64 pkgs** on 2026-07-20 with no intentional dep change. The Dockerfile clearly *anticipates* a lockfile (that glob exists for it) — it was just never generated. **Fix:** `npm install --package-lock-only` in `apps/nebula/container/app/`, commit the lockfile, and consider `npm ci` in the Dockerfile. ⚠️ Deliberately **not** done unilaterally on 2026-07-20 — pinning changes which versions the next deployed image gets, so it wants a build+`/live` verify alongside, not a drive-by. Surfaced by [experiments/container-dep-restore-bench](../experiments/container-dep-restore-bench/RESULTS.md).

## Nebula Auth

- [ ] 🔴 **Turn Turnstile ON in prod** — DEFERRED to post-pre-alpha (re-enable trigger: public signup / alpha). Bypass ✅ built (`isTurnstileBypassed`, `packages/nebula-auth/src/router.ts`) + canary-verified end-to-end (`drive.ts turnstile-canary`); remaining = provision a widget (CF dashboard) + fold login to one gated call so a user faces Turnstile once. Design, bypass-mechanism rationale, blocker catalog (B0/B1 + latents), and phases now homed in **[tasks/on-hold/turnstile-on-in-prod.md](on-hold/turnstile-on-in-prod.md)**. Findings: `apps/nebula/harness/FINDINGS.md` § Turnstile canary. *(Also in the top banner.)*

- [ ] **Internal email reliability — guaranteed delivery for Nebula's OWN flows (outbox / Workflows).** Distinct from [nebula-outside-world.md](nebula-outside-world.md): that task is the **dev-user** capability (safe sandbox-escape so *their* App code can send email/webhooks/cron — untrusted, safety-gated); this is **our own platform infrastructure** (signup magic links, invites — trusted, far more flexibility). They overlap only on the send *primitive* (`AUTH_EMAIL_SENDER` / the provider), not on the reliability model. Today `#sendEmail` (`packages/nebula-auth/src/nebula-auth-registry.ts`) awaits a single `sender.send()` — a **synchronous** failure surfaces as a failed POST, but **async delivery failure (bounce/spam after a 200) is invisible** (no Resend-webhook / CF-Queue wired up). The star-founder **resumable-claim** ([nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md) Phase 2) makes this non-urgent for signup — a graceful retry re-sends, so the user self-recovers from *any* delivery failure without detection — but a **true outbox** (commit intent → retry-until-sent → bounce handling behind an abstraction over Resend-webhook **and** CF-Queues) is the eventual shape for flows with **no human in the loop to retry**. ⚠️ **CF-Workflows cost note (Larry, 2026-07-21):** durable-execution billing is fine for **important 1:1 flows** (signup, password reset) but too expensive **per-email for broadcasts** — so Workflows is a per-flow choice, not the default carrier. Not pre-alpha.
- [ ] **Deletion-warning enrichment — step 2 (see-all + last-login).** Split off from [nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md) Phase 1 2026-07-21. Phase 1 ships the *bounded* warning: `affectedUsers: { total, sample }` (a `COUNT(DISTINCT email)` + a ≤25-email `LIMIT` read, replacing `#otherUsers`'s unbounded materialization). This item is the "**Do you need more information to decide?**" step 2 — pagination / see-all past the 25, plus a per-user **last-login**. Last-login has two zero-cost, ISO-8601-per-ADR-011 mechanisms (pick when built): (1) piggyback the `UPDATE` `getAndVerifyIdentity` ([nebula-auth-registry.ts](../packages/nebula-auth/src/nebula-auth-registry.ts)) already does on every login — a nearly-free column; (2) derive with **no new column** from `MAX(RefreshTokenIndex.expiresAt) − REFRESH_TOKEN_TTL` (fixed 30d, written only at login; stale index rows survive expiry). 🚫 Do **NOT** copy `packages/auth`'s `lastLoginAt INTEGER` epoch column (ADR-011 violation), and 🚫 no new hot write on the singleton registry. Not on any critical path — a UX refinement for when Stars have many tenants.
- [ ] **delete-any-Galaxy — the Universe-admin delete + its bigger warning (OQ 7 from star-founder).** Deferred from [nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md) 2026-07-21. Phase 1's block removal is uniform per ADR-015, so a covering admin can *already* delete a Galaxy mechanically — this item is the **richer warning** that makes it an informed decision, which is a **similar-but-separate method** from Star deletion: a Star's danger is its attached *users* (`affectedUsers`), but a **Galaxy's danger is its child Stars**, so its pre-"need more info?" preview is `affectedStars: { total, sample }` (count + ≤25 child Stars), and its `affected` cascade set can itself be large → cap that too (Star deletion's `affected` is trivially small, so Phase 1 didn't need to). Also decide whether/how the UI surfaces galaxy-level delete distinctly. Post-alpha concern (galaxies have no tenant Stars until self-signup ships).
- [ ] **Admin demote / revocation endpoint — no way to un-admin anyone (surfaced 2026-07-19).** `setIdentityAdmin(sub, isAdmin)` ([nebula-auth-registry.ts:624](../packages/nebula-auth/src/nebula-auth-registry.ts)) already handles both directions **and** converges the denormalized `isAdmin` into every live KV refresh record — but it has **zero production callers**; the collaborator-invite task adds the first, **promote-only**. So a mistakenly-granted admin can only be undone by DB surgery. Consequence scales with tier: the same invite path can mint a **Universe admin** and (at the reserved `nebula-platform` scope, whose `buildAuthScopePattern` → `'*'`) a **super-admin**. Accepted gap for the interim — the reach invariant means an inviter can never grant beyond their own reach, so this is *mis*-grants, not escalation. Fix = a thin admin-gated `POST /{scope}/demote` wrapper over the existing method (gate it exactly like `/invite`: router scope-match + `admin === true`), plus a **last-super-admin guard** so the platform can't be locked out. Do NOT expand into general org-management UI (strategy check #7).
- [ ] **Email-reuse / identity-lifecycle hardening (alpha/beta — negligible pre-alpha).** The registry `Identities` row keys off a durable surrogate `sub`; `email` is a mutable `UNIQUE(email, scope)` column that `getAndVerifyIdentity` matches at login (post-[archive/nebula-auth-surrogate-sub.md](archive/nebula-auth-surrogate-sub.md); the old `Subjects` schema is gone). So if a mailbox is **reassigned to a different human** while the old `Identities` row persists, the new human logs in via that email and **authenticates as the old `sub`** — inheriting its access + attribution (classic email-recycling). Attribution *storage* is stable (snapshots carry `changedBy.sub`, opaque UUID — a reassigned email never rewrites history); the hole is the **login mapping** (`getAndVerifyIdentity`), not storage. **Rare + pre-existing**; **negligible pre-alpha** (F&F, short window). Fix at alpha/beta: on offboarding **tombstone the email** at the registry's `email→sub` authority (the ONE place now — surrogate-sub made this a single-point fix), and/or require re-verification that mints a **new** `sub` on reassignment. Surfaced 2026-07-06.
- [ ] **"Login flows" restoration task (UI + test infra) on the new no-mint login model — a task file when we reach it (LATER, before the batch wipe; not now).** After [archive/nebula-auth-surrogate-sub.md](archive/nebula-auth-surrogate-sub.md), **login never mints** (mint is authority-point-only), which retires the old "first magic-link login founds the scope" UX. Two surfaces need re-grounding: **(1) the SPA login flows** (`apps/nebula-studio-ui/src/App.vue` — claim-universe to found, invite to add members, `email-magic-link` only to re-login an existing identity; no more implicit founding-by-login); **(2) the baseline test infra** (`apps/nebula/test/test-helpers.ts` `browserLogin`/`createSubject` — the whole **baseline** vitest lane, ~243 tests / ~44 files, reds at the login step today; the `nebula-auth` package's own 190 tests + the `unit`/`frontend` lanes are green). Test-side fix: `browserLogin(browser, star, email)` claims the star's universe once to mint the founder (its `{u}.*` token covers descendant stars — refresh with `activeScope=star`) and re-logins via `email-magic-link` when already claimed; `createSubject` already maps to **invite**; also propagate the response field rename `magic_link`→`magicLinkUrl` (3 sites). **Finishing step — the ADR-009 mint sweep** (folds in the decomposed [on-hold/auth-shortcut-audit.md](on-hold/auth-shortcut-audit.md) Phase 2, which is *downstream* of this restoration — you can't migrate mints *to* real login until `browserLogin` is real again): once login is restored, sweep the `createNebulaTestToken` sites (`grep -rn createNebulaTestToken apps packages` — **9 files** as of 2026-07-14, incl. the two `profile-*.test.ts` that went rung-3 *because* the login helper was red), migrate each to real login (rung 1) or test-mode issuance (rung 2), justify any rung-3 survivor in-place, and **decide `create-nebula-test-token`'s fate** — delete it + its `@lumenize/nebula-auth/testing` export if none remain, else keep with documented survivors; add a `testing.md` note pointing at ADR-009. Auth-model-driven + stable now, but sequence it with profile/presence (LATER). Surfaced 2026-07-13 (mint-sweep folded in from auth-shortcut-audit 2026-07-15).
- [ ] **The LLM-writes-the-blob flow.** The Profile `privateNotes` field + its `requireOwnerOrAdmin` read/write gate **ship now** (Phase 2, tested by seeding the blob directly), but Nebula *extracting / updating* the "what the LLM knows about you" blob — the actual write path where the LLM (acting as owner) persists personal understanding — is a **later feature**. The gate is ready; the writer isn't. Surfaced 2026-07-15.
- [ ] **Profile-DO teardown on scope deletion (deferred to P2).** `executeScopeDeletion` drops the `Identities` rows + KV + tokens but **not** the now-orphaned Profile DOs (fields + `privateNotes` + subscriber rows) — in P1 each deleted member orphans one. **Acceptable pre-alpha** (unreachable after the ≤15-min token window; the pending greenfield wipe clears it). Proper fix homed in [nebula-auth-identity-mint.md](nebula-auth-identity-mint.md) § Relationships (delete the Profile DO when `Identities WHERE profileId` is empty, in the scope-deletion cascade). Surfaced 2026-07-15; re-homed 2026-07-19 when the P2 design was un-iced and merged into the identity-mint task.
- [~] **ADRs owed by the profile store — DRAFTED 2026-07-15, pending Larry's review (uncommitted).** [ADR-012](../docs/adr/012-global-profile-visibility.md) (global cross-scope profile visibility via the unguessable handle — Accepted) + [ADR-013](../docs/adr/013-identity-profileid-resolution.md) (`profileId` resolved live off `sub`, never copied — Accepted). Both drafted + indexed in `workflow.md` but **not committed** — review, then commit. Surfaced 2026-07-15.

- [x] ✅ **Registry sweep of expired login-channel tokens (`MagicLinks`/`InviteTokens`) — SQL — DONE 2026-07-13.** Implemented in the `NebulaAuthRegistry` **constructor** (the raw-DO `onStart`-equivalent — a raw `extends DurableObject` does one-time setup in the constructor, which completes before dispatch; there is no mesh `onStart` hook): `DELETE FROM MagicLinks/InviteTokens WHERE expiresAt < nowIso` on every wake, mirroring `packages/auth`'s `#ensureSchema` sweep. **Stayed in SQL** (per Larry's decision) — the by-scope teardown + by-`sub` (`RefreshTokenIndex`) convergence enumerations need SQL's secondary index, which KV can't do. **No index on `expiresAt`** (deliberate — reads are ~1/1000th a write, so a full-scan sweep on these small tables beats an index write per insert). `RefreshTokenIndex` is deliberately NOT swept (stale rows are harmless no-op deletes; auto-cleaned as the KV records TTL out). Tested: registry-migrations.test.ts "wake sweep". (Was: surfaced 2026-07-13.)

- [ ] **Nebula email sender domain — `noreply@lumenize.io` stopgap SHIPPED; brand-aligned switch to `nebula.lumenize.com` now unblocked (Resend-verified).**
  - **DONE 2026-06-26:** `NebulaEmailSender` default `from` changed `auth@nebula.lumenize.com` → verified **`noreply@lumenize.io`** (`packages/nebula-auth/src/nebula-email-sender.ts:36`); the prior default was unverified → silently dropped (CF) / rejected (Resend).
  - **Live DNS state (2026-06-26):** `lumenize.io` = CF Email Routing (inbound) + `_dmarc` `p=reject` + verified on **both** CF Email Sending and Resend. `nebula.lumenize.com` = **send-only** (no inbound MX), **no DMARC**, Resend-verified (`send.` subdomain carries SPF+MX, `resend._domainkey` carries DKIM — coexists cleanly with lumenize.io's CF setup). `lumenize.com` = **Google Workspace MX** (direct — not via Cloudflare) + **NO DMARC**.
  - **Morning checklist — switch `from` to the brand/issuer-aligned `noreply@nebula.lumenize.com`:**
    1. `nebula-email-sender.ts:36` default → `noreply@nebula.lumenize.com`.
    2. Add `_dmarc.nebula.lumenize.com` TXT `v=DMARC1; p=none; rua=mailto:dmarc@lumenize.io` (monitor → tighten to `p=reject`). Subdomain-specific so it won't touch lumenize.com's Gmail. (Not a blocker — sends work via DKIM without it — but wanted on an auth-sending domain.)
    3. Set `replyTo` to a routed address (e.g. `support@lumenize.io`) — nebula.lumenize.com has no inbound, so replies would otherwise bounce. (Bounces are already handled by Resend's `send.` Return-Path; only add CF Email Routing to nebula.lumenize.com if replies must land on-brand.)
    4. Add a dedicated nebula `e2e-resend` canary (real send from the verified domain → deployed email-test Worker → WS assert), mirroring `packages/auth/test/e2e-email-resend/`. NOTE: the `createEmailTransport`→Resend path is ALREADY transitively canaried by auth's e2e-email-resend (nebula inherits it) — this is nebula-specific assurance, not new path coverage. KEEP the capturing test sender (`packages/nebula-auth/test/test-worker-and-dos.ts`); don't make all 308 unit tests hit Resend.
  - **Stale comments to fix (the default changed):** `apps/nebula/wrangler.jsonc:41,131` + `apps/nebula/scripts/deploy.sh:60,63` still warn the default is the *unverified* `auth@nebula.lumenize.com` (now false). Plus the original misleading-comment item: the *mesh* harness calls `auth@nebula.lumenize.com` "a verified sending domain" in 3 spots (`packages/mesh/test/browser/worker/index.ts:40`, `packages/mesh/test/browser/README.md:60`, `packages/mesh/test/browser/ws-roundtrip-browser.test.ts:10`) — false; those pass only in test-mode and never actually send.
  - **Separate finding — `lumenize.com` (primary brand domain) has NO DMARC** (`_dmarc.lumenize.com` empty) → spoofable. Add a policy (p=none→monitor→reject); broader, since it affects Google Workspace mail — verify Workspace DKIM/SPF align before tightening.
  - History: surfaced 2026-06-25 (deploy-task review, Larry). The original "wait for CF subdomain support" framing is superseded 2026-06-26 — Resend (now the primary provider) verifies subdomains directly, so the switch no longer waits on Cloudflare.

- [ ] **`FallbackEmailTransport` — email runtime resilience (Axis B).** A `@lumenize/email` transport that retries the *other* provider when the primary fails (Cloudflare ↔ Resend). DEFERRED — no consumer + unspecified behavior: prod-only (needs a prod Resend key), and double-send semantics are unsettled (a primary that half-succeeds, then a fallback re-send = duplicate). `createEmailTransport` (single-provider-by-env) covers the current need; a behavior-less public class now is a premature name-lock. Pick up when a real consumer needs cross-provider failover. Moved from the archived `tasks/archive/lumenize-email.md` (2026-06-26).

- [ ] **`nebula-auth` "who-can-claim-what" — pin the hierarchical claim model, then make the code match (founder-promotion is currently uniform/un-gated).** Surfaced 2026-06-25 (deploy-task review). `#loginSubject` (`packages/nebula-auth/src/nebula-auth.ts:1346`) sets `shouldPromote` (`:1377`) `= isBootstrap || (subjectCount===0 && existingRows.length===0)` — a **uniform** rule applied to *every* scope, with two gaps: (i) the `isBootstrap` branch (`:1350`) fires **regardless of instance/scope**, so the bootstrap email becomes **scope-local admin of every scope it logs into** — the DO JSDoc (`:1258-1259`) claims "only at the reserved nebula-platform instance," which is **false**; (ii) the `subjectCount===0` "first-user-is-founder" path is correct for a *top-level Universe self-signup* but **too permissive for child scopes** (it would let a stranger claim a Galaxy/Star under someone else's Universe just by being first to its auth DO). (The `*` super-admin grant IS correctly instance-gated — `parse-id.ts:115`; this is about the scope-local `isAdmin=1` promotion.) Benign for a single trusted operator (pre-alpha); a **multi-tenant trap**. **Target model (Larry, 2026-06-25) — make the code + JSDoc match this:**

  | Scope | Who can claim |
  |---|---|
  | **Universe** (top-level) | anyone — open self-signup (Turnstile + email) → founder-admin |
  | **Galaxy** (`{u}.{g}`) | the parent **Universe admin** (downward authority), *not* open first-user |
  | **Star** (`{u}.{g}.{s}`) | the parent **Galaxy admin** |
  | any scope, **cross-tree** | superadmin (`*`) via explicit, **audited act-as** — never silent login-promote |

  So: gate the `isBootstrap` login-promotion (superadmin reaches via act-as, not by logging in); make the founder-promotion **scope-level-aware** (open first-user only at the Universe top level; child claims require parent-admin authority). Verify whether a child scope is currently first-user-claimable without parent authority (the concrete gap to confirm). Folds into the broader "audit nebula-auth's internal gating" item below — this is its *target model*. Security-review-gated; capable-of-failing negative tests.

- [ ] **Audit nebula-auth's internal operation gating (who-can-do-what) — never been reviewed.** The per-endpoint / per-method authorization inside `nebula-auth` (claim/create, delegated-token, subject-delete, actor management, the future deprovision/instance-removal, …) has grown organically and has **never had a security review**. Enumerate every mutating operation + its current gate, check against the intended model (super-admin vs scope-admin vs self), and close gaps. Security-review-gated; capable-of-failing negative tests. *Later — not pre-alpha-blocking, but on the path to real users.* Surfaced 2026-06-24 (during the teardown/deprovision design).

- [ ] **Authenticate `discover(email)` — confirm email ownership BEFORE discovery (re-introduce an intermediate token/cookie).** Today `discover(email)` (`nebula-auth-registry.ts:106`) is **unauthenticated + unthrottled** (the CF rate-limiter keys on JWT `sub`, `router.ts:425`, so it never covers the pre-auth `discover`) → an `email → {instanceName, isAdmin}` **enumeration oracle**: anyone (esp. with Turnstile OFF) can probe arbitrary emails to learn which scopes they belong to and which they admin. Dropping `isAdmin` is **not** a real fix — at Galaxy/Universe tiers membership *is* admin-ship (regular users only land at Star scopes), so the `instanceName` alone leaks the sensitive bit; and the UI keys off `isAdmin`. Proper fix = **flow re-order**: confirm email ownership first (a single magic-link → an intermediate token/cookie tied to the email, issued *before* discovery, consumed at the scoped login), then gate `discover` on it AND key the CF rate-limiter on that token. (We *had* an intermediate-token/cookie design and "simplified" it away — this reconsiders that; current flows in `website/docs/nebula/auth-flows.md`.) **Accepted as a pre-alpha residual** (F&F / obscure URL / Turnstile-toggle — release-process ③ B2 § Security model); partially mitigated by Turnstile-ON, but the who-is-admin leak persists even then. Pairs with the gating audit above. Surfaced 2026-06-26 (B2 self-provision `/review-task` conformance).

- [ ] Promote `activeScope` (`aud`) and `authScopePattern` to named, typed scope accessors instead of ad-hoc `originAuth.claims.*` lookups
  - **What**: every Nebula authorization site currently fishes the two scopes out of untyped `originAuth.claims` by hand — `originAuth.claims.aud` for the active scope, `originAuth.claims.access.authScopePattern` for the grant (e.g. `NebulaClientGateway.onBeforeCallToClient` [apps/nebula/src/nebula-client-gateway.ts:14](../apps/nebula/src/nebula-client-gateway.ts:14), `dag-tree.ts`, `subscriptions.ts`). Replace with a typed Nebula-side view (a helper/getter pair) so each check has to *name* which axis it consults.
  - **Why**: the two scopes mean different things — **active** (`aud`) = the star you're bound to *right now*; **auth** (`authScopePattern`) = the grant of *which stars you may bind to*. Conflating them is a security bug in both directions: check the grant where you meant the active scope and a star-A session receives star-B's data (downscoping defeated); check the active scope where you meant the grant and you reject legitimate access. The broadcast-origin-transparency review (below) showed how easy this is to get wrong — the delivery check is correctly exact-`aud`, but nothing structural stops a future site from reaching for the wrong field. Named, typed accessors force intent and make the next feature reason about scope correctly by construction.
  - **Layering constraint** (this is why it's a Nebula task, not a mesh one): mesh's `OriginAuth` is generic (`{ sub?, claims? }`) and **must stay that way** — the dependency-direction rule forbids `@lumenize/mesh` importing Nebula concepts like `aud`/`authScopePattern`. So this is a Nebula-side typed wrapper over `originAuth.claims`, **not** new fields on mesh's `OriginAuth`.
  - **Discovered during**: the 2026-06-08 trust/scope review. Would directly de-risk [tasks/nebula-do-scope-isolation.md](archive/nebula-do-scope-isolation.md) Fix 1 (the structural `onBeforeCall` check reads `aud` + `authScopePattern` out of untyped `claims` — typed accessors make its intent unambiguous). Also referenced from [tasks/on-hold/broadcast-origin-transparency.md](on-hold/broadcast-origin-transparency.md). Not a blocker for either; the generalized explicitness win.

- [ ] Integrate Cloudflare Account Abuse Protection for disposable email and email risk detection
  - **Ref**: https://blog.cloudflare.com/account-abuse-protection/
  - **What**: Cloudflare's fraud detection computes `cf.fraud_detection.disposable_domain` (boolean) and `cf.fraud.email_risk` (low/medium/high/unknown) on eligible requests. These can be injected as request headers via Managed Transform Rules, then read in the Worker — no client-side widget needed, no siteverify API call. Simpler than Turnstile integration.
  - **Where in nebula-auth**: `src/router.ts`, alongside the existing Turnstile check on public unauthenticated endpoints (`claim-universe`, `claim-star`, `email-magic-link`). Read injected headers, reject disposable emails, escalate high-risk emails to require admin approval.
  - **Graceful degradation**: If the headers aren't present (user-developer hasn't configured Transform Rules), skip the check — same pattern as Turnstile being optional when `TURNSTILE_SECRET_KEY` isn't set.
  - **Caveat**: Currently Early Access for Bot Management Enterprise customers only. As of March 2026, this appears to require paid WAF/Bot Management — there is no free-tier or standalone API equivalent. If Cloudflare opens this up more broadly (GA expected later in 2026), it becomes a natural addition. Until then, a lightweight disposable-domain blocklist (MIT-licensed lists exist) could serve as a free stopgap.

- [x] ✅ **Orphaned tokens on subject-delete — RESOLVED by [archive/nebula-auth-surrogate-sub.md](archive/nebula-auth-surrogate-sub.md)** (built 2026-07-13, code green; deploys at the batch wipe). The per-scope `NebulaAuth` DO + its dead-`ON DELETE CASCADE` `RefreshTokens` are gone: refresh tokens now live in Workers KV with a fixed TTL (auto-expire) and the registry deletes the KV record + `RefreshTokenIndex` entry on scope-deletion (`executeScopeDeletion` → `#invalidateRefreshTokensForSub`, KV-delete-first). A deleted identity also fails the registry lookup on next refresh. Surfaced 2026-07-11.

## `@lumenize/ts-runtime-parser-validator`

- [ ] **`@default` input/output type asymmetry — dual-type exposure.** Promoted from [`tasks/archive/nebula-5.2.4.1-validator-engine-upgrade.md`](archive/nebula-5.2.4.1-validator-engine-upgrade.md) Phase -1 (closed 2026-04-24). `@default` on a field creates an input/output type mismatch: input-side the field is absent-allowed, output-side it's always present. Current rule requires `?` (input-honest; consumers pay null-check tax on post-parse data). Most promising fix: add a typia-style `Default<T>` branded type alongside JSDoc `@default`, plus a `Parsed<T>` utility that non-optional-ifies branded fields — Zod-style dual views without dropping the JSDoc path. **Trigger**: users complaining about null-check noise on parsed `data`, or Nebula hitting pain when generating TypeScript client code for ontology consumers. Full analysis in the archived 5.2.4.1 Phase -1.