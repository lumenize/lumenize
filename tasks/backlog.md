# Backlog

Small tasks and ideas for when I have time (evening coding, etc.)

> 🔴 **SECURITY-CRITICAL — Turnstile is OFF in prod (pre-alpha); turn it ON before public signup (alpha).**
> A deliberate, dated deferral (`TURNSTILE_SECRET_KEY` unset — confirmed 2026-07-06), acceptable **only** while access is F&F-invite-only / obscure-URL / cost-ceiling-watched. The automation **bypass** — the hard part — is ✅ built + canary-verified, so this is unblocked; **deferred to post-pre-alpha**, with the design + blocker catalog + phases now homed in **[tasks/on-hold/turnstile-on-in-prod.md](on-hold/turnstile-on-in-prod.md)**.

## Immediate work backlog

- [x] ~~**Switch local Nebula test email from Resend (~8s) to Cloudflare (~1s)**~~ — ❌ **REJECTED 2026-07-21, measured: there is no gap to win.** Median magic-link loop is **Cloudflare 1451 ms (n=9) vs Resend 1400 ms (n=10)** — indistinguishable, Resend marginally faster at the median, neither with a cold-start penalty. Results + method: [tasks/email-latency-cf-vs-resend.md](email-latency-cf-vs-resend.md). **Kept as a closed entry, not deleted, so the switch isn't re-proposed off the same false premise.**
  - **The `~8s` that justified this was never measured on Resend.** It traces to one uninstrumented, hand-wall-clocked figure (ADR-009, 2026-07-06) citing `apps/nebula/harness/prod.ts spin` — a command with no timer that, per the config in effect that day, sent via the **Cloudflare** binding. ADR-009's cost line is now corrected. **Do not re-cite ~8s.**
  - **Doing the switch would cost, not save:** the CF send binding **requires `remote: true`** (verified — without it miniflare simulates the send and nothing leaves the machine), which binds at pool load and needs CF creds. That is precisely the dependency the `LUMENIZE_NO_CF_REMOTE` opt-out exists to work around. Paying it for ~50 ms of noise is backwards. Also note the local `/live` harness lane is **already** on Cloudflare; only ui-smoke + browser/bench are on Resend, and they chose it deliberately to run creds-free — the lanes this item said to leave alone.
  - ✅ **There is no 200/day concern here — do not reintroduce it.** The beta cap applies only to the arbitrary-recipient **Email Sending** feature. Verified addresses/domains send via the **Routing** feature instead, so the cap never touches `*@lumenize.io` test email (double-confirmed with Cloudflare employees; `cf-email-sending-vs-routing` has said this since 2026-07-14). This has been misread repeatedly — including once in this very entry.
  - ⚠️ **Still open, now for the opposite reason:** ADR-009 rung 2 was justified on "~8s is too slow for every unit test". At **~0.9 s marginal** that rationale is gone entirely — not merely weakened. The ADR now flags rung 2 as open, standing only on flake/external-dependency grounds. **Larry's call whether it survives.**

- [ ] **Finish moving the test lanes onto real email login** (started 2026-07-21; the measurement that unblocked it is [tasks/email-latency-cf-vs-resend.md](email-latency-cf-vs-resend.md) — the loop is ~1.4s standalone / ~0.9s marginal, so **speed is not a reason to use a mint**). Landed already: one shared `@lumenize/email-test/client`, unique-recipient parallel lanes, and `loginViaEmail` (`apps/nebula/test/lib/email-login.ts`, verified against prod). Remaining:
  - ✅ **DONE 2026-07-21 — `connectDriver` logs in for REAL by default.** `provisionAndLogin` (`apps/nebula/test/lib/email-login.ts`) claims the universe (the one open, founder-minting entry — it mints with `isAdmin: true` *then* sends the link), consumes the real magic link, creates the galaxy/star beneath with that founder's token, and re-issues at the target scope. Verified on the running stack: `message-roundtrip` PASSED 19.2s on the real path; `superadmin-reach` (explicit `mint: { reason }`) still PASSED. The synthetic mint survives only behind `mint: { reason }`.
    - ⚠️ Logging in **directly at a fresh star** remains impossible — `createStar` writes a `Scopes` row with no founder. That is open Star self-signup ([nebula-star-founder-provisioning.md](nebula-star-founder-provisioning.md)), tracked by the existing `it.skip('claim-star: open self-signup mints an exact-star founder…')` in `packages/nebula-auth/test/nebula-auth-routes.test.ts`. **Don't add a second marker.** `provisionAndLogin` sidesteps it by claiming the universe above.