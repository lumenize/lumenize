# Phase 5.6: HTTP Transport

**Status**: Pending
**Depends on**: Phase 5.1 (Storage Engine)

## Scope

`GET` → read, `PUT` → upsert, `DELETE` → delete. `If-Match` header for optimistic concurrency. `GET /discover` endpoint. Content type: `application/vnd.lumenize.structured-clone+json`.
