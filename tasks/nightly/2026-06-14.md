# Nightly digest — 2026-06-14 (DRY RUN)

**Verdict:** ⚠️ One gap — the **Mn9 collection-entry-interior probe** is an open v3-port obligation that isn't pinned in the canonical §5.3.8 list, so v3 wiring would miss it.
**Reviewed:** backlog **#5** — "Mn9 collection-entry-interior probe captured in §5.3.8?" — [factory-collection-sync.md:9](factory-collection-sync.md) → [nebula-frontend.md §5.3.8](nebula-frontend.md) (mode: findings-only)

## Findings
- The Mn9 obligation (a *capable-of-failing* probe for `map.get('k').field = v`: either it syncs, or it doesn't-sync **and** emits a `@lumenize/debug` warning) lives **only** in `factory-collection-sync.md:9`. `nebula-frontend.md` has zero mention of it — `grep -i 'interior|map.get|Mn9'` → no matches; §5.3.8's nearest probe (`v-for inline-array iteration`, :526) covers array push/pop/reorder, **not** entry-interior property writes.
- **Why it matters:** §5.3.8 is the canonical probe checklist v3 wiring works from. An obligation absent from it can ship as exactly the *silent local-only divergence* (the M10 failure mode) this detour exists to close.

## Your move
Add one line to §5.3.8 (Framework-agnostic probes), matching the documented "document-as-footgun + warning" lean — **proposed text, not applied:**
```
- [ ] **Collection-entry interior write (Mn9):** `map.get('k').field = v` on an object read FROM a collection entry does NOT sync (Vue hands back its own reactive, not our path-proxy) AND emits a `@lumenize/debug` warning — capable-of-failing, so the leaky edge can't ship as silent local-only divergence. (If v3 instead picks recursion through Vue's instrumented `get`, flip the probe to assert it DOES sync.) Source: factory-collection-sync.md Mn9.
```

## Considered, not picked
- **#1 Atomic-batch precedence (FB4 non-disclosure)** — also HIGH, but §5.3.8:513 already has an atomic-batch precedence probe, so the question is subtler and verifying it means reading the full spike test (higher review-cost).
- **#19 nebula-do-scope-isolation prerequisite still valid** — MEDIUM, decidable, but judging the dependency needs two task files read end-to-end (more reasoning to trust the verdict).

---
Mode: findings-only (tree dirty — this session's own nightly-loop work). Baseline known-RED: disconnect-cleanup (`task_3e018327`, allowlisted) — full suite **not run** (v0). Backlog **#5 reviewed** (cooldown applies); the underlying fix stays **open** until the probe line is added.
