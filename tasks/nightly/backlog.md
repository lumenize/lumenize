# Nightly loop — review-eligible candidate backlog

This file is the **source-C candidate pool** for the nightly self-improvement loop's selector.
The loop does ONE kind of overnight work: a code/design OPTIMIZATION review pass — like a `/review-task` or `/code-review` over a spec/doc/source file, producing findings (+ optionally a proposed diff on a dedicated worktree).
It NEVER commits, deploys, runs experiments, or builds.

**Review-eligible items** (below) are the selector's input.
**Parked items** are captured for visibility but need a build, deploy, live experiment, benchmark, or a human design ruling before they can become nightly-shaped; a human must promote them.

Seeded 2026-06-14 from the detour + on-hold task files (`factory-collection-sync.md`, `factory-textmerge.md`, `factory-conflict-outcome.md`, `debounce-serial-queue.md`, `broadcast-origin-transparency.md`, `dag-client-supplied-nodeid.md`, `debug-production-transport.md`, `distributed-cpg-security-analysis.md`, `mesh-overload-backpressure-handling.md`, `mesh-resilience-testing.md`, `nebula-5.5-schema-evolution.md`, `http-transport.md`, `nebula-5.7-docs-coverage.md`, `nebula-orm-and-queries.md`, `nebula-release-process.md`, `nebula-resource-history-r2.md`, `release-process-improvements.md`, `wire-merge-patch-sync.md`).
Meant to be hand-curated over time. See [nebula-nightly-loop.md](../nebula-nightly-loop.md) for the full loop design.

> Caveat (seeding pass): mined by Sonnet agents from the task files, not verified against current source. Leverage ratings and `file:line` citations are a first draft — curate before trusting. Several HIGH items reference the active critical path and are worth a human glance first.

---

## Review-eligible (nightly-shaped)

Ranked highest-leverage first. "Leverage" = HIGH if the item unblocks the active demo critical path or closes a known security/correctness gap; MEDIUM for pre-port gap prevention or spec consistency on queued work; LOW for housekeeping / on-hold items with no near-term impact.

> Removed 2026-06-15: #17, #32–#35 (the mesh reconnect/resilience review cluster) — now covered directly by the active nebula-frontend v4 build (real-browser harness + WS-disconnect tooling), so reviewing it in parallel is redundant. Numbering gaps left intact so ledger/digest `#`-references stay stable. (Siblings #60/#61 — supersession 4409, access-gate — left in place; pull them too if v4 ends up covering the whole resilience suite.)
> Removed 2026-06-15 (curation): the v3-frontend-factory detour cluster — Review-eligible #1, #2, #5–#15, #42–#44 and Parked #1–#4 — all sourced from the four detour task files (now COMPLETE; archived to `tasks/archive/`, and their `apps/nebula/spike/vue-factory/` source was deleted in 5.3.7/P11). The 5.3.7 v1–v5 build shipped and was verified by its /build-task verifiers + real-Star/chromium e2e, so re-reviewing it overnight is moot. Numbering gaps left intact (per the note above).
> Removed 2026-06-16: #19 (scope-isolation-vs-broadcast prerequisite check) — cross-check done. `nebula-do-scope-isolation.md` is **not gated by** the broadcast fix; it's a standalone tenant-isolation + shared-Galaxy-multi-tenancy correctness task. Broadcast Option A changes no scope/auth behavior, so the sequencing was never hard-coupled (broadcast is merely lower-priority behind it). Task status re-anchored in-file 2026-06-16. Numbering gap left intact.

| # | Candidate | What the pass does | Source | Leverage |
|---|---|---|---|---|
| 3 | SC own-property survival for overload error tags through WS hop | Review `lumenize-client-gateway.ts:604–615` and `lumenize-client.ts:1126` preprocess/postprocess against structured-clone rules to confirm `.overloaded`/`.retryable` own-properties survive the CALL_RESPONSE WS round-trip — Phase 1 open sub-question that blocks shipping the overload feature | `mesh-overload-backpressure-handling.md` | HIGH |
| 4 | Server-side replay-idempotency of `newETag` in Star transaction handler | Review `apps/nebula/src/star.ts` transaction handler against ADR-005 eTag idempotency to confirm a resubmit of an already-applied `newETag` returns `committed` (not double-apply, not spurious conflict) — the lynchpin correctness gate for the read-only retry path | `mesh-overload-backpressure-handling.md` | HIGH |
| 16 | callRaw catch sites completeness and tagging consistency | Review `lmz-api.ts:305`, `lumenize-client-gateway.ts:604`, and the Star transaction handler catch sites against Phase 1 requirements — confirm all three tagging locations are present and consistent with the `MeshOverloadedError` typed shape | `mesh-overload-backpressure-handling.md` | MEDIUM |
| 18 | Broadcast tree-path origin-fix spec correctness (Option A) | Review `tasks/on-hold/broadcast-origin-transparency.md` Phase 0–3 spec + A1–A4 test matrix against `packages/mesh/src/broadcast.ts`, `lumenize-worker.ts`, and `apps/nebula/src/star.ts` for internal consistency and test-case coverage (recursion-depth A3, reconnect-race A4 timing) | `broadcast-origin-transparency.md` | MEDIUM || 20 | `createNode` idempotency design vs client-supplied-id spec | Review `apps/nebula/src/dag-tree.ts` `createNode` + Nodes/Edges/Permissions schema against the on-hold spec (INSERT OR IGNORE idempotency, text PK, WITHOUT ROWID, drop `last_insert_rowid`) — enumerate every site that must change before the detour build | `dag-client-supplied-nodeid.md` | MEDIUM |
| 21 | OrgTreeState wire format and `dag-ops.ts` string-id compatibility gaps | Review `apps/nebula/src/dag-ops.ts` (`ROOT_NODE_ID`, `OrgTreeEdgeKey`, `buildOrgTreeView`, nodes Map key) and `OrgTreeState` type against the spec's string-id changes — emit a diff of type/constant changes needed | `dag-client-supplied-nodeid.md` | MEDIUM |
| 22 | Default-fill vs migration boundary spec completeness | Review the `Default-fill vs migration boundary` section in `tasks/on-hold/nebula-5.5-schema-evolution.md` against `packages/ts-runtime-parser-validator` source to confirm `@default-fillable` cases are correctly enumerated and migration-needed cases have no gaps or overlaps | `nebula-5.5-schema-evolution.md` | MEDIUM |
| 23 | `OntologyVersionRow` structure for 5.5 `migrationBundle` readiness | Review current `OntologyVersionRow` type against the 5.5 spec's `migrationBundle` field requirement — identify structural gaps or conflicts with existing fields | `nebula-5.5-schema-evolution.md` | MEDIUM |
| 24 | Migration facet design vs validator facet pattern | Review the 5.5 migration facet spec (`bundleId`-scoping, per-version cache, one-combined-facet recommendation) against the actual validator facet implementation in `apps/nebula/src` — confirm the pattern is faithfully described and inherited correctly | `nebula-5.5-schema-evolution.md` | MEDIUM |
| 25 | Private-workspace `@lumenize/*` dep declarations for caret-with-major-0 trap | Review `apps/nebula/package.json` and all doc-test `*/package.json` files against the Phase 1 rule that `@lumenize/*` deps must use `"*"` not `"^0.X.Y"` — emit list of files still using the caret form | `release-process-improvements.md` | MEDIUM |
| 26 | `experiments/` workspace entries for removed/broken experiments | Review root `package.json` workspaces list against `experiments/` dirs on disk — flag entries whose `package.json` or transitive deps are stale or broken (Issues 7–8 caused `npm install` to abort mid-release) | `release-process-improvements.md` | MEDIUM |
| 27 | Test files for orphaned imports and dead `wrangler.jsonc` `class_name` references | Review every `packages/*/test/*.ts` re-export and every `wrangler.jsonc` `class_name` field against actual source exports — emit list of broken references (Issues 3–4 surface as non-deterministic failures mid-release) | `release-process-improvements.md` | MEDIUM |
| 28 | `packages/rpc` test files for missing cleanup causing DataCloneError rejections | Review `packages/rpc/test/` (`websocket-integration.test.ts`, `map-set-identity.test.ts`, siblings) against Phase 2 spec — identify module-scope or `beforeEach`-cached stubs lacking explicit disconnect/disposal in `afterEach` | `release-process-improvements.md` | MEDIUM |
| 29 | `release.sh` vs Phase 3 split spec (version/publish separation + dry-run gate) | Review `scripts/release.sh` against the Phase 3 design (drop redundant test re-run, split into `version.sh` + `publish.sh`, add `.last-dry-run-sha` gate) — emit a concrete diff of changes needed | `release-process-improvements.md` | MEDIUM |
| 30 | Phase 1 version-stamp design vs wrangler.jsonc and `/_version` endpoint spec | Review `apps/nebula/src/` and `wrangler.jsonc` against the Phase 1 spec (`define __GIT_SHA__`/`__DIRTY__`, `/_version` endpoint) — identify gaps or implementation drift before build work starts | `nebula-release-process.md` | MEDIUM |
| 31 | `deploy-nebula.sh` design vs `release.sh` and `packaging.md` | Review `scripts/release.sh` and `packaging.md` against the Phase 3 `deploy-nebula.sh` spec — identify ordering gaps, missing version-lock checks, or workspace-symlink vs registry-tarball conflicts | `nebula-release-process.md` | MEDIUM |
| 36 | DAG-tree sync server-side spec vs existing `DagTree` implementation | Review `apps/nebula/src/star.ts` and `apps/nebula/src/frontend/dag-tree.ts` against the Phase A spec in `wire-merge-patch-sync.md` — identify gaps in fanout, ETag chaining, and patch-history retention | `wire-merge-patch-sync.md` | MEDIUM |
| 37 | Lazy access-control cold-mode spec vs `DagTree` implementation | Review `apps/nebula/src/dag-tree.ts` against the `DagTreeReader` adapter spec — verify interface boundaries, index coverage, and test-coverage-drift risk | `wire-merge-patch-sync.md` | MEDIUM |
| 38 | R2 key-namespacing spec vs security rules for cross-tenant isolation | Review the R2 key-isolation spec in `nebula-resource-history-r2.md` (scope-prefix requirement, Star-constructs-keys rule) against `security.md` and the trust-boundary model — verify no path allows a Star to accept client-supplied keys or access another tenant's prefix | `nebula-resource-history-r2.md` | MEDIUM |
| 39 | D2 tiered-residency eviction design vs DO instance-state rules | Review the D2 hybrid-tier design (ephemeral in-RAM CLOCK/LRU + size-triggered batch sweep + `residentBytes` tracking) in `nebula-resource-history-r2.md` against `durable-objects.md` (no-instance-state rule + its ephemeral-cache exception) | `nebula-resource-history-r2.md` | MEDIUM |
| 40 | Mesh `@mesh()` surface enumeration vs lowering-pass design | Review `packages/mesh` source (`@mesh` decorator, `ctn<X>()` proxy, `lmz.call` 4-arg form) against the lowering-pass spec in `distributed-cpg-security-analysis.md` — identify gaps or mismatches in static-edge extraction | `distributed-cpg-security-analysis.md` | MEDIUM |
| 41 | `callChain` observability design vs V0 distributed-call-graph spec | Review `tasks/mesh-call-tracing-and-ids.md` against the V0 spec (auth-check present, arg-schema present, mesh-vs-fanout annotations) — find missing coverage or spec conflicts; V0 is the prerequisite substrate for the CPG V1/V2 | `distributed-cpg-security-analysis.md` | MEDIUM |
| 45 | `disconnect-cleanup` test header fix actually committed | Read `apps/nebula/test/nebula-client-disconnect-cleanup.test.ts` header and cross-check against Phase 3's claim that the JSDoc/header fix was done in the 2026-06-08 session — confirm it landed or flag as still pending | `broadcast-origin-transparency.md` | LOW |
| 46 | Codebase audit for other server-generated IDs (`last_insert_rowid`, AUTOINCREMENT) | Search `apps/nebula/src/` and `packages/` for `last_insert_rowid()`, `AUTOINCREMENT`, `INTEGER PRIMARY KEY` patterns and review each hit against the client-supplied-id principle — flag any non-resource server-generated ids | `dag-client-supplied-nodeid.md` | LOW |
| 47 | `debug/sink.ts` Phase 1 design decisions vs current implementation | Review `packages/debug/src/sink.ts` against the 7 Phase 1 design decisions (multi-transport, `replaceConsole`, test-sink precedence, sync/async, filter bypass, per-isolate, error-in-transport) — flag implementation gaps or conflicts | `debug-production-transport.md` | LOW |
| 48 | `addDebugTransport`/`setDebugSink`/`clearDebugSink` API shape vs `@lumenize/debug` exports and coding-style | Review proposed Phase 2 API surface in `debug-production-transport.md` against `packages/debug/src/*.ts` and `coding-style.md` — flag naming, type, or export conflicts | `debug-production-transport.md` | LOW |
| 49 | Per-isolate installation caveat vs mesh DO construction patterns | Review `packages/mesh/src/lumenize-do.ts` constructor and `onStart` patterns against the per-isolate constraint in `debug-production-transport.md` — determine whether an `installInDurableObject` helper is needed or documentation suffices | `debug-production-transport.md` | LOW |
| 50 | 5.5 open questions closeable by existing code or ADRs | Review each open question in `tasks/on-hold/nebula-5.5-schema-evolution.md` (write-back timing, version-skew, migration error handling, standalone extraction) against `docs/adr/` and existing nebula/mesh source — identify which are already answerable from current commitments | `nebula-5.5-schema-evolution.md` | LOW |
| 51 | HTTP method→operation mapping vs `resources.ts` spec | Review `tasks/on-hold/http-transport.md` HTTP mapping table against `packages/nebula/src/resources.ts` `OperationDescriptor` variants — verify 1:1 coverage, flag gaps (MOVE, PATCH), check If-Match/eTag semantics align with ADR-005 | `http-transport.md` | LOW |
| 52 | Part A JSDoc constraint tag vocabulary vs 5.2.4.1 Phase 3 decisions | Review `packages/ts-runtime-parser-validator` source and any 5.2.4.1 Phase 3 docs against the tag table in `nebula-orm-and-queries.md` Part A (`@min`/`@max`/`@format` vs typia-aligned names) | `nebula-orm-and-queries.md` | LOW |
| 53 | Part C open questions for design-decision readiness | Review the nine open questions in Part C of `nebula-orm-and-queries.md` against the existing ontology/resource spec and ADR-001/ADR-004 — sort answerable-from-existing-specs from human-decision-required | `nebula-orm-and-queries.md` | LOW |
| 54 | Part C success criteria vs current `resources.ts` and mesh spec for drift | Review Part C success criteria checklist in `nebula-orm-and-queries.md` against current `apps/nebula/src/resources.ts`, ontology implementation, and DAG-tree permission model to flag criteria that have drifted | `nebula-orm-and-queries.md` | LOW |
| 55 | Phase 4 reproducibility options vs `prepare-for-publish.sh`/`restore-dev-mode.sh` | Review `scripts/prepare-for-publish.sh` and `restore-dev-mode.sh` against Phase 4 options A/B/C in `nebula-release-process.md` — produce a ranked recommendation and flag workspace-symlink traps | `nebula-release-process.md` | LOW |
| 56 | Email-test worker staleness vs Phase 1/2 version-stamp spec | Review `packages/auth/test/e2e-email/` (email-test worker) against the Phase 1 `/_version` spec in `nebula-release-process.md` — determine whether it can inherit the same version-stamp pattern with no changes | `nebula-release-process.md` | LOW |
| 57 | Debug/exploratory test artifacts that don't belong in CI | Review `packages/*/test/*.test.ts` files for markers of debug artifacts (docstrings containing 'debug'/'exploratory', excessive `console.log` spam) — produce a list of candidates for removal | `release-process-improvements.md` | LOW |
| 58 | Phase 4 CI release workflow spec security review | Review the Phase 4 GitHub Actions workflow design in `release-process-improvements.md` against `security.md` (secrets handling, automation token scope) — emit security findings; the human decision on lerna drop is separate | `release-process-improvements.md` | LOW |
| 59 | `audit-release-readiness.sh` Phase 5 spec completeness | Review the Phase 5 audit script spec (four checks: caret deps, experiments runnable, wrangler `class_name` resolution, test re-export existence) in `release-process-improvements.md` against codebase structure — verify each check is implementable and sufficient | `release-process-improvements.md` | LOW |
| 60 | Supersession (4409) handling in Gateway and Client vs Phase 4 spec | Review `lumenize-client-gateway.ts` (4409 close on duplicate `instanceName`) + `lumenize-client.ts` (stale-close handling) against Phase 4 success criteria (A gets 4409, B gets `subscriptionRequired:false`, routing to B) | `mesh-resilience-testing.md` | LOW |
| 61 | Access-gate rejection path vs Phase 6 spec | Review `lumenize-client-gateway.ts` auth-hook entry path against Phase 6 success criteria (`adminApproved:false` / `emailVerified:false` → 401 at Worker level) — confirm the gate is exercised on WS upgrade, not only on message receipt | `mesh-resilience-testing.md` | LOW |
| 62 | Per-resource read-sync spec vs `resources.ts` and `NebulaClient` | Review `apps/nebula/src/resources.ts` and `nebula-client.ts` against the Phase B per-resource read-sync spec in `wire-merge-patch-sync.md` — check ETag threading, unchanged-path, and ring-buffer retention design consistency | `wire-merge-patch-sync.md` | LOW |
| 63 | D7 presigned-URL spec vs security and secrets rules | Review the D7 direct-client-R2-access design in `nebula-resource-history-r2.md` against `security.md` and `critical.md` — check R2 Access Key secret handling, prefix-scoping, TTL constraints, and bypass of per-call mesh auth | `nebula-resource-history-r2.md` | LOW |

---

## Parked (needs build / deploy / experiment / human ruling)

These items are captured so they are not lost, but they cannot run as overnight review passes. A human must promote them when the gate clears.

| # | Candidate | Why parked | Source | Leverage |
|---|---|---|---|---|
| 5 | Implement Phase 0–3 of broadcast tree-path origin fix (Option A) | Code changes to `broadcast.ts` + `lumenize-worker.ts` + test harness. Not gated by scope-isolation (Option A changes no scope/auth), but lower-priority behind `nebula-do-scope-isolation.md`. | `broadcast-origin-transparency.md` | HIGH |
| 6 | Build and test client-supplied `nodeId` in isolation (the dag detour) | Requires code changes, test runs, and schema rewrites | `dag-client-supplied-nodeid.md` | HIGH |
| 7 | Design: overloaded-retry vs shed behavior — human design ruling needed | Explicit open question ("Decide this explicitly"); no review pass resolves it | `mesh-overload-backpressure-handling.md` | HIGH |
| 8 | Phase 2 + 2b: `readOnly` flag, `callRaw` retry, app-level write retry loop | Build + test execution; blocked on open design decisions (item #7 above) | `mesh-overload-backpressure-handling.md` | HIGH |
| 9 | Build Phase 1–6 resilience integration tests (mini-app harness) | Requires building a new mini-app test harness and running live Workers integration tests | `mesh-resilience-testing.md` | HIGH |
| 10 | R2 write-path vs DO output-gate: spec review narrows but benchmark closes D1 | Final D1 verification requires a live R2-from-DO benchmark; spec review alone cannot close the decision | `nebula-resource-history-r2.md` | HIGH |
| 11 | Benchmark R2 read latency from a DO (D4) | Requires deploy + live measurement; prerequisite for D2 tiering commitment | `nebula-resource-history-r2.md` | HIGH |
| 12 | Probe Joern TS frontend CPG quality on a representative Mesh DO | Requires running Joern (a tool/experiment); not a review pass | `distributed-cpg-security-analysis.md` | HIGH |
| 13 | Implement DAG-tree patch sync (Phase A) + lazy cold-mode permission checks | Code changes and integration tests against a live DO; on hold until a real workload exposes the scale concern | `wire-merge-patch-sync.md` | MEDIUM |
| 14 | Nebula release-process Phase 2: `assert-deployed-version` helper build | Requires live deploy to `/_version` to test end-to-end; needs Phase 1 first | `nebula-release-process.md` | MEDIUM |
| 15 | Open question: Nebula app versioning vs Lerna package version stream — human ruling | Explicit human design ruling needed before Phase 3 can be implemented | `nebula-release-process.md` | MEDIUM |
| 16 | Nebula client UX for overload surface (busy state vs silent backoff) — human ruling | Product/UX judgment needed; listed as explicit open question | `mesh-overload-backpressure-handling.md` | MEDIUM |
| 17 | Serial txn queue drain-pause on overload vs per-transaction backoff — human ruling | Architectural ruling needed before Phase 2b implementation | `mesh-overload-backpressure-handling.md` | MEDIUM |
| 18 | D5/D6: hot-read frequency and cache TTL from empirical data | Requires runtime instrumentation and D4 benchmark output; no review pass closes it | `nebula-resource-history-r2.md` | MEDIUM |
| 19 | Implement migrationBundle generator, Star migration runner, cross-resource callback | On hold until post-funding; requires building new packages and human design decisions | `nebula-5.5-schema-evolution.md` | LOW |
| 20 | Resolve HTTP batch endpoint scope and status code mapping — human ruling | All four open questions require human design decisions | `http-transport.md` | LOW |
| 21 | Audit `@skip-check` annotations in nebula resources doc | Blocked until the doc file exists (Phases 5.1–5.6 must complete first) | `nebula-5.7-docs-coverage.md` | LOW |
| 22 | Verify nebula resources doc code examples against source | Blocked until the doc file exists | `nebula-5.7-docs-coverage.md` | LOW |
| 23 | Check `sidebars.ts` entry for nebula resources doc | Blocked until the doc and sidebars entry are written | `nebula-5.7-docs-coverage.md` | LOW |
| 24 | Branch/statement coverage gap analysis for nebula resources | Requires `vitest --coverage` run — a build/run step, not a review pass | `nebula-5.7-docs-coverage.md` | LOW |
| 25 | Part B: M:N join-table design — implement and build | Full feature build; on hold pending demo | `nebula-orm-and-queries.md` | LOW |
| 26 | Part C: multi-resource `query()` — implement and build | Full feature build; unresolved design decisions; on hold | `nebula-orm-and-queries.md` | LOW |
| 27 | Part A: JSDoc constraint tag extraction — implement and build | Code implementation in `ts-runtime-parser-validator`; on hold | `nebula-orm-and-queries.md` | LOW |
| 28 | Nebula release-process Phase 5 CI wiring and rollback | Explicitly deferred until Phases 1–4 land; premature to review | `nebula-release-process.md` | LOW |
| 29 | Phase 4 CI strategy: human decision on lerna drop, secret rotation cost | Explicit open question before Phase 4 CI work can proceed | `release-process-improvements.md` | LOW |
| 30 | Phase 7 sub-request ceiling experiment | Requires deploying and running a live fan-out benchmark | `mesh-resilience-testing.md` | LOW |
| 31 | Write `docs/mesh/resilience.mdx` | Depends on Phase 1–6 integration tests being built first | `mesh-resilience-testing.md` | LOW |
| 32 | Implement per-resource read-sync (Phase B) | Code changes + integration tests; explicitly deferred post-demo | `wire-merge-patch-sync.md` | LOW |
| 33 | Run perf verification benchmarks for DAG patch sync (Phase C) | Requires deploy + live measurement; after Phase A/B ship | `wire-merge-patch-sync.md` | LOW |
| 34 | Design review: cross-binary edge schema (Joern CPG extension vs side graph) — human ruling | Open question explicitly unresolved; needs architectural decision, not review | `distributed-cpg-security-analysis.md` | LOW |
| 35 | V2 AI query-generator framing vs Nebula LLM harness | Depends on V0+V1 being built; Studio LLM surface not yet shipped | `distributed-cpg-security-analysis.md` | LOW |
