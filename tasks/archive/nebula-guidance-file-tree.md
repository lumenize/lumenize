# The guidance file tree — platform guidance in code, Galaxy guidance in the Workspace repo, both read every turn

**Status:** ✅ **DONE + ARCHIVED 2026-09-06.** Built 2026-09-05 — all seven phases, per-phase record in § *Build log* at the end; item ① of the run to the wipe in [nebula-pre-alpha.md](../nebula-pre-alpha.md) (now one line in its § *Shipped*), **deploy-gated** (it only has to be in the bundle), and it **gated ② personas**, whose home it shipped. Archived 2026-09-06 with the 2026-09-06 follow-ups closed out in the last § *Build log* entry. It delivers the tree with its platform and Galaxy layers, the always-on resource core, the docs and three seed skills behind a read tool, an edit tool, the chat history in the prompt, and one assembly for every turn, with every loop tool at the chat floor.

**Objective — every Studio turn reads a tree of guidance files with two owners: the platform layer we ship in code, and the Galaxy layer that lives in the user-developer's Workspace repo beside the app it describes.**

Five goals, in the order they matter:

1. **Guidance has one home, so the next file class lands in it instead of beside it.** The persona file is the first LLM-authored content; the vision and permissions files ship as seeded declarations with headings and are filled in as the app is built. Today each would invent its own storage and its own read path.
2. **The model knows how to build a data-bound app, so hand driving generates signal instead of re-finding the same gap.** The prompt teaches it nothing about resources today — no `store`, no `client`, no subscribe — and a hand-driven wishlist came back not using them at all (Larry, 2026-09-03).
3. **The user-developer's app carries its own guidance, and the LLM evolves it.** A Galaxy's `AGENTS.md` starts from a platform-written seed, and the end of a piece of work is where it changes — the loop this repo runs, pushed by the platform layer rather than left for the user-developer to discover ([docs/vision/_ai-security.md](../../docs/vision/_ai-security.md) § *Level 2*).
4. **The Galaxy layer reads correctly to any agent, not only ours.** The Workspace is a git repo any agent may open — Claude Code on a clone, once a remote exists — so the file carries the cross-vendor name and layout.
5. **Shipping a platform-layer change costs a deploy and nothing more.** One authoritative copy, bundled at deploy, with no second copy to keep in sync; iterating it locally is regenerate-then-`wrangler dev`, with no deploy (§ *Storage is files in git*).

## Relationships

- **Gated by nothing** — no other task must land first; every mechanism this extends is in the running system or listed under § *Missing*.
- **Gates ② personas** ([nebula-pre-alpha.md](../nebula-pre-alpha.md) § *② Personas*): the persona file is the first LLM-authored file in the Galaxy layer, and ②'s executor is the last step of a skill this task seeds.
- **Changes two siblings' premises.** [nebula-data-plane-owns-its-guards.md](../nebula-data-plane-owns-its-guards.md) § *Context* carries `writeSource` over with its `requireDominionHere` decorator; this task changes that guard to `requireChatWrite` (§ *Who writes each layer*), so ④ owes the correction. [nebula-ontology-history-file.md](../nebula-ontology-history-file.md) replaces the `.nebula/ontology/` registry; nothing here depends on that registry, only on the `.nebula/` convention.
- **Absorbs one sibling's phase.** [on-hold/nebula-skills.md](../on-hold/nebula-skills.md) § *Phase 0* lands here (§ *Context* says what is taken and what stays parked); that file stays on hold for the rest and gets its dated edit in Phase 7.
- **Changes what these files say, each edited in the phase named.** [docs/vision/auth.md](../../docs/vision/auth.md) § *Why upward exists* and [docs/vision/_ai-security.md](../../docs/vision/_ai-security.md) § *Level 2*'s "Guidance as Resources" bullet (Phase 4); `.claude/rules/security.md`'s line that dominion guards must not be weakened, which gains the test-bypass-versus-design-floor distinction (Phase 1); `.claude/rules/ui-theming.md`'s and [ADR-020](../../docs/adr/020-one-theme-per-surface.md)'s pointers at the prompt constant (Phase 4); `CLAUDE.md`'s rules index, which gains `studio-guidance.md` (Phase 7); [nebula-pre-alpha.md](../nebula-pre-alpha.md) § *What remains*, where ① is marked landed (Phase 7).
- **Backlog rows.** In [backlog.md](../backlog.md): the wizard-style authoring row gains `define-ontology` as its first rung; the URL-synced view-state row's premise — ADR-017 reaches generated apps through "one sentence in the codegen prompt" — repoints to `.platform/AGENTS.md`; one new row captures the partial build, and one records parser-validator feedback — the tool-args facet needs a mode that refuses excess keys (all Phase 7).
- **Scenarios extended, and one added.** `four-party-chat` (Phases 1, 6), `first-app-built` (Phases 1, 4, 5, 6), `build-box` (Phase 3), `studio-codegen-rest` (Phase 5), and the new `studio-guidance-loop` (Phase 6). Un-skip obligations: none — `chat-trigger.test.ts`'s T5 is retired for a successor in Phase 5, not skipped.

## Context and current state

**Built already, with what becomes of each part:**

- **`STUDIO_LOOP_SYSTEM_PROMPT`** ([galaxy.ts](../apps/nebula/src/galaxy.ts)) — three kinds of content: identity ("You are Studio…"), the tool contract (`write_file` is a pure save, `build` reports per step, findings are advisory, `mark_complete`), and the rules — Vue 3 with `<script setup>`, styling through Tailwind and daisyUI, theming's warn-and-proceed, the URL-as-view-state rule ([ADR-017](../../docs/adr/017-the-url-is-the-view-state.md), added 2026-09-04), and the import allow-list. **Split:** the rules become the platform layer's first file; the tool contract stays in code beside the tool definitions it describes (§ *Design intent*).
- **`assembleCodegenPrompt({ systemBundles })`** ([codegen-loop.ts](../apps/nebula/src/codegen-loop.ts)) — the system layer is already an ordered array of bundles joined by blank lines, kept that shape for exactly this insertion (the archived codegen loop's D7); one bundle is passed today, then the pinned ontology block. **Adapted:** the tree's layers are the bundles, and the ontology leaves the system block for the dynamic tail (§ *Design intent*). Its test in `codegen-loop.test.ts` asserts bundle presence, which carries over, and the ontology's place in the system block, which inverts when Phase 4 moves it — a red that says the test was right about the old shape, not that the code is wrong.
- **What a turn is assembled from** — exactly four inputs: the system bundles, the ontology, the current `src/App.vue`, and the one user request. No transcript reaches the model; the only link between turns is the stored Message's `replyTo`, which it never sees. The chat's ordered read already exists — the data plane's parent-child query `Message where chat == DEFAULT_CHAT_ID`, ordered by `validFrom`, the same one the UI subscribes to, local to the Galaxy DO. **Extended:** that read feeds a history bundle (§ *Design intent*).
- **The plain-answer fork** (`#answerTurn`) — the discriminator's non-codegen path: an inline system string, the one message, no tools, no source, no history. **Deleted:** every turn runs one assembly with every layer and the full tool set, and the model decides whether to act (§ *Design intent*). **The discriminator** (`discriminate`) — today its `codegen` verdict forks the prompt and fires the container warm, failing open to codegen; `respond` is hardwired yes. **Adapted:** the verdict becomes a warm hint and decides nothing about the prompt; un-hardwiring `respond` stays the fast-follow's Item 6.
- **The tool surface** — `write_file`, `build`, `mark_complete`, guarded by `assertSafeRelPath` against `..` traversal and shape-checked by a typia validator before dispatch. There is no read tool, so the harness pre-injects `src/App.vue` and `src/ontology.d.ts` at turn start (`runCodegenTurn`) and the model sees nothing else. **Extended:** a fourth tool reads, which is what carries the docs and, later, skill bodies (§ *Design intent*). The always-on layers stay harness-injected.
- **The Workspace repo** — seeded from `container/app/` through `SCAFFOLD_FILES` (`gen-scaffold.mjs` walks every file there; the drift test reds when the embed and the directory disagree) and committed once as `scaffold`. **Carried over, and it is the seed mechanism:** an `AGENTS.md` added to `container/app/` rides into every new Galaxy with no new code. Galaxies created before the change keep their old tree, which the wipe makes moot.
- **`writeSource`** — the one write path (`ws.fs.writeFile`, `git.add`, a commit titled `edit <path>` authored `Nebula`, the Workspace's `defaultGitIdentity`), reached by the loop's `write_file`. **Adapted:** a guidance edit is an ordinary source write under the poster's claims, and the entry's guard changes to `requireChatWrite` so a direct call and a turn agree (§ *Who writes each layer*).
- **`appendWorkspaceOntology`** — the dev Apply with its wipe bit, `@mesh(requireDominionHere)`. No Studio or client path calls it today; its callers are tests and the harness. **Adapted:** the guard changes to `requireChatWrite`; the wipe bit keeps dominion, decided in the body (§ *The door is the guard*); and the poster-confirmed Apply step is where the product first gains a caller.
- **`warmPreview`** — the Studio's initial-load refresh cue, `@mesh(requireDominionHere)`; immediate by its own JSDoc since the collapse, and the Studio sets the iframe source before it fires. **Deleted:** it does nothing the client has not already done, and today it is refused silently on every collaborator's load (§ *Who writes each layer*).
- **`.nebula/ontology/<version>.json`** — the untracked, machine-owned directory the ontology registry lives in. **Left alone:** guidance is tracked and hand-shaped, so it lives at the tree root like any source file; `.nebula/` stays the home for platform-written rows only.
- **The turn's actor** — a committed human Message triggers a turn that runs under the poster's own `callContext`, with Nebula stamped as the actor on the reply (`#onChatCommitted`; the `four-party-chat` scenario asserts the byline). **Carried over, and it answers who edits the Galaxy layer** (§ *Design intent*).
- **The agent reply** — on a `complete` stop, `#codegenTurn` hardwires the reply to "Updated the preview." or "Done — no changes.", and the model's own text lands in `thought`, a collapsed panel. **Adapted:** the model's final text becomes the durable Message's `content` on every stop, with the fixed strings as the fallback when it is empty — the retro's one-sentence acknowledgement and the history bundle's "agent's reply" both read it.
- **`.claude/rules/ui-theming.md`** names the constant as the second synced home of the color rule. **Adapted:** its pointer follows the rule into the platform file.
- **[on-hold/nebula-skills.md](../on-hold/nebula-skills.md)** — **Carried over:** the three tiers with progressive disclosure, and integration point 1's storage answer. **Taken:** its Phase 0 — the `SKILL.md` format, a catalog in the always-on file, model-driven activation through the read tool, three seed skills; sub-agents, executable steps, versioning and eval gating stay parked, and its own un-park trigger still names a registry-DO tree this file's Decisions reject. **Changed:** the Galaxy trust grade — that file treats a user-developer's files as data never instructions behind a Galaxy-tier product-alignment verifier; this task injects the Galaxy `AGENTS.md` as instructions under the poster's own authority (§ *The layers add*), and the verifier's tier is decided in § *Decisions*. It owes a dated edit for all three, and for two more this task settles: its open question on discovery — the model activates from a catalog through the read tool — and its integration point 1's `rules/*.md`, which pass 1 does not ship.
- **[docs/vision/auth.md](../../docs/vision/auth.md) § *Why upward exists*** (accepted) — three levels with three owners, reading is free, editing takes dominion over the owning scope; its *Today's code differs* block says nothing stores, reads, or writes guidance yet. **Half-closed by this task, and it owes an edit:** the Galaxy layer lands at the chat floor (§ *Who writes each layer*), so the section gains that path beside its direct-edit rule; the Universe layer, its consumer of upward passage, waits.
- **`website/docs/nebula/*.md`** — roughly 200 KB across eight pages, none of it reaching the model today. It is **already the LLM-facing set**: `coding-your-ui.md` declares in its own opening that it is the reference Studio's hosted LLM consults when generating UI code, and the `nebula-docs-audience-is-llm` memory records the same intent. **Shipped as-is, behind the read tool** — there is no second audience to fork it for, and no rewrite in this task (§ *Design intent*).
- **The model lane and its caching** — `runModel` picks the Workers AI REST lane when `WORKERS_AI_TOKEN` is set and the `env.AI` binding otherwise, with AI Gateway as a header on the REST lane. Workers AI does **prefix caching by default**, and the `x-session-affinity` header that maximizes it is **not sent today**. **Adapted:** the header lands with this task (§ *Design intent*).

**Missing:**

1. The platform layer as files in the platform repo, embedded at deploy the way `scaffold-seed.ts` embeds `container/app/`, and assembled into the system layer every turn.
2. The Galaxy seed in `container/app/` — `AGENTS.md`, a one-line `CLAUDE.md`, and `docs/vision.md`, `docs/personas.md`, `docs/permissions.md` with headings — and the per-turn read of `/workspace/AGENTS.md` into the system layer on every turn.
3. The platform layer telling the model that the tree exists, what each layer is for, in what order they apply, to update the Galaxy layer when a convention is stated or a lesson learned, and when to report a doc that misled it (the placement table's Reporting row).
4. A **read tool**, path-guarded, reaching both the Workspace and the platform docs.
5. The **always-on resource core** in the platform `AGENTS.md` — a few hundred words the docs cannot replace, because a model that does not know resources exist never reads about them.
6. The **docs embedded** in the Worker bundle behind that tool, shipped as-is.
7. Three **seed platform skills** — define an ontology with the user-developer; wire a view to a resource; define the cast and decide each relationship's shape — each naming the doc to read and when, with the catalog generated from their frontmatter.
8. A `/live` scenario in which a data-bound request produces resource-backed code, and one in which a line in a Galaxy `AGENTS.md` changes what the model produces.
9. **The chat history in the prompt** — the human's words, the agent's reply, and each turn's manifest, read by the chat's own ordered query and placed as an append-only bundle.
10. **An `edit_file` tool** — an anchor and a replacement, refusing an absent or ambiguous anchor — beside `write_file`, which stays for new files and deliberate rewrites.
11. **The prompt ordered stable-first** for the cache, with `x-session-affinity` set to `{u}.{g}:main`.
12. **One assembly for every turn**, the answer fork deleted, with the container warm fired by the first `write_file` and the discriminator's verdict demoted to a hint that fires it earlier.
13. **The chat floor on every source entry** — the guards on `readSource`, `writeSource`, `buildNow` and `appendWorkspaceOntology` change to `requireChatWrite`, the check a post already passes, so a collaborator's direct call and their turn agree; `warmPreview` is deleted; the tool table is tested for nothing above the floor.

The section that follows is the contract the phases conform to; after it come what is settled, the phases in executable order, and what is left out by name.

## Design intent, constraints, and future state

Four parts follow: the tree and the files in it; where each layer is stored and which tier it rides; what one turn assembles and in what order; who writes each layer and how the loop pushes itself.

**The tree, assembled.** What the model sees each round, in an order chosen so that what never changes comes first and what changes every turn comes last:

```
system layer, in order — stable first, so the cached prefix survives across rounds and turns
  1  TOOL_CONTRACT (the `tools` field rides beside it)   byte-identical until a deploy
  2  platform   .platform/AGENTS.md        ours — embedded at deploy
  3  [universe  .universe/AGENTS.md]       later — inserts here when built
  4  galaxy     AGENTS.md                  theirs — changes only when the model records something
  5  the chat history                      append-only: the human's words, the agent's reply, each turn's manifest
dynamic tail — changes most turns, so it comes last
  6  the ontology + current src/App.vue    src/ontology.d.ts leaves the system block (D7, re-derived for the cache)
  7  the request
```

**The Galaxy file, for real.** A user-developer's `AGENTS.md` a few turns into an app looks like this, and the model wrote every line of it:

```markdown
# Secret Santa — guidance for this app
- One group per event; everyone in a group sees the wishlists.
- Only the organizer sees who drew whom. Never show the draw to anyone else.
- Tone: warm and brief. No countdown timers, no gamification.
```

**The platform file, for real.** Six lines of the intended `.platform/AGENTS.md`, the file this task exists to produce; the rules that live in the prompt today move in beneath them:

```markdown
# Nebula — platform guidance, read every turn
- Resources are the only place user data lives. The ontology (src/ontology.d.ts) defines their
  shape, and `store` and `client` are how a component reaches them.
- Before any data-bound change, read .platform/docs/resources.md; for the store API in a
  component, .platform/docs/coding-your-ui.md.
- Skills (this list is generated from each skill's frontmatter):
  define-ontology — interview the user-developer and propose a shape: .platform/skills/define-ontology/SKILL.md
- Layers, in order: this file, then the app's AGENTS.md. The app's file adds; it never subtracts.
- The app declares itself in docs/vision.md, docs/personas.md and docs/permissions.md. Read them
  when they bear on the request; write them when the conversation settles something.
- When the user states a convention or corrects you, add the line to the app's AGENTS.md with
  edit_file, and say so in one sentence of your reply.
```

**The Workspace tree, laid out.** Two standard pieces and a handful of files we declare, at fixed paths the platform layer names:

```
/workspace
  AGENTS.md                 standard — always-on instructions for this app; the LLM writes it
  CLAUDE.md                 one line, `@AGENTS.md` — so the same tree reads in Claude Code
  .agents/skills/<name>/    standard — procedures, loaded on match (arrives with the skills work)
  docs/vision.md            ours — what the app is for; may link to sibling files
  docs/personas.md          ours — the cast, and how to re-establish it after a `.dev` wipe
  docs/permissions.md       ours — the permission model as decided, with a decisions table
  src/ontology.d.ts         ours — the data model; exists today
  .nebula/                  machine-owned rows; never guidance
  .platform/AGENTS.md       VIRTUAL — the platform layer, served from the bundle by the read tool
  .platform/docs/, skills/  VIRTUAL — the docs and seed skills, same mount
  .universe/AGENTS.md       VIRTUAL, later — the Universe layer, a Galaxy-held copy refreshed by continuation
```

Three of those paths are not in the repo: `.platform/` and `.universe/` are mounts, not files (§ *A read tool* says how each resolves), and the write path refuses both prefixes. Everything else is the git repo the user-developer owns. The app's own `AGENTS.md` sits at the repo root, because that is where every agent that reads the standard looks for it, and its skills sit under `.agents/skills/`, the cross-client convention; `.claude/`-style directories hold nothing here.

**Standard pieces carry instructions and procedures; our files carry declarations.** `AGENTS.md` is always-on prose and a skill is a reusable procedure, which is the Agent Skills spec's own test for what belongs in one. Every Nebula app must also hold a set of declarations about that one app — its vision, its cast, its permission decisions, its data model — and the spec has no place for those, which is freeing. Each lives at a fixed path under `docs/`; the platform layer names the path and says when to write it; the seed creates each file with headings and a one-line "write this when", so an empty section reads as a gap rather than a missing file. This is the shape this repo already runs: the rules point at `docs/adr/` and `docs/vision/`, and a skill reads them.

| Declared file | Holds | How the standard pieces reach it |
|---|---|---|
| `docs/vision.md` | what the app is for, who uses it, tone and values — the master of a set, linking to siblings: Jennifer's five-file Gigi Kaizen spec becomes this file plus the four it links to | the platform `AGENTS.md` names it and tells the retro where to write; Studio reads it today, and it is the intended source for the end-user assistant's tone in [nebula-pre-alpha-fast-follow.md](../nebula-pre-alpha-fast-follow.md) § *Item 2* — a second reader of one file, never a second store |
| `docs/personas.md` | the cast — § *Personas* | the third seed skill elicits it; ②'s executor re-establishes it after a `.dev` wipe |
| `docs/permissions.md` | which DAG shape each relationship got and why — a grant for limited sharing, a second parent for co-ownership — in prose plus a decisions table | the platform `AGENTS.md` points here; the vocabulary is [access-control.md](../../website/docs/nebula/access-control.md) § *When to use which* |
| `src/ontology.d.ts` | the data model | pinned into the prompt today, unchanged |

**A permission model is decided, never specified.** The DAG lets the same intent be a grant or an edge, and which is right turns on whether either party may leave — a judgment per relationship, which no file format captures without becoming the DAG's own schema. So `docs/permissions.md` records decisions the way this repo's Decisions tables do: one row per relationship, the alternative rejected and why. That row is what stops the model re-proposing a grant where an edge was chosen. The model comes out of the personas conversation, so the third seed skill runs it: elicit the cast, ask "co-owner or limited share" per relationship, record the row, write the files, and hand the cast to ②'s executor as its last step. One row, from the Secret Santa app: *the organizer and a participant's wishlist — a grant, participant `write` on their own list and organizer `read`; co-ownership rejected, because the organizer must never edit a wish.*

**Personas: the tree gives the cast a home and the skill that elicits it; ② builds what runs it.** `docs/personas.md` is a numbered prose procedure — *Step 1: make Sally the `.dev` Star's stand-in founder: admin on the root node and scopeAdmin at `.dev`. Step 2: create the node `lists`. Step 3: add `example` beneath it. Step 4: grant Gerry admin on `example`.* — keyed on persona NAME and edited in place, because subs are re-minted by every wipe; every persona is synthetic, on an RFC-reserved dead domain (`@example.com`, `*.test`, `*.invalid`), never `@lumenize.io`. The seed carries a short template naming the four verbs any org-tree shape composes from — mint, node, edge, grant — with that worked example, the spec's "template for output format" pattern. This task ships the file, its template, the third seed skill that fills it, and the property of its run record: machine-owned under `.nebula/`, untracked, and wipe-independent, with the path decided alongside the executor that writes it. How the steps run — a typed `provision(plan)` the model translates the prose into, applied as a poster-confirmed step outside the loop, its deterministic executor, the impersonate-or-login choice, and where the first call that creates the `.dev` Star should come from — is ②'s design, recorded as its inputs in [nebula-pre-alpha.md](../nebula-pre-alpha.md) § *② Personas*.

**Decisions tables, not ADRs.** A user-developer's durable decisions live as a `## Decisions` table inside the doc that owns the topic — `docs/permissions.md` first. The function is an ADR's, a decision beside its rejected alternative so nobody re-litigates it, without the numbered directory, the append-only discipline, or the status lifecycle — none of which a domain expert asked for, and all of which could only be advisory here. A numbered, statused record earns its place at the Universe tier, where governance wants an audit shape, and can arrive with that layer.

**Storage is files in git, in the repo that owns the layer.**
- **Platform layer:** `apps/nebula/platform/` (lean) — `AGENTS.md` and `skills/` — plus the docs read unchanged from `website/docs/nebula/`; one committed generator embeds both into the Worker bundle, with a drift test — the `scaffold-seed.ts` pattern, so nothing runs at dev time and the deploy is atomic by construction.
- **Local loop:** edit `apps/nebula/platform/`, run the generator, and `wrangler dev` picks it up — no deploy. When the offline replay bench un-parks (master plan § *Data-bound generation*) it imports this same embed.
- **Galaxy layer:** `AGENTS.md` at the Workspace root, tracked and committed like `src/App.vue`.
- **Measured cost (2026-09-03):** the docs add about 66 KiB gzipped (201 KiB raw) to a 519 KiB gzipped bundle against a 10 MB paid-plan limit, and `wrangler check startup` on the real bundle with the docs injected showed active startup CPU unchanged within run-to-run variance (an in-session measurement whose profiles were not kept, so the bundle size is the durable number) — a string constant does no work at import (`workflow.md` § *Startup cost is the criterion*).
- **History:** git holds the tree's; the agent Message's `codegen` record holds each turn's `sourceCommit`, the tree it read, and `appliedPaths`, the files it wrote.

**Each layer has one authoritative copy.** The platform layer lives in code because we are its only writer; a deploy ships it and nothing else holds a copy — [ADR-010](../../docs/adr/010-random-opaque-keys.md)'s replication rule, where every second copy owes a convergence mechanism nobody wants to build. What expires this is a writer who is not us: a support engineer, or a per-customer override that must change without a deploy. That is a trigger to re-derive (§ *Future state* carries the shape a switch would take).

**The harness walks the tree, every turn.** Every turn reads the Galaxy file at turn start and places it after the platform layer. Reading per turn is what makes an edit take effect on the next message with nothing to restart; the platform layer is an import, so the read costs one VFS read per turn.

**Two tiers, and the boundary between them is what a model can be trusted to ask for.** The always-on layers are harness-injected and every turn pays for them, so only what changes behaviour on a typical turn belongs there. Everything else is read on request, which is the Agent Skills progressive-disclosure model: a catalog of names and descriptions costs about a hundred tokens each, a body loads on activation, bundled references load when the instructions call for them.

**On-demand loading has one failure mode: the model does not read what it does not know exists.** So the always-on core carries the three facts no reference can supply — resources are the only place user data lives; the ontology defines their shape and `store` and `client` reach them; any data-bound change means reading the resource reference first — and it names the file and the trigger, never a directory, which is the Agent Skills best-practices rule for on-demand loading.

**A read tool, and the four things it unlocks.** It is one addition to a three-tool surface and it carries this task's payload:

- **The docs on demand**, which is the whole delivery mechanism for goal 2.
- **Skill bodies**, the standard's own activation path for an agent whose model reads files.
- **Multi-file apps.** The harness pre-injects `src/App.vue` and the ontology and nothing else, so the first app to grow a `components/` directory is edited blind. That defect is latent today and this closes it.
- **The Galaxy's own `docs/`**, so `vision.md` and `permissions.md` are read when relevant rather than riding every turn.

Its shape is `read_file({ path })`, path-grained: it returns a whole file, so platform links point by path and anchors buy nothing this round (a `path#anchor` section read is § *Future state*'s answer to window pressure). It reuses `write_file`'s guard — `assertSafeRelPath`, so a model-chosen path cannot traverse — and the write side gains one derived rule, enforced in the source entries themselves so a direct call and a tool agree: **only the user-owned tree is writable; every path whose first segment starts with `.` is reserved, except `.agents/`** — `.platform/` and `.universe/` are mounts, `.nebula/` is machine-owned, `.git/` is git's, and `.env` is where a secret would go — which makes a user app shadowing a platform doc, or a `write_file` planting a registry row, impossible at the entry rather than merely unlikely. This round `.platform/*` resolves to the embedded map and anything else to the Workspace; `read_file({ path: '.universe/AGENTS.md' })` returns `error: ".universe/ is reserved — no Universe layer exists yet"` until that layer lands, when it resolves to the Galaxy-held copy of that layer (§ *Future state*).

**Skills carry the conversation; docs carry the reference.** The spec's test is that a skill teaches how to approach a class of problems rather than what to produce for one instance. *"Interview the user-developer about what belongs on a wishlist item and propose a shape"* is a procedure and is a skill; `store.Todo.create(...)` is reference and is a doc. So the three seed skills are conversational — `define-ontology`, `wire-a-view`, `define-the-cast` (personas and permissions) — and each names the doc to read and the trigger for reading it. A skill is a directory with a `SKILL.md` whose head is three lines:

```markdown
---
name: define-ontology
description: Interview the user-developer about what their app stores and propose a typed shape. Use when a request names data the app does not yet hold.
---
```

The always-on catalog — one line per skill: name, description, location — is **generated from that frontmatter** by the same generator that embeds the tree, so a stale description cannot ship; the model activates a skill by reading its location. Backlog's wizard-style authoring row gains a pointer at `define-ontology` as its first rung (§ *Relationships*).

**Where each kind of guidance goes — one home per fact, and every other place points.** The tool contract is stated three times today, and two of the copies disagree on what to do after a timeout, so the placement rule is the mechanism that stops the next drift. The table is design intent here and moves into a path-scoped rule, `.claude/rules/studio-guidance.md`, as a phase deliverable — a rule rather than an ADR because it is a convention that must be in front of whoever edits these files at the moment they edit them, which is what `paths:` does and an ADR's always-loaded one-liner cannot. A procedure for changing guidance waits until the eval exists and "did this change help" is answerable; until then the steps are the generator, the drift test and the `/live` criteria, which `/build-task` already runs.

| Kind | Example | Home |
|---|---|---|
| Identity | "You are Studio, an assistant that builds a small web app…" | `TOOL_CONTRACT`, its first line |
| Tool mechanics | what `build` runs and reports | the tool's `description`, sent as `tools` every round |
| State-dependent result reading | "timed out — retrying the same code will time out again" | the build report itself, on the step that failed |
| Cross-turn harness policy | findings are advisory and you judge; tools not prose; every file, then one build | `TOOL_CONTRACT`, a string beside `CODEGEN_TOOLS`, the first system bundle |
| Reporting | a doc misled you or an API misbehaved: call `report_finding` | `TOOL_CONTRACT` — the line lands with ③'s tool |
| Platform conventions and the tree | resources are the only data home; styling; layer order; declared paths; the catalog | `.platform/AGENTS.md` |
| Procedures | interview the user-developer; wire a view to a resource | `.platform/skills/` |
| Reference | the resources API | `.platform/docs/` |
| App conventions | "only the organizer sees the draw" | the Workspace's `AGENTS.md` |
| App declarations | vision, personas, permissions | the Workspace's `docs/` |
| Org practices | later | `.universe/AGENTS.md` |

The self-correction message says "fix and call build again" and nothing else. The timeout line lives on the build report's failed step (row 2 of the table), in the description's wording: "simplify" is not a move the model can make without a build it can run in parts, and that build is a product gap — a backlog row Phase 7 adds, not prompt text.

**`publish` is renamed `preview` throughout the build tool** — `BuildArgs.preview`, the report step `preview: { refreshed, why }`, `decidePreview`, the tool description, the feedback message — because what the flag decides is whether the `.dev` preview reloads, and the product's *publish* is the shipped-tag `dist` served to tenant Stars, which is not built. The word is tool-internal today, so no user-facing copy moves; the tool-args bundle id bumps, as its comment already requires on any change.

**Cost is small, and the shape is the cheap one.** The system layer is byte-identical across every round of a turn, which is the ideal case for prefix caching, and Workers AI applies it by default. Measured from the vendor's own model pages, 2026-09-03:

| Model | Context | Input | Cached input |
|---|---|---|---|
| `@cf/moonshotai/kimi-k2.7-code` (today) | 262,144 | $0.95/M | $0.19/M |
| `@cf/zai-org/glm-5.3` | 1,048,576 | $1.40/M | $0.26/M |

Cached input is about five times cheaper on both, and the caching docs note an input must be large enough to cache at all — so today's roughly 500-token system layer is the case that caches worst. **Send `x-session-affinity` with this task**: it is one header on both lanes and it is what makes the per-turn hit rate real, keyed as the next passage says.

**The transcript is the session; the Galaxy file is the memory.** Coding agents send the whole conversation on every call, append-only, and rely on the prefix cache to make it affordable; when it nears the window they compact, replacing the history with a summary and re-injecting the stable instructions after it. Our turns are heavy — whole files and build reports — so the full transcript would outrun a 262k window in a long session. But the durable Message already stores each turn's manifest (`appliedPaths`, `rounds`, `stop`, the build findings, `sourceCommit`), and old file contents are noise once the current source is supplied fresh. So the history bundle is an exclusive projection: per message, its speaker — kind (agent or human) and a stable per-`sub` label with the display name where cheap, from `deriveParticipants` on the stamped `actingToken` — its `content`, and from `codegen` only `appliedPaths`, `rounds`, `stop`, `build.findings` and `sourceCommit` — never `toolCalls`, whose `write_file` args carry whole files, and never `thought`. The request bundle marks the current poster. A participant's message reaches the model on the same terms as a participant's `AGENTS.md` line: the most either can make a later poster's turn do is what that poster could do by hand (§ *Lower layers add*). The chat is four-party by construction, so a bundle of bare `content` could not tell the owner's convention from a coach's aside. A few hundred tokens a turn, twenty thousand after sixty. Real compaction waits for threads — a thread is a second `Chat` under the Galaxy; multi-chat is deferred with the management UI, per `DEFAULT_CHAT_ID`'s JSDoc — whose "pull the relevant context into a new thread" step is compaction by another name; a thread is then the session, and the Galaxy's `AGENTS.md` plus `docs/` is the memory across threads.

**Two rules keep the prefix stable.** The cache reuses the longest unchanged prefix, so the order in § *The tree, assembled* puts what never changes first and what changes every turn last; nothing non-deterministic rides the prefix — no timestamps, no reordered keys; and `x-session-affinity` is `{u}.{g}:main` — the Galaxy's `instanceName` plus a segment naming the conversation — because the header routes a request to the model instance holding the cached tensors ("prefix caching only works when a request routes to the same model instance"). How a thread is keyed when threads land is § *Future state*'s.

| Twenty thousand tokens of history, Kimi | Per round, uncached | Per round, cache hit |
|---|---|---|
| One round | $0.019 | $0.004 |
| Eight rounds | $0.15 | $0.03 |

Cost is not what to economize; the window is, which is why the compact representation matters more than the cache does.

**Full-file loss is answered by `edit_file`, not by a guard.** `write_file` saves complete contents, so a model adding one line to `AGENTS.md` re-emits the whole file and a dropped line leaves no signal. An `edit_file({ path, anchor, replacement })` tool — refusing an anchor that is absent or matches twice — can only change what it names, so the safe path becomes the cheap path, and the standing instruction picks it for existing files; the preference — edit an existing file, write a new one or a deliberate rewrite — lives in the two tools' descriptions, where mechanics live. Its failure is loud, an error the model sees and retries. The archived loop plan already names it as whole-file regeneration's successor, and it pays outside guidance too: a one-line change to a four-hundred-line `App.vue` emits a few lines instead of the file. Rejected alternatives are in § *Decisions*.

**One assembly, every turn; the model decides whether to act.** The loop already ends a turn that makes no tool call as `no-tool-calls` with the reply as the answer, which is exactly how a question is answered. So the answer fork is deleted, every turn gets every layer and the full tool set, and a question that turns out to need a one-line fix gets the fix, under the poster's authority and named in the reply. The property the fork asserted — a plain question starts zero containers — is dropped: a question may now cost one speculative start, and every warm still owes the turn's teardown — the `finally` that calls `#destroyBuildContainer()` around the codegen body, which every exit reaches (`no-tool-calls`, the round cap, a `mark_complete` without a build) — which the hint and the `write_file` warm both ride: at most one warm per turn, latched, and a teardown per warm. `chat-trigger.test.ts`'s T5 asserts the dropped property, zero warms on a plain question, and is retired for its successor.

The one job the classifier did that the model cannot is timing the container warm: its verdict fired before round one, hiding the roughly three-second cold start and mount behind ten to thirty seconds of generation. The structural replacement is the first `write_file` of round one, a certain predictor of a coming `build`, which hides only round two's short generation — a second or two of exposed latency. So the cheap call stays as a **warm hint only**, fired detached so it delays nothing: a wrong "yes" wastes one container start, a fraction of a cent; a wrong "no" costs the second or two, because the `write_file` warm fires regardless. A hint with a structural backstop is a different thing from a fork. Whether the hint earns its sub-second call is measurable, and Phase 6 measures it.

**Lower layers add to higher ones; the security floor is not in the prompt.** The platform layer states the order and that a lower layer adds to the layers above it. A token reaches what its claims allow, and nothing the model is told changes that ([docs/vision/auth.md](../../docs/vision/auth.md) § *When Nebula is the actor*). What bounds an injected line is a rule the tool table is tested against: **no tool whose floor exceeds the chat floor joins the loop's surface** — an operation above it is a poster-confirmed step outside the loop, the rule the loop's header already has for install and wipe (`compileAndInstallOntology`, `resetDevData`, `setOntology` fire after the loop, human-gated) and the shape ADR-015 gives destructive restraint. A collaborator's line in `AGENTS.md` is injected on every later poster's turn, so per-turn guarding alone would let it steer the owner's turn toward what only the owner may do; under the rule, the most any line can reach is what every chat participant can reach anyway.

**Who writes each layer.** We write the platform layer, by deploy. For the rest, one rule: **a guidance layer's write floor is the write floor of the thing it describes.** The Galaxy layer sits in the app's source tree, so it takes source's floor — DAG `write` at the chat node, the door that let the message land — for every path, client and agent alike: a collaborator who can change `src/App.vue` can change the `AGENTS.md` beside it. The Universe layer describes an organisation's practices, so *editing takes dominion* stays verbatim there. The LLM is an intermediary that is good, not perfect, at refusing a bad edit; every write is a commit and the chat is visible, so the risk is low and accepted (Larry, 2026-09-04).

**The door is the guard.** A turn runs under the poster's own `callContext` — the commit path's JSDoc states it — and it exists only because a Message landed, which required DAG `write` at the chat node. So every poster who can start a turn holds the same floor, and a per-tool check under the same claims would re-prove the door on every call. What must hold instead is that no loop tool reaches above that floor, and that is a property of the tool table, not of the model: the model can call only what the table lists, and our code implements each entry. The proof is a test on the table, in the shape of Star's `@mesh`-surface freeze.

The loop's internal `this.writeSource()` is then the ordinary call it looks like — once the entry's own guard agrees with the door. Today it does not: `readSource`, `writeSource`, `buildNow` and `appendWorkspaceOntology` carry `requireDominionHere`, so a collaborator's turn writes and builds while their direct call is refused, and their Apply is refused outright. Each guard changes to `requireChatWrite`:

```ts
/** The chat floor — the check a Message create runs at the door. */
export function requireChatWrite(instance: Galaxy) {
  instance.dagTree().requirePermission(CHAT_NODE_ID, 'write');
}
```

A Galaxy admin passes it through the confined scope-admin bypass inside that same check, so nothing changes for the owner. `warmPreview` is deleted rather than changed: its JSDoc says the answer is immediate since the collapse, the Studio sets the iframe source before it fires, and today it is refused silently on every collaborator's load; the build reply keeps its delivery path. `setGalaxyConfig` and `ensureChat` keep dominion — neither is a source operation. The Apply's wipe bit is the one destructive effect on that surface, and it is priced where it lands: the body checks dominion over the `.dev` Star before writing `wipeOnInstall`, because the Star's install path wipes on the row's say-so with no check of its own, and a caller with the chat floor and no bit can exist — a Galaxy invite co-mints `.dev` with the inviter's dominion, and a node inviter may hold none. That check also stamps the acting principal, which ADR-016 asks of every wipe. And a `.dev`-scoped session holds no chat floor on the Galaxy host by construction: each membership row mints its own `sub`, the invite's grant names the Galaxy one, and the confined bypass covers dominion over the host alone.

| Loop tool | Reaches | Floor |
|---|---|---|
| `read_file` | `readSource` — guard changes to `requireChatWrite` | chat: DAG `write` at the chat node |
| `write_file` | `writeSource` — guard changes to `requireChatWrite` | chat |
| `edit_file` | `readSource`, then `writeSource` | chat |
| `build` — compile for feedback, bundle, refresh the `.dev` preview | `#buildAndAnnounce`, the body behind `buildNow` — whose guard changes to `requireChatWrite` | chat |
| `mark_complete` | none; it ends the turn | chat |
| *not loop tools:* the ontology Apply (`appendWorkspaceOntology`, guard changes to `requireChatWrite`; its wipe bit is decided in the body at dominion over the `.dev` Star); `resetDevData` (dominion over `.dev`) | their own entries | poster-confirmed steps after the loop |

`auth.md` owes two sentences, both in § *Why upward exists*: "a Galaxy admin evolves that Galaxy's guidance" becomes the source-floor rule above, and its write-versus-propose sentence narrows to the Universe layer, where proposing still routes; its *Today's code differs* block shrinks to the Universe layer. Phase 4 makes the edit, once there is guidance to describe.

**The retro: the platform layer pushes the loop as a standing instruction.** When a participant states a convention or corrects you — the history bundle says who — or you fix something non-obvious, read the app's `AGENTS.md`, add the line with `edit_file` where it fits — a convention near the top, a lesson where it belongs, no fixed section — and say so in one sentence of your reply. The transcript in the prompt is what lets the model see a correction in the turn it arrives, and the sentence in the reply — the reply is the model's own text, § *Context* — is what lets the user-developer see the file evolve, since Studio shows no files (goal 3). The signal only acceptance can provide — undo, abandon, feedback — feeds the platform's own scaffold evolution and is recorded with ③.

**Constraints.**
- [ADR-010](../../docs/adr/010-random-opaque-keys.md) — one authoritative copy per layer (above).
- [ADR-016](../../docs/adr/016-record-the-acting-principal.md) — a guidance edit is a source write. A turn's writes are recorded by the agent Message — `meta.actingToken` through the shared projection, plus `codegen.appliedPaths` naming each write — and a direct `writeSource`, which Phase 1 opens to collaborators, is recorded by the entry itself: `projectActingToken(claims)` on its log line when the call has a client origin. The Apply's wipe is recorded the same way (§ *The door is the guard*).
- [ADR-017](../../docs/adr/017-the-url-is-the-view-state.md) binds the codegen scaffold; its URL rule moves with the other rules into `.platform/AGENTS.md`, and the backlog row that cites "one sentence in the codegen prompt" repoints there (§ *Relationships*).
- `workflow.md` § *No build during development* — the platform layer is embedded by a committed generator, never a dev-loop build.
- `critical.md` — the tree is prompt content, so no secret ever lands in it.
- `ui-theming.md` — the color rule moves with the prompt and stays synced on both surfaces.
- The `nebula-docs-audience-is-llm` memory — the files are written for the model: reference-dense, one canonical form, no hand-holding.
- `calibration.md` § 8 — `AGENTS.md` and the Agent Skills layout are the direction, and we bet on them.

**Future state.**
- ⚠️ Design consideration: the assembly takes an ordered list of layers, never two named slots, so the Universe layer is an insertion. It needs a Workspace on the Universe DO (none today — `universe.ts` holds config only) and arrives by continuation, never an await inside the turn (ADR-003): the Galaxy holds a copy refreshed out of turn by a fire-back — the Star's `#pullOntology` and `onOntologyPulled` pair is the conforming precedent — so `assembleCodegenPrompt`'s list stays synchronous strings; the guidance hierarchy is the named consumer of `requirePassage`'s upward branch. The empty read path is not built this round: an unpopulated layer is a fixture built in the safe shape, green whether or not it composes, and its trust grade — semi-trusted, verifier-checked — is not the Galaxy's.
- ⚠️ Design consideration: the platform seed skills ship here and activate through the read tool; Galaxy-authored skills under `.agents/skills/` wait for the skills work, and nothing here forecloses them.
- ⚠️ Design consideration: `Message.codegen.scaffold` is absent until the scaffold store exists; once guidance is files, a scaffold version is the platform deploy plus the Galaxy tree commit, and `sourceCommit` already holds the second half.
- ⚠️ Design consideration: personas share the Workspace write path with the ontology history but not its append-only chokepoint — guidance files are edited, history is not.
- ⚠️ Design consideration: the whole Galaxy file rides every turn; a size cap belongs to the skills tier, not to a guard added now.
- ⚠️ Design consideration: on-demand loading means **no model swap is required**, which is a property worth keeping. A 1M-context generator would let the docs ride the prefix instead, and the eval's judge-independence invariant no longer stands in the way — the judge may now run off Cloudflare on any provider (`nebula-studio-self-improvement.md` § *Decisions*, relaxed 2026-09-03), so a GLM generator only requires a non-GLM judge, which is a purchase rather than a constraint.
- ⚠️ Design consideration: reads are exempt from the identical-call loop detector — a read is idempotent, and the retro's read → edit → read would otherwise end the turn `loop-detected`.
- ⚠️ Design consideration: a `search_docs`-style retrieval tool over the **user-developer's own codebase** comes later — it needs chunking, embeddings and Vectorize, and adds retrieval quality as a failure surface; paths listed in a catalog are enough while the tree is this small.
- ⚠️ Design consideration: commits are authored `Nebula` today; author = poster and committer = Nebula would mirror the Message's `{ sub, act }`. The join through the Message answers "who loosened this rule and when" already; revisit when a remote makes the repo readable outside Nebula.
- ⚠️ Design consideration: a git remote is read-only until a push lands through the same floor with the same record — a commit with no Message gets no `actingToken` and no `appliedPaths`, so a push-shaped remote would be a second write path outside § *Constraints*' claim.
- ⚠️ Design consideration: when threads land, a thread's affinity key leans `{u}.{g}:<threadId>` — it re-pays the shared platform-and-Galaxy prefix once and gains a cache that is not competing with a years-long main chat (Larry, 2026-09-04); decided then.
- ⚠️ Design consideration: the `ASSETS` binding the Studio UI ships through could carry the docs instead of the bundle — a subrequest per read, inside the UI's build output, publicly fetchable — revisited only if the tree reaches megabytes.
- ⚠️ Design consideration: if a writer who is not us ever needs to change the platform layer without a deploy, the switch is a platform-level DO holding the same repo shape on `@cloudflare/computer` — a switch, never a mirror.
- ⚠️ Design consideration: a `path#anchor` section read is the answer to window pressure when whole-page reads grow costly (`api-reference.md` is ~76 KB); platform links gain anchors only then.

## Decisions

| Decision | Rejected alternative — why |
|---|---|
| Guidance is files in git, in the repo that owns the layer | *Guidance as Resources* (`_ai-security.md` § *Level 2*, rough draft) — rejected 2026-09-02, "just files": a Resources home would split guidance from the app tree it describes, and it buys nothing that git and the Message do not already record (§ *Design intent*). The doc owes an edit. |
| The platform layer stays in code, shipped by deploy | A platform registry DO read over mesh (the answer `nebula-skills.md` integration point 1 used to give) — rejected 2026-09-02: switch, do not mirror; two authoritative copies violate ADR-010's replication rule, and we are the only writer. |
| `AGENTS.md` | `NEBULA.md` — decided 2026-09-02: one cross-vendor name, because a user-developer's tree is read by our model and by whatever else they point at it. |
| The Universe layer is skipped this round | Building all three now — minimal is enough; the Universe DO has no Workspace, and the Universe tier is the enterprise-governance surface (`nebula-skills.md` § *Future extension*), gated on a proven self-serve wedge. |
| The harness injects the always-on layers every turn | The model fetching them itself through the read tool — a layer the model must remember to ask for is a layer it can forget, so the read tool serves only the on-demand tiers; `nebula-skills.md` integration point 1 says *read per turn during prompt assembly*, and model-driven activation stays the skills tier's. |
| `.agents/skills/` is the Galaxy skills directory | `.claude/skills/` — the spec's client-implementation guide names `.agents/skills/` as the cross-client convention; the same reason as `AGENTS.md`. |
| The seed writes a one-line `CLAUDE.md` containing `@AGENTS.md` | Seeding `AGENTS.md` alone — Claude Code reads `CLAUDE.md` and not `AGENTS.md`, so a user-developer opening the Workspace there would get no guidance. The import syntax is Claude-only, so the platform file itself points with plain paths. |
| Declared files live under `docs/` | Root-level files, which clutter the root beside the standard files and the vite project; or `.nebula/`, which is machine-owned. `docs/` mirrors this repo's convention and reads naturally to a human. |
| The seed creates `AGENTS.md` and the `docs/` files with headings and a "write this when" line | Copying the platform rules into the Galaxy seed — ADR-010's one copy; the platform layer is never copied down. |
| Personas and permissions are markdown | A file-format spec for the permission model — the DAG's flexibility would make the spec its own schema (§ *Design intent*). |
| Decisions tables inside the owning doc; no ADR convention for user-developers | `docs/adr/` in the Workspace — the numbered directory, append-only rule and status lifecycle are conventions a domain expert never asked for; the decision-plus-rejected-alternative function survives in the table. |
| A read tool joins the surface | Keeping the three-tool surface and pre-injecting more — the harness cannot know at turn start which file the model will need, and pre-injection is what makes a multi-file app invisible to it (§ *Design intent*). |
| Resources knowledge is pulled into this task, not left to the post-wipe data-bound generation loop (master plan § *Data-bound generation*) | Shipping the tree with styling rules only — a tree that carries nothing load-bearing never tests whether it carries weight, and every hand-driven session until then re-finds the same gap (goal 2). |
| The docs ship as-is from `website/docs/nebula/` | Writing an LLM-facing edition — they already are one, so a second set is a fork to maintain rather than a gap to fill; tailoring waits for eval data. Their outbound links — to other site sections, a validator page, GitHub — may dangle inside the mount; a later mechanical embed-time transform of those links would not be a rewrite. |
| The docs sit behind the read tool, not in the always-on prefix | Riding the whole set in the system layer — affordable but unnecessary, and it would foreclose nothing that on-demand does not already give (§ *Design intent*). |
| A short always-on resource core rides every turn anyway | Pure on-demand — § *Design intent* carries why, and the wishlist is the evidence. |
| Skills carry the user-developer conversation; docs carry the API reference | One combined tier — the spec's own test splits them, and a procedure and a reference have different triggers. |
| Pass 1 is one platform `AGENTS.md` plus the seed skills; `rules/` waits | Adding `rules/*.md` now — nothing in the platform layer is conditional yet, skills already cover conditional loading, and the read tool gives them an activation path; the file is sectioned so a later split is mechanical. |
| One `read_file` tool; platform docs at the reserved `.platform/` prefix, refused by the write path | Two tools that cannot collide by construction (`read_file` + `read_doc` with an enum of names) — collision-proof and needs no guard, but a skill's `references/` and an app's own files are read the same way where this is heading, and the enum's benefit comes free from listing the paths in the always-on catalog. A retrieval tool (`search_docs`) is a later option, not a pass-1 one. Decided 2026-09-03. |
| The platform tree ships inside the Worker bundle | Static assets via the existing `ASSETS` binding (a subrequest per read, docs inside the Studio UI's dist, publicly fetchable) or a platform DO with a file system (a second authoritative copy, a mesh hop, and a push story nobody wants once deploys are the update path). The bundle costs 66 KiB gzipped against a 10 MB limit (§ *Design intent*). |
| Tool mechanics live in the tool descriptions; harness policy in a `TOOL_CONTRACT` bundle beside them; the prompt paragraph's copy is deleted | Everything in `.platform/AGENTS.md` — deploy-sync is not the issue (that file ships by deploy too); locality is: the constant and `codegen-loop.ts` changed in the same commits repeatedly and three copies still drifted inside one file, so the contract sits in the file the tool editor is already in. Decided 2026-09-03. |
| `publish` → `preview` in the build tool | Keeping `publish` — it would collide with the tenant-facing publish (fast-follow Item 5) the moment that lands, and the flag only ever decided a `.dev` reload. |
| The placement table becomes a path-scoped rule, not an ADR | An ADR — the bar is a repo-shaping commitment, and this is a convention that has to load when its files are touched; a skill alone — a procedure needs steps that matter, and those arrive with the eval. |
| The app's `AGENTS.md` at the Workspace root; skills under `.agents/skills/` | A subdirectory for the app's guidance — every reader of the standard looks at the root; `.claude/skills/` — vendor-specific, and the Workspace is the user-developer's repo. |
| `.universe/` is a virtual mount over the Universe DO, read-only from the Galaxy | Files in the Workspace — the layer is the Universe admins' to edit, so it lives where their authority does and arrives by continuation, never an await in the turn. |
| The chat history rides the prompt, compact by construction | The full round-by-round transcript — whole files and build reports would outrun the window in a long session, and old contents are noise once the current source is supplied; the stored manifest already carries what a later turn needs. |
| Stable-first prompt order; the ontology moves to the dynamic tail | D7's system-block pin — a structure choice borrowed from vibesdk, not a caching argument, so re-derived: anything that changes most turns invalidates every cached token after it. |
| `x-session-affinity` is `{u}.{g}:main` | Bare `{u}.{g}` — it leaves no room for a per-conversation segment when threads land (§ *Future state*). |
| `edit_file` joins the surface; `write_file` stays for new files and rewrites | `append_to_file` — fits only a file whose tail is one kind of thing; a shrink guard on `AGENTS.md` writes — a guard where a tool deletes the class. |
| No fixed section in the app's `AGENTS.md` | A prescribed `## Gotchas` — the spec prescribes it for a `SKILL.md`, not a project file; conventions and lessons are placed by kind, and `edit_file` needs no homogeneous tail. |
| The retro is a standing instruction; acceptance-triggered learning is ③'s | A harness reflection step after every turn — a call and seconds per turn for lessons that mostly do not exist, the repo's per-phase retro applied at the wrong grain. |
| The answer fork is deleted; one assembly and the full tool set on every turn, and the model decides whether to act | Keeping a classifier-chosen fork — the split was never the classifier's to make, and a fork without tools cannot record a stated convention or fix a one-line bug a question exposes; the loop's `no-tool-calls` end already answers a question. Decided 2026-09-04. |
| The discriminator's verdict is a warm hint; the first `write_file` is the structural warm | Dropping the call outright — one to two seconds on the first build of every codegen turn; keeping it as a fork — decides nothing it should. The respond-or-not cascade is [nebula-pre-alpha-fast-follow.md](../nebula-pre-alpha-fast-follow.md) § *Item 6*'s. |
| The persona executor and its record's path and shape are ②'s; this task ships `docs/personas.md`, its four-verb seed template, the skill that elicits it, and the record's property — machine-owned under `.nebula/`, wipe-independent | Designing `provision(plan)`, its executor and the stand-in founder's grants here — ②'s file is Larry's to write, and pass 2 would otherwise transcribe ②'s phases into ①; the design is preserved as ②'s inputs in the master plan. Decided 2026-09-04. |
| A guidance layer's write floor is the write floor of the thing it describes — the Galaxy layer takes source's floor, DAG `write` at the chat node, for client and agent alike; the Universe layer keeps dominion | Dominion-gated Galaxy guidance edits — a collaborator who can change `src/App.vue` could not change the `AGENTS.md` beside it, and a dominion check on the agent's path breaks every write-granted collaborator's turn. Risk accepted by Larry, 2026-09-04 (§ *Who writes each layer*); `auth.md` owes the two sentences named there. |
| The door is the guard: no per-tool check runs inside the loop; the source entries' guards change to `requireChatWrite` and the tool table is tested for nothing above the floor | Running each entry's own guard in-process under the turn's `callContext` — every poster who can start a turn already holds the floor of every tool, so the check re-proves the door on every call, and a refusal mid-turn is the half-completed turn a uniform floor exists to prevent; a guard nobody needs is a guard nobody maintains (calibration § *Deleting a problem beats hardening it*). Decided 2026-09-05. |
| `warmPreview` is deleted | Changing its guard — the method answers immediately by design since the collapse and the Studio sets the iframe source before it fires, so it does nothing the client has not done; a silent refusal on every collaborator's load is its only behaviour today. |
| The Galaxy `AGENTS.md` is injected as instructions under the poster's own authority, and no loop tool sits above the chat floor |  `nebula-skills.md`'s data-never-instructions treatment behind a Galaxy product-alignment verifier — a write-granted peer can already have `App.vue` rewritten, so the guidance file adds no reach, and a per-turn verifier is the disproportionate friction `_review-lens.md` flags; the verifier stays the Universe tier's, where one file steers many apps. Per-turn guarding alone — an injected line persists across posters, so it would steer the owner's turn toward what only the owner may do; the floor rule bounds it by construction. |
| The skills file's Phase 0 is absorbed here: the `SKILL.md` format, the always-on catalog, activation through the read tool, three seed skills — `define-ontology`, `wire-a-view`, `define-the-cast` | Leaving skills parked until the post-wipe data-bound generation loop — Larry's 2026-09-03 pull-forward; the mount and catalog cost the same with zero or three bodies, and the bodies are the procedure half of goals 2 and 3; the cast-and-permissions conversation had no owner on either side until it became the third; sub-agents, executable steps, versioning and eval gating stay parked. |
| The always-on skills catalog is generated from each `SKILL.md`'s frontmatter by the embedding generator | Hand-listing the catalog in `.platform/AGENTS.md` — a second copy of the frontmatter that a link check and a presence check both pass while its description is stale. |
| Only the user-owned tree is writable, and the rule binds in the source entries for client and agent alike; every leading-dot path except `.agents/` is reserved | Enumerating refused prefixes one at a time — `.nebula/` would have been writable, and a `write_file` there commits a registry row `#registryRows` parses; a rule in the tool layer only — a direct `writeSource` under the chat floor would bypass it and plant the same row. |

## Phases

Seven phases, in executable order. Each is one commit that leaves the suite green and the running system consistent, and each carries the pass-1 criteria it proves. A criterion marked **Exploratory** watches the model: it ships as a capable-of-failing check plus a findings note and never gates a phase (master plan § *Data-bound generation*); its deterministic twin is the gate. Every refusal a criterion asserts is matched on its message, never on a boolean or an error class, because a custom error loses `instanceof` across a hop (`mesh.md`). Every phase that changes the loop or the Galaxy's mesh surface ends with the registry sweep, `drive.ts all` — `--fast` only when no scenario the phase touches needs a container (`live.md`'s sweep rule).

1. **Every source entry sits at the chat floor, its path rules live in the entry, and a collaborator's direct call agrees with their turn.** `requireChatWrite` lands in `galaxy.ts` beside the entries it guards, as § *The door is the guard* shows it — reading the tree through `dagTree()` today, which ④ repoints to the `resources()` gate — and the guards on `readSource`, `writeSource`, `buildNow` and `appendWorkspaceOntology` change to it; `buildNow` gains the option the loop's `build` passes — `{ publish }` today, renamed in Phase 3 — so the loop reaches the entry rather than the private body. One exported table, `LOOP_TOOL_ENTRIES`, maps each tool to the entries it reaches — `write_file → writeSource`, `build → buildNow`, and in Phase 3 `read_file → readSource`, `edit_file → readSource, writeSource` — and `runCodegenTurn` builds the entry-reaching deps from it, so a tool cannot reach an entry the table does not name. [nebula-data-plane-owns-its-guards.md](../nebula-data-plane-owns-its-guards.md) § *Context*'s carried-over list is corrected here; `auth.md`'s edit waits for Phase 4, where guidance first exists.
   - **The path rules move into the entries, in this same commit.** Today `assertSafeRelPath` and the reserved-directory rule live only in the loop's tool dispatch; `writeSource` strips a leading slash and nothing else, and `wsPath` passes `..` through. That was moot under dominion and is an open door the moment the floor drops: a chat-`write` caller's direct `writeSource('.nebula/ontology/<hash>.json', …)` lands a row `#registryRows()` parses and every tenant Star installs, and `writeSource('../x')` leaves the tree. So one `assertModelPath(path, { write })` lands beside `requireChatWrite` — normalising a leading `./`, then refusing an absolute path, a `..` segment, and for a write any first segment that starts with `.` other than `.agents` — and runs first in `readSource` and `writeSource`. The tools inherit it; Phase 3's rule is then a property of the entry.
   - **The wipe bit keeps dominion, decided in the body.** `appendWorkspaceOntology` checks dominion over the `.dev` Star it wipes — `hasDominionOver(claims.access, '{u}.{g}.dev')` — before writing `wipeOnInstall`, and stamps `projectActingToken(claims)` on its log line, the ADR-016 record a wipe owes. § *The door is the guard* says why a caller with the chat floor and no bit exists.
   - **A direct write is recorded.** `writeSource` logs `projectActingToken(claims)` when the call has a client origin, so a collaborator's direct edit answers "who and when" as a turn's does (§ *Constraints*).
   - **The cue is deleted.** `warmPreview` goes on the Galaxy, on `NebulaClient`, in the Studio's connect path and in the baseline test app's echo stand-in; `deliverPreviewReady` stays for the build reply. `nebula-client-preview-ready.test.ts`, whose only driver was that stand-in, retargets its `onPreviewReady` assertion to the surviving path — the build reply `reload-version-contract.test.ts` T1 already drives — and `ui-smoke/smoke.test.ts`'s auto-refresh assertion, which rode the cue, goes with it.
   - **Standing guidance edited here.** `security.md`'s line that `requireDominionHere` paths MUST NOT be weakened outside test bindings gains the distinction it lacks — a test-mode bypass is forbidden; a guard whose floor is a recorded design decision is this file's — and names `requireChatWrite` as inheriting the DAG check's obligations. `galaxy.ts`'s header and its chat data-plane comment stop saying the codegen and source methods carry dominion.
   - **Success criteria, the floor (capable of failing):** (a) `galaxy-resource-surface.test.ts`: every entry `LOOP_TOOL_ENTRIES` names carries `requireChatWrite` (read through `getMeshGuard`); the dominion list is exactly `setGalaxyConfig` and `ensureChat`; neither `Galaxy.prototype` nor `NebulaClient.prototype` has `warmPreview`; the deps `runCodegenTurn` builds from the table have exactly the table's keys. (b) In-lane, the real-host block of `confine-dag-plane.test.ts` as the shape: an invited client's sub granted `write` at the chat node calls `writeSource` on the Galaxy and a commit lands; the same sub without the grant is refused with `write permission required on node`. (c) `four-party-chat` gains a limb. On Austen's Galaxy session — the sub whose turn already writes — direct `readSource('src/App.vue')` returns the file, `writeSource` returns an oid, `buildNow()` returns a report, and `setGalaxyConfig` is refused with exactly `Admin access required`. On Austen's `.dev` session — a different sub, holding no grant on the Galaxy's tree — `readSource` is refused with `write permission required on node`, and `setGalaxyConfig` with `Admin access required for {u}.{g} — your admin scope is {u}.{g}.dev`.
   - **Success criteria, the entries and the cue (capable of failing):** (d) `first-app-built` gains a limb after its build: `page.reload()`, then the preview's rendered document — the injected `<base href>` inside the frame, not the iframe `src` — with no preview-ready push observed on the fresh client; the one case the cue ever acted on. Needs Docker, so this phase's sweep runs without `--fast`. (e) A chat-`write` token's direct `writeSource('.nebula/ontology/x.json', …)`, `writeSource('../x', …)` and `writeSource('.env', …)` are refused with the reserved-path and traversal messages, distinct from the denial message and from a shape error; `git log` shows no new commit and `#registryRows()` is unchanged; `./src/App.vue` is accepted. (f) A chat-`write` caller with no bit — the owner grants a collaborator `admin` at the chat node, who invites a peer — has `{ wipe: false }` accepted and `{ wipe: true }` refused with a message naming the `.dev` Star; the owner's `{ wipe: true }` writes the row, and the debug sink carries the acting principal's projection. (g) A direct `writeSource` emits the projection on its debug line.
   - **Mutation note:** restore `requireDominionHere` on `writeSource` → (a), (b) and (c) red. Route `build` to `#buildAndAnnounce` outside the table → `vi.spyOn(Galaxy.prototype, 'buildNow')` sees no call during a scripted `build`, and the keys assertion reds. Drop the message match in (c) → a boundary refusal passes as a chat-floor refusal, the collapse `live.md` names. Move the path rule back to the tool layer → (e) red. Drop the in-body wipe check → (f)'s refused wipe writes the row. Strip the claims from either record → (f) or (g) red.

2. **The platform layer exists as files and ships in the bundle, with a catalog that cannot go stale.** `apps/nebula/platform/AGENTS.md` is authored in full: the lines of § *The platform file, for real*, the rules that live in `STUDIO_LOOP_SYSTEM_PROMPT` today (Vue 3 and `<script setup>`, styling, theming's warn-and-proceed, the URL rule, the import allow-list), the resource core, the layer order, the declared files and the retro, with one marker line where the generator renders the catalog. Three skills land under `platform/skills/<name>/SKILL.md` with the frontmatter of § *Skills carry the conversation*: `define-ontology` interviews and proposes a typed shape; `wire-a-view` binds a component to a resource through `store`; `define-the-cast` elicits the personas, asks co-owner or limited share per relationship, and writes `docs/personas.md` and `docs/permissions.md`. `scripts/gen-platform.mjs` walks `platform/` and `website/docs/nebula/*.md` into `src/platform-embed.ts`, exporting `PLATFORM_FILES` keyed by `.platform/…` path; it renders the catalog from frontmatter and exits non-zero on a `.platform/` link or bare path with no target, or a skill without a `description`.
   - **Drift is gated in two halves, as the scaffold's is.** `gen-platform.mjs --check` joins the `test` script for what needs the disk — byte drift, links and bare paths, frontmatter — and `test/platform-embed-drift.test.ts` asserts from `PLATFORM_FILES` alone, since a workerd test has no filesystem: every catalog line equals an embedded `SKILL.md`'s frontmatter, and every `.platform/` path in any embedded file is a key.
   - **An interim, two phases long:** the prompt constant stays live until Phase 4 deletes it, so until then the rules exist in both places and the file is read by nothing.
   - **Success criteria (capable of failing):** (a) `gen-platform.mjs --check` runs in the `test` script and passes; the vitest asserts from the embed that every `.platform/` path any embedded file names is a key of `PLATFORM_FILES`. Anchors are not checked, because the read tool is path-grained; the docs' own outbound links are out of scope (§ *Decisions*). (b) The embedded `AGENTS.md` lists all three skills' name, description and `.platform/skills/<name>/SKILL.md` location, and each line equals that skill's frontmatter. (c) `--check` reds when `platform/` or the docs change without a regenerate. (d) `wrangler deploy --dry-run` bundle size is recorded in the phase note beside the pass-1 measurement — reported, not gated.
   - **Mutation note:** rename `resources.md` → (a) red on the resource core's bare path. Delete `define-the-cast`'s `description` → the generator exits non-zero. Hand-edit one catalog line in the embed → the vitest reds; edit `platform/AGENTS.md` without regenerating → `--check` reds.

3. **The model can read what it is told about and change one line of a file, and the write path refuses every reserved path.** `read_file({ path })` resolves in one order: a `.platform/` path answers from `PLATFORM_FILES`, or an error naming the missing file; a `.universe/` path answers `error: ".universe/ is reserved — no Universe layer exists yet"`; anything else passes the entry's path rule and reads the Workspace through `readSource`. `edit_file({ path, anchor, replacement })` reads the file, counts the anchor, refuses zero or two-plus matches with an error and no write, and otherwise writes the replaced file through `writeSource`. A read or edit of an absent Workspace path returns a tool error naming the path and the turn continues. `read_file` is exempt from the identical-call detector as `build` already is. `publish` becomes `preview` throughout — `BuildArgs`, the report step, `decidePreview`, the description, the feedback message — with `TOOL_ARGS_BUNDLE_ID` bumped and `gen-validator-seeds.ts` regenerated, whose `--check` in the `test` script reds otherwise.
   - **Also lands here.** The recorded tool call for `read_file` is `{ path, bytes }`, never the content: the model already holds it as the in-turn result, and the stored `codegen.toolCalls` fans to every subscribed client. Tool descriptions carry the mechanics — `write_file` is for a new file or a deliberate rewrite, `edit_file` for a change, and `read_file` names `.platform/` and the catalog; the build report's failed container step carries the timeout line, and the feedback message says "fix and call build again" and nothing else; `LOOP_TOOL_ENTRIES` gains `read_file → readSource` and `edit_file → readSource, writeSource`. An unknown key on any tool call is refused by name, checked in `#validateToolArgs` against the tool's declared `parameters.properties`, because the typia facet is `createValidate` and ignores excess keys — the parser-validator feedback goes to the backlog in Phase 7. `build-box` asserts the four `publish` fields that become `preview.refreshed` and `preview.why`, so this phase's sweep runs without `--fast`.
   - **Success criteria, the tools (capable of failing):** (a) `codegen-loop.test.ts`: `write_file` and `edit_file` under `.platform/`, `.universe/`, `.nebula/` and `.git/`, and to `.env` and `.gitignore`, are rejected before any write; `.agents/skills/x/SKILL.md` and `./src/App.vue` are accepted. (b) `read_file` of `.platform/docs/resources.md` returns the embedded page byte-for-byte; `read_file` of `.universe/AGENTS.md` returns the reserved-prefix error; a Workspace path returns the file; an absent Workspace path returns a tool error and the turn's reply still lands. (c) `edit_file` with an anchor matching twice, or not at all, returns an error and the file is unchanged; a unique anchor changes only the matched span, every other byte identical; an absent file returns a tool error. (d) Two consecutive identical `read_file` calls do not end the turn `loop-detected`.
   - **Success criteria, the rename and the record (capable of failing):** (e) No `publish` survives in the build tool path; on a build with findings, `build({ preview: true })` refreshes the `.dev` preview and `build({ publish: true })` does not, because the key is dead; `build({ preview: 'yes' })` is refused as a type error; `build({ publish: true })` is refused by name as an unknown key. (f) After a turn whose only tool call reads `.platform/docs/api-reference.md`, the serialized `codegen` value is under a kilobyte.
   - **Mutation note:** drop the entry's path rule → (a) red, and a generated app could shadow a platform doc or plant a registry row. Point `.platform/` at the Workspace → (b) red. Let the throw escape the read dep → (b)'s absent-path turn ends without a reply. Remove the anchor count → (c)'s first match is edited silently. Remove the read exemption → (d) red. Restore a `publish` key on `BuildArgs` → (e)'s dead-key assertion red; drop the unknown-key check → a stale `publish: true` is silently ignored, the shape the rename exists to prevent. Record the read's content → (f) red by two orders of magnitude.

4. **One system layer, stable first, and every new Galaxy is seeded with its own.** `TOOL_CONTRACT` lands beside `CODEGEN_TOOLS`: the identity line and the cross-turn policy — findings are advisory and the model judges, tools not prose, every file then one build. `STUDIO_LOOP_SYSTEM_PROMPT` is deleted. `assembleCodegenPrompt` takes the bundles in § *The tree, assembled*'s order — `TOOL_CONTRACT`, `.platform/AGENTS.md` from the embed, the Workspace's `AGENTS.md` when present, the history when there is one — and the ontology moves to the user message ahead of the current source. The history bundle is § *The transcript is the session*'s projection of the chat's ordered query, the triggering message excluded because the request bundle carries it, a message with no reply carried as itself, speakers labelled `agent`, and `human 1`, `human 2` in first-appearance order with the display name appended when the profile store already caches it. `runModel` sends `x-session-affinity: {u}.{g}:main` on both lanes. `container/app/` gains `AGENTS.md` with a title and a "write this when" line, `CLAUDE.md` holding `@AGENTS.md`, and `docs/vision.md`, `docs/personas.md` with the four-verb template and `docs/permissions.md` with a Decisions table skeleton; the scaffold is regenerated.
   - **Standing guidance edited here:** `ui-theming.md` and ADR-020's third consequence point at `apps/nebula/platform/AGENTS.md` instead of the constant; `auth.md` § *Why upward exists* gains the source-floor path and its *Today's code differs* block narrows to the Universe layer; `_ai-security.md` § *Level 2*'s "Guidance as Resources" bullet becomes files in git with the Message as the record. `codegen-loop.test.ts`'s placement assertion — the ontology in the system block — inverts to the user message; its presence assertion carries over.
   - **Success criteria, the layers (capable of failing):** (a) `messages[0]` of every turn contains the resource core's sentences and the `.platform/docs/resources.md` catalog line, contains no ontology, and carries the bundles in the stated order. (b) A line added to `/workspace/AGENTS.md` appears byte-for-byte in the next turn's `messages[0]`; a Workspace without `AGENTS.md` runs a turn whose `messages[0]` omits the layer and whose reply lands. (c) The affinity header is present in the options `runModel` passes on the binding lane and in the REST header map. (d) `SCAFFOLD_FILES` holds the five seeded paths and `CLAUDE.md` is exactly `@AGENTS.md` plus a newline; a fresh Galaxy in `first-app-built` reads its own `AGENTS.md` back through the client. (e) `git grep -n STUDIO_LOOP_SYSTEM_PROMPT -- apps .claude docs` returns nothing — today it hits `galaxy.ts`, `ui-theming.md` and ADR-020, so it reds against pre-fix code; `tasks/` is excluded because its archive is frozen and holds the name.
   - **Success criteria, the history (capable of failing):** all in-lane on the test app's scripted turns, which capture `messages`. (f) Turn N+1's bundle carries turn N's request text, its speaker label, the agent's `content` and its `appliedPaths`; two turns identical except that one wrote a 400-line `App.vue` produce bundles identical apart from the `appliedPaths` entry, with no `toolCalls` and no `thought` in either. (g) The triggering message's id is never in the history bundle, and the request bundle carries it once. (h) Turn one emits no history bundle. (i) Two messages posted during one generation, then a third turn: its history carries the skipped message with no reply after it.
   - **Mutation note:** stop injecting the Galaxy layer → (b) red. Let a missing `AGENTS.md` throw → (b)'s second half red. Include `toolCalls` in the projection → (f) red. Drop the speaker → the owner's line and a coach's aside are one string, so (f) reds. Drop the exclusion → (g) sees the request twice. Project only replied-to pairs → (i)'s skipped message vanishes. Remove the header on one lane → (c) red on that lane. Leave the ontology in the system block → (a) red.

5. **Every turn is one assembly with every tool, the model decides whether to act, and the container warm is structural.** `#answerTurn` is deleted. `#chatTurnBody` fires `discriminate` detached, racing round one — a `codegen` verdict calls the turn's warm and decides nothing else — and runs `#codegenTurn` without waiting for it. The warm is a closure-local latch created per turn and shared with the `write_file` and `edit_file` deps, so a turn warms at most once whichever fired first; the teardown closes it, so a late verdict cannot start a container after `#destroyBuildContainer()`, and `#turnInFlight` stays the only instance field. The reply is the model's final text on every stop, with the fixed strings only when it is empty. `chat-trigger.test.ts`'s T5 is retired and replaced by the one-warm and teardown tests below; `#destroyBuildContainer` gains a marker so the teardown is assertable.
   - **Success criteria (capable of failing):** (a) No `#answerTurn` exists, and every turn's assembly carries the current source and the full tool set. (b) In-lane, as marker order: with the hint pinned to "no" and a `write_file` in the script, exactly one warm marker precedes the build's progress step; on a `no-tool-calls` script that warmed, exactly one teardown marker follows; a verdict resolving after the turn ends warms nothing. (c) On every stop the durable Message's `content` equals the model's final text when it is non-empty, and is the fixed string only when it is empty. (d) Live, one Docker limb in `first-app-built`: a plain question posted after the build gets its reply, and no container is running afterwards; `studio-codegen-rest` still lands its reply through the single path. This phase's sweep runs without `--fast`.
   - **Mutation note:** withhold the current source from the assembly → (a) red. Remove the `write_file` warm → zero warm markers on the pinned-"no" script. Remove the latch → two markers. Remove the teardown → no teardown marker, and (d)'s container outlives the turn. Leave the latch open past teardown → a late verdict leaks a container. Keep the hardwired "Updated the preview." → (c) red.

6. **The model in the loop, observed.** The exploratory halves run against the real model and are recorded, never gated: the pass-1 rows' twins are Phases 2 through 5's criteria. A new scenario, `studio-guidance-loop`, drives a fresh Galaxy through one session with `needsContainer = false`, the `studio-codegen-rest` pattern, so every sweep runs it; the limbs that need four parties join `four-party-chat`, and the one that needs a build joins `first-app-built`. Each limb is capable of failing on its own and its outcome, pass or fail, goes into the master plan § *Data-bound generation* as a dated findings note — one line each: date, limb, pass or fail, one sentence — a format this phase adds to that section before it writes the first.
   - **Limbs, each with its positive control:** (a) asked for a wishlist, the app writes `src/App.vue` that reads and writes through `store` — the 2026-09-03 hand-driven failure, reproduced or not. (b) "no countdown timers" stated in turn one is honoured by the next turn's output and is still in `AGENTS.md` after ten further turns. (c) After a stated convention, the reply names `AGENTS.md`. (d) A request matching `define-ontology` reads the skill body, visible in the read tool's call log. (e) In the four-party thread, "what does this app do so far?" gets a reply naming a feature the app has. (f) Asked what was requested first, the reply names it. (g) The hint's worth, measured with no knob: on real turns in `first-app-built`, the interval from the first-write warm marker to the `build` call, against the container step's cold start. When that interval regularly exceeds the cold start, the write warm alone hides it and the hint is dead weight — the keep-or-drop number, decided in the findings note.
   - **Mutation note:** exploratory limbs are not mutated; each rides a deterministic twin in an earlier phase that is. What can fail here is the model, and a failure is a finding for ③ and for the platform file's next edit, never a red build.

7. **Standing guidance and the sibling files say what is now true.** The placement table becomes `.claude/rules/studio-guidance.md`, path-scoped to `apps/nebula/platform/**`, `apps/nebula/src/codegen-loop.ts`, `apps/nebula/src/galaxy.ts`, `apps/nebula/container/app/**` and `website/docs/nebula/**`, and `CLAUDE.md`'s rules index gains its row. [on-hold/nebula-skills.md](../on-hold/nebula-skills.md) gets the dated edit § *Context* owes it: Phase 0 absorbed, the Galaxy trust grade, discovery through the catalog, `rules/*.md` not shipped, and its un-park trigger's registry-DO tree rejected. `tasks/backlog.md`: the wizard-style authoring row gains `define-ontology` as its first rung; the URL-synced view-state row's "one sentence in the codegen prompt" repoints to `.platform/AGENTS.md`; a new row captures the partial build the timeout line needs; a new row records the parser-validator feedback — the tool-args facet needs a mode that refuses excess keys. The master plan § *What remains* marks ① landed with a pointer here. This phase is last because every home the rule names exists only after Phase 5.
   - **Success criteria (capable of failing):** (a) `node scripts/check-prose.mjs` passes on every prose file this task touched. (b) Every markdown link in the touched files resolves. (c) The rule's `paths:` glob lists exactly the five homes above, and the index row cites the same file. (d) The bare-handle grep `workflow.md` § *Referring to things across files* gives is clean by eye over the changed source files — no task-file handle shipped in code.
   - **Mutation note:** these are checked properties, not behaviour: a dangling link, a prose file over budget, or a rule whose glob misses a home reds its check on the spot.

## Non-goals

Each exclusion names its home, so nothing leaves by omission.

- **The Universe layer** — its mount is reserved and refused this round; the layer arrives with the Universe Workspace (§ *Future state*; [on-hold/nebula-skills.md](../on-hold/nebula-skills.md) § *Future extension*).
- **Galaxy-authored skills under `.agents/skills/`** — the directory is named and reserved as writable; bodies wait for the skills work.
- **Threads and compaction** — a thread is a second `Chat`; multi-chat is deferred with the management UI (`DEFAULT_CHAT_ID`'s JSDoc); the thread affinity key is decided then (§ *Future state*).
- **A retrieval tool over the user-developer's codebase** — chunking, embeddings and Vectorize; paths in a catalog are enough at this size (§ *Future state*).
- **Splitting the platform layer into `rules/*.md`** — nothing in it is conditional yet (§ *Decisions*).
- **Un-hardwiring the discriminator's `respond`** — [nebula-pre-alpha-fast-follow.md](../nebula-pre-alpha-fast-follow.md) § *Item 6*.
- **`report_finding`** — the placement table's Reporting row lands with ③'s tool ([nebula-pre-alpha.md](../nebula-pre-alpha.md) § *③ Capture live*).
- **The persona executor, `provision(plan)`, and its record's path** — ②'s, with its inputs in [nebula-pre-alpha.md](../nebula-pre-alpha.md) § *② Personas*.
- **A partial build** — the move the timeout line cannot ask for; the backlog row Phase 7 adds.
- **An LLM edition of the docs, or a transform of their outbound links** — shipped as-is (§ *Decisions*).
- **A per-customer platform override, a model swap, `path#anchor` reads, a git remote, commit author as poster** — each a design consideration in § *Future state*, none foreclosed.

## Build log

One entry per phase, written at the phase's end: what landed, where it diverged from the phase text, and the mutation results the verifier panel reads instead of re-running.

**Phase 1 — 2026-09-05.** Landed as written. `requireChatWrite` and `assertModelPath` live in `galaxy.ts` beside the entries (exported, and from the package index); `LOOP_TOOL_ENTRIES` maps `write_file → writeSource`, `build → buildNow`, and `Galaxy.loopToolDeps()` builds the loop's `deps.tools` from it through a `via(tool, entry)` helper typed against the table, so `via('build', 'writeSource')` does not compile. The loop's deps changed shape to match: `CodegenLoopDeps.tools` keyed by tool name replaces the flat `writeFile` / `build`, and `assertSafeRelPath` is deleted from the loop — the entry's throw is captured as the tool error. The Apply's wipe check reads the caller's claims through a `#clientOrigin()` helper that tolerates a direct in-DO call with no mesh context (the dev-studio probes call entries that way). The baseline test app's `GalaxyTest.build` now compiles an ontology job in place, the dev-studio probe's shape, so the Apply is drivable under real invited identities.
- *Diverged:* the `.git` head is read from `.git/refs/heads/main` (trailing newline trimmed) to prove "no new commit" — the design said `git log`, which the entries do not expose. (d)'s "no push observed" is asserted as the iframe src staying the bare dev path after a reload, with limb 5 first proving a build reply does bump it (`?t=`) — the positive control the negative needs. (f)'s no-bit chat-write caller is an invited member granted `write` at the chat node directly (F3's `ownerAndMember`), not the design's collaborator-admin co-mint; the claim shape under test is the same — the chat floor with no dominion anywhere, asserted on the token's `scopeAdmin` being unset.
- *Mutations, each restored by a reverse edit:* dominion back on `writeSource` → F1 red on the dominion message, both surface partitions red. Path rule off the entry + wipe check dropped + claims stripped from the write's log line (one cycle, three code paths) → F2, F3, F4 red respectively, F1 green; the loop's three entry-rule cases red. `build` routed to `#buildAndAnnounce` + an extra dep key → the `buildNow` spy test and the keys test red, nothing else. Generator-side and live limbs: `four-party-chat` and `first-app-built` green on the first run with the new limbs; the message matches are exact strings, so a boundary refusal (`Active-scope mismatch`) cannot satisfy them.
- *Counts:* dev-studio 83 tests; the five touched baseline files 22 tests. The registry sweep for this phase is recorded under Phase 3's entry (one sweep after the loop-changing phases landed together, `--fast` off).

**Phase 2 — 2026-09-05.** Landed as written. `apps/nebula/platform/AGENTS.md` (the resource core, the catalog marker, layers, declared files, the retro, the rules moved from the prompt constant) and three skills; `scripts/gen-platform.mjs` embeds them with `website/docs/nebula/*.md` into `src/platform-embed.ts` (`PLATFORM_FILES`, `PLATFORM_AGENTS_MD`) and renders the catalog line `- name — description (location)` into the marker; `--check` joined the `test` script; `test/platform-embed-drift.test.ts` asserts the catalog and every `.platform/` reference from the embed alone. `.gitattributes` marks the embed generated (`-diff`), so review the sources.
- *Measured:* the embed is 222 KiB of source, 12 files. `wrangler deploy --dry-run` reports 2435.97 KiB / 520.54 KiB gzipped — unchanged from the pass-1 519 KiB because nothing imports the embed yet, so esbuild drops it; re-measured under Phase 3, which imports it.
- *Mutations:* `resources.md` renamed → the generator exits 1 naming `.platform/AGENTS.md → .platform/docs/resources.md`. `define-the-cast`'s `description` deleted → exits 1 naming the file. One catalog line hand-edited in the embed → the vitest catalog test red on the description compare. A line appended to `platform/AGENTS.md` without regenerating → `--check` exits 1 with DRIFT. All restored (the embed by regenerating).

**Phase 3 — 2026-09-05.** Landed as written. The loop dispatches per tool after one shared gate — malformed args, unknown tool, typia shape plus the by-name unknown-key refusal (`unknownToolArgKeys`, pure, in `codegen-loop.ts`; `#validateToolArgs` calls it after the facet), then the identical-call detector with `build` and `read_file` exempt (`LOOP_DETECTOR_EXEMPT`). The `read_file` and `edit_file` deps live in `Galaxy.loopToolDeps()` beside the others: the read resolves `.platform/` from `PLATFORM_FILES`, `.universe/` to `UNIVERSE_RESERVED_MESSAGE`, else `readSource` with an absent file rethrown as `no such file: <path>`; the edit runs the write rule first, reads, counts, refuses zero or two-plus, then writes. `publish` → `preview` throughout (`BuildReport.preview.refreshed`, `decidePreview`, `PREVIEW_UNDECIDED`, the tool description, `BuildArgs`, the feedback line "Fix and call build again."); `TOOL_ARGS_BUNDLE_ID` is v4 and the seeds were regenerated. The read's record is `{ path, bytes }`.
- *Diverged:* the recorded `read_file` error for an absent Workspace path is normalised from the VFS's own message (a `noSuchFile` helper) so the model sees the path, not an errno. `announceBuildToRequester` now reads the requester through the tolerant origin helper — a direct in-DO `buildNow` whose preview refreshed used to throw on the missing call context, which the Phase 1 spy test had been swallowing into the report. Phase 2's bundle number is superseded: with the embed imported, `wrangler deploy --dry-run` reports 2669.19 KiB / 594.85 KiB gzipped — 74 KiB gzipped over the 520.54 baseline (the pass-1 estimate was 66 for the docs alone; the platform file and skills are the difference). Reported, not gated.
- *Mutations, two cycles of independent code paths, each restored by a reverse edit.* Cycle D (`galaxy.ts`): the edit dep's reserved-path pre-check removed → the `edit_file` reserved rows red on the message (all but `.git/config`, which the write-side rule still refuses — the designed backstop); the `.platform/` branch removed → the read-resolution test red; the anchor count removed → the edit-span test red; the unknown-key refusal removed → the rename test's `buildNow` spy is called. Cycle E (`codegen-loop.ts`): the read's try/catch removed → the absent-path unit test and the read-resolution probe test crash; `read_file` dropped from the exemption → the identical-reads test ends `loop-detected`; a `publish` property restored on the tool → `unknownToolArgKeys` and the dead-key assertion red; the read's content recorded → the unit record test and the baseline (f) test red at 77,463 bytes against the 1,024 bound.
- *Counts:* dev-studio 106 tests (was 83 after Phase 1); the baseline read-record file 1. ⚠️ The Phase 1 sweep was started during this phase's edits and its later scenarios reported `Your worker restarted mid-request` 503s — `wrangler dev` hot-reloads on a source save, so a sweep and a source edit cannot overlap (the same rule `testing.md` states for a running suite). It was killed; the clean sweep runs after Phase 5, once no loop edit is pending.

**Phase 4 — 2026-09-05.** Landed as written. `TOOL_CONTRACT` sits beside `CODEGEN_TOOLS`; `STUDIO_LOOP_SYSTEM_PROMPT` is deleted and the (e) grep returns nothing. `runCodegenTurn` walks the tree — contract, `PLATFORM_AGENTS_MD`, the Workspace's `AGENTS.md` behind a one-line preface when present, the history when any — and `assembleCodegenPrompt` puts the ontology, the source and the labelled request in the user message. The history is `#historyBundle`, rendered by `renderHistoryBundle` under `HISTORY_HEADER`. `runModel` sends `x-session-affinity: {u}.{g}:main` on both lanes through `modelCallHeaders()` — the binding's `extraHeaders` option, and `workersAiRestHeaders` (pure) for REST — behind two seams a probe pins, `modelLane()` and `aiBinding()`. The scaffold gained the five seed files (14 in the embed), `CLAUDE.md` exactly `@AGENTS.md\n`.
- *Diverged:* no display names on the history labels — the Galaxy holds no server-side profile cache, so "where cheap" resolves to never; labels are `human N` alone. The history PAIRS a reply with its request rather than listing purely by `validFrom`: a reply commits after any message posted during its generation, so the chronological order put a skipped message between a request and its answer (found by (i) on the first run); a message with no reply is carried in place, an orphan agent message too. The task text placed the ADR-020 pointer in its "third consequence"; the constant is named in Decision 3, which is where the edit landed.
- *Mutations, each restored by a reverse edit:* F1 (three paths) — the Galaxy layer not injected → (b) red; the ontology left in the system block → (a)'s real-turn test and the unit placement test red; `extraHeaders` dropped from the binding call → (c) red. F2 — the `AGENTS.md` read made to throw → (b)'s "without" half red (`WorkspaceFsError`). G1 — the speaker dropped + only replied-to pairs projected → test 1's label assertion and (i)'s three-line list red. G2 — `toolCalls` appended to the projection → all three history tests red (the 400 lines reach the bundle). G3 — the trigger exclusion dropped → (h) red on turn one and (i) at four lines; (g)'s own assertion sits after (h) in the same test, so it is covered by the same mechanism rather than observed separately.
- *Counts:* dev-studio 109, unit 76 (8 drift tests), the baseline subset 26 across seven files. Prose gate: `auth.md`, `_ai-security.md`, ADR-020 and `ui-theming.md` at or better than their HEAD metrics (`_ai-security.md`'s one 157-word bullet predates this task).

**Phase 5 — 2026-09-05.** Landed as written. `#answerTurn` is deleted (`grep -c answerTurn src/galaxy.ts` is 0); `#chatTurnBody` fires `discriminate` detached and runs `#codegenTurn` without waiting; a `codegen` verdict calls the turn's latch (`#turnWarmLatch`, closure-local — `#turnInFlight` stays the only instance field) with `by: 'hint'`, and `runCodegenTurn`'s `onFirstWrite` wraps the `write_file` and `edit_file` deps to fire it with `by: 'write'`; the `finally` closes the latch, then destroys. `warmBuildBox(by)` and `#destroyBuildContainer` carry markers (`nebula.Galaxy.warm`, `nebula.Galaxy.teardown`), and `#buildAndAnnounce` logs `cycle start` so "the warm precedes the build" is an order. The reply is the model's final text on every stop, the fixed strings when empty. `chat-trigger.test.ts`'s T5 is retired for T5a–d (the latch, as marker order), T8 (the reply) and T9 (one assembly: the current source in the user message, the full tool set on the call — via a tool-names capture on the test app).
- *Diverged:* the verdict's log line moved to its own namespace, `nebula.Galaxy.hint`. The mutation cycle showed why: it used to log under `nebula.Galaxy.trigger`, which T4 and T6 count as "zero AI spend", and a verdict landing late from an earlier turn — now possible, since the hint is detached — polluted a later test's window. A hint is not a trigger.
- *Mutations, each restored by a reverse edit:* H1 (four paths) — the write warm removed → T5a zero warms; the teardown removed → T5b no marker (and T5c's teardown count); the hardwired reply kept → T8 (and T1's content); the current source withheld → T9. H2 — the latch removed → T5d two warms; `warm.close()` removed → T5c one warm after the turn ended.
- *Counts:* chat-trigger 12 tests (was 7 with T5). (d)'s live limb — a plain question answered through the single path with no build box left running, on `first-app-built` — and the registry sweep run below.

**Phase 6 — 2026-09-05 (exploratory; reported, never gated).** `studio-guidance-loop` joined the registry (`needsContainer = false`, the `studio-codegen-rest` pattern, ten further turns by default — `HARNESS_GUIDANCE_TURNS` overrides) and prints its findings table; limb (e) rides `four-party-chat`, limbs (g) plus the two deterministic ones (the seeded `AGENTS.md` read back, a plain question through the single path with no box left running) ride `first-app-built`, which now sets `bootVars.DEBUG` to four namespaces and reads the Worker's markers back off the dev stack's stdio (`DevStack.logs`, a new optional capture in `harness/lib/harness.ts`). The findings format was added to the master plan § *Data-bound generation* § *Findings* ahead of the first line, and the first pass is recorded there: (a) (b1) (b2) (e) (f) (g) pass, (c) (d) fail.
- *What the fails say:* (c) — the model DID record the convention with `edit_file` on the turn it was stated (`applied=AGENTS.md`) but did not say so in its reply; the platform file's "say so in one sentence" half is its next edit. (d) — a request naming data the app did not hold activated `wire-a-view` (whose description also says "data") and wrote the ontology under it, never reading `define-ontology`; the two descriptions need a sharper split on "the type is absent". Both are the platform file's, not this task's — a change would need its own live pass to observe.
- *The keep-or-drop number for the hint:* on one real turn, write→build 32.3 s against a 3.2 s cold start; the hint warmed 49.8 s ahead. The write warm alone hides the cold start on this shape. Recorded as n=1 with the drop deferred to a second pass.
- *Diverged:* limb (e) rides a Galaxy that holds the seed only, so it tests that the reply names the seed state — the placeholder, the starter shell, nothing built yet — rather than a built feature, the weaker property on the thread the limb had. Limb (g) was designed against "the container step's cold start", which no marker reports; the measured comparison is the first-write→cycle-start interval against `containers.md`'s 3.2 s local figure, with the cycle's own duration reported beside it. The design's "no container is running afterwards" is asserted on the Worker's own `warm` and `teardown` markers, paired per turn (the verifier panel's first pass showed the bare `docker ps` check could not fail — see that entry), with `docker ps` polled for 30 s as the outside witness (build-box names only; proxy sidecars excluded).
- *Runs:* the clean sweep after Phase 5 came back 25/27 — every container-free scenario green, `build-box` and `first-app-built` red. `build-box` passed alone on the rerun (110 s); its sweep red coincided with the `harness.ts` edit that added the stdio capture landing mid-sweep, and the sweep does not keep a child's output, so the cause is recorded as unattributed. `first-app-built`'s red was this task's own limb-5 positive control, which waited for the build reply to refresh the iframe — a refresh the model grants only on a findings-free build; it now clicks the Studio's Reload control instead, and the scenario passed on the next two runs (62.6 s, 64.3 s). `four-party-chat` 17.3 s, `studio-guidance-loop` 130.4 s.

**Verifier panel, first pass — 2026-09-05.** Seven verifiers fanned out; three returned before the session's usage limit cut the other four off (re-run below). Phases 2 and 3 conformed with minors, Phase 5 did not; every finding was fixed in the tree before the re-run:
- *Phase 5, major — (d)'s live "no container running afterwards" could not fail on its own:* the real discriminator answers "no" to a plain question, so the question's turn rarely warms and the `docker ps` check passed with the teardown deleted. Limb 8 now reads the Worker's own `warm` and `teardown` markers off stdio and asserts the PAIRING per turn — the build turn (which certainly warmed; the positive control) and the question's turn if it warmed, reported as not-exercised when it did not — with Docker as the outside witness.
- *Phase 5, major — `testing.md`'s stamped-discriminator MUST:* the warm, teardown and `cycle start` markers now carry `instanceName`, and T5a–d filter by it before asserting order within one Galaxy's turn; T5c waits for the teardown instead of reading it synchronously (a race it had been winning).
- *Phase 5, minors:* T8 gained the failure stops — a `loop-detected` turn with text replies with that text, and with none it gets the fixed apology; five comments that still described the deleted answer fork were retensed (`runModel`'s JSDoc, `runCodegenTurn`, the baseline test app, `turn-heartbeat.ts`, the scenario's "two namespaces").
- *Phase 3, minors:* `mark_complete` now passes the shape and unknown-key gate before ending the turn (a stray key is refused and the turn continues; unit-tested); the read record's `bytes` is measured with `TextEncoder` (it was UTF-16 code units); the `write_file` description and `assertModelPath`'s message say "a path whose first segment starts with a dot", since `.env` is a file; two backlog rows that still named `publish` were repointed.
- *Phase 2, minors:* the generator's walk skips dot-entries and embeds only `.md` (a `.DS_Store` would have shipped as a key), and refuses a YAML block-scalar `description` (both gates would have agreed on a broken catalog line). One finding is outside this task and now a backlog row under § *Documentation*: `coding-your-ui.md` and `resources.md` teach lowerCamel `typeName`s (`'todoList'`, `'todo'`) for interfaces declared `TodoList`/`Todo`, while the runtime keys by the declared name — a doc the platform layer now sends the model to.
- *After the fixes:* dev-studio 110 tests (the `mark_complete` stray-key test is the one added), chat-trigger 12, the embed drift 4. `first-app-built` reddened once at limb 5 — the preview answered 404 with no body after a completed turn, before the run reached the limb-8 change; nothing kept that run's Worker output, so the cause is unattributed, though the shape is the one `tasks/backlog.md`'s `build-box` dist-arrival row describes (a build whose dist never arrives serves 404 until the next build) — and passed on the rerun (107.8 s): limb 8 reported the build turn's warm torn down and the question turn not warming, and (g) measured 7.5 s, the second number in the master plan's register.

**Verifier panel, second pass — 2026-09-05.** All seven returned. Phases 1 through 5 conformed with minors. Phases 6 and 7 did not, on three majors. Every major and most minors were fixed in the tree before the closing run below:
- *Phase 7, major — six source comments carried this task's own handle* (`the guidance file tree's Phase N`, in five baseline test headers and `four-party-chat`'s chat-floor limb). The `[SD]N` grep the build log cited cannot see the `Phase N` class. Each now cites the rule that owns the fact: `security.md`'s design-floor line for the chat floor, `studio-guidance.md` for the tree. The grep that catches the class is `grep -nE '\bPhase [0-9]\b'` over the changed source. It returns only the archived codegen loop's phases, which `codegen-loop.ts` and its test cited before this task; their header carries the archive path.
- *Phase 7, major — `ui-theming.md`'s glob still loaded on `galaxy.ts` and not on the platform file.* The entry is now `apps/nebula/platform/**`, so the sync MUST loads at the one edit it governs.
- *Phase 6, major (Phase 5's minor said the same) — limb 8 could not pass in the deployed venue.* `DevStack.logs` is absent on a `HARNESS_TARGET_URL` target, so the marker half asserted over nothing. The limb reads the capture once. It asserts the pairing and the Docker witness only where the stdio exists, and reports the pairing as not observable deployed. (g) reports itself unmeasured there. Three more things landed with it. A warm is the fired marker (`container warm fired`), not the namespace, so a failed start's warning cannot inflate the count. The markers are filtered to this Galaxy's stamp; `job report` gained `instanceName` for it. And the turn-level teardown is isolated: a build cycle destroys its own box before its `job report`, so "a teardown follows the build turn's last report" reds on the `finally` alone. The pairing could not, since the cycle's destroy paired every warm. Mutation-checked live, below.
- *Phase 6, minors:* (b1) reported a pass with no `App.vue` to judge; an unexercised limb is now a fail. Limb 7's seed is read before the model's first turn, so a model that edits `AGENTS.md` cannot red a limb that gates the scaffold. The header's limb list carries 7, 8 and (g). "Three namespaces" is four. (e)'s reinterpretation is recorded under *Diverged*.
- *Phase 4, minor — the REST lane's affinity header was proven on the pure map only:* `extra: {}` at `#callModelRest`'s call site stayed green. A `restFetch` seam, the shape of `aiBinding`, lets the probe capture what the lane puts on the wire. (c) asserts it there. `HISTORY_HEADER` describes the paired projection it heads. The platform file's "the first system message" says the first section of the one system message.
- *Phase 1, minors:* F3's fixture shape is recorded under *Diverged*. The keys test's tautological compare, the table against its own `Object.keys`, is dropped for the literal list. `writeSource`'s JSDoc says a turn's writes carry the projection too. The backlog row that named `warmPreview` as shipped repoints to the build reply's push.
- *Phase 2, minors:* `wire-a-view` no longer names the list field `ids`; that is the ontology's to choose. The generator refuses a frontmatter line that is not `key: value`, the plain-scalar continuation the block-scalar gate missed. *Phase 3:* the baseline test app's `publish` comment. *Phase 5:* `markerIndices` says why order within one turn is the property and not a stand-in for `testing.md`'s count. `turn-heartbeat.ts` names one detached call. *Phase 7:* the memory that pointed at the constant repoints to the platform file. `studio-guidance.md` cites § *Other Nebula backlog*.
- *Not fixed, by design:* (e)'s `git grep` sees tracked files only. A plain `grep -rn` over the tree is also empty, and the gap closes at commit. Five files cite this file's pre-archive path; repoint them in the commit that archives it.
- *The sweep's two reds, same pass:* `first-app-built`'s limb 3 sampled the transcript before the strip was a tail: 75 vs 75 chars, a modal holding exactly the whole stream. The check waits for the truncation ellipsis now. `build-box` reddened at 76.6 s with no output kept; it is rerun in the sweep below.
- *Runs, after the last source edit:* dev-studio 110 tests, the six touched baseline files 59, the two drift files 8. Mutations: `extra: {}` at the REST call site → (c) red alone (1 of 62), restored to 62; the turn's `finally` teardown removed → `first-app-built` red on the new isolation assertion after limbs 1 through 7 green (71.8 s), restored. The closing suite is the number Phase 7's entry carries. The registry sweep, `--fast` off: 28 of 28 green — `build-box` 113.5 s, `first-app-built` 91.7 s, `studio-guidance-loop` 157.7 s. The sweep keeps no child output, so this pass's register lines come from one more run of the three model scenarios alone: `studio-guidance-loop` (c) passed this time and (a) did not — the model read and replied without a write; (d) reproduced; `four-party-chat` (e) passed.
- *`first-app-built`'s 404, attributed as far as it can be:* it reddened at limb 5 on the same 404 twice more — three of nine runs that day, the turn completed each time — so the limb now prints the reply's manifest on every run (stop, rounds, applied paths, the last build's preview verdict) and a red says whether the model never reached a successful bundle or the dist never arrived. The run after that passed (78.6 s) and its manifest shows the shape a red could take: `max-depth` after eight rounds, one build, type findings, the refresh withheld — and the dist still served. (g)'s third number is 31.7 s, n=3.

**Phase 7 — 2026-09-05.** Landed as written, one phase early relative to Phase 6's runs (prose only, so nothing it touches is watched by the sweep's `wrangler dev`). `.claude/rules/studio-guidance.md` carries the placement table with a `paths:` glob of exactly the five homes; `CLAUDE.md`'s rules index gained its row; [on-hold/nebula-skills.md](../on-hold/nebula-skills.md) carries the dated edit (Phase 0 absorbed, the Galaxy trust grade, discovery through the catalog, `rules/*.md` not shipped, the un-park trigger's registry-DO tree rejected); `tasks/backlog.md`'s wizard row names `define-ontology` as its first rung, the URL-synced view-state row repoints to the platform file, and two rows were added — the partial build under § *Other Nebula backlog*, the excess-keys facet mode under § *`@lumenize/ts-runtime-parser-validator`*; the master plan marks ① built and its § *Data-bound generation* gained the § *Findings* format ahead of the first line.
- *Closing run, after the last edit:* `npm test` in `apps/nebula`, re-run after the verifier panel's second-pass fixes — 839 passed, 3 skipped, 0 failed across 102 files (838 before the first pass added the `mark_complete` stray-key test) (the previous run's 840 became 841 with the one chat-floor tier test added to `scope-isolation.test.ts`'s B5). The run before it had reddened B5 — its "non-admin" partition counted every guard that was not dominion, so the four chat-floor entries landed in the shared-data allow-list; they are now a third frozen tier there — and the contention-fragile alarm test (`T-local-skip`), green alone and green on the rerun (`testing.md` § the isolation-flips-the-result signature). One unhandled rejection is reported on both runs: the invite facade's own asserted "requires a verified identity" refusal, surfaced by workerd — class (B) in `testing.md`, in a file this task never touched.
- *Checks:* (a) `check-prose` — the new rule and this file within budget; every other touched prose file at or better than its HEAD metrics (the two over-budget files, `security.md` and `_ai-security.md`, were over by the same amounts at HEAD). (b) every relative link in the twenty touched markdown files resolves, except twenty pre-existing `file.ts:line` rows in `tasks/backlog.md` that this task did not write (a register; its rot is outside this diff). (c) the glob is the five homes, and the index row cites the file. (d) the bare-handle grep over the changed source returns only the archived codegen loop's `D`-series that `codegen-loop.ts` has cited since it was written, with the archive path in its header. The NUL/NBSP sweep over the 52 changed files is clean.

**Closed 2026-09-06 — the follow-ups after the build, each recorded in its own home.** The registry sweep ran green (28 of 28) and the closing suite at 841 on the final tree. What the day changed, with the pointer that carries it: the round cap to 32 and the generation deadline to 14 minutes with its token coupling retired (`galaxy.ts` JSDoc; the turn's authority is pinned at the post — `security.md`); the local dist-arrival 404 fixed at its mechanism (`containers.md`, the closed `build-box` row in `backlog.md`); a failed container step and a failed model call each ending the turn with a reply that names the cause (`studio-guidance.md`, `codegen-loop.ts`); the classifier and its warm hint deleted at n=5 (the master plan's § *Findings* register); finding (d) fixed by disjoint skill descriptions and the ask-or-propose judgment written into `define-ontology`; the sweep keeping every child's output and detecting a mid-sweep source save (`live.md`). The five files that linked this file's pre-archive path were repointed in the archiving change.
