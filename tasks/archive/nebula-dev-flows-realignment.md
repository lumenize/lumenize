# Dev-flows realignment — execution tracker

> 🗄️ **Archived 2026-06-22.** Frozen point-in-time record — its job is done (findings absorbed into the 2026-06-20 consolidation). Do not update.

> ⚠️ **SUBSUMED BY THE CONSOLIDATION 2026-06-20** (`nebula-studio-consolidation-plan.md`). Most per-file findings below target files that were **archived or deleted** in the consolidation — their realignment was absorbed into the fold (salvage → `nebula-studio.md` / `nebula-agentic-development-engine.md`) rather than applied in place, so **do not apply them to the gone files**. Findings that survive: `nebula.md` (✅ applied in place) and the `DevStar`→`Star@.dev` term-swaps in still-parked on-hold files (`nebula-skills`, `nebula-studio-eval-suite`, `nebula-5.5-schema-evolution`) — **deferred to un-park**. Kept as the audit record.

Canonical source of truth: [`nebula-dev-flows.md`](nebula-dev-flows.md). Realignment is **one-way** (files conform to it; never the reverse; never relitigate). Generated from the 2026-06-20 read-only conformance audit (18 agents). Detail per file below; check items off as applied.

## Decisions
- [x] **DEV_STAR binding — RESOLVED 2026-06-20 (user-confirmed): collapse to one `STAR` binding.** Remove the `DEV_STAR` binding + smart-match guard; `#starBinding()` collapses to always `STAR`; dev/prod is the `.dev` instance suffix. Matches canonical Decision 2. (The code change lands in the Phase-3.5 build; task files just describe it.)

## ADR conformance — **NO ADR change needed** (all 7 conform; verified by the audit). ✓

## Cross-cutting purge sweeps (grep across all in-scope files)
- **Registry-call vocabulary** (Decision 9 eliminates it; source re-homes to DevStudio Workspace): `appendOntologyVersion | getLatestOntologyVersion | getOntologyVersion | OntologyVersionRow | ontology:_index | compileOntologyVersion | getOntologySource`. **The audit's biggest catch — under-covered by the per-file sweeps; lives in the schema-evolution / resource-metadata cluster.**
- **Galaxy-stores/returns SOURCE** (not just dual-write): any "Galaxy returns the raw .d.ts" → DevStudio Workspace (Decision 7).
- **Command vocab**: `deploy_to_dev | deploy_to_main | deployToDev` → DevStudio-orchestrated `applyChanges` (dev) / publish (Decisions 4/9).
- **Term swaps**: `DevStar` class → `Star` at `.dev`; "agent"/"Studio agent" orchestrator → DevStudio; DevContainer-as-source-of-truth / `DevSource` → DevStudio; in-DO SFC compile/serve → vite-in-DevContainer.
- **Three source-durability designs purge** (Galaxy dual-write / DevContainer DO store / dev-Star `file` resources) → one home, DevStudio.

---

## P0 — source-of-truth reversal (rewrite-section; re-supersede Phase-3.5 banners)

### nebula-container-dev-loop.md — 26 findings · rewrite-section
  - ⚠ **banner-hazard:** YES — the file's own Status line (line 3) and the §Work-safety / Phase-2 / Phase-3.5 bodies carry the recent "Phase 3.5 — REVIEWED 2026-06-18 via /review-task" framing, and Phase 3.5's Q#2 inline-superseding language ("Superseded by Q#2 + the Phase-2 DONE-record above", line 103; "the old nebula-studio.md save-orchestration is superseded, not relocated", line 200) ENSHRINES the DevContainer-source-of-truth/DevSource conclusion as the settled answer. nebula-dev-flows.md §Downstream Headline explicitly says this 2026-06-18 Phase-3.5 conclusion is REVERSED by the 2026-06-19 pivot. So every Phase-3.5 "superseded/resolved" banner here is itself now re-superseded and must NOT be trusted: the "resolution" (DevContainer DO SQL store) is the wrong answer. The new banner must re-supersede the Phase-3.5 superseding banners, not sit beneath them.
  - [ ] **[banner]** The Status line presents the 2026-06-18 Phase-3.5 design (incl. its Q#2 'DevContainer DO source-of-truth' conclusion) as the current, build-ready answer. nebula-dev-flows.md (2026-06-19) supersedes that whole design round: DevStudio is the source-of-truth, DevContainer is a disposable checkout, there is no DevStar class, and Galaxy is published-only. The file has no pointer to the new canonical doc.
        → Prepend a dated superseding banner immediately under the title: '> ⚠️ SUPERSEDED 2026-06-19 by [nebula-dev-flows.md](nebula-dev-flows.md) — the canonical dev/publish model. The 2026-06-18 Phase-3.5 conclusion below (DevContainer DO = dev source-of-truth via a DevSource SQL table) is REVERSED: DevStudio (shell Workspace + isomorphic-git) is the source-of-truth; DevContainer is a disposable checkout DevStudio pushes to (applyChanges); there is no DevStar class (the dev data Star is a plain Star at the {u}.{g}.dev instance); Galaxy is published-only. Realign the sections below to that doc before /build-task; do not trust the Phase-3.5 superseding banners in this file — they enshrine the now-reversed answer.'
  - [ ] **[rewrite]** REVERSED SOURCE-OF-TRUTH (the headline pivot reversal). nebula-dev-flows.md Decision 5 + §Topology: 'DevStudio is the sole writer of source and the durable source-of-truth (its shell Workspace + local git)'; Decision 7 explicitly purges the three conflicting source designs incl. '(b) DevContainer DO store'. The DevContainer holds NO durable source; the 'DevSource' SQL table does not exist in the new model.
        → Rewrite to: 'Resolved by [nebula-dev-flows.md] (2026-06-19): source durability lives in DevStudio (the shell Workspace — SQLite+R2 — plus local git via isomorphic-git), which is the sole writer + source-of-truth. The container working tree is a disposable checkout DevStudio repopulates on cold boot by pushing changed source (applyChanges over mesh). There is NO DevContainer DO source store and NO DevSource table. A Star wipe (resetDevData) never touches source — source lives in a different DO entirely (DevStudio).'
  - [ ] **[rewrite]** Directly inverts Decision 5/Decision 7: the DevContainer owns NO source-of-truth and there is no dual-write to a DO SQL store. DevStudio is the sole writer; the seam is push (applyChanges), not dual-write. Cold-boot repopulation is a DevStudio push of the full tree (Flow 1c), not a re-hydrate from a DevContainer DO table.
        → Replace the whole Q#2 bullet list with the canonical split: 'DevStudio owns the dev source-of-truth (shell Workspace + local git via isomorphic-git) and is the sole writer; LLM turns read/write locally against it. The DevContainer is a disposable checkout: on save DevStudio commits locally then PUSHES changed files to DevContainer.applyChanges() (mesh, @mesh requireAdmin), which writes them to the working tree (no git in the container, no DO SQL store). On cold boot DevStudio re-populates via ensureUp() + applyChanges(full tree) — Flow 1c. Star (.dev) keeps only the ephemeral dev DATA; resetDevData() is the .dev-guarded wipe and never touches source. Galaxy keeps only the immutable published app-version (built bundle); publish = DevContainer vite build → push built artifact to Galaxy.' Delete every 'DevSource'/'dual-write'/'re-hydrate from DO SQL' reference.
  - [ ] **[delete]** The DevSource table is the concrete artifact of the reversed decision. In the canonical model source durability is DevStudio's shell Workspace (SQLite+R2 via @cloudflare/shell) + isomorphic-git, NOT a hand-rolled per-file SQL table on the container DO. nebula-dev-flows.md §Source abstraction: the DevContainer side is only applyChanges(files) writing to the working tree — 'no git in the container', no durable store.
        → Delete the DevSource-table paragraph entirely. Replace with a pointer: 'Source storage = DevStudio's shell Workspace (SQLite+R2) + local git (isomorphic-git); see nebula-dev-flows.md §Source abstraction (the seam). The DevContainer has no source store — applyChanges(files) just writes DevStudio's pushed files into the working tree.' Note: this removes the [resolves M3/N2/n3] inline tags, which referenced the now-deleted store.
  - [ ] **[rewrite]** The whole section frames work-safety as solved by the DevContainer DO store (reversed). Canonical answer: work-safety holds because DevStudio is the durable source-of-truth and re-pushes on cold boot; the container being ephemeral is by design, not a hazard requiring an in-DO store.
        → Rewrite the section to: 'The container working tree is ephemeral by design — on any sleep/restart it reverts to the baseline image. That is safe because the source-of-truth is DevStudio (shell Workspace + local git), NOT the container. On cold boot DevStudio re-populates the tree by pushing the full source (ensureUp + applyChanges, Flow 1c in nebula-dev-flows.md). No DevContainer DO store, no DevStar/Galaxy dependency for work-safety.' Replace 'DevStar deleteAll() wipe' with 'Star (.dev) resetDevData wipe' and note source is unaffected (different DO).
  - [ ] **[pointer]** The Phase-3.5 mermaid diagram encodes the reversed model end-to-end: 'Studio agent' (→ DevStudio), 're-hydrate from DevContainer DO SQL', 'dual-write to DO SQL', 'DO SQL persists'. nebula-dev-flows.md replaces this entirely with Flow 1/1b/1c where DevStudio is the source-of-truth and pushes (applyChanges); there is no DO SQL store. §Downstream instruction for this file: 'replace the single Phase-3.5 diagram with a pointer here'.
        → Delete the mermaid diagram and replace with a pointer: 'See the canonical sequence diagrams in [nebula-dev-flows.md]: Flow 1 (dev loop), Flow 1b (ontology-change wipe prompt — server calls the client over mesh), Flow 1c (cold spin-up = ensureUp + applyChanges full tree). Participants: DevStudio (codegen + source-of-truth), DevContainer (disposable checkout), Star (.dev instance), Galaxy (published-only).'
  - [ ] **[term-swap]** There is no DevStar class (Decision 2: 'No DevStar class. Collapse to one Star ... the dev Star is the {u}.{g}.dev instance'). DevContainer is a sibling of Star (the class), differentiated only by instance, not a sibling of a DevStar subclass.
        → Term-swap: 'a sibling of the Star DO (different base class), per-sandbox, addressed at the {u}.{g}.dev instance (the node type's M3 addressing).' Apply the same DevStar→'Star (.dev instance)' swap throughout the DevContainer design section.
  - [ ] **[rewrite]** References dev-star.md's DEV_STAR guard as the established analogue, but the DEV_STAR binding's fate is now an open question (nebula-dev-flows.md §Downstream cross-cutting: 'keep a DEV_STAR binding (→ Star, .dev instances) or route via the STAR binding ... the realignment must decide'). Decision 2 says '#starBinding() collapses to always STAR' and 'the DEV_STAR binding + its smart-match guard disappear.'
        → Per Decision 2 the DEV_STAR binding + smart-match guard are slated to disappear (#starBinding collapses to STAR). Reframe the analogy as unresolved: 'the DEV_CONTAINER smart-match guard is analogous to the (being-retired) DEV_STAR guard; per nebula-dev-flows.md the DEV_STAR binding question is open — DEV_CONTAINER's guard stands regardless.' Flag for the coordinator: the canonical doc leans toward removing DEV_STAR.
  - [ ] **[term-swap]** DevStar (the class) does not exist; data calls go to the Star at the {u}.{g}.dev instance (Decision 2). Also 'no DO→DO hop' framing is fine, but the participant name is wrong.
        → Term-swap 'DevStar' → 'the Star (.dev instance, {u}.{g}.dev)' in the Goal. Sweep all Goal/Requirements 'DevStar' occurrences (e.g. 'login survives a DevStar wipe' → 'login survives a Star .dev-data wipe (resetDevData)').
  - [ ] **[term-swap]** 'Agent'/'Studio agent' for the server orchestrator is the old term; canonical name is DevStudio (Decision 1: 'DevStudio names the codegen engine ... not using [agent] term to avoid confusion with Cloudflare's agent toolkit'). The @mesh(requireAdmin) + command-method design is CORRECT and stays; only the actor name changes. Also 'writeFile' is now part of the applyChanges push seam.
        → Term-swap 'agent'/'Studio agent' → 'DevStudio' wherever it denotes the server orchestrator (this bullet, the Phase-3.5 mermaid actor 'A', the Phase-1/2 prose 'the agent writes...'). Note the push seam: writeFile/applyChanges is DevStudio pushing source to the container. Preserve the requireAdmin gating (matches nebula-dev-flows.md Flow 1: 'applyChanges (mesh, @mesh requireAdmin)' and ADR-007). Leave 'agent-authored server code runs elsewhere (a Star facet / Worker-Loader isolate)' as-is — that 'agent' is the future facet feature, not the orchestrator.
  - [ ] **[rewrite]** Routes to 'DEV_STAR' via #starBinding's .dev marker; Decision 2 collapses #starBinding to always STAR and removes DEV_STAR. The scope-injection footgun guard is still valid and important (server-derived, never request-supplied), but the routing target description is stale.
        → Rewrite the consequence: 'a request-influenceable or empty activeScope would silently route the client's data calls to the wrong Star instance (e.g. prod {u}.{g} instead of the dev {u}.{g}.dev) — the wrong-Star footgun. #starBinding() routes by the injected activeScope to the STAR binding (the dev/prod distinction is the instance name, per nebula-dev-flows.md Decision 2), so an empty/spoofed scope mis-routes silently.' Keep the server-derived guard intent intact.
  - [ ] **[rewrite]** Two contradictions: (1) 'DevStar.compileSFC' and 'DevStar itself stays' — DevStar the class is eliminated (Decision 2); compileSFC is deleted entirely because vite owns compile (Decision 2: 'compileSFC is deleted anyway (vite owns compile)'; Decision 9: 'the Star never compiles'). (2) 'data from DEV_STAR' — the DEV_STAR binding is slated for removal; data goes to the Star at the .dev instance via STAR.
        → Rewrite: 'The Star class stays as the single data Star; the {u}.{g}.dev INSTANCE is the dev data Star (no DevStar subclass — nebula-dev-flows.md Decision 2). compileSFC is deleted, not relocated — vite owns compile and the Star never compiles (Decisions 2, 9). Phase 4 deletes the in-DO compile/serve machinery; the Star (.dev) keeps only subscribe/transactions/reload. Shell from DEV_CONTAINER; data from the Star (.dev instance).' Swap all DevStar→Star(.dev) and reconcile the DEV_STAR routing per the open binding decision.
  - [ ] **[rewrite]** Reversed source-of-truth again: 'source is owned by the DevContainer'. Canonical: source is owned by DevStudio. Also 'DevStar wipe' / 'DevStar = dev DATA only' — there is no DevStar class; it is the Star at .dev with a .dev-guarded resetDevData (Decision 2, Decision 11).
        → Rewrite the cross-file reconciliation bullet: 'dev-star.md retires to "the dev Star is a Star at the .dev instance"; source is owned by DevStudio (not the container), so resetDevData (the .dev-guarded wipe) never touches source — precondition satisfied by the architecture. Home the .dev-guarded resetDevData + the server→client wipe-prompt (nebula-dev-flows.md Flow 1b) on the Star.' Swap DevStar→Star(.dev) and DevContainer→DevStudio for source ownership.
  - [ ] **[rewrite]** Instructs realigning nebula-studio.md to 'DevContainer DO is the dev source-of-truth' — the reversed answer. nebula-dev-flows.md §Downstream says nebula-studio.md should become 'DevStudio = source of truth', and the dual-write mechanism does not exist.
        → Rewrite: 'nebula-studio.md § Durable draft ownership → retitle to "DevStudio is the dev source-of-truth" (shell Workspace + local git); the per-turn-autosave / persist-before-wipe / re-hydrate orchestration is superseded by DevStudio's local commit + push (applyChanges); fix the "Galaxy hosts chat session state" drift (→ DevStudio hosts chat/session); add the Preview app / Studio UI / App UI cast distinctions (nebula-dev-flows.md Cast).' Remove 'DevContainer dual-write'.
  - [ ] **[rewrite]** 'the dev-draft-source store is the DevContainer's' is the reversed answer. Canonical: the dev source home is DevStudio. Also this bullet retains 'DevStar'. nebula-dev-flows.md Decision 9 further eliminates the ontology-version registry and unifies app-version = ontology version — relevant to nebula-app-versioning.md.
        → Rewrite: 'nebula-app-versioning.md → drop the Galaxy dual-write source design and the DevStar source store; the dev source home is DevStudio (shell Workspace + git); Galaxy keeps only the immutable published app-version (built bundle); adopt one unified version (app-version = ontology version, Decision 9 — no separate ontology-version registry; eliminate appendOntologyVersion/getLatestOntologyVersion); "publish" = DevContainer vite build → push built artifact to Galaxy.'
  - [ ] **[rewrite]** Re-asserts the reversed DevContainer dual-write as 'the resolution'. Canonical: durability is DevStudio's shell Workspace + git; the container dual-write/re-hydrate mechanism does not exist. This is one of the inline Phase-3.5 superseding statements that is itself re-superseded (phase35BannerHazard).
        → Rewrite: 'the old save-orchestration is superseded — durability is DevStudio's local commit (isomorphic-git) in its shell Workspace; DevStudio pushes changed source to the container (applyChanges) and re-pushes the full tree on cold boot (Flow 1c). resetDevData (the Star .dev wipe) never touches source (different DO), so the confirm-durable gate is moot. What remains for Studio/#1b is the publish trigger + the DevContainer→Galaxy build-push.'
  - [ ] **[term-swap]** 'login survives a DevStar wipe' — DevStar the class does not exist; the wipe is resetDevData on the Star (.dev instance), Decisions 2 + 11.
        → Term-swap: 'login survives a Star .dev-data wipe (resetDevData)'. The Galaxy-admin active-scope / auth-DO-separate facts remain correct.
  - [ ] **[rewrite]** Lists compileSFC and deployToDev as live DevStar dev-mutators. compileSFC is deleted (vite owns compile, Decision 2/9); deployToDev (pull-from-Galaxy) is replaced by DevStudio applying the compiled validator directly to the .dev Star (Decision 9, nebula-dev-flows.md Open/to-confirm: 'the Flow 1 opt should read "apply compiled validator," not "deployToDev (pull from Galaxy)"'). 'DevStar dev-mutators' → Star (.dev) ops.
        → Rewrite the comparator to the surviving op set: 'methods strictly more privileged than the already-admin-gated Star (.dev) dev op resetDevData (requireAdmin)'. Drop compileSFC (deleted) and deployToDev (replaced by DevStudio pushing the compiled validator to the .dev Star — Decision 9).
  - [ ] **[rewrite]** Conclusion (vite owns compile, delete in-DO machinery) is CORRECT and conforms to Decisions 2/9 — keep it. But it still calls the orchestrator 'agent' (→ DevStudio) and omits that the ONTOLOGY also compiles (in DevStudio for dev, Galaxy for prod; the Star never compiles — Decision 9). Vite owns the .vue/SFC compile; the ontology .d.ts → runtime validator compile is DevStudio's via @lumenize/ts-runtime-parser-validator.
        → Keep the 'vite owns SFC compile, delete in-DO machinery' resolution. Term-swap 'agent' → 'DevStudio'. Add a clarifying note: 'Two compiles, both off the Star: vite owns the .vue/SFC compile (in the container); DevStudio owns the ontology .d.ts → runtime validator compile (dev) and Galaxy owns it at publish (prod) — the Star never compiles (Decision 9). DevStudio pushes the compiled validator to the .dev Star on save (replacing the old deployToDev pull-from-Galaxy).'
  - [ ] **[rewrite]** The {u}.{g}.dev instance addressing for DEV_CONTAINER is fine, but the DEV_STAR side of the disjointness argument is stale — the DEV_STAR binding is slated for removal (Decision 2); the data side is the Star at the .dev instance, possibly via the STAR binding (open question).
        → Keep the DEV_CONTAINER addressing resolution. Rewrite the DEV_STAR comparison: 'DEV_CONTAINER/{u}.{g}.dev addresses the container; the dev data Star is the Star at instance {u}.{g}.dev (no DevStar class; the DEV_STAR binding is being retired per nebula-dev-flows.md Decision 2 — the realignment decides keep-DEV_STAR vs route-via-STAR). The DEV_CONTAINER smart-match guard stands either way.'
  - [ ] **[rewrite]** References DEV_STAR removal in Phase 4 as if DEV_STAR is a binding that serves shells today. With no DevStar class and #starBinding collapsing to STAR (Decision 2), the entrypoint serving-target story should be expressed in terms of STAR + the .dev instance, not a DEV_STAR binding. (The DEV_CONTAINER serving-target switch + HMR-WS allow themselves remain correct mechanisms.)
        → Keep the DEV_CONTAINER serving-target + HMR-WS edits (they conform). Reconcile the STAR/DEV_STAR removal language to the open binding decision: if DEV_STAR is removed (Decision 2), Phase 4 removes only STAR from the ungated passthrough and there is no separate DEV_STAR binding to flip. Flag for coordinator.
  - [ ] **[rewrite]** The Phase-4 residual grep + its DevSource carve-out reference the now-deleted DevSource store. With DevSource gone (source lives in DevStudio), the grep should not need to exempt anything in dev-container.ts, and the AppBundle/putAsset/getAsset deletions become unconditional.
        → Keep the residual-grep teardown approach (it correctly deletes the in-DO compile/serve machinery, conforming to Decisions 2/9). Delete the DevSource carve-out sentence ('The DevContainer's DevSource store ... is NOT a residual match'). Note that source durability now lives in DevStudio's shell Workspace, outside apps/nebula's in-DO serve code, so the AppBundle/putAsset/getAsset matches are deleted outright.
  - [ ] **[rewrite]** Reversed source-of-truth in the Dependencies section: 'DevContainer DO's own storage' owns source durability. Canonical: DevStudio owns it. Also 'agent' → DevStudio, 'DevStar wipes' → Star .dev wipe (resetDevData).
        → Rewrite: 'Source durability — owned by DevStudio (shell Workspace + local git via isomorphic-git), the source-of-truth; the disposable container is re-populated by DevStudio pushing the full tree on cold boot (applyChanges, Flow 1c). Not a DevContainer DO store; survives Star .dev-data wipes (different DO). Versioned/published source is the separate #1b layer (Galaxy published-only registry — built artifact, no dev source). See nebula-dev-flows.md.'
  - [ ] **[pointer]** References dev-star.md as 'the data-side sibling' with a DEV_STAR smart-match guard and a durability precondition gated on source ownership — all stale per Decisions 2/5/11. dev-star.md is itself slated to retire to 'the dev Star is a Star at the .dev instance' (nebula-dev-flows.md §Downstream). Also missing a reference to nebula-dev-flows.md as the canonical authority.
        → Add nebula-dev-flows.md as the FIRST reference ('the canonical dev/publish model — authority for this file'). Update the dev-star.md descriptor to 'the dev data Star = a Star at the .dev instance; homes resetDevData (.dev-guarded) + the Flow 1b wipe-prompt'. Drop the 'durability precondition' and 'DEV_STAR smart-match guard' framing (the durability precondition is moot; source lives in DevStudio).
  - [ ] **[term-swap]** 'data/agent comms over mesh' uses the stale 'agent' term for the orchestrator (→ DevStudio); the mesh-only invariant is correct and conforms to ADR-003/007 + nebula-dev-flows.md ('All node↔node edges are mesh (lmz.call), never raw Workers RPC').
        → Term-swap: 'data + DevStudio comms over mesh, never raw primitives.' Optionally strengthen to match nebula-dev-flows.md: any node can call any other over mesh, including the browser client (Flow 1b server→client mesh call).
  - [ ] **[rewrite]** Pinned-bullet restatement of the reversed source-of-truth (DevContainer DO storage + DevSource table). Canonical: DevStudio. This is inside the 'Pinned (carried from the experiment + the reviewed original specs)' block, so it reads as a settled commitment.
        → Replace with: 'Source durability — owned by DevStudio (shell Workspace + local git). The container working tree is ephemeral; DevStudio re-populates it by pushing the full tree on cold boot (applyChanges, Flow 1c). No DevContainer DO store / DevSource table. Survives Star .dev-data wipes (different DO).'

### nebula-studio.md — 26 findings · rewrite-section
  - ⚠ **banner-hazard:** YES — the § "Durable draft ownership — the DevContainer DO is the dev source-of-truth (UPDATED 2026-06-18; was 'Galaxy is the source of truth')" header (line 117) plus its sub-bullet "Superseded by tasks/nebula-container-dev-loop.md Q#2" (line 118) IS a Phase-3.5 superseding banner whose conclusion the 2026-06-19 pivot now RE-supersedes. The banner correctly retired the OLD "Galaxy is source of truth" design, but its replacement ("DevContainer DO is source-of-truth with dual-write to DevContainer DO SQL / re-hydrate from DO SQL on cold boot") is now itself WRONG per Decision 5/7: DevStudio (shell Workspace + local git) is the source-of-truth; DevContainer is a disposable checkout that holds NO durable source and is repopulated by DevStudio's applyChanges push on cold boot (Flow 1c). The re-supersede must flip the destination from DevContainer→DevStudio and the mechanism from "DevContainer dual-write / DO-SQL re-hydrate" → "DevStudio shell Workspace + git, push (applyChanges) to a disposable container," and repoint the banner from nebula-container-dev-loop.md Q#2 to nebula-dev-flows.md (Decisions 5/7).
  - [ ] **[rewrite]** States the dev Star is a `DevStar extends Star` subclass with `deployToDev()`. Authority Decision 2 eliminates the `DevStar` class entirely — the dev Star is a plain `Star` at the `{u}.{g}.dev` instance; only `resetDevData` (the .dev-guarded wipe) differs. `compileSFC` and `deployToDev` are deleted (Decision 9: ontology edits are normal writeSource→commit→push; DevStudio applies the compiled validator to the dev Star; the Star never compiles).
        → Replace with: "and the dev Star (`tasks/dev-star.md`, `fa9d4fb` — a plain `Star` at the `{u}.{g}.dev` instance, with the `.dev`-guarded `resetDevData()` wipe; there is no `DevStar` subclass and no `deployToDev`/`compileSFC` — see `tasks/nebula-dev-flows.md` Decisions 2 & 9)."
  - [ ] **[term-swap]** References `DevStar` as a class and a sibling base class. Authority Decision 2: no `DevStar` class; the data Star is a plain `Star` at `.dev`. The static path is also better described per the canonical Topology/DevContainer-internals diagrams (Preview app → DevContainer fetch() proxy; data → Star via /gateway mesh WS).
        → Term-swap `DevStar` → `the Star at `.dev`` (or `the `{u}.{g}.dev` Star instance`) in both occurrences; drop "different base class" / "sibling … not the Star" framing (DevContainer is its own node type per nebula-devcontainer-node-type.md, but the data Star is just `Star`). Optionally repoint the static-vs-data path description to `tasks/nebula-dev-flows.md` § Topology.
  - [ ] **[rewrite]** Names "Galaxy dual-write/rehydrate" as the source-durability answer. Authority Decision 7 explicitly purges all three conflicting source-durability designs — (a) Galaxy dual-write, (b) DevContainer DO store, (c) dev-Star file-resources — collapsing them into DevStudio's shell `Workspace` + local git. Galaxy is published-only (Decision 4/9).
        → Rewrite to: "Source durability is owned by **DevStudio's shell `Workspace` + local git** (isomorphic-git); the container working tree is a disposable checkout DevStudio re-populates by pushing (`applyChanges`) on cold boot. The Galaxy dual-write / DevContainer-DO-store / dev-Star-file-resource designs are all eliminated — see `tasks/nebula-dev-flows.md` Decisions 5–7."
  - [ ] **[pointer]** The reshape description is now superseded by the canonical model. The compile pipeline does not live in a dev Star/Star at all (Decision 9: compile runs in DevStudio for dev, Galaxy for prod; Star never compiles). The app-versioning concept is further changed: Decision 9 drops the app-vs-ontology-version distinction (one unified app-version = ontology version) and eliminates the Galaxy ontology-version registry.
        → Repoint to the canonical doc: "The dev-loop architecture is now canonically specified in `tasks/nebula-dev-flows.md` (DevStudio = orchestrator + source-of-truth; Galaxy = published-only; one unified app-version = ontology version). The #1a/#1b reshape conforms to that doc — compile moves into DevStudio (dev)/Galaxy (prod), the Star never compiles, and there is no Galaxy ontology-version registry."
  - [ ] **[term-swap]** Uses "Agent" for the server orchestrator and describes its home generically. Authority Decision 1: the orchestrator is named **DevStudio** (the term "agent" is deliberately avoided to prevent confusion with Cloudflare's agent toolkit). DevStudio is a Nebula-owned server DO (LumenizeDO-family).
        → Term-swap "Agent home" → "DevStudio home" and rephrase: "**DevStudio** (the codegen engine) is a Nebula-owned server DO, co-located with the user for low-latency iteration … Because it's our class, our scope isolation applies. (We avoid the term 'agent' to prevent confusion with Cloudflare's agent toolkit — see `tasks/nebula-dev-flows.md` Decision 1.)"
  - [ ] **[rewrite]** Says Galaxy hosts generated artifacts (source), current ontology, and chat/session state. Authority: Galaxy is the **published-only** app-version registry (Decision 4) — it never holds dev source or the ontology-version registry (Decision 9), and DevStudio (not Galaxy) is the source-of-truth (Decision 5) and the orchestrator the chat session talks to (Topology: Studio UI talks only to DevStudio). Source + session/working state belong to DevStudio's shell Workspace.
        → Rewrite to: "**DevStudio hosts the dev source + chat/session state** — the source tree (shell `Workspace` + local git), per-session chat history/working state, and accumulated memory/patterns. **Galaxy hosts only the published app-version** (built bundle + assets), never dev source or an ontology-version registry. See `tasks/nebula-dev-flows.md` Decisions 4, 5, 9."
  - [ ] **[rewrite]** Calls the dev Star "the built `DevStar`" and says Studio drives `deployToDev()`. Authority Decision 2 (no DevStar class — plain Star at `.dev`) and Decision 9 (`deployToDev` is replaced — DevStudio applies the compiled validator to the dev Star on save; no pull-from-Galaxy step). Only `resetDevData` (.dev-guarded) survives.
        → Rewrite to: "Each chat session works against the dev Star — a plain `Star` at the `{u}.{g}.dev` instance (own SQLite, isolated from prod). DevStudio applies the compiled validator to it on save (replacing the old `deployToDev` pull-from-Galaxy) and calls the `.dev`-guarded `resetDevData()` on a breaking ontology change (Flow 1b). See `tasks/nebula-dev-flows.md` Decisions 2, 9, 11."
  - [ ] **[delete]** This entire section makes dev-Star `file` resources the home for application source. Authority Decision 7 explicitly eliminates design (c) 'file-resources on the dev Star' as one of the three conflicting source-durability designs — source now lives ONLY in DevStudio's shell `Workspace` + git. The dev Star is data-only (Decision 11). The `store.resources.file['App.vue']` example and the 'edits files like UI resource writes' rationale are both stale.
        → Delete the section (and its `store.resources.file[...]` code block + 'Open/post-demo' bullets that depend on it), replacing with a short pointer: "Application source is owned by **DevStudio's shell `Workspace` + local git** (the source seam), NOT by `file` resources on the dev Star. The dev Star is data-only. See `tasks/nebula-dev-flows.md` § Source abstraction and Decisions 5–7." (The export-to-GitHub / big-file notes can move to the file-storage-backend task if still wanted.)
  - [ ] **[banner]** REVERSED SOURCE-OF-TRUTH. This Phase-3.5 banner names the **DevContainer DO** as source-of-truth with dual-write to DO SQL and DO-SQL re-hydration. Authority Decision 5/7: **DevStudio** (shell `Workspace` + local git) is the source-of-truth; DevContainer holds NO durable source — it is a disposable checkout DevStudio pushes to (`applyChanges`) and which reverts to the baseline image on cold boot, repopulated by DevStudio's push (Flow 1c). Decision 5 explicitly REJECTS 'truth in DevContainer DO' as Option 1. Also 'agent' → DevStudio, and `writeFile`/dual-write → `writeSource` (local commit) + `applyChanges` (push). Pointer is stale (→ nebula-container-dev-loop.md Q#2).
        → Rewrite the heading to "### Durable source ownership — DevStudio is the dev source-of-truth (RE-SUPERSEDED 2026-06-19; was 'DevContainer DO'/'Galaxy')" and the body to: "**Superseded by [`tasks/nebula-dev-flows.md`](nebula-dev-flows.md) Decisions 5–7.** The user-developer's source (ontology `.d.ts` + `.vue` files) is durably owned by **DevStudio's shell `Workspace` (SQLite+R2) + local git** (isomorphic-git). DevStudio's `writeSource` writes the working copy + `git commit` locally, then **pushes** changed files to the container via `applyChanges` (mesh). The DevContainer holds **no durable source**: its disk reverts to the baseline image on cold boot and DevStudio re-populates it by pushing (Flow 1c). The earlier 'DevContainer DO is source-of-truth / dual-write to DO SQL / re-hydrate from DO SQL' design is rejected (nebula-dev-flows.md Decision 5, Option 1)."
  - [ ] **[rewrite]** Correct that the dev Star is data-only and the reset-precondition is moot, but says the source role 'moved to the DevContainer' — wrong destination per Decision 5/7 (moved to DevStudio). Also 'DevStar' class term (Decision 2).
        → Rewrite to: "- **The dev Star (plain `Star` at `.dev`) = dev DATA only.** Its former source-`file`-resources role moved to **DevStudio** (shell `Workspace` + git), not the DevContainer. A breaking-edit `resetDevData()` wipes data but cannot lose source (source lives in a different DO — DevStudio), so the old 'persist-before-wipe + re-hydrate-from-Galaxy' gate is moot."
  - [ ] **[rewrite]** Galaxy-published-only and 'DevContainer runs vite build → pushes built bundle to Galaxy' are correct (Flow 2). But 'Its former dev-draft-source role moved to the DevContainer' is wrong — it moved to DevStudio (Decision 5/7). Publish is also DevStudio-ORCHESTRATED (Decision 4: Studio UI → DevStudio → DevContainer → Galaxy), which this bullet omits.
        → Rewrite to: "- **Galaxy = published app-version registry only.** Its former dev-draft-source role moved to **DevStudio**, not the DevContainer. **Publish** is DevStudio-orchestrated (Studio UI → DevStudio → DevContainer → Galaxy): DevStudio issues a fast `build & publish` command; the DevContainer runs `vite build` from its current checkout and pushes the **built bundle** to Galaxy; prod Stars lazy-pull. Galaxy receives the built artifact, never dev source. See `tasks/nebula-dev-flows.md` Flow 2 & Decision 4."
  - [ ] **[rewrite]** Attributes the supersession to 'the DevContainer dual-write'. Per Decision 5/7 the durable mechanism is DevStudio's local git commit + push, not a DevContainer dual-write. Also the data-reset trigger is now a server→client mesh prompt (Flow 1b), and on any ontology change the user decides (no breaking-change detection).
        → Rewrite to: "- **What remains DevStudio-owned:** the data-reset **trigger** (a server→client mesh prompt on **any** ontology `.d.ts` change — Flow 1b — the user judges breaking-ness; no detection logic) and the **publish trigger** (DevStudio orchestrates DevContainer→Galaxy build-push). The old Galaxy-draft-store / per-turn-autosave / confirm-durable-before-wipe gate are superseded by DevStudio's local git commit + push (`applyChanges`) — no longer needed."
  - [ ] **[rewrite]** Checkpoints are described as a tag of 'the DevContainer DO source store'. There is no DevContainer DO source store (Decision 5/7). Authority Decisions 6/10: 'checkpoint' = a git tag (raw isomorphic-git) on DevStudio's local git.
        → Rewrite to: "- **Named checkpoints** are a **git tag** (raw isomorphic-git) on DevStudio's local git history — *not* a DevContainer store and *not* dev-Star snapshot history (a reset destroys that). See `tasks/nebula-dev-flows.md` Decisions 6 & 10."
  - [ ] **[rewrite]** Preview refresh is attributed to 'the dev Star's compile→reload broadcast'. Authority: the dev Star does not compile (Decision 9) and does not drive preview reload — the preview is served by the **DevContainer** running real vite, and updates land via **vite HMR** (Flow 1, Flow 1c, DevContainer internals), not a Star broadcast.
        → Rewrite to: "**Auto-reload is via real vite HMR from the DevContainer** (the container runs vite; DevStudio pushes changed source with `applyChanges` → vite HMRs the preview). The old dev-Star compile→reload broadcast is gone (the Star no longer compiles). See `tasks/nebula-dev-flows.md` Flow 1."
  - [ ] **[rewrite]** Directly contradicts authority: checkpoint UX 'rides the durable Galaxy draft store, NOT git'. Authority Decisions 6/10: checkpoint IS a git tag on DevStudio's local git; there is no Galaxy draft store.
        → Rewrite to: "- **Checkpoint UX** rides DevStudio's local **git** (a checkpoint is a git tag, Decisions 6/10) — not a Galaxy draft store."
  - [ ] **[rewrite]** Marked open, but authority Decision 3 RESOLVES it: Studio UI is served from **Workers Assets** (same origin as /auth, /gateway, /dev-container).
        → Replace with a resolved note: "**Studio UI hosting — RESOLVED:** served from **Workers Assets**, same origin as `/auth`, `/gateway`, `/dev-container` (`tasks/nebula-dev-flows.md` Decision 3)." Remove from the Open-questions list (line 187) as well.
  - [ ] **[delete]** Listed under Open questions but resolved by Decision 3 (Workers Assets).
        → Delete this bullet (resolved — see the § Authoring environment edit above).
  - [ ] **[banner]** Direction is right (in-DO compile superseded), but the section below it still describes `compileSFC`/reload-fanout landing on `DevStar`. Authority Decision 9: compile lives in DevStudio (dev)/Galaxy (prod), Star never compiles; Decision 2: no DevStar. The banner should also point at the canonical doc.
        → Strengthen the banner to point at the canonical model: "⚠️ SUPERSEDED — the dev loop is now canonically specified in `tasks/nebula-dev-flows.md`. In-DO/Star compile is gone: real `vite` runs in the DevContainer and compile lives in DevStudio (dev)/Galaxy (prod); the Star never compiles (Decision 9). There is no `DevStar` (Decision 2). This section survives only as the `in-do-compile-baseline` fallback description."
  - [ ] **[term-swap]** Says SFC compile + reload-broadcast land on `DevStar`. Authority Decision 2 (no DevStar) + Decision 9 (Star never compiles; compile in DevStudio/Galaxy; preview reload is vite HMR from the DevContainer).
        → Since this is fallback-only text under the superseded banner, leave the historical spike description but prefix the paragraph with: "(Historical, `in-do-compile-baseline` only — superseded; the live model compiles in DevStudio/Galaxy and reloads via vite HMR, not a Star broadcast.)" Term-swap `DevStar` → `the dev Star` where it must remain.
  - [ ] **[rewrite]** Describes the three-step deploy_to_dev: (1) publish ontology version to a Galaxy registry, (2) `DevStar.deployToDev()`, (3) compile+publish UI bundle. All three steps contradict authority: no Galaxy ontology-version registry (Decision 9), no DevStar/deployToDev (Decisions 2/9), and the Star doesn't compile (Decision 9). The live flow: DevStudio writeSource→commit→push (applyChanges)→vite HMR; ontology edit compiles the validator in DevStudio and DevStudio applies it to the dev Star.
        → Rewrite the tool description to the live model: "`apply_change` — DevStudio writes source (`writeSource` + local git commit) and pushes changed files to the DevContainer (`applyChanges`) → vite HMRs the preview. On an ontology `.d.ts` change, DevStudio compiles the validator and applies it to the dev Star (after the Flow 1b wipe decision). No Galaxy ontology-version publish, no `deployToDev`, no in-Star compile — see `tasks/nebula-dev-flows.md` Flow 1 & Decision 9."
  - [ ] **[pointer]** Under the (correct) superseded banner at line 206, but the #1a description still says 'Compile .vue SFCs in DevStar → serve from Star.onRequest'. Authority: no DevStar (Decision 2), Star never compiles/serves the app bundle (Decision 9; DevContainer vite serves the shell, Workers Assets serves prod).
        → Since line 206 already flags #1a/#1b as superseded, append a pointer rather than restate stale mechanics: after the line-206 banner add "The live dev-loop mechanics are canonical in `tasks/nebula-dev-flows.md` (DevStudio source-of-truth + push; DevContainer vite serves the preview; Star never compiles or serves the bundle)." Optionally term-swap `DevStar` → `Star at `.dev`` in the retained historical text.
  - [ ] **[rewrite]** 'parallel source durability to Galaxy' is the eliminated Galaxy-dual-write design (Decision 7, design (a)). Also Decision 9 drops the app-vs-ontology-version distinction (one unified version) and eliminates the Galaxy ontology-version registry. The lazy-pull description is fine (Flow 2b) but is keyed by app-version=ontology-version now.
        → Repoint and correct: "#1b — App versioning realigns to `tasks/nebula-dev-flows.md`: **one unified app-version = ontology version** (Decision 9; the app-vs-ontology-version distinction is dropped), Galaxy is **published-only** (no ontology-version registry, no Galaxy source dual-write — source durability lives in DevStudio, Decision 7). Prod distribution is the Star lazy-pull on a version mismatch (Flow 2b)."
  - [ ] **[other]** Cross-reference / cast gap: the file never establishes the canonical cast distinctions (Studio UI / Preview app / App UI / DevStudio) that nebula-dev-flows.md § Downstream explicitly asks this file to add. The file conflates 'Studio' (the orchestrator) with the browser chat app and the preview.
        → Add a short Cast subsection near the top (e.g. under § Goal) introducing the canonical participants per `tasks/nebula-dev-flows.md` § Cast: **Studio UI** (browser chat, served from Workers Assets), **Preview app** (the running generated app in the iframe, loads shell+HMR from DevContainer, data from the Star), **DevStudio** (server DO orchestrator + source-of-truth), **DevContainer** (disposable vite checkout), **Star** (plain `Star`; dev = `.dev` instance), **Galaxy** (published-only). Then use these terms consistently throughout.
  - [ ] **[pointer]** Points dev-Star lifecycle SoT to `tasks/dev-star.md`, which is itself being retired/realigned (nebula-dev-flows.md § Downstream lists dev-star.md for retirement to 'the dev Star is a Star at .dev'). The canonical home for the ontology-change → wipe lifecycle is now nebula-dev-flows.md Decision 11 / Flow 1b.
        → Repoint: "the dev-Star data-on-ontology-change lifecycle (any ontology change → server→client wipe prompt; user decides; `.dev`-guarded `resetDevData`) is canonical in `tasks/nebula-dev-flows.md` Decision 11 & Flow 1b."
  - [ ] **[rewrite]** Scope/version injection is attributed to `Star.onRequest`. Authority: the preview shell is served by the **DevContainer** (vite), and scope injection happens at the DevContainer fetch() proxy (DevContainer internals: 'scope injected'); the Star no longer serves the shell HTML (Decision 9). Prod serving is Workers Assets.
        → Rewrite to: "`appVersion`/`authScope`/`activeScope` are injected by the serving layer — in dev by the **DevContainer fetch() proxy** as it serves the vite shell (server-derived scope), in prod by the Workers-Assets serving path. Not `Star.onRequest` (the Star no longer serves the shell). See `tasks/nebula-dev-flows.md` Topology & DevContainer internals."
  - [ ] **[rewrite]** Minor/secondary: 'Generated TS is deployed to DWL isolates' and 'tsc-in-DWL' describe the pre-pivot in-DO compile/serve model. Under the pivot, generated `.vue`/TS is built by real vite in the DevContainer (dev) / at publish (Galaxy); the ontology `.d.ts` compiles to a validator in DevStudio (dev)/Galaxy (prod) via `@lumenize/ts-runtime-parser-validator`. The Star/DWL-isolate compile path is gone.
        → Term-adjust: "Generated `.vue`/TS is built by real **vite** in the DevContainer (dev) and at publish (Galaxy); the ontology `.d.ts` compiles to a runtime validator in **DevStudio** (dev)/**Galaxy** (prod) via `@lumenize/ts-runtime-parser-validator` (shipped). The Star never compiles."

### dev-star.md — 29 findings · rewrite-section
  - ⚠ **banner-hazard:** YES — two "UPDATED 2026-06-18 / superseded by nebula-container-dev-loop.md Q#2" banners (the § In-dev data lifecycle blockquote at line 44, and the P3 final "Out of scope" item at line 95) both conclude that "source now lives in the DevContainer DO's own storage" and that the wipe "cannot lose source (different DOs)". That conclusion is itself now RE-superseded by nebula-dev-flows.md Decision 5: source-of-truth is DevStudio (shell Workspace + local git), NOT the DevContainer. The banners' relief reasoning happens to land on the same safe outcome ("a Star wipe never touches source") but for the WRONG reason (DevContainer storage vs DevStudio). Both banners must be re-pointed: replace "DevContainer DO's own storage / re-hydrated into the container tree on cold boot" with "DevStudio (shell Workspace + local git); the DevContainer is a disposable checkout DevStudio pushes source to (applyChanges)", and re-target the pointer from nebula-container-dev-loop.md Q#2 to nebula-dev-flows.md (Decision 5 + Flow 1c). The parenthetical "(Historical: ... dev Star held source file resources dual-written to Galaxy; that role moved to the DevContainer ...)" at line 44 is doubly wrong — that role moved to DevStudio, not the DevContainer.
  - [ ] **[banner]** The whole file describes built work predicated on a DevStar class + DEV_STAR binding + Galaxy-pull eager-apply, all of which nebula-dev-flows.md reverses (Decisions 2, 5, 9). The status omits any pointer to the canonical doc that now supersedes its architecture, and presents pre-pivot code as conformant.
        → Add a dated superseding banner at the very top of the file: '⚠️ SUPERSEDED 2026-06-19 by tasks/nebula-dev-flows.md (canonical dev/publish model). There is NO DevStar class — the dev data Star is a plain Star at the {u}.{g}.dev instance (Decision 2); the DEV_STAR binding, slug-derived #starBinding(), and eager deployToDev-pull-from-Galaxy are all eliminated. Source-of-truth is DevStudio (shell Workspace + local git), not the dev Star or the DevContainer. What survives from this file: the .dev-guarded data-wipe (now resetDevData) + the additive-preserved/breaking-wipe data bargain, re-homed onto plain Star and triggered via the Flow 1b server→client wipe prompt.' Keep the historical body below for provenance.
  - [ ] **[rewrite]** nebula-dev-flows.md Decision 2 eliminates the DevStar subclass entirely: 'No DevStar class. Collapse to one Star on one STAR binding; the dev Star is the {u}.{g}.dev instance.' The pivot also removes eager version application and the in-DO compile+reload pipeline (Decision 9: Star never compiles; DevStudio pushes the compiled validator). The only dev-only behavior that survives is the .dev-guarded resetDevData.
        → Rewrite to: the dev sandbox is a plain Star at the {u}.{g}.dev instance (its own SQLite already isolates it from prod). No DevStar subclass. The only dev-specific surface is a single hard-guarded resetDevData that throws unless the instance name ends in '.dev'. Setting the current ontology (the compiled validator DevStudio pushes) is a general Star op, not dev-only.
  - [ ] **[term-swap]** References the nonexistent-going-forward DevStar class. The scope-isolation point is correct and should be retained, but attributed to a plain Star at the .dev instance, not a DevStar subclass (Decision 2).
        → Term-swap: '`DevStar` inherits it unchanged' → 'the dev Star (a plain Star at {u}.{g}.dev) gets it unchanged'; '...doesn't touch DevStar' → '...doesn't touch the dev Star.'
  - [ ] **[rewrite]** Three of the four 'dev-only behaviors' are now eliminated by the pivot: (1) 'eager version application' — Decision 9 replaces deployToDev-pull-from-Galaxy with DevStudio pushing the compiled validator on save; (2) 'the Studio compile + reload pipeline' — Decision 9 says the Star never compiles (compile lives in DevStudio for dev, Galaxy for prod), and reload is vite HMR in the DevContainer; (3) the isolating 'DevStar subclass' itself is removed (Decision 2). Only 'data reset' (resetDevData) survives.
        → Rewrite: 'The dev Star is a plain Star at {u}.{g}.dev — same SQLite, DAG tree, subscriptions, per-Star permissions, scope-isolation, and Galaxy-admin bypass as any Star. The ONLY dev-specific addition is a .dev-guarded resetDevData (the data wipe). It never compiles (DevStudio compiles in dev, Galaxy at publish — Decision 9) and never pulls versions from Galaxy (DevStudio pushes the compiled validator on save).'
  - [ ] **[delete]** nebula-dev-flows.md Decision 2 eliminates the DEV_STAR binding and slug-derived routing: 'The DEV_STAR binding + its smart-match guard disappear; #starBinding() collapses to always STAR.' So 'dev' is NOT structurally a separate binding; it is just an instance name on the single STAR binding. The reservation reverts to a pure name-claim (still valid, but not binding-backed).
        → Delete the 'Stronger under slug-derived binding' sub-bullet (the structural-binding reinforcement no longer exists). The plain reservation point above it stands; optionally add a one-liner that with the single STAR binding, the dev sandbox is the {u}.{g}.dev instance and the reservation is enforced wherever a Star-creation gate eventually lands.
  - [ ] **[other]** Not a contradiction — this reasoning rests on inherited Star onBeforeCall scope checks + per-Star permissions, all of which survive on a plain Star at .dev. Keep as-is.
        → No change required (the reasoning is binding-agnostic). Optionally drop any incidental 'dev DO' phrasing that implies a separate class, but the substance conforms.
  - [ ] **[rewrite]** The 'eager-apply' mutator (deployToDev) is eliminated by Decision 9 (no Galaxy version-pull; DevStudio pushes the compiled validator). Only 'reset' (resetDevData) remains as a dev mutator. The @mesh(requireAdmin) gating principle is correct and still applies to resetDevData and to setting the current ontology (which, per Decision 11, is a general Star op DevStudio calls).
        → Rewrite the list of dev mutators to just resetDevData: 'The dev wipe resetDevData is a bespoke @mesh method bypassing the DAG permission checks, so it carries @mesh(requireAdmin) directly (matching the claims.access.admin model). Without it, anyone able to mint a valid {u}.{g}.dev aud could trigger a data-wiping reset.' Drop 'eager-apply' / 'forced re-apply' references.
  - [ ] **[banner]** Phase-3.5 banner now RE-superseded by nebula-dev-flows.md Decision 5: source-of-truth is DevStudio (shell Workspace + local git), NOT the DevContainer DO. The DevContainer holds no durable source — it is a disposable checkout DevStudio pushes to (applyChanges). The conclusion ('a wipe never touches source') is still true but the stated reason (source in DevContainer storage) is wrong. The 'DevStar wipe' phrasing also references the eliminated class.
        → Rewrite the banner: '⚠️ Reset precondition — RESOLVED by tasks/nebula-dev-flows.md (Decision 5): source-of-truth is DevStudio (shell Workspace + local git); the DevContainer is a disposable checkout DevStudio pushes source to (applyChanges). The dev Star holds ONLY dev DATA (Resources). So resetDevData wipes data but cannot lose source — it lives in a different DO (DevStudio). No source-durability precondition remains.' Re-point the link from nebula-container-dev-loop.md Q#2 to nebula-dev-flows.md. Term-swap 'DevStar wipe' → 'dev-Star wipe'.
  - [ ] **[rewrite]** Doubly wrong vs nebula-dev-flows.md Decision 7 (the convergence): the three conflicting source-durability designs — (a) Galaxy dual-write, (b) DevContainer DO store, (c) dev-Star file resources — ALL collapse into DevStudio's shell Workspace. The role did NOT move to the DevContainer; it moved to DevStudio. The 'Galaxy = published-only' half is correct (Decision 4/9).
        → Rewrite the historical aside: '(Historical: earlier designs scattered dev source across three homes — Galaxy dual-write, a DevContainer DO store, and dev-Star `file` resources. All three are superseded; source now lives solely in DevStudio (shell Workspace + local git — Decision 7). Galaxy holds only the published app-version registry, not dev-draft source.)'
  - [ ] **[rewrite]** nebula-dev-flows.md Decision 11 + Flow 1b now RESOLVE the trigger question: DevStudio does NOT detect breaking-ness; on ANY ontology .d.ts change it prompts the user via a direct server→client mesh call (promptWipe → wipeDecision callback), and a standalone Studio-UI 'Wipe .dev data' button is the manual path. Both call the .dev-guarded resetDevData. The file leaves this open and points to nebula-studio.md instead of the now-canonical Flow 1b.
        → Rewrite: 'The reset trigger is resolved in tasks/nebula-dev-flows.md (Decision 11 + Flow 1b): DevStudio does not auto-detect breaking changes — on any ontology .d.ts change it asks the user via a direct server→client mesh call (promptWipe, with a wipeDecision callback so DevStudio isn't billed waiting), and a standalone Studio-UI "Wipe .dev data" button is the manual safety net. Both call the .dev-guarded resetDevData; when a wipe is part of a save the source-push gates on the wipe decision.'
  - [ ] **[rewrite]** Directly contradicts Decision 2 (no DevStar class) and Decision 9 (Star never compiles; no eager version application via Galaxy-pull; reload is vite HMR in the DevContainer, not a Star broadcast pipeline). The 'data wipe must not exist on a tenant Star' safety concern is now satisfied not by a subclass but by hard-guarding resetDevData to .dev instances (Decision 2: 'if (!instanceName.endsWith('.dev')) throw').
        → Rewrite the Implementation shape opener: 'There is no DevStar subclass. The dev sandbox is a plain Star at {u}.{g}.dev. The single dev-specific method resetDevData lives on Star but is hard-guarded to .dev instances (if (!instanceName.endsWith('.dev')) throw) so a tenant Star can never wipe — the guard, not a separate class, is the footgun barrier. The dev Star never compiles (DevStudio/Galaxy do — Decision 9) and never eager-pulls versions from Galaxy (DevStudio pushes the compiled validator on save).'
  - [ ] **[delete]** Decision 2 eliminates the entire DEV_STAR binding + slug-derived binding-selection apparatus: 'The DEV_STAR binding + its smart-match guard disappear; #starBinding() collapses to always STAR.' This whole 'Naming & binding selection' subsection (the footgun about the Gateway trusting the client-supplied binding, the choke-point design, the dual-namespace risk) is moot — there is one STAR binding and one routing path.
        → Delete the entire 'Naming & binding selection' bullet and its sub-bullets (Client #starBinding(), DO→DO callers naming DEV_STAR, routeDORequest smart-match, Connection binding). Replace with a one-liner: 'Binding: there is one STAR binding; the dev Star is the {u}.{g}.dev instance on it. #starBinding() collapses to always STAR (Decision 2); no DEV_STAR binding, no slug-derived binding selection.'
  - [ ] **[delete]** Decision 2: '#starBinding() collapses to always STAR.' The binary DEV_STAR/STAR helper is removed; all Star-targeting calls go to STAR regardless of slug. This sub-bullet describes machinery the pivot deletes.
        → Delete this sub-bullet. (If a residual note is wanted: '#starBinding() is gone — every Star call targets STAR; the .dev slug selects the instance, not the binding.')
  - [ ] **[delete]** Decision 2 removes the DEV_STAR binding; DO→DO callers use STAR with the .dev instance name. Also note the canonical topology has DevStudio (not Galaxy) pushing to the dev Star ('apply ontology · mesh'); Galaxy is published-only (Decision 4/9), so a 'Galaxy → dev Star' example is itself stale.
        → Delete. If a DO→DO example is needed, use: 'DevStudio applies the compiled validator to the dev Star via lmz.call("STAR", "{u}.{g}.dev", …) (Decision 9; mesh only — ADR-003/007).' Galaxy does not call the dev Star.
  - [ ] **[delete]** The DEV_STAR binding and the dev-star smart-match concern are eliminated by Decision 2. There is no dev-star binding to smart-match and no MultipleBindingsFoundError risk from it.
        → Delete this sub-bullet. No dev-star binding exists post-pivot.
  - [ ] **[delete]** References the eliminated DevStar class and the eliminated #starBinding() choke point (Decision 2). The underlying gateway/connection facts are unchanged, but the framing ('DevStar needs no connection-time change', 'the per-call #starBinding() choke point') is stale.
        → Delete or fold into the single-binding one-liner: 'The WS connection targets the gateway binding; each call carries its own target binding (now always STAR). No dev-specific connection wiring.'
  - [ ] **[rewrite]** nebula-dev-flows.md Decision 9 eliminates this entire mechanism: 'DevStudio applies the compiled validator to the dev Star (replacing the old deployToDev-pulls-from-Galaxy step).' There is no separate ontology-version registry on Galaxy ('appendOntologyVersion/getLatestOntologyVersion subsystem is eliminated'), and the Star never compiles. The dev Star receives the compiled validator via a DevStudio push (Decision 11: 'Setting the current ontology ... is a general Star op — DevStudio pushes it to the .dev Star on save'). The Open/to-confirm section confirms: 'The Flow 1 opt should read "ontology .d.ts changed → apply compiled validator," not "deployToDev (pull from Galaxy)."'
        → Replace the entire 'Eager version application, reusing the existing apply path' bullet with: 'Ontology application — DevStudio pushes the compiled validator. The dev Star does not pull versions from Galaxy. Per Decision 9/11, ontology is just a source file; DevStudio compiles the .d.ts to a runtime validator and pushes it to the dev Star on save via lmz.call("STAR", "{u}.{g}.dev", setCurrentOntology, …) (a general Star op, not dev-only). Setting the current ontology touches no data. The lazy Galaxy-pull (getLatestOntologyVersion) remains PROD-only behavior on a stale Star (Flow 2b); it is not the dev path.' Drop the protected applyFetchedState-hook discussion as a dev-loop concern (it may remain as a general prod-lazy-pull detail, but not as the dev mechanism).
  - [ ] **[banner]** The entire P2 phase implements deployToDev pulling getLatestOntologyVersion from Galaxy — the exact mechanism Decision 9 eliminates (no version registry; DevStudio pushes the compiled validator). The built work predates the pivot and no longer reflects the canonical model.
        → Mark P2 as superseded: '⚠️ P2 SUPERSEDED by nebula-dev-flows.md Decision 9 — there is no deployToDev / Galaxy version-pull in the dev loop. DevStudio compiles the ontology .d.ts and PUSHES the compiled validator to the dev Star on save (setCurrentOntology, a general Star op). The eager-Galaxy-pull (deployToDev, applyFetchedState via getLatestOntologyVersion) is replaced; that path survives only as PROD lazy-pull (Flow 2b).' Keep the historical checklist beneath the banner for provenance.
  - [ ] **[banner]** P1's entire deliverable — the DevStar class, the DEV_STAR binding, slug-derived binding selection, the 16 #starBinding() call sites, the dev-star-client-binding structural guard test — is reversed by Decision 2 (no DevStar class; #starBinding() collapses to always STAR; DEV_STAR binding disappears).
        → Mark P1 as superseded: '⚠️ P1 SUPERSEDED by nebula-dev-flows.md Decision 2 — no DevStar class, no DEV_STAR binding, no slug-derived #starBinding(). The dev Star is a plain Star at the {u}.{g}.dev instance on the single STAR binding; #starBinding() collapses to always STAR. The realignment must unwind the DEV_STAR binding (wrangler + test wranglers), the #starBinding() helper + its 16 call sites, and the dev-star-client-binding structural guard test.' Keep the historical checklist for provenance.
  - [ ] **[term-swap]** References DevStar.resetDevData (the eliminated class). The reset method itself survives but moves onto plain Star, hard-guarded to .dev instances (Decision 2). The onStart re-init mechanics are otherwise valid and binding-agnostic.
        → Term-swap 'DevStar.resetDevData' → 'Star.resetDevData (.dev-guarded)'. The mechanics (onStart nulls #row/#facet, etc.) carry over unchanged since resetDevData now lives on Star directly.
  - [ ] **[rewrite]** DevStar.resetDevData references the eliminated class. Per Decision 2 the method moves to Star and gains the .dev instance-name hard-guard ('if (!instanceName.endsWith('.dev')) throw') in ADDITION to @mesh(requireAdmin) — the guard is what replaces the subclass as the footgun barrier.
        → Rewrite: 'Star.resetDevData() = @mesh(requireAdmin) async, hard-guarded to .dev instances (if (!instanceName.endsWith(".dev")) throw — the guard, not a subclass, prevents a tenant Star from wiping), body blockConcurrencyWhile(deleteAll → this.onStart()).'
  - [ ] **[rewrite]** Decision 2 reverses the footgun barrier: resetDevData now DOES live on base Star, and the barrier is the .dev instance-name hard-guard instead of a subclass. 'Defined on DevStar only' / 'not reachable on a STAR instance' is exactly the inverted invariant.
        → Rewrite the footgun-guard criterion: 'Footgun guard: resetDevData lives on Star but throws unless the instance name ends in .dev — verified behaviorally (calling resetDevData on a {u}.{g}.tenant instance throws; on {u}.{g}.dev it wipes). Capable-of-failing: remove the .dev guard → a tenant-instance call wipes that tenant.'
  - [ ] **[rewrite]** References DevStar as a distinct ctor with its own prototype. Post-pivot there is no DevStar; resetDevData is a Star method, so the surface-freeze assertion must be expressed over Star's added dev methods (or simply assert resetDevData is admin-gated + .dev-guarded).
        → Term-swap/retarget: 'assert resetDevData (now on Star) is admin-gated — nonAdminMeshMethods(Star) excludes resetDevData (or === [] for the dev-method set). The own-prototype helper now runs against Star, not a DevStar subclass.' Adjust the helper-walk wording to drop the DevStar-vs-Star inheritance framing.
  - [ ] **[term-swap]** DevStarTest references the eliminated DevStar class hierarchy. Post-pivot the test hook lives on a Star test subclass (StarTest), exercising the .dev instance.
        → Term-swap 'DevStarTest' → 'StarTest' (the inspection hook moves onto the Star test subclass, exercised against a {u}.{g}.dev instance). Apply the same swap to all DevStarTest references throughout P1–P4 (e.g. 'callDevStarWhoAmI', 'inspectReset', 'resetAndProbeRootAdmin').
  - [ ] **[banner]** Second Phase-3.5 banner with the now-re-superseded conclusion: source-of-truth is DevStudio, NOT the DevContainer (Decision 5). Also the trigger question is now resolved by Decision 11 + Flow 1b (any-ontology-change prompt via server→client mesh call + standalone button), not deferred to nebula-studio.md. 'source durability is the DevContainer's' is wrong — it is DevStudio's.
        → Rewrite: 'Reset trigger — RESOLVED in nebula-dev-flows.md Decision 11 + Flow 1b (any ontology change → server→client promptWipe + wipeDecision callback; standalone Studio-UI wipe button). Precondition — RESOLVED: source-of-truth is DevStudio (shell Workspace + local git, Decision 5); the dev Star holds only DATA, so a wipe can't touch source (different DO). The Galaxy draft store, DevContainer DO store, and dev-Star file resources are all superseded — one home, DevStudio (Decision 7).' Re-point the link to nebula-dev-flows.md.
  - [ ] **[delete]** The 'Studio compile + reload-broadcast pipeline' is eliminated: Decision 9 says the Star never compiles (DevStudio compiles in dev, Galaxy at publish) and reload is vite HMR delivered to the Preview app by the DevContainer (Flow 1 · B: 'DC-->>PV: HMR js-update'), not a Star-broadcast pipeline.
        → Delete every reference to a dev-Star 'compile + reload(-broadcast) pipeline' (appears in Goal, Implementation shape, and the inherit-everything paragraph). The Star receives a pre-compiled validator push from DevStudio and never compiles; code reload is vite HMR in the DevContainer.
  - [ ] **[rewrite]** This note legitimizes 'devstar' as ongoing vocabulary; the pivot retires the term (Decision 2: no DevStar class). The spike path is a frozen point-in-time artifact (nebula-dev-flows.md § Do NOT touch lists spike records as historical), so it shouldn't anchor live terminology.
        → Rewrite the note to mark the term retired: '"devstar" appears in the frozen spike apps/nebula/spike/sfc-devstar-loop/ — historical only; the term is retired (no DevStar class — nebula-dev-flows.md Decision 2). The dev Star is a plain Star at {u}.{g}.dev.'
  - [ ] **[rewrite]** Not a reversal — the additive-preserved / breaking-resets data bargain SURVIVES the pivot (Decision 11: additive changes leave data valid; breaking changes look corrupted → user-decided wipe). This section is the one that should be RETAINED and re-homed onto plain Star, per nebula-dev-flows.md Downstream ('home + hard-guard resetDevData + the server→client wipe-prompt (Flow 1b)'). Flag only so the coordinator preserves it while purging the surrounding DevStar/binding/deployToDev machinery.
        → Keep this section's data-bargain rule (it's the surviving core). Re-home it onto 'the dev Star (plain Star at .dev)' instead of 'DevStar', and align the breaking-change handling with Decision 11 (DevStudio does not auto-detect; it prompts the user via Flow 1b; the user decides). It remains the single source of truth for the additive/breaking data bargain.
  - [ ] **[other]** Not a contradiction with the pivot — and the ADR-004 carve-out reasoning is correct (ADR-004 governs the resource write path, not wholesale sandbox disposal). Retain. Only the surrounding 'Star wipe' framing on a plain .dev Star (not a DevStar) needs the term alignment already covered above.
        → No substantive change — the ADR-004 out-of-scope reasoning holds for a plain Star at .dev. Ensure the wipe is described as the .dev-guarded resetDevData on Star.

### nebula-app-versioning.md — 30 findings · rewrite-section
  - ⚠ **banner-hazard:** YES — the file already carries Phase-3.5 superseding banners whose conclusion the pivot now RE-supersedes. Decision 3 (line 34) banner "Source durability — owned by the DevContainer DO (UPDATED 2026-06-18; superseded the Galaxy-draft-store dual-write)" plus its nested ⚠️ at line 36, and the Phase 2 banner at line 58 ("Superseded by nebula-container-dev-loop.md Q#2 ... owned by the DevContainer DO's own storage"), all assert DevContainer-as-source-of-truth (its own DO SQL, agent writeFile dual-write to disk + DO SQL, re-hydrate on cold boot). nebula-dev-flows.md Decision 5/6/7 explicitly REVERSES this: DevStudio (shell Workspace + git) is the sole source-of-truth and DevContainer holds NO durable source (disposable checkout repopulated by DevStudio push/applyChanges on cold boot). So each Phase-3.5 banner must be re-superseded with a new dated banner pointing to nebula-dev-flows.md (Decision 5/6/7) — do NOT trust the "UPDATED 2026-06-18" banners as current; they are the most-recent-but-now-wrong round. Per the dev-flows Downstream headline, this is the highest-contradiction area.
  - [ ] **[banner]** Declares the file 'build-ready' with 'DevStar = sole compiler' as a resolved fork. nebula-dev-flows.md Decision 9 eliminates compilation from the Star entirely (Star NEVER compiles; compile lives in DevStudio for dev / Galaxy for prod), and Decision 2 deletes the DevStar class. The whole premise of the front matter is reversed; the file is no longer build-ready.
        → Replace the Status block with a dated superseding banner: '⚠️ SUPERSEDED 2026-06-19 by tasks/nebula-dev-flows.md (canonical dev/publish model). Source-of-truth = DevStudio (shell Workspace + local git); DevContainer holds no durable source; there is no DevStar class (dev Star = plain Star at {u}.{g}.dev); Galaxy is published-only with a single app-version (= ontology version, no separate ontology-version registry); compile runs in DevStudio (dev) / Galaxy (prod), never in the Star. The Decisions/Phases below predate the pivot and must be rewritten — see § Downstream in nebula-dev-flows.md.' Then rewrite Decisions/Phases to conform.
  - [ ] **[rewrite]** References the DevStar class and Star-side compile/serve/reload mechanics as #1a. Decision 2 deletes DevStar; Decision 9 removes compile from the Star.
        → Rewrite to '#1b (the Galaxy publish/registry half; #1a = the DevContainer vite compile/serve/HMR + DevStudio source-of-truth mechanics).' Drop 'DevStar'.
  - [ ] **[rewrite]** Two reversals: (a) 'move all compilation to DevStar' — Decision 9 says the Star never compiles; compile lives in DevStudio (dev) and Galaxy (prod). (b) The 'app-version record' bundling ontology + validator implies a registry that stores ontology/validator; Decision 9 makes the ontology just a source file with no separate ontology-version registry, and Galaxy stores only the built bundle at publish. The lock-step single app-version (= ontology version) part is correct, but its home and mechanism are wrong.
        → Rewrite to: 'The published app-version record — Galaxy holds one immutable app-version = the built bundle + assets at one source commit (ontology compiled into the bundle by Galaxy at publish). There is no separate ontology-version registry and no stored source/validator draft. Compile runs in DevStudio (dev) and Galaxy (prod); the Star never compiles.'
  - [ ] **[delete]** The Galaxy dual-write of source is one of the three conflicting source-durability designs nebula-dev-flows.md Decision 7 explicitly purges; Decision 5 makes DevStudio the sole writer/owner of source (shell Workspace + git). Saving source to DevStar (deleted class) and Galaxy (published-only) are both wrong.
        → Delete this as a 'what this file owns' item. Source durability is owned solely by DevStudio (shell Workspace + local git); it is no longer this file's concern (see nebula-dev-flows.md § Source abstraction). This file owns only the Galaxy published-app-version registry.
  - [ ] **[pointer]** Points the reset/re-hydrate seam at dev-star.md, which is being retired (Downstream: 'the dev Star is a Star at the .dev instance'). The wipe is now resetDevData (.dev-guarded) on a plain Star, and the wipe-prompt is the server→client mesh call of Flow 1b. 're-hydrate seam' is moot: source lives in DevStudio (a different DO), so a Star wipe touches no source.
        → Repoint: 'Reset (resetDevData, .dev-guarded) + the server→client wipe-prompt → Flow 1b in nebula-dev-flows.md (the dev Star is a plain Star at {u}.{g}.dev; there is no DevStar). No re-hydrate seam — source is durable in DevStudio, a different DO, so a Star wipe never touches source.'
  - [ ] **[rewrite]** Relocates compile 'to DevStar' (deleted; Star never compiles) and keeps appendOntologyVersion as a store-only Galaxy method. Decision 9 ELIMINATES the appendOntologyVersion/getLatestOntologyVersion subsystem entirely (ontology is a source file, no version registry); deployToDev's Galaxy-pull is replaced by DevStudio applying the compiled validator to the dev Star, not by a Star-local compile.
        → Rewrite to: 'Eliminates the Galaxy ontology-version registry (appendOntologyVersion / getLatestOntologyVersion) per Decision 9; Galaxy receives only the built app-version at publish. In dev, an ontology .d.ts edit is a normal writeSource→commit→push; DevStudio compiles the validator and applies it to the dev Star (replacing deployToDev's Galaxy-pull). Breaking change; bump semver, flag the release.'
  - [ ] **[rewrite]** Entire reframe is reversed. Decision 9: the Star never compiles (dev or prod); the .vue compile is owned by vite in the DevContainer; the ontology validator compile is owned by DevStudio (dev) and Galaxy (prod). Galaxy DOES compile (the ontology into the bundle at publish), so 'Galaxy stops compiling' is wrong. Prod Stars receive the compiled validator/bundle (lazy-pull) and serve — that part survives, but 'DevStar is the sole compiler' is fully reversed.
        → Replace with: 'Compile homes split by node: vite (in the DevContainer) compiles .vue; DevStudio compiles the ontology validator in dev and applies it to the dev Star; Galaxy compiles the ontology into the built bundle at publish. The Star NEVER compiles — in dev it receives DevStudio's pushed validator, in prod it lazy-pulls the published app-version (validator + bundle) and serves.'
  - [ ] **[rewrite]** The lock-step idea is correct but the conclusion (broaden the ontology-version registry row) is wrong: Decision 9 drops the app-vs-ontology distinction into ONE app-version (= ontology version), with NO separate ontology-version registry row to broaden. There was no surviving 'ontology row' to broaden — it is eliminated.
        → Rewrite: 'Single app-version — ontology + UI ship from one source commit, so there is exactly one version (app-version = ontology version; the old app-vs-ontology-version distinction is dropped, Decision 9). Galaxy stores that one built app-version; there is no separate ontology-version registry.'
  - [ ] **[rewrite]** 'Galaxy ... never compiles' contradicts Decision 9 (Galaxy compiles the ontology into the bundle at publish). 'DevStar-compiled validatorBundle' uses the deleted class and the wrong compile home. Storing 'ontology types + validatorBundle' as registry rows conflicts with ontology-as-source-file (no ontology-version registry); Galaxy holds the built bundle + assets, not raw ontology types/validator drafts.
        → Rewrite the whole decision: 'Galaxy stores one immutable app-version = the built bundle + static assets produced by DevContainer vite build, with the ontology compiled into the bundle by Galaxy at publish (Galaxy DOES compile; the Star does not). No stored ontology types / validatorBundle draft and no ontology-version registry. Carry relationships forward if still needed by the prod lazy-pull.'
  - [ ] **[rewrite]** The record shape stores ontology types + validatorBundle in a KV row — a stored ontology/validator registry that Decision 9 eliminates (ontology is just a source file, no version registry). The 'AppBundle' / #1a in-DO compiled-bundle table belongs to the now-reversed in-Star/DevStar compile-and-serve model; under the pivot the built bundle is produced by DevContainer vite build and pushed to Galaxy as an asset set (R2), not assembled per-path in a Star SQLite table.
        → Replace with a Galaxy published-bundle shape: one app-version = the built bundle + asset set (R2) at version N, produced by DevContainer vite build and pushed at publish (Flow 2). No 'types'/'validatorBundle' columns (ontology is source, compiled into the bundle at publish). If a #1a AppBundle table is retained at all, reconcile it against the DevContainer-builds-and-pushes model rather than an in-Star per-path compiled store.
  - [ ] **[term-swap]** 'DevStar-local in dev' uses the deleted class and the reversed compile home. Per Decision 9, in dev the dev Star receives the compiled validator PUSHED by DevStudio (DevStudio compiles, not the Star); in prod the Star lazy-pulls the published app-version from Galaxy. The Star installs but never compiles.
        → Rewrite: 'The Star installs the compiled validator + bundle but never compiles. In dev it receives the validator pushed by DevStudio (DevStudio compiled it); in prod it lazy-pulls the published app-version from Galaxy.'
  - [ ] **[pointer]** Cross-file pointer asserting this file is the authority for built-artifact storage/versioning. nebula-dev-flows.md is now the canonical source for the publish/registry model; this file is downstream and must conform to it, not act as the superseding authority.
        → Repoint: the canonical model for built-artifact storage/versioning is now nebula-dev-flows.md (Flow 2 + Decision 9). This file conforms to it rather than superseding nebula-studio.md.
  - [ ] **[rewrite]** This describes an in-DO per-file SQLite source/asset store (the DevContainer-DO-store / DevSource design) as a demo mechanism later swappable to shell Workspace. Decision 6/7 makes @cloudflare/shell's Workspace the SHIPPING source-of-truth in DevStudio NOW (not a later swap), and the per-file SQLite source store is purged. Compiled assets are not stored per-path in a Star/DO at all — the built bundle is produced by vite and pushed to Galaxy as an asset set.
        → Rewrite: 'Source is owned by DevStudio's shell Workspace (SQLite+R2) + local git TODAY (not a later swap) — see nebula-dev-flows.md Decision 6. This file does not define a source storage shape. Galaxy's published bundle is the vite-built asset set pushed at publish.' Delete the per-file SQLite source store as a demo mechanism.
  - [ ] **[rewrite]** Posits a 'mutable dev-draft store' as a Galaxy/registry-side concern. Decision 7 purges the dev-draft store from Galaxy and from the dev Star; the in-progress source lives only in DevStudio's shell Workspace + git. Galaxy holds only the immutable published app-version. Publish does not 'bridge two stores' here — it is DevContainer vite build → push built bundle to Galaxy (Flow 2).
        → Rewrite: 'One store on the Galaxy side — the immutable published app-version registry. In-progress source is not a Galaxy/registry concern; it lives in DevStudio (shell Workspace + git). Publish = DevContainer vite build → push built bundle to Galaxy (Flow 2).'
  - [ ] **[rewrite]** 'recompiled on re-hydrate' presumes the in-Star/DevStar compile + DO-store re-hydrate model. Under the pivot, dev save = DevStudio writeSource→commit→push (applyChanges); vite in the DevContainer HMRs; there is no re-hydrate-recompile of a stored bundle (the container is disposable, repopulated by DevStudio push).
        → Rewrite: 'Dev save = DevStudio writeSource→git commit→push changed files (applyChanges) to the DevContainer; vite HMRs. The built bundle is produced only at publish (Galaxy), never stored per dev-save. The disposable container is repopulated by DevStudio push on cold boot — no re-hydrate of a stored bundle.'
  - [ ] **[banner]** DIRECT REVERSAL — the highest-contradiction item. This is the recent Phase-3.5 conclusion that nebula-dev-flows.md Decision 5/6/7 explicitly re-supersedes: DevStudio (shell Workspace + git) is the sole source-of-truth; DevContainer holds NO durable source (it is a disposable checkout repopulated by DevStudio push/applyChanges on cold boot). 'agent's writeFile dual-writes to container disk + DevContainer DO SQL' and 're-hydrated on cold boot' are now wrong. 'agent' should be DevStudio. This banner is itself now re-superseded (see phase35BannerHazard).
        → Replace the whole decision with a re-superseding banner + correct model: '⚠️ RE-SUPERSEDED 2026-06-19 by nebula-dev-flows.md Decision 5/6/7. Source-of-truth = DevStudio (shell Workspace SQLite+R2 + local git, isomorphic-git). DevStudio is the SOLE writer of source and pushes changed files to the DevContainer (applyChanges, mesh). The DevContainer is a disposable checkout that holds NO durable source — there is no DevContainer DO SQL source store / DevSource table, no dual-write, no re-hydrate of source. Publish = DevContainer vite build → push built bundle to Galaxy.'
  - [ ] **[rewrite]** States 'the DevContainer DO owns source' as the corrected position — now itself reversed by Decision 5 (DevStudio owns source; DevContainer holds none). 'vite in the DevContainer compiles' is correct for .vue, but the ontology validator compile is DevStudio's, not the container's. This nested ⚠️ is a stale-correction-of-a-stale-claim.
        → Rewrite: 'Reconciled by nebula-dev-flows.md: DevStudio owns source (shell Workspace + git); vite in the DevContainer compiles .vue; DevStudio compiles the ontology validator (dev) and Galaxy compiles it into the bundle (prod); the Star never compiles. The published-version registry + prod lazy-pull survive; the in-DO/DevStar-compile, the DevContainer-DO source store, and any Galaxy source-draft are all gone.'
  - [ ] **[rewrite]** Built on the deleted DevStar-as-sole-compiler model. Correct mechanics per pivot: ontology edit → DevStudio compiles validator + applies to dev Star (Decision 9); .vue edit → DevStudio writeSource→push→vite HMR in the DevContainer (vite compiles, not the Star). 'dev needs no Galaxy pull' is correct conceptually but for the wrong reason and via the wrong nodes.
        → Rewrite: 'Dev needs no Galaxy pull. Ontology edit → DevStudio compiles the validator locally and applies it to the dev Star. .vue edit → DevStudio writeSource→push (applyChanges) to the DevContainer where vite compiles and HMRs the preview. Lazy-pull from Galaxy is prod-only.'
  - [ ] **[rewrite]** Publish source/actor is wrong. Flow 2 / Decision 4: publish is DevStudio-orchestrated; the DevContainer runs vite build and pushes the built bundle to Galaxy. DevStar (deleted) does not push, and Galaxy compiles the ontology into the bundle at publish rather than receiving a separately-compiled validator from a Star.
        → Rewrite: 'Publish (deliberate, less frequent): DevStudio orchestrates (Studio UI → DevStudio → DevContainer → Galaxy); the DevContainer runs vite build and pushes the built bundle + assets to Galaxy; Galaxy compiles the ontology into the published app-version. See Flow 2.'
  - [ ] **[rewrite]** Largely SURVIVES (prod Star lazy-pulls the published app-version from Galaxy and serves; the Star does not compile — consistent with Flow 2b). Minor drift: it should reference the single app-version (= ontology version, one version, Decision 9) rather than a separately-versioned validator/bundle pair, and the dev Star being a plain Star means lazy-pull is general Star behavior.
        → Light edit: 'Prod: a prod Star lazy-pulls the published app-version (one version = ontology version, Decision 9 — built bundle + assets) from Galaxy on a version mismatch and serves it; the Star never compiles. The only place the pull is used (Flow 2b). Since a .dev Star is just a Star, this is general Star behavior.'
  - [ ] **[rewrite]** Both writers are wrong. Source is NEVER written to Galaxy (Decision 7 purges the Galaxy source draft / dual-write; source lives only in DevStudio). The compiled/built version is pushed by the DevContainer (vite build), not DevStar (deleted), at publish (Flow 2).
        → Rewrite: 'Who writes Galaxy: ONLY the built app-version at publish, pushed by the DevContainer (vite build). Galaxy never stores source — there is no Galaxy save API / source dual-write. Source lives in DevStudio (shell Workspace + git).'
  - [ ] **[rewrite]** Phase 1 is built around relocating compile to DevStar (deleted; Star never compiles) and an AppVersionRow that stores types/validatorBundle (eliminated ontology-version registry). The grep targets (OntologyVersionRow/getLatestOntologyVersion/compileOntologyVersion/appendOntologyVersion) are now to be ELIMINATED, not renamed to App-* equivalents, per Decision 9. deployToDev's rework (Galaxy-pull → Star-local-compile) is wrong; it becomes DevStudio compiles + applies.
        → Rewrite Phase 1 around: (a) Galaxy holds one published app-version = built bundle + assets pushed by DevContainer at publish; (b) ELIMINATE appendOntologyVersion/getLatestOntologyVersion/compileOntologyVersion + the OntologyVersion* types (no ontology-version registry, Decision 9) — except the prod lazy-pull carve-out, which switches to the single app-version (getLatestAppVersion); (c) no Star/DevStar compile method — DevStudio owns dev validator compile + applies to the dev Star; Galaxy compiles at publish. Drop the AppBundle PK-migration work if the in-Star per-path compiled-bundle store is dropped in favor of DevContainer-builds-and-pushes (reconcile against #1a's post-pivot shape).
  - [ ] **[rewrite]** Success criteria assert a DevStar-local-compile marker and a deployToDev path. Under the pivot the dev compile + apply is DevStudio's (not the Star's); deployToDev as a Star pull-from-Galaxy is gone. The 'no Galaxy call in dev' intent survives but the marker location/owner is wrong.
        → Rewrite criteria to assert: in dev, DevStudio compiles the validator and pushes/applies it to the dev Star with NO Galaxy fetch; the prod (non-.dev) Star path DOES fetch the published app-version from Galaxy — the mirror-image pair proving the split. Drop deployToDev / DevStar-local-compile markers.
  - [ ] **[banner]** DIRECT REVERSAL (second instance of the source-of-truth flip). The 2026-06-18 superseding banner concludes DevContainer-DO-owns-source — re-superseded by Decision 5/6/7: source lives in DevStudio; the DevContainer holds none, no DO SQL source store, no re-hydrate. 'agent' → DevStudio. The retained kernel (published app-version registry + publish-time build-push) is correct but the actor is the DevContainer pushing to Galaxy, with DevStudio orchestrating.
        → Replace banner: '⚠️ RE-SUPERSEDED 2026-06-19 by nebula-dev-flows.md Decision 5/6/7 + Flow 2. Source durability is owned by DevStudio (shell Workspace + git), not the DevContainer DO (which holds no durable source — no DO SQL source store, no dual-write, no re-hydrate). What #1b retains: the immutable published app-version registry on Galaxy + the publish-time build-push (DevStudio-orchestrated; DevContainer vite build → push built bundle to Galaxy).' Remove the DevContainer-DO-owns-source / re-hydrate language.
  - [ ] **[delete]** The folded-away 'original' spec describes the Galaxy source draft store + dual-write to DevStar — two of the three purged source-durability designs (Decision 7). Even as a 'kept for the record' detail it asserts a Galaxy save API for source that no longer exists. As historical record inside an already-superseded section it is lower-priority, but a reader could mistake the @mesh(requireAdmin) save-API guidance as live.
        → Leave folded as historical record but ensure the surrounding banner makes clear it is doubly-superseded (Galaxy source draft store + DevStar dual-write are both eliminated; source lives in DevStudio). Optionally delete the <details> block entirely since it documents a design that is now two reversals stale.
  - [ ] **[rewrite]** Describes a Galaxy-owned save API for draft SOURCE — eliminated (no Galaxy source draft store; source lives in DevStudio). The requireAdmin command-auth concern survives but moves to DevStudio (Decision 4: Studio UI talks only to DevStudio, so command-auth lives in one place; applyChanges/viteControl are @mesh(requireAdmin)).
        → Within the (historical) Phase 2 fold, note the surviving auth principle relocates: command-auth (requireAdmin) lives on DevStudio's commands (publish) and the DevContainer's applyChanges/viteControl, not a Galaxy source-save API (which is gone). See nebula-dev-flows.md Decision 4 + DevContainer internals.
  - [ ] **[delete]** Tests a DevStar+Galaxy dual-write whose entire premise is purged. There is no DevStar leg and no Galaxy draft store; source durability is single-homed in DevStudio (shell Workspace + git).
        → Delete these dual-write failure criteria. If any source-durability test survives, it asserts DevStudio's shell Workspace + git holds source across DevContainer cold boots (the container is repopulated by DevStudio push), not a dual-write.
  - [ ] **[rewrite]** Mostly SURVIVES (prod Star lazy-pulls + serves; the sync-onRequest cold-miss seam is real). Drift: it should reflect the single app-version (= ontology version, Decision 9) and that Studio UI / App UI are served from Workers Assets (Decision 3 / Flow 2b note), and the publish artifact is the DevContainer-vite-built bundle pushed to Galaxy (Flow 2). Aligns with Flow 2b (out of demo scope).
        → Light edit: keep Phase 3 as exploratory/post-demo but align to Flow 2b — prod Star lazy-pulls the single published app-version (= ontology version) from Galaxy on version mismatch and serves; static assets via Workers Assets; the published artifact is DevContainer's vite build pushed at publish. Cross-reference Flow 2b in nebula-dev-flows.md.
  - [ ] **[pointer]** Missing the now-canonical pointer to nebula-dev-flows.md. References frame galaxy.ts as 'registry broadened / compile relocating out' (wrong: ontology-version registry eliminated; compile to DevStudio/Galaxy, not relocated within the Star path). The shell Workspace is framed as a 'backend to swap in later' (Decision 6: it is the SHIPPING source-of-truth in DevStudio now, not a later swap).
        → Add 'tasks/nebula-dev-flows.md (CANONICAL dev/publish model — supersedes this file's pre-pivot Decisions/Phases)' as the first reference. Reframe galaxy.ts (ontology-version registry eliminated; Galaxy = published app-version only). Reframe nebula-file-storage-backend.md: shell Workspace is DevStudio's source-of-truth NOW (homed in DevStudio), not a later swap.
  - [ ] **[rewrite]** Largely SURVIVES (prod Stars never compile — consistent with Decision 9). But it cites the in-Star #1a compile machinery as resident in the prod Star; under the pivot the .vue compile is vite's (in the DevContainer, dev-only) and the ontology compile lives in DevStudio (dev) / Galaxy (prod), so the prod Star should not carry the tsc/compile bundle at all. The cold-start concern shifts toward Galaxy (which compiles at publish), not the Star.
        → Rewrite: 'Prod Stars never compile (Decision 9) and should NOT carry compile machinery — the .vue compile is vite's (DevContainer, dev) and the ontology validator compile lives in DevStudio (dev) / Galaxy (prod). If the tsc/validator bundle is anywhere in the Star path, removing it is the cold-start win, not isolating it behind a facet.'

## P1 — master + active critical path

### nebula.md — 8 findings · rewrite-section
  - [ ] **[rewrite]** Per nebula-dev-flows.md (canonical), 'Studio' is now a multi-participant system, not a single chat UI: DevStudio (server DO = codegen engine, sole writer of source, AND source-of-truth via shell Workspace + local git), DevContainer (disposable vite+HMR checkout), Studio UI (the chat SPA, served from Workers Assets), and Preview app (the running generated app in an iframe). The bare 'chat UI + tool orchestration' framing omits this entire cast and the source-of-truth role. The canonical doc's Downstream section explicitly lists nebula.md to '(update) the dev-loop model + phase descriptions (DevStudio = orchestrator + source-of-truth; no DevStar class; Galaxy = published-only)'.
        → Reframe item 7 to name the cast and the orchestrator/source-of-truth split, e.g.: '**Studio** (renamed from "Vibe Coding IDE") — the chat-first authoring experience. Its participant model is canonical in `tasks/nebula-dev-flows.md`: **DevStudio** (server DO — codegen engine, sole writer of source, and source-of-truth via a shell `Workspace` + local git) orchestrates everything; **DevContainer** (disposable Cloudflare Container running vite+HMR — a working copy DevStudio pushes source to via `applyChanges`); **Studio UI** (the chat SPA, served from Workers Assets); **Preview app** (the running generated app in an iframe). `tasks/nebula-studio.md` + `tasks/nebula-dev-flows.md`.'
  - [ ] **[rewrite]** The '~3s reload' preview mechanism is superseded. Per nebula-dev-flows.md Flow 1/1c and the DevContainer-internals seam, the Preview app loads its shell + HMR from a DevContainer running real vite; updates land as HMR 'js-update' patched in place (reference memories cite ~114ms js-update), not a ~3s full iframe reload. The iframe-spike full-reload model is no longer the dev-loop preview path.
        → Drop or relabel the '~3s reload' iframe-spike as superseded by the container+vite HMR preview model, pointing to `tasks/nebula-dev-flows.md` (Flow 1, Flow 1c, DevContainer internals): the Preview app gets its shell + HMR from DevContainer (vite), updates patch in place via HMR js-update — no full reload.
  - [ ] **[rewrite]** Decision 11 of nebula-dev-flows.md changes the ontology-change lifecycle mechanism: DevStudio does NOT auto-detect breaking-ness; on ANY ontology .d.ts change it prompts the user (server->client mesh call, Flow 1b) to decide whether to wipe .dev data, executed via the `.dev`-guarded `resetDevData`, plus a standalone Studio-UI 'Wipe .dev data' button. 'breaking edits reset the dev Star' (auto, on breaking-ness detection) is the old behavior.
        → Update to the prompt-driven wipe model: 'In-dev ontology edits: additive changes leave data valid; on ANY ontology change DevStudio prompts the user (Flow 1b, server->client mesh call) to wipe `.dev` data via the `.dev`-guarded `resetDevData` (plus a standalone Studio-UI wipe button) — no breaking-ness auto-detection; lazy migration deferred. See `tasks/nebula-dev-flows.md` Decision 11.'
  - [ ] **[other]** Substantively correct and CONFORMANT with Decision 2 (no DevStar class; the dev Star is a plain Star at the {u}.{g}.dev instance). Flag only as a confirm-and-reinforce point so the realignment does not accidentally introduce a 'DevStar' class here when adding the new cast: the canonical model is one `Star` class, dev/prod is an instance concern.
        → No reversal needed; optionally add an explicit reinforcement that this is a plain `Star` instance (no `DevStar` subclass) per `tasks/nebula-dev-flows.md` Decision 2, so the dev/prod split stays an instance concern. Do NOT introduce a DevStar class when reframing the surrounding items.
  - [ ] **[pointer]** This section describes Studio only as a 'conversational interface' and points solely to nebula-studio.md (a pre-pivot file the canonical doc itself lists for realignment). It omits the now-canonical dev-loop architecture and lacks any pointer to nebula-dev-flows.md, which the canonical doc declares 'the canonical source of truth for the dev-loop architecture.'
        → Add a pointer and one-line architecture summary, e.g. append: 'The canonical dev/publish participant model and sequence diagrams (DevStudio = orchestrator + source-of-truth; DevContainer = disposable vite checkout; Star-at-`.dev` = dev data; Galaxy = published-only) live in `tasks/nebula-dev-flows.md`.' Keep nebula-studio.md as the detail file but subordinate it to the canonical flows doc.
  - [ ] **[rewrite]** Per Decision 9 of nebula-dev-flows.md, the ontology is just another source file living in DevStudio's source tree; in the dev loop the source-of-truth (incl. the ontology .d.ts the codegen reads) is DevStudio's shell Workspace, read LOCALLY by the LLM hot path — not fetched from Galaxy. Galaxy is published-only and no longer the dev-time source of the raw ontology for codegen. 'exposing the raw .d.ts through Galaxy ... for Studio's AI' reflects the pre-pivot Galaxy-as-dev-source model.
        → Reword so the dev-time raw .d.ts the codegen model reads comes from DevStudio's source-of-truth (local read), not Galaxy: e.g. 'annotation conventions on the ontology `.d.ts`, which DevStudio reads locally (its source-of-truth) to feed Studio's codegen.' Galaxy's role is published-only (Decision 9 / canonical doc). Verify against nebula-resource-metadata.md before finalizing wording.
  - [ ] **[pointer]** The master phase-table row for Studio points only to nebula-studio.md and gives no hint of the dev-loop participant model now canonical in nebula-dev-flows.md. As the index file, it should surface the canonical dev-flows doc alongside (or instead of) the pre-pivot studio file.
        → Update the Task File cell (or add a sibling row) to reference `tasks/nebula-dev-flows.md` as the canonical dev/publish model, with `tasks/nebula-studio.md` as the (to-be-realigned) detail file. E.g. `tasks/nebula-dev-flows.md` (canonical model) + `tasks/nebula-studio.md`.
  - [ ] **[rewrite]** Mostly conformant (Kimi 2.7 / no Think / no codemode all match the canonical Cast). Minor drift: the canonical doc homes the codegen loop specifically in DevStudio (a server DO running the agentic tool-calling loop, calling Kimi server-side), not the generic 'Workers+DO'. Worth tightening so the engine's home matches the named participant.
        → Tighten 'on Workers+DO' to name DevStudio as the home of the tool-calling loop: 'driven by a thin native-tool-calling loop in **DevStudio** (server DO), which calls Kimi 2.7 server-side (Workers AI binding).' Per nebula-dev-flows.md Cast + Decision 1.
  - [ ] **(critic)** L56 + L91 "expose raw .d.ts through Galaxy" → through DevStudio's Workspace (registry-source residue the auditor passed over).

### nebula-resource-metadata.md — NEW · critic PRIORITY 1 (active critical path)
  - [ ] Re-home "Galaxy returns ontology source via OntologyVersionRow / getOntologySource" → "DevStudio's Workspace exposes the .d.ts source tree" (Decisions 5/7/9). **Keep** the @title/@description/@inverse annotation conventions.

## P2 — banners / minor (audited)

### nebula-do-scope-isolation.md — 8 findings · superseding-banner
  - [ ] **[banner]** The file has no pointer to the now-canonical dev-loop model. Per nebula-dev-flows.md §343 this file must be realigned (`DevStar` refs → `Star` at `.dev`; `{u}.{g}.dev` is a dev *instance*, not a class) and per Decision 2 the `DEV_STAR` binding is eliminated. A reader landing here gets the pre-pivot `DevStar`/`DEV_STAR`/Galaxy-ontology-registry world with no signal it is superseded.
        → Add a dated superseding banner immediately under the Status line: '> **Superseded on dev-loop details (2026-06-19) — see [`nebula-dev-flows.md`](nebula-dev-flows.md) (canonical).** The structural `onBeforeCall` tier-gate that this file shipped is unchanged and correct. But the dev-loop framing herein is stale: there is **no `DevStar` class** (the dev data Star is a plain `Star` at the `{u}.{g}.dev` *instance*), the **`DEV_STAR` binding is eliminated** (`#starBinding()` always routes `STAR`, Decision 2), Galaxy is **published-only** with **no ontology-version registry** (`appendOntologyVersion`/`getLatestOntologyVersion` eliminated, Decision 9), and source-of-truth is **DevStudio** (not relevant to this file but governs `dev-star.md`). Read class/binding/Galaxy-registry mentions below as historical.'
  - [ ] **[term-swap]** `DevStar` is named as a class/tier alongside `Star`. nebula-dev-flows.md Decision 2 abolishes the `DevStar` class — the dev Star is a plain `Star` at the `.dev` instance, so there is no separate `DevStar` tier whose `onStart` could shadow anything.
        → Replace `shadowed on `Star`/`DevStar`` with `shadowed on the `Star` tier (incl. its `.dev` instance)` — drop the `DevStar` class reference.
  - [ ] **[rewrite]** States `dev-star.md` builds a `DEV_STAR` binding. Decision 2 eliminates the `DEV_STAR` binding entirely (collapses to always-`STAR`), and §340 retires `dev-star.md` to 'the dev Star is a `Star` at the `.dev` instance'. The dev case no longer needs a distinct binding — it is a `STAR`-binding instance named `{u}.{g}.dev`.
        → Replace `it needs the `DEV_STAR` binding that task builds` with `it needs the `{u}.{g}.dev` *instance* exercised against one Galaxy — now a plain `STAR`-binding instance (no `DEV_STAR` binding; see `nebula-dev-flows.md` Decision 2)`.
  - [ ] **[rewrite]** Two stale dependencies: (a) `getLatestOntologyVersion` is an eliminated Galaxy method — Decision 9 removes the Galaxy ontology-version registry (DevStudio applies the compiled validator to the `.dev` Star; prod gets it via the published app-version), so the cross-tier call this paragraph hinges on no longer exists; (b) the `dev-Star decision (tasks/dev-star.md)` framing implies a `DevStar` class, which §340 retires. The underlying TOFU-rejection point (two Stars under one Galaxy) still illustrates the bug, but the mechanism cited is stale.
        → Reframe to a still-valid shared-Galaxy call: e.g. `the `{u}.{g}.dev` instance and any production `{u}.{g}.{s}` are both plain `Star`s sharing Galaxy DO `{u}.{g}` and both call a shared galaxy-level `@mesh` method (e.g. `getGalaxyConfig`) with star-level `aud`, so TOFU rejects whichever arrives second.` Drop `getLatestOntologyVersion` and the `dev-Star decision`/`DevStar` framing; if a pointer is wanted, point at `nebula-dev-flows.md` (dev Star = `Star` at `.dev`) rather than `dev-star.md`.
  - [ ] **[pointer]** Lists the Galaxy ontology-version-registry methods (`appendOntologyVersion`, `getLatestOntologyVersion`, `getOntologyVersion`, `listOntologyVersions`) as the live `@mesh` surface. Decision 9 eliminates the entire Galaxy ontology-version registry — none of these four methods survive the pivot (Galaxy keeps only the published-app-version/built-bundle role). This is a code-as-it-was snapshot in a shipped record, but it now describes a removed subsystem.
        → This is a historical audit snapshot of the M8 raw-RPC surface; rather than rewrite the enumerated list, rely on the top banner to flag that the Galaxy ontology-version methods (`append/get/getLatest/listOntologyVersions`) are eliminated by `nebula-dev-flows.md` Decision 9. Optionally append a parenthetical: `(the four `*OntologyVersion*` methods are eliminated post-pivot — Galaxy is published-only; see banner.)`
  - [ ] **[pointer]** The B5 'frozen non-admin-`@mesh` allow-list' design is anchored on the ontology-version-registry methods (`getLatestOntologyVersion`, `getOntologyVersion`, `listOntologyVersions`) and `getLatestOntologyVersion` is called the 'load-bearing' cross-tier read that proves the `.*` widening sound. Decision 9 eliminates all three ontology-version methods and the cross-tier `getLatestOntologyVersion` Star→Galaxy call (DevStudio now pushes the compiled validator to the `.dev` Star; prod uses the published app-version). The widening-soundness argument must be re-anchored on a surviving shared-tenant method (e.g. `getGalaxyConfig`).
        → Add a parenthetical caveat (or rely on the banner): note that the `*OntologyVersion*` methods cited here are eliminated post-pivot (Decision 9) and the 'load-bearing cross-tier read' role transfers to the surviving shared-tenant method (`getGalaxyConfig`) — the B5 allow-list mechanism is unaffected, only its membership snapshot is stale.
  - [ ] **[term-swap]** Second `DevStar` class reference (in the Migration section). Per Decision 2 there is no `DevStar` class — only the `Star` class (with a `.dev` instance). The `onStart` shadowing concern is purely a `Star`-class property.
        → Replace `the `Star`/`DevStar` tier` with `the `Star` tier (incl. its `.dev` instance)`.
  - [ ] **[pointer]** Dead/redirected pointer. nebula-dev-flows.md §340 retires `dev-star.md` to 'the dev Star is a `Star` at the `.dev` instance'; the consumer of the shared-Galaxy fix is now framed by the canonical dev-flows doc, and the 'dev Star + prod Star' relationship is an instance concern, not a separate `DevStar`/`dev-star.md` design.
        → Repoint to the canonical doc: `- [nebula-dev-flows.md](nebula-dev-flows.md) — the dev Star is a plain `Star` at the `{u}.{g}.dev` instance sharing one Galaxy with prod `{u}.{g}.{s}` (consumer of the shared-Galaxy fix).` Keep a `dev-star.md` mention only if it still exists as a retired stub.

### nebula-self-hosted-assets.md — 16 findings · superseding-banner
  - ⚠ **banner-hazard:** The file's existing top banner (lines 3-13, "⚠️ PARKED 2026-06-17 — proven baseline, likely superseded") is a pre-Phase-3.5 supersession that the pivot now RE-supersedes on its specifics. It correctly anticipates the direction ("dev-only Cloudflare Container running real vite + vite build → static assets for prod ... the UI-build half here ... is superseded") but is stale on three counts the canonical doc resolves: (1) it points to `tasks/container-vite-spike.md` as the gating spike and frames the pivot as still-tentative ("If that pivots", "Don't build on this until the spike's go/no-go") — but nebula-dev-flows.md Decision 6 marks the spike GOed/proven 2026-06-19 (experiments/interim-dev-loop) and locks the design; (2) it does not name DevStudio as source-of-truth nor the Workers-Assets hosting of Studio UI; (3) it lacks any pointer to the now-canonical nebula-dev-flows.md. So this is the rarer "banner-now-itself-re-superseded" hazard: the banner's conclusion (likely-superseded) is now firm (definitely-superseded), and its forward pointer must be repointed from the spike file to nebula-dev-flows.md. Replace, don't merely append.
  - [ ] **[banner]** The banner frames the vite/container pivot as a still-pending spike gated on a go/no-go, pointing to `tasks/container-vite-spike.md`. Per nebula-dev-flows.md Decision 6, that pivot is DECIDED and PROVEN (2026-06-19, experiments/interim-dev-loop; ship on @cloudflare/shell, vite owns compile, Studio UI from Workers Assets). The spike file is a frozen point-in-time record, not the live successor; the canonical successor is nebula-dev-flows.md. The banner is itself now re-superseded (see phase35BannerHazard).
        → Replace the entire top banner with a dated superseding banner: "⚠️ SUPERSEDED 2026-06-20 — the UI-build half of this file (in-DO SFC compile, the specifier-rewrite, vendored same-origin asset serving, per-icon Lucide) is superseded by the dev-loop pivot. Canonical model: `tasks/nebula-dev-flows.md` (Decisions 3/6/9). The pivot is DECIDED, not a pending spike — vite owns compile (Star never compiles), prod assets come from a DevContainer `vite build`, and Studio UI is served from Workers Assets. This file is kept only for: (a) the two latent bug fixes below (`nodejs_compat`, `bindingMetadata`) which remain correct, and (b) the data-layer note (untouched by the pivot). Do not build on the UI-build approach here."
  - [ ] **[rewrite]** States the supersession as conditional ("If that pivots ... is superseded"). Per nebula-dev-flows.md Decision 9, in-DO SFC compile is unconditionally gone — vite owns compile and the Star never compiles. The conditional framing is now false.
        → Fold this into the new banner as an unconditional statement (the UI-build half IS superseded; the data layer is untouched). Drop the conditional 'If that pivots' phrasing.
  - [ ] **[banner]** The whole platform-asset-serving-from-Star-code mechanism is superseded. Per nebula-dev-flows.md Decisions 3/6: the preview app loads its shell + HMR from DevContainer (real vite), prod assets come from a DevContainer `vite build` pushed to Galaxy, and Studio UI itself is served from Workers Assets. The Star does not serve platform assets in the pivot model; `Star.onRequest` resolving Vue/DaisyUI/Lucide is dead.
        → Annotate the § As-built section as historical-only under the new banner; mark the platform-asset-from-code mechanism as superseded by vite (dev: DevContainer vite serves shell+HMR; prod: vite build → Galaxy bundle; Studio UI → Workers Assets). Do not delete the prose (it records what was built) but make clear it is not the path forward.
  - [ ] **[term-swap]** `DevStar` is named as a class with a 'write boundary'. Per nebula-dev-flows.md Decision 2 there is NO DevStar class — the dev Star is the plain `Star` at the `{u}.{g}.dev` instance, and `compileSFC` is deleted (vite owns compile), so there is no in-DO compile write boundary at all.
        → term-swap `DevStar` → `Star at .dev` and note the rewrite boundary is moot post-pivot (vite owns compile). Since the bindingMetadata fix itself remains a valid record, keep the [[sfc-compile-needs-bindingmetadata]] pointer but reframe: the fix was correct for the in-DO compile path that the pivot retires.
  - [ ] **[term-swap]** References a `DEV_STAR` binding and a `compileSFC` op on the dev Star. Per nebula-dev-flows.md Decision 2 the `DEV_STAR` binding disappears (`#starBinding()` collapses to always `STAR`) and `compileSFC` is deleted (vite owns compile). Decision 9: the Star never compiles.
        → term-swap `'DEV_STAR'` → `'STAR'` (`.dev` instance) and mark `compileSFC` as deleted in the pivot (vite owns compile); this whole chromium-adapter compile path is superseded by DevContainer/vite serving the preview.
  - [ ] **[pointer]** The `/dev-star` proxy + worker-injected preview serving is replaced. Per nebula-dev-flows.md (Topology, DevContainer internals) the Preview app loads its shell + HMR from DevContainer via `/dev-container/{u}.{g}.dev/` (DO `fetch()` proxy to vite :5173), not from a `/dev-star` proxy serving compiled modules. The route name itself drifted (`/dev-star` → `/dev-container`).
        → Mark superseded under the banner; if any forward-looking note is kept, point the preview-serving path to DevContainer (`/dev-container/...`, vite :5173) per nebula-dev-flows.md DevContainer-internals, not a `/dev-star` proxy.
  - [ ] **[pointer]** Not a direct architectural contradiction, but the motivating problem (CDN-served Vue under loose CSP) is resolved differently in the pivot: vite serves the preview shell from DevContainer in dev and a built static bundle (Workers Assets / Galaxy) in prod, where same-origin self-hosting is the natural result of a vite build — not an in-DO vendored-asset + specifier-rewrite scheme. The 'Why' is still valid as motivation but the resolution it implies is superseded.
        → Keep the security motivation (still true) but add a one-line pointer that the pivot achieves same-origin/strict-CSP via vite build output (Workers Assets / DevContainer-served preview) rather than the in-DO vendoring + rewrite described below. Reference nebula-dev-flows.md.
  - [ ] **[rewrite]** Names the `DevStar` class, a `dev-star.ts`, an in-DO `compileSFCToModule`, and an `App.js` write boundary — all superseded. Per nebula-dev-flows.md Decision 2 (no DevStar class) and Decision 9 (vite owns compile; Star never compiles), there is no in-DO compile step and no specifier-rewrite write boundary; vite resolves specifiers natively from baked deps.
        → Mark the entire Goal/Resolution mechanism superseded under the banner; term-swap `DevStar` → `Star at .dev` for any residual prose, and note specifier resolution is now handled by vite (deps baked in the DevContainer image), eliminating the in-DO rewrite. Point to nebula-dev-flows.md Flow 1c (baked deps, no npm install).
  - [ ] **[banner]** The specifier-rewrite + per-icon vendoring scheme is superseded by vite (real bundler resolves bare specifiers from baked deps; no hand-rolled allowlist/rewrite, no per-icon vendored ESM). Per nebula-dev-flows.md Decisions 6/9 vite owns compile/resolution.
        → Mark superseded; the rewrite + vendored-per-icon scheme is replaced by vite resolution against baked node_modules. No realignment of the mechanism itself — flag the whole subsystem as not the path forward, pointer to nebula-dev-flows.md.
  - [ ] **[banner]** The vendored-artifact + regen-script sourcing model is superseded. Per nebula-dev-flows.md Decision 6 / Flow 1c, dependencies (incl. styling/icons) are normal npm deps baked into the DevContainer image and resolved by vite — not committed vendored artifacts produced by a bespoke regen script. Tailwind JIT, etc., come free from real vite (the As-built section even lists 'no Tailwind JIT' as a ceiling the pivot removes).
        → Mark superseded; note that DaisyUI/Lucide are ordinary deps baked into the DevContainer image and resolved/built by vite (Tailwind JIT included), retiring the vendoring + regen-script approach. The ATTRIBUTIONS.md entries for any installed deps remain valid bookkeeping. Pointer to nebula-dev-flows.md Flow 1c.
  - [ ] **[banner]** Phase 1's deliverables (vendor same-origin assets + specifier-rewrite at the DevStar write boundary) are entirely superseded by the vite-owns-compile pivot. `DevStar` class is gone (Decision 2); in-DO rewrite is gone (Decision 9).
        → Mark Phase 1 superseded under the banner. If retained for history, term-swap `DevStar write boundary` → note vite resolution replaces it. Do not present Phase 1 as actionable.
  - [ ] **[term-swap]** Success criteria are predicated on a `DevStar` serving compiled SFCs through a same-origin proxy — both retired. Per nebula-dev-flows.md the preview is served by DevContainer/vite (`/dev-container/...`), and the dev Star is a plain `Star` at `.dev` that holds data only (never compiles or serves SFCs).
        → term-swap `DevStar` → `DevContainer` (preview serving) / `Star at .dev` (data) as appropriate, and mark the SFC-served-by-Star success criteria superseded (vite serves the preview now). Pointer to nebula-dev-flows.md Flow 1.
  - [ ] **[banner]** Phase 2 edits `star.ts`'s `APP_CSP` to govern the served app. In the pivot the Star no longer serves the app shell/scripts (DevContainer/vite in dev, Workers-Assets/Galaxy bundle in prod), so CSP for the served preview belongs to the serving surface (DevContainer fetch() proxy / Workers Assets), not `Star.onRequest`. Per nebula-dev-flows.md Decision 3 + DevContainer-internals.
        → Mark Phase 2 superseded; note that strict CSP for the preview is now a property of the DevContainer-served preview / Workers-Assets-served Studio UI, not `star.ts` `APP_CSP`. Reference nebula-dev-flows.md.
  - [ ] **[delete]** Deferred items all concern the in-DO specifier-rewrite / vendored-bundler subsystem (`rewriteServedSpecifiers`, per-app Tailwind JIT, tree-shaking bundler in a DO). These are moot under the pivot — vite is a real bundler that handles dynamic imports, tree-shaking, and Tailwind JIT natively. The As-built section already names these as the ceilings the pivot removes.
        → Mark the Deferred list moot/superseded: real vite supplies dynamic-import handling, tree-shaking, and Tailwind JIT, so the rewrite-extension / in-DO-bundler backlog items are obviated. Pointer to nebula-dev-flows.md.
  - [ ] **[pointer]** References point only to pre-pivot task files (themselves on the realignment list in nebula-dev-flows.md § Downstream) and a `dev-star.ts` write boundary; there is no pointer to the now-canonical dev-loop spec. Per nebula-dev-flows.md the canonical model lives there and these referenced files are slated to be realigned to it.
        → pointer — add `tasks/nebula-dev-flows.md` as the canonical dev-loop/publish reference at the top of References (it supersedes the compile-pipeline/app-versioning specifics cited here). Note the referenced task files are themselves being realigned to nebula-dev-flows.md.
  - [ ] **[banner]** The `VUE_RUNTIME_JS` re-export shim + vendored Vue is part of the superseded in-DO serving substrate (vite serves Vue from baked deps now). Listed only to ensure the term-swap sweep catches `app-bundle.ts VUE_RUNTIME_JS` references as historical, not actionable.
        → Mark as historical under the banner; vite resolves `vue` from baked node_modules, so the `VUE_RUNTIME_JS` re-export shim is retired. No forward action.

### nebula-studio-compile-pipeline.md — 33 findings · superseding-banner
  - ⚠ **banner-hazard:** The file's top banner (line 3) is a 2026-06-18 SUPERSEDED banner that re-homes this file as the "Phase-4 teardown reference for the container reshape" pointing to nebula-container-dev-loop.md. That banner's premise is itself now re-superseded by the pivot: it preserves the in-DO compile/serve as a "zero-cost fallback" (`in-do-compile-baseline` tag) and frames the container reshape as still needing a Phase-4 teardown of in-DO compile. Under nebula-dev-flows.md the in-DO compile model is not a fallback to keep warm — it is permanently retired (vite in DevContainer owns compile; Star never compiles), and the canonical pointer is now nebula-dev-flows.md, not nebula-container-dev-loop.md. The banner must be rewritten so it points to nebula-dev-flows.md and drops the "fallback / Phase-4 teardown reference" framing; the old banner's conclusion is stale, not authoritative.
  - [ ] **[rewrite]** Title frames the file as in-DO 'compile + serve' on 'DevStar/Star'. Per nebula-dev-flows.md Decision 9, the Star NEVER compiles (vite in DevContainer compiles SFCs; DevStudio compiles only the ontology validator), and there is no DevStar class (Decision 2 — the dev Star is a plain Star at the {u}.{g}.dev instance). The whole premise of the title is dead.
        → Retitle to reflect that this is a retired/superseded approach, e.g. '# Nebula Studio — in-DO SFC compile + serve (RETIRED — superseded by container vite + DevStudio)'. The DevStar/Star compile mechanism is no longer the dev-loop model; canonical model is tasks/nebula-dev-flows.md.
  - [ ] **[banner]** This existing superseding banner is itself re-superseded by the pivot. It (a) points to nebula-container-dev-loop.md as canonical, but nebula-dev-flows.md is now THE canonical dev-loop source of truth (and that doc itself realigns nebula-container-dev-loop.md); (b) preserves the in-DO compile/serve as a 'zero-cost fallback' / 'Phase-4 teardown reference', but the pivot permanently retires in-DO compile (Decision 9: Star never compiles), so it is not a fallback to keep warm.
        → Replace the banner with a new dated banner: '⚠️ SUPERSEDED 2026-06-19/20 — the dev loop no longer compiles in-DO at all. Canonical model: tasks/nebula-dev-flows.md. vite (in DevContainer) owns SFC compile; DevStudio compiles only the ontology validator; the Star NEVER compiles. This file's entire compile-in-DevStar/serve-from-onRequest/reload-channel mechanism is retired (not a fallback). Kept for historical reference only — do NOT /build-task. Icebox/archive.' Drop the 'zero-cost fallback' and 'Phase-4 teardown reference' framing and the nebula-container-dev-loop.md pointer as canonical.
  - [ ] **[other]** Scope is 'DevStar/Star compile + serve + reload' — all three mechanisms are eliminated by the pivot. No DevStar class (Decision 2); the Star never compiles or serves the app (Decision 9 + the DevContainer serves the preview shell/HMR per the Topology + DevContainer-internals diagram); the in-DO reload channel is replaced by vite HMR pushed from DevContainer.
        → Mark the Status as historical under the new banner; do not claim this is the live build. Note that compile/serve/reload have all moved off the Star: compile → vite (DevContainer) / ontology-validator-compile → DevStudio; serve → DevContainer vite; reload → vite HMR.
  - [ ] **[rewrite]** Per Decision 5/7, dev source-durability is owned by DevStudio (shell Workspace + local git), NOT by nebula-app-versioning.md / Galaxy. Per Decision 9, there is no Galaxy 'ontology-version registry'; Galaxy holds only the published app-version (built bundle). The Galaxy dual-write source design is one of the three conflicting source-durability designs the pivot explicitly purges (Decision 7).
        → Rewrite to: source durability lives in DevStudio (shell Workspace + local git), per nebula-dev-flows.md Decisions 5/7. Galaxy is published-only (built bundle; no source, no ontology-version registry). Remove the implication that dev source-durability belongs in nebula-app-versioning.md/Galaxy.
  - [ ] **[rewrite]** Two contradictions: (1) `DevStar extends Star` — no DevStar class exists (Decision 2: plain Star at {u}.{g}.dev). (2) `deployToDev()` (eager ontology apply pulling from Galaxy) is eliminated (Decision 9 / Open-to-confirm: ontology is a normal writeSource→commit→push and DevStudio applies the compiled validator to the dev Star — there is no deployToDev-pulls-from-Galaxy step). Only resetDevData survives, now .dev-instance-guarded on the plain Star.
        → Replace with: the dev Star is a plain `Star` at the `{u}.{g}.dev` instance (no DevStar subclass); the only dev-specific method is `resetDevData()` hard-guarded to `.dev` instances. Delete `deployToDev()` — DevStudio pushes the compiled ontology validator to the dev Star on save (Decision 9/11).
  - [ ] **[rewrite]** Decision 9 eliminates the Galaxy ontology-version registry entirely (`appendOntologyVersion`/`getLatestOntologyVersion` subsystem removed). There is one unified app-version (= ontology version) and Galaxy keeps only the published built-bundle role. Saying the registry is being 'broadened to an app-version registry' contradicts the pivot's collapse to a single published-app-version with no separate ontology registry.
        → Replace with: there is no Galaxy ontology-version registry (eliminated, Decision 9). Galaxy holds only the published app-version (built bundle); app-version = ontology version (lock-step by construction). The dev Star receives the compiled validator via a DevStudio push, not a Galaxy lazy-pull, in dev.
  - [ ] **[rewrite]** Direct contradiction of Decision 9: the Star NEVER compiles. SFC compilation is owned by vite running inside DevContainer; the preview shell + HMR are served from DevContainer (Topology + DevContainer-internals diagrams), not from the Star's onRequest. The Star's role is data only (Resources/transactions/subscribe).
        → Rewrite the Goal to state the in-DO compile/serve approach is retired. In the canonical model: vite in DevContainer compiles `.vue` SFCs and serves the preview shell + HMR; DevStudio pushes changed source (applyChanges); the Star serves data only and never compiles or serves app code.
  - [ ] **[rewrite]** Per the pivot, the Star does not serve app bundles at all in dev — DevContainer's vite serves the preview shell/HMR (Topology). Source durability is in DevStudio, not nebula-app-versioning.md/Galaxy (Decisions 5/7). The 'lazily pulled from Galaxy' dev path is replaced by DevStudio's push of source to DevContainer + push of the compiled validator to the Star.
        → Remove the 'onRequest serves the resident bundle' / 'lazily pulled from Galaxy / durability in nebula-app-versioning.md' model. In dev, the preview is served by DevContainer vite; durable source is in DevStudio. (Prod app-bundle serving is Galaxy→Star lazy-pull of the published app-version, Flow 2b — but that serves the built bundle, not an in-DO-compiled one.)
  - [ ] **[delete]** The entire 'serve the app bundle from Star.onRequest()' decision is dead. The preview shell + HMR are served by DevContainer (its fetch() proxy → vite :5173, per DevContainer-internals). The Star does not host the running app's static shell.
        → Strike/mark-historical the whole 'Serve via Star.onRequest()' decision and its consequence. App-shell serving belongs to DevContainer's vite in the canonical model; the Star is data-only.
  - [ ] **[delete]** Contradicts Decision 9 (Star never compiles), Decision 2 (no DevStar class), and the serving topology (DevContainer serves the preview, Galaxy serves the published bundle in prod via Flow 2b). `compileSFC` is explicitly deleted (Decision 2: 'compileSFC is deleted anyway (vite owns compile)'). 'reload-broadcast' on a DevStar class is replaced by vite HMR.
        → Delete this decision. compileSFC is removed (vite owns compile); there is no DevStar class; in-DO reload-broadcast is replaced by vite HMR pushed from DevContainer (Flow 1 · B).
  - [ ] **[rewrite]** The preview is loaded from DevContainer at `/dev-container/{u}.{g}.dev/` (Topology: 'PV -->|shell + HMR · /dev-container| DC'; Flow 1 · A: 'GET /dev-container/{u}.{g}.dev/'), NOT from a `/dev-star/` route on the Star. Also `DEV_STAR` binding is eliminated (Decision 2: 'The DEV_STAR binding + its smart-match guard disappear; #starBinding() collapses to always STAR').
        → Replace with: the preview loads from DevContainer at `/dev-container/{u}.{g}.dev/` (same origin). There is no `/dev-star/` serving route and no DEV_STAR binding (collapsed to STAR with a .dev instance, Decision 2).
  - [ ] **[term-swap]** Relies on the DEV_STAR binding, which Decision 2 eliminates ('#starBinding() collapses to always STAR'). The dev/prod split is an instance concern (.dev), not a binding/class concern.
        → Remove DEV_STAR references; routing to the dev Star is the STAR binding with a `{u}.{g}.dev` instance name.
  - [ ] **[rewrite]** The public, ungated static-shell GET path now lives on DevContainer's fetch() proxy (DevContainer-internals: 'Preview | :5173 vite | public / ungated'), not on the Star's onRequest. The Star does not serve the shell. The 'open the entrypoint onBeforeRequest for GETs to Star' mechanism is therefore moot.
        → Re-home the public-ungated-shell-GET reasoning to DevContainer (its fetch() proxy serves the preview shell + HMR ungated; the command-server :9000 is host-DO-only). The Star's onRequest no longer serves a shell, so the entrypoint-onBeforeRequest gate change is unnecessary.
  - [ ] **[rewrite]** 'render functions precompiled server-side → runtime-only Vue, no compiler' is the in-DO compile rationale. With vite owning compile in DevContainer (Decision 9), there is no server-side precompile-into-the-Star path; the dev preview is served by vite (which includes HMR/dev tooling), not by a Star SPA host.
        → Remove the Star-as-SPA-host / server-precompiled-render-functions framing. Compile is vite's job in DevContainer; the Star serves no app assets.
  - [ ] **[delete]** Storing a compiled app bundle in Star storage contradicts Decision 9 (Star never compiles; receives only the compiled validator) and the serving topology (DevContainer holds the working tree / vite serves it). There is no compiled-app-bundle-in-Star-storage in the canonical model.
        → Delete the bundle-storage-contract. The dev app is in DevContainer's working tree (pushed by DevStudio via applyChanges); vite serves it. The only thing DevStudio pushes to the Star is the compiled ontology validator (Decision 9/11).
  - [ ] **[rewrite]** Scope injection now happens in DevContainer's serve path ('server-derived scope (meta)' in Flow 1 · A; DevContainer-internals: 'fetch() proxy ... scope injected'), not in Star.onRequest. The cited footgun ('#starBinding() picks STAR not DEV_STAR') no longer exists because DEV_STAR is eliminated (Decision 2 — #starBinding() always picks STAR; dev/prod is the .dev instance name).
        → Re-home server-side scope injection to DevContainer's fetch() proxy (Flow 1 · A 'server-derived scope'). Drop the STAR-vs-DEV_STAR footgun rationale (no DEV_STAR binding); the dev/prod distinction is the `.dev` instance name carried into mesh calls.
  - [ ] **[term-swap]** Term drift: 'DevStar wipe'. No DevStar class (Decision 2). The wipe is resetDevData on a plain Star at the .dev instance.
        → Change 'DevStar wipe' → 'a resetDevData wipe of the `.dev` Star'.
  - [ ] **[delete]** The whole tsc-in-workerd / nodejs_compat_v2-for-@vue/compiler-sfc concern exists only because the DO was doing SFC compile. With vite owning compile in DevContainer (Decision 9), the Star/DO does not import typescript or @vue/compiler-sfc at all, so this constraint is moot. (Also memory note: nodejs_compat_v2 was a footgun — use nodejs_compat — but that's secondary to the whole concern being dead.)
        → Delete the tsc-transpile / nodejs_compat_v2 constraint — no in-DO SFC compile means no typescript/@vue/compiler-sfc import in the Star or any nebula DO.
  - [ ] **[delete]** This entire spike (bundling typescript to run transpileModule inside the DevStar DO) is for in-DO compile, which Decision 9 eliminates. The vendored tsc bundle (apps/nebula/vendor/tsc-transpile.bundle.mjs), bundle:tsc script, and compile-module.ts are all dead artifacts under the pivot.
        → Mark the whole Phase-1 spike section as historical/dead. In-DO SFC transpile is gone (vite compiles in DevContainer). The findings are retained only as a record of why we did NOT keep compile in the DO. The vendored tsc bundle and compile-module.ts should be removed (track teardown under the new banner).
  - [ ] **[delete]** Phase 1's deliverable — a compile method on DevStar that runs SFC compile in the dev Star DO — is exactly what Decision 9 forbids (Star never compiles) and Decision 2 forbids (no DevStar class). compileSFC is explicitly deleted (Decision 2).
        → Mark Phase 1 as a retired mechanism. There is no compile method on the Star/DevStar; SFC compile is vite's job in DevContainer; the only compile DevStudio does is the ontology .d.ts → validator (Decision 9).
  - [ ] **[delete]** Adds a compile method writing compiled modules to Star storage — directly violates Decision 9 (Star never compiles; receives only the compiled validator) and references the deleted DevStar class + its @mesh-admin freeze and the dead bundle-storage-contract.
        → Delete. No compile method on the Star. (The realigned dev-star.md homes only resetDevData; setting the current ontology validator is a general Star op pushed by DevStudio — Decision 11.)
  - [ ] **[delete]** Assumes the bundle/scaffold live in Star storage and that the Star 'serves' the app and recompiles via compileSFC — all dead. resetDevData wipes dev DATA only; source lives in DevStudio (a different DO), so the reset precondition is moot (Downstream note for dev-star.md: 'the reset precondition is moot (source is in DevStudio, a different DO)').
        → Delete. resetDevData wipes only dev data on the .dev Star; app source/bundle is not in the Star (it's in DevStudio + DevContainer). No compileSFC, no Star serving.
  - [ ] **[delete]** Serving the running app from Star.onRequest is replaced by DevContainer vite serving the preview shell + HMR (Topology + Flow 1 · A + DevContainer-internals). The Star does not serve app shells/assets in the canonical model.
        → Mark Phase 2 retired. The running preview app is served by DevContainer's vite (its fetch() proxy → :5173); the Star does not implement an app-serving onRequest.
  - [ ] **[delete]** References the eliminated DevStar 'serving target' and the now-unnecessary entrypoint-gate change (the Star doesn't serve the shell; DevContainer does). Term + mechanism drift.
        → Delete. No Star/DevStar serving target; the public preview-shell GET is served by DevContainer's fetch() proxy (host-DO trust boundary on the :9000 command-server).
  - [ ] **[delete]** onRequest reading a resident bundle and the DEV_STAR binding header are both dead (Decision 2/9). Path/SPA-fallback serving belongs to DevContainer vite.
        → Delete. SPA serving + path handling is vite's in DevContainer; there is no resident bundle in the Star and no DEV_STAR binding.
  - [ ] **[delete]** The in-DO per-Star reload channel (subscribeReload/broadcastReload + DevStar.compileSFC calling broadcastReload) is replaced by vite HMR. Flow 1 · B: 'DC-->>PV: HMR js-update — patch in place'. There is no Star-hosted reload channel, no compileSFC, no DevStar.
        → Mark Phase 3 retired. Live reload is vite HMR pushed from DevContainer to the Preview app (Flow 1 · B), not a Star-hosted subscribeReload/broadcastReload channel.
  - [ ] **[delete]** DevStar.compileSFC is doubly dead: no DevStar class (Decision 2), and compileSFC is explicitly deleted ('compileSFC is deleted anyway (vite owns compile)'). broadcastReload (in-DO reload) is replaced by vite HMR.
        → Delete. compileSFC does not exist; reload is vite HMR from DevContainer.
  - [ ] **[rewrite]** This Phase 4 tears down the OLD sfc-devstar-loop spike but leaves the in-DO compile pipeline (compile-module.ts, vendor/tsc-transpile.bundle.mjs, the DevStar compile/serve/reload methods) in place as 'the build'. Under the pivot, those ported artifacts are themselves now dead and need teardown — the file's notion of 'teardown complete' is stale.
        → Under the new banner, note that the genuine teardown now extends to the in-DO compile pipeline itself (compile-module.ts, vendor/tsc-transpile.bundle.mjs, scripts/bundle-tsc.mjs, the Star/DevStar compile/serve/reload methods, the DEV_STAR binding, deployToDev). That teardown is the pivot's container-dev-loop build (see nebula-dev-flows.md Downstream), not this file.
  - [ ] **[rewrite]** Describes Galaxy as an 'ontology + bundle' app-version registry with the Star lazy-pulling the dev version. Decision 9: no Galaxy ontology registry; one unified app-version = built bundle only; ontology compiles into the bundle. In dev there is NO Galaxy lazy-pull — DevStudio pushes the validator to the dev Star directly (Decision 9/11). Galaxy lazy-pull is a prod-only path (Flow 2b) and pulls the built bundle, not a separate ontology version.
        → Realign the cross-references: Galaxy = published-only built-bundle registry (no separate ontology version; app-version = ontology version, Decision 9). Dev distribution is not a Galaxy lazy-pull — DevStudio pushes source to DevContainer (applyChanges) and the compiled validator to the dev Star. Galaxy→Star lazy-pull is prod-only (Flow 2b).
  - [ ] **[delete]** 'Parallel source durability to Galaxy' is the Galaxy dual-write source design — one of the three conflicting source-durability designs the pivot explicitly purges (Decision 7: all collapse into DevStudio's shell Workspace). Galaxy receives the built artifact, never dev source (Decision 4/Cast).
        → Delete the Galaxy parallel-source-durability item. Source durability is solely DevStudio's shell Workspace + local git (Decision 5/7); Galaxy never stores dev source.
  - [ ] **[rewrite]** The 'DaisyUI precompiled bundle (no per-app JIT) for the demo' constraint exists because there was no real vite in the dev loop (in-DO serving couldn't run Tailwind JIT). With real vite in DevContainer (nebula-dev-flows.md Cast/Flow 1c, deps baked), per-app Tailwind JIT is available in dev — the precompiled-only limitation is an artifact of the dead model.
        → Re-evaluate / drop: real vite in DevContainer can run Tailwind JIT; the precompiled-DaisyUI-only constraint was a limitation of in-DO serving, not the container model. (Defer the exact decision to the container dev-loop task.)
  - [ ] **[pointer]** References point at superseded designs/artifacts: nebula-app-versioning.md and dev-star.md are themselves being realigned (Downstream list); compile-module.ts + bundle-tsc.mjs are the dead in-DO-compile artifacts; dev-star.md's 'DevStar' framing is gone. No pointer to the canonical nebula-dev-flows.md.
        → Add tasks/nebula-dev-flows.md as the canonical pointer at the top. Update References: dev-star.md → 'the dev Star is a plain Star at .dev (resetDevData only)'; nebula-app-versioning.md → 'Galaxy = published built-bundle only'; drop compile-module.ts/bundle-tsc.mjs as live references (they are dead in-DO-compile artifacts to be removed).
  - [ ] **[other]** These in-DO compile latency measurements describe the dead in-DO compile path. Under the pivot, compile runs in vite (DevContainer) — the spike timings are historical and no longer characterize the dev loop.
        → Retain only as a historical spike record under the banner; note that compile no longer runs in a DO, so these numbers do not describe the canonical loop (which uses vite in DevContainer).

### on-hold/nebula-file-storage-backend.md — NEW (critic)
  _(not in audited set — see critic guidance below)_

### nebula-devcontainer-node-type.md — 11 findings · minor-edits
  - [ ] **[term-swap]** ADRs are authority #1. docs/adr/007-shared-node-security-core.md is now **Status: Accepted** (since 2026-06-17 — the gate was met: Q5 spike GOed + this 4th node type landed). The workflow.md index also lists ADR-007 as Accepted. Labeling it 'Proposed' contradicts the live ADR.
        → Replace 'Realizes **ADR-007 (Proposed)**.' with 'Realizes **ADR-007 (Accepted)**.'
  - [ ] **[rewrite]** The flip has already happened — ADR-007's Status line reads 'Accepted' with a 2026-06-17 note crediting this very task's landing. The instruction is stale (describes a future action that is now complete) and implies the ADR is still Proposed.
        → Rewrite to past tense / done state, e.g.: '→ ADR-007 was flipped Proposed→Accepted on landing (`docs/adr/007-shared-node-security-core.md` Status: Accepted, 2026-06-17).'
  - [ ] **[term-swap]** Same stale-status drift in the References section: ADR-007 is Accepted, not Proposed (authority #1, docs/adr/007).
        → Change '**ADR-007 (Proposed)**' to '**ADR-007 (Accepted)**'.
  - [ ] **[term-swap]** Two contradictions vs nebula-dev-flows.md: (1) There is NO `DevStar` class (Decision 2 — the dev Star is a plain `Star` at the `{u}.{g}.dev` instance). (2) The public-shell-serving role is not a Star `onRequest` at all — in the canonical model the Preview app loads its shell + HMR from **DevContainer's `fetch()` proxy** (Topology + Flow 1c + DevContainer-internals diagram); the analogy to a 'DevStar onRequest' is doubly stale.
        → Drop the DevStar analogy. Rewrite to: 'it serves only the **public preview shell** (frontend code + ontology types every client gets anyway) — this is exactly the DevContainer `fetch()` preview-proxy surface the #1a dev-loop builds on top of this node type. No tenant data flows through it.'
  - [ ] **[term-swap]** nebula-dev-flows.md Decision 1 renames the server orchestrator **DevStudio** and explicitly avoids the word 'agent' ('technically an agent but not using that term to avoid confusion with Cloudflare's agent toolkit'). The code that runs in the container is what DevStudio generates / the user-developer's app code; 'agent-authored' is the pre-pivot framing.
        → Replace 'agent-authored code' with 'DevStudio-generated / user-developer app code' (and 'the agent-code path' later in the same bullet → 'the generated-code path').
  - [ ] **[term-swap]** The server node that drives the command channel and talks to DevContainer is **DevStudio** per nebula-dev-flows.md (Topology: STU -->|applyChanges (mesh)| DC; DevContainer-internals: STU -->|applyChanges (push) · mesh| M). 'the agent' is the stale orchestrator name.
        → Replace 'the agent command channel' → 'the DevStudio command channel' and 'The agent talks to DevContainer over the mesh' → 'DevStudio talks to DevContainer over the mesh (`applyChanges`/`viteControl`)'.
  - [ ] **[term-swap]** Same 'agent' → DevStudio terminology drift (Decision 1). The code path is the generated/app code DevStudio writes, not 'agent-authored'.
        → Replace 'the agent-authored code path' with 'the generated-code path (the app code DevStudio writes)'.
  - [ ] **[term-swap]** 'the in-scope Nebula agent DO' is the orchestrator, renamed **DevStudio** in nebula-dev-flows.md (Decision 1). Per the canonical model the command channel is reachable only by the host DevContainer DO via internal `containerFetch`, driven by DevStudio over mesh.
        → Replace 'the in-scope Nebula agent DO' with 'the in-scope DevStudio DO (over mesh, via the host DevContainer DO's internal `containerFetch`)'.
  - [ ] **[pointer]** nebula-dev-flows.md Decisions 5/7/9 make **DevStudio's shell `Workspace` + local git the sole source-of-truth** and collapse the three competing source-durability designs (Galaxy dual-write / DevContainer DO store / dev-Star file-resources). Source durability no longer homes in nebula-app-versioning.md (Q6); Galaxy is published-only. This pointer sends the reader to a now-superseded home.
        → Repoint to the canonical doc: 'Source-of-truth / source durability → DevStudio (shell `Workspace` + local git); canonical model in `tasks/nebula-dev-flows.md` (Decisions 5/7/9). Prod static-asset serving → the prod-serve task.'
  - [ ] **[pointer]** Not a contradiction in itself (correctly scopes the file as role-free, matching nebula-dev-flows.md line 342), but the file has NO pointer to the canonical dev-loop doc anywhere — a reader can't find where DevContainer's source-of-truth / DevStudio-push role is now specified. The canonical doc is the new single source of truth for the dev loop.
        → Append a pointer: '(The Studio-specific `DevContainer` ... this file builds only the node type. The dev-loop roles composed on top — DevStudio = source-of-truth + orchestrator, DevContainer = disposable checkout — are canonical in `tasks/nebula-dev-flows.md`.)'
  - [ ] **[other]** Not drift, but verification per the audit charge: nebula-dev-flows.md line 355 says 'No ADR change — DevStudio is a LumenizeDO-family node, not a new node type.' This file correctly defines only LumenizeContainer/NebulaContainer (the 4th node type) and does NOT mis-claim DevStudio as a 5th node type — consistent with the authority. No edit needed; flagged only to confirm the role-free framing holds.
        → No edit — confirms conformance (the file builds the container node type only; DevStudio is composed on top as a LumenizeDO-family node, per nebula-dev-flows.md and ADR-007).

### nebula-studio-llm-strategy.md — 5 findings · minor-edits
  - [ ] **[rewrite]** The canonical doc (nebula-dev-flows.md, Decision 1 + Cast) names the codegen engine **DevStudio** and deliberately avoids the term "agent" ("It's technically an 'agent' but not using that term to avoid confusion with Cloudflare's agent toolkit"). It also classifies DevStudio as a Nebula-owned **server DO** that is the orchestrator AND source-of-truth — not a co-located facet. Decision 8 explicitly defers facets ("DevStudio running user-provided app code") to a future feature, not the current home. The downstream realignment list (line 345) calls this out: "'agent home' → DevStudio."
        → Replace with: "- **Home: DevStudio** — the Nebula-owned **server DO** that is the codegen orchestrator and source-of-truth (shell `Workspace` + local git). See `tasks/nebula-dev-flows.md` for the canonical cast/topology. (Facets — DevStudio running user-provided app code — are a deferred future capability, not the authoring-loop home.)"
  - [ ] **[term-swap]** Refers to the authoring loop running in/around a generic home without naming DevStudio, and presents "facet sandbox" as the script-per-step substrate. Per the canonical model the authoring loop IS DevStudio (the server DO), and facets are a deferred future capability (Decision 8). Framing the optimization as a near-term facet sandbox conflicts with facets being explicitly not-yet-designed.
        → Term-swap: name the loop owner — "**Studio authoring loop** (this file) — DevStudio's per-call tool-calling to start." Keep script-per-step as a pocketed optimization but mark the facet sandbox as the deferred-facet mechanism, consistent with Decision 8 (facets are a future feature), e.g. "...run in a facet sandbox (a deferred capability — see nebula-dev-flows.md Decision 8) with a mesh/full-type bridge."
  - [ ] **[term-swap]** "Same substrate: a Nebula-owned facet" presents facets as the current substrate. The canonical doc (Decision 8) defers facets to a future feature and is the source-of-truth for the node/topology model. This is the in-app-AI context (not the Studio dev loop), so it is lower-priority drift, but the bare "facet" claim should be qualified as future per the canonical doc.
        → Soften/qualify: "Same substrate (a Nebula-owned **facet** — a deferred capability, see nebula-dev-flows.md Decision 8) + our own dynamic execution (no Think, no codemode-JSON)."
  - [ ] **[pointer]** No drift in the content, but the file has no pointer to the now-canonical `tasks/nebula-dev-flows.md`, which is the authoritative cast/topology/orchestration model for the dev loop. Since this file describes orchestration ("Agent home", "Studio authoring loop"), a reader needs the canonical topology reference to interpret "DevStudio" correctly. nebula-dev-flows.md is the canonical model that supersedes earlier dev-loop framing.
        → Add a pointer sentence to the Parent/Status area, e.g.: "Canonical dev-loop cast & topology (DevStudio = orchestrator + source-of-truth; no `DevStar` class; Galaxy = published-only): `tasks/nebula-dev-flows.md`."
  - [ ] **[other]** Secondary point on the same line: the claim "co-located with the user for low latency (facet = isolated child DO sharing the supervisor's colo)" describes DevStudio as a child facet of a per-user supervisor. The canonical Cast lists DevStudio as a standalone server DO and the Topology shows it as a top-level node (Studio UI → DevStudio over mesh /gateway WS). The latency rationale survives, but the facet/child-DO mechanism is now a deferred capability rather than DevStudio's structure.
        → If the rewrite above is applied this is subsumed; otherwise drop the "facet = isolated child DO sharing the supervisor's colo" parenthetical and state DevStudio is a per-session server DO co-located via instance placement, per nebula-dev-flows.md Cast/Topology.

### on-hold/nebula-skills.md — NEW (critic)
  _(not in audited set — see critic guidance below)_

### backlog.md — AFFECTED (was mis-flagged as skip; 3 real findings)
  - [ ] Per nebula-dev-flows.md Decision 5 + the source-of-truth reversal headline, there is NO DevContainer-DO source store: DevStudio (shell Workspace + local git) is the source-of-truth and the DevContainer is a disposable checkout that DevStudio re-populates by PUSHING (applyChanges) on cold boot. 'Re-hydrate from the DevContainer-DO source store' is the reversed (dead) design. → Replace 'Q#2 (DevContainer-DO source store + re-hydrate-on-cold-boot)' with 'Q#2 (the on-cold-boot source re-population — now DevStudio pushing the tree via applyChanges, per nebula-dev-flows.md)'. And change '...survive a sleep/wake with no reinstall and no re-hydrate' to '...survive a sleep/wake with no reinstall and no re-population push'.
  - [ ] This names the dev-loop source-of-truth as the 'Q#2 DO store' or 'Galaxy #1b'. The canonical model (nebula-dev-flows.md Decision 5/7 + reversal headline) makes DevStudio (shell Workspace + local git) the sole durable source-of-truth; the DevContainer-DO store is reversed, and 'Galaxy dual-write' is explicitly one of the three source-durability designs Decision 7 says to PURGE. Galaxy is published-only (built bundle), never a source backstop. → Replace 'a durable source-of-truth backstop (the Q#2 DO store, or Galaxy #1b) is probably still needed for instance-loss + portability + upgrade — snapshots **reduce** the Q#2 need (the common sleep/wake path) but may not **eliminate** it' with 'the durable source-of-truth (DevStudio — shell Workspace + local git, per nebula-dev-flows.md) is still needed for instance-loss + portability + upgrade — snapshots **reduce** the cold-boot re-population push (the common sleep/wake path) but may not **eliminate** it'.
  - [ ] The 'DO-store' (Q#2 DevContainer-DO source store) is no longer a correct/shippable decision: nebula-dev-flows.md reverses it to DevStudio-as-source-of-truth with a push (applyChanges) re-population model. Q#1 (bake deps) does survive (canonical Flow 1c bakes deps), so only the DO-store half is wrong. → Replace 'revisit Q#1 (bake) and Q#2 (DO-store) in nebula-container-dev-loop.md; the bake + DO-store decisions remain correct and shippable for the demo regardless' with 'revisit Q#1 (bake) and the cold-boot source re-population path (now DevStudio push, per nebula-dev-flows.md) in nebula-container-dev-loop.md; the bake decision and DevStudio-push source model remain correct and shippable for the demo regardless'.

## P3 — schema-evolution cluster (NEW · on-hold; banner re-homing registry → DevStudio + single app-version)
- [ ] **on-hold/nebula-5.5-schema-evolution.md** — superseding banner → Decisions 9+11; purge appendOntologyVersion/OntologyVersionRow/ontology:_index registry foundation → single app-version + per-resource ontology-version stamp + version-on-request on a plain Star.
- [ ] **on-hold/nebula-lazy-schema-migrations.md** — same banner; re-home the per-version Galaxy registry + dev-Star-special migration path to the DevStudio/single-app-version model (Decision 11: rides the per-resource version stamp on a plain Star, no dev-special path).
- [ ] **on-hold/nebula-studio-eval-suite.md** — light: drop `appendOntologyVersion` from the harness; swap the "SFC compiles in-DO" gate for "vite build succeeds"; retire the "§5.3.7 SFC substrate" resumption trigger.

## P4 — light / scratch (NEW)
- [ ] **nebula-scratchpad.md** — the registry-as-"already addressed" claim (Studio Follow-On L125-147) + `deploy_to_main`→OntologyVersionRow are stale; note git-in-DO/checkpoints is now the SHIPPING design (Decision 6), not a future idea.
- [ ] **nightly/backlog.md** — cull/re-scope review candidates #22/#23/#24 (premised on the eliminated OntologyVersionRow/registry).
- [ ] **vibesdk-llm-patterns.md** — L47 `deploy_to_dev` token only (borderline; the file mines an external repo so most "agent" refs are out of scope).

## Confirmed SKIP (false positives — generic "source of truth")
- nebula-tenant-ai-billing.md · on-hold/nebula-resource-history-r2.md · on-hold/wire-merge-patch-sync.md

## Do NOT touch (frozen / historical)
- `archive/**` (frozen on entry) · spike records (`container-vite-spike.md`, `preview-iframe-spike.md`) · `kimi-ui-gen-viability.md`
