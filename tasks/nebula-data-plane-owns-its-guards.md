# Every Resources guard lives in the Resources plane

**Status:** Pass 1 — design intent only, phases NOT written. Scoped to the **Galaxy and the Star**, the two hosts that compose the plane. **The Resources leg of the security-legibility work**, after `docs/vision/auth.md`, [ADR-015](../docs/adr/015-passage-and-dominion.md)'s passage/dominion vocabulary, and the Registry routing rewrite that put its guards in one readable table. Judge this as a change to how the access-control model is reasoned about, not as a refactor.

**Objective — answering *"who is allowed to do this to a resource?"* takes reading one bounded region of code.**

**Three goals, in the order they matter:**

1. **The answer stops being split across the host and the plane.** It is in seven places today: `requirePassage` in `onBeforeCall`, the `requireDominionHere` decorators, the identity derivations inside host method bodies, the permission calls inside `Resources`, those inside `Subscriptions`, none in `QuerySubs` (which filters at delivery instead), and the `OrgTree` sites including the dominion short-circuit. Seven places is not an audit, it is an archaeology.
2. **No guard sits where a reader would not think to look.** Those derivations are not plumbing. `clientId` decides whose subscriber row is written and whose row a delete removes, and the code says so: *"`clientId` is supplied by the Handler-1 wrapper from `callChain[0]` (NEVER a param), so a client can only drop its OWN row."* That is a guard, it lives on the host, and it is copied per host — `unsubscribe` matches statement-for-statement across `star.ts` and `galaxy.ts`.
3. **Every name answers "what is this?" without being asked, starting with the plane itself — it is the RESOURCES plane, never the "data plane"** (Larry, 2026-08-23: *"what we will forever more call the resources plane"*). The temporal storage engine is a thing called `Resources` *inside* a thing called `ResourceDataPlane`, and `QuerySubs` opens by calling itself *"a near-clone of `Subscriptions`"*. Names you have to ask about are a model you cannot hold.

⚠️ **Every count in this file is a measurement, not a fact — the phase re-runs each one with an ANCHORED pattern.** An unanchored grep counts the JSDoc prose that discusses a decorator alongside the decorator itself, and both hosts discuss theirs at length; the dominion tally was written wrong twice for exactly that reason before anyone anchored it. The instruments: `grep -rcE '^\s*@mesh\(requireDominionHere' apps/nebula/src/*.ts` and `grep -cE 'callContext\.callChain' apps/nebula/src/star.ts apps/nebula/src/galaxy.ts` (2026-09-22: **5**, and **14 + 10**), and `grep -cE '^\s*@mesh\(' <host>` (**21** Galaxy, **20** Star).

The sections below give the guard model these goals imply, the surface it allocates, and the three questions still open.

## Decisions

Settled unless marked OPEN. A row here is the whole record — the body states each decision positively, as if its alternatives were never on the table, so nothing in the prose exists to hold an earlier version at arm's length.

| # | Decision | Date |
|---|---|---|
| D1 | **One `@mesh()` door per host.** A host exposes `resources()` and nothing else on the request leg. The `@mesh() orgTree()` gate (`dagTree()` before D16) does not survive beside it — `OrgTree` is part of the plane, and a capability inside it does not get its own entrance. | 2026-08-23 |
| D2 | **The per-host data-plane entries are left behind**; their derivation moves in and the client's per-entry continuations collapse to `ctn<Host>().resources().<op>(...)`. | 2026-08-23 |
| D3 | **Ontology acquisition stays OUTSIDE the plane**, as a required constructor injection. It is a seam with one shape and one supplier, not a second path a caller can take. | 2026-08-23 |
| D4 | **Guards go IN the method, not in a chain before it.** A Resources operation is not uniform and orderable the way the Registry's route table is; what this buys is locality, not tabulation. | 2026-08-23 |
| D5 | **A second accessor, `resourcesResults()`, carries no `@mesh()`** and is reachable only on the response leg. There are two legs, so there are two gates; a third has nothing to reach. | 2026-09-21 |
| D6 | **The plane owns two surfaces, `requests` and `results`, and each accessor returns one** — never the plane itself. A host does one `new` (Larry, 2026-09-22; an earlier sketch had the host assemble two facade objects). | 2026-09-21 |
| D7 | **The three `on*BroadcastResult` reapers shed their `@mesh()`.** | 2026-09-21 |
| D8 | **`fireInvite` moves onto `ResourceHostBridge`**, which is what lets `invite`'s host forward be deleted at all. | 2026-09-21 |
| D9 | **The node operation keeps the name `invite`.** It keys on an EMAIL, because a person new to Nebula has no `sub` to name — so it mints the membership that produces one and starts a 7-day acceptance flow. `grant` would name only the durable half and would sit beside `orgTree.setPermission`, the `sub`-keyed grant, putting the word on the wrong method. | 2026-09-21 |
| D10 | **The Profile is CUT** to [on-hold/nebula-profile-storage.md](on-hold/nebula-profile-storage.md), and the injected authorizer with it, which keeps the plane at six constructor arguments. | 2026-09-22 |
| D11 | **The three config pairs are CUT** to [nebula-upgrade-universe-with-a-data-plane.md](nebula-upgrade-universe-with-a-data-plane.md), the file that authors their replacement and carries the two preconditions their deletion owes. | 2026-09-22 |
| D12 | **The subscriber registries merge.** They are one kind keyed one way; § *The registries are one kind* says which and why. | 2026-09-22 |
| D13 | **Neither surface may expose a path back to the plane.** TypeScript's `private` is erased and `ocan/execute.ts` dispatches `get` by raw property lookup, so a `private readonly plane` field would put every plane method on the wire behind `resources()`. A `#` field is safe; closures over the plane are safer still, because then there is no property to walk back through at all. | 2026-09-22 |
| D14 | **The surface is UNIFORM — every host's `requests` and `results` carry the same members.** `subscribeTree` included: an orgTree is a sub-capability every plane builds, so there is no host that lacks one. A Galaxy orgTree that holds few nodes today returns a small answer, not an error, and may not stay small. | 2026-09-22 |
| D15 | **`onDagChanged` is DELETED and the plane fans out orgTree changes itself**, through a fourth `ResourceHostBridge` member beside `broadcastResourceUpdate` / `broadcastQueryUpdate` / `broadcastRosterUpdate`. The constructor drops from six arguments to five. | 2026-09-22 |
| D16 | **`OrgTree` is renamed `OrgTree`**, and every internal identifier with it. It rides this task as its own first phase. | 2026-09-22 |
| D17 | **`ReloadSubscriptions` is DELETED.** The plane is not the mechanism for `.dev` Star reloads and will not become one — [nebula-scope-moves-to-subdomain.md](nebula-scope-moves-to-subdomain.md) § *Decisions* weighed a plane subscription for exactly this and rejected it. | 2026-09-22 |
| D18 | **The storage engine becomes `Snapshots`** and **the merged registry becomes `Subscriptions`**, class and table alike. Both reuse words already in the repo rather than coining; § *The two names* has the rejected alternatives. | 2026-09-22 |

## Context and current state

**Built already, and what becomes of each part:**

- **`ResourceDataPlane`** — one class, composed by a host with a single `new`, holding five sub-capabilities. **Adapted**: it becomes the plane itself and takes the name, so a host reads `#resources = new Resources(...)`.
- **`Resources`** (683 LOC) — the **temporal storage engine**: Snodgrass snapshot sequences over the host's SQLite, calling `OrgTree.requirePermission` at five sites. **Adapted**: keeps its job, and becomes `Snapshots` (D18) so the plane can take the name.
- **`OrgTree`** (528 LOC) — nodes, edges, grants, resolution, and the `hasDominionOver` short-circuit. **Carried over unchanged**, and it is the model the rest should match: its guards are already in the methods they protect.
- **The per-host data-plane entries** — bare `@mesh()` methods that derive `clientId`/`subscriberBinding`, fail closed, then dispatch. **Left behind**: the derivation moves into the door. Of the Galaxy's 21 decorators and the Star's 20, eight per host are the resource entries and one more is `invite`.
  - **Nothing here is dead, so this is a regrouping and not a deletion.** Every entry still has callers outside its own file; `subscribeQuerySubscribers` has 19. A phase that expects to delete rather than move will find nothing to delete.
- **The host capabilities** — `resetDevData` on the Star, and the three config pairs (D11). **Carried over unchanged**; they are not Resources operations. `setOntology` is not among them: it carries no decorator at all, and its JSDoc makes that absence the not-remotely-callable boundary, so adding `requireDominionHere` would open a door rather than close one. Only the config setters carry dominion; `getStarConfig` and `getGalaxyConfig` are bare `@mesh()`.
- **The Galaxy's four source entries** — `readSource`, `writeSource`, `buildNow`, `applyOntology` — keep their own `@mesh(requireChatWrite)` rather than collapsing behind the door. They look consumer-less, and that reading is wrong: `LOOP_TOOL_ENTRIES` maps each codegen tool to the entries it may reach, so those decorators **are** the model's authority model, and a test reads the table to assert every named entry sits at the chat floor. A door would hide a mapping a reader currently sees at a glance.
  - **`applyOntology`'s absence from that table is already structural.** `LoopEntry` is a closed union of the three source entries and the table is `as const satisfies`, so naming `applyOntology` in it does not compile (TS2322, reproduced). The obligation is to keep `LoopEntry` closed, not to add a mutation test for a hole the compiler already shuts.
- **`getCurrentOntology` and `getOntologyVersion` collapse into `getOntology(version?)`**, absent meaning current. Two call sites, both `star.ts`'s lazy pull, already share one result handler whose `onOntologyPulled('')` encodes "I asked for current". **Both stay BARE**: their access control is upward passage, which every member has, so adding a guard because the neighbours have one would break the pull for ordinary members (`calibration.md` §14). A subscription was weighed and rejected — a Star runs the version a client PINNED, not whatever is current, so a push would invert the model.
- **`invite`, the reapers, and the eviction drains** — host code acting on a *third party's* rows, which is what § *The surface, allocated* exists to place.

### The registries are one kind

**`Subscriptions` (263) · `QuerySubs` (204) · `QuerySubscriberListSubs` (91) · `TreeSubscriptions` (69) merge into one, which keeps the name `Subscriptions` (D18).** All four carry the identical triple `clientId · subscriberBinding · subscribedAt` — the delivery address — and differ on two orthogonal axes: an optional TOPIC column, and an optional AUTHORIZATION identity.

| Registry | PRIMARY KEY | Topic | Authz identity |
|---|---|---|---|
| `Subscribers` | `(resourceId, clientId)` | `resourceId` | `sub` + dominion bit |
| `QuerySubscribers` | `(queryHash, clientId)` | `queryHash` | `sub` + `profileId` + dominion bit |
| `QuerySubscriberListSubs` | `(queryHash, clientId)` | `queryHash` | none |
| `TreeSubscribers` | `(clientId)` | — | none |

**Every key is `(topic, clientId)` with the topic degenerate where there is none.** That is the merge: one table keyed `(topic, clientId)`, `sub` and the dominion bit nullable, and a `kind` discriminant. **The line that exists in the data is AUTHORIZATION, not resource-ness** — a row with a `sub` gets a per-push permission evaluation and a row without does not — and the merged table says so where the nullable column is declared, because that is the only place a reader learns why it is nullable. `QuerySubscriberListSubs` opens by calling itself *a near-clone of `TreeSubscriptions`*, which is the same fact stated from the inside.

- **`profileId` becomes REQUIRED on rows that carry a `sub`.** Its optionality exists for a pre-rollout TOKEN — `SubscriberEntry`'s JSDoc states it as prose — and what closes it is not the wipe but [ADR-013](../docs/adr/013-identity-profileid-resolution.md): every post-wipe mint carries the claim. The chat fallback render subscribes to the profile this id names, so an absent id is a render hole rather than a valid state. It stays absent on the kinds that carry no `sub` — there is no person on those rows to name.
- ⚠️ **The merge silently widens what the drains touch, and the phase owes a decision there.** `clearSubscribers`, `clearQuerySubscribers` and `clearWatchers` are three calls on three tables today, so the Star's ontology install wipes resource and query subscriptions and leaves the org-tree ones alone. Against one table, "clear" has to mean *clear by kind* or an install starts dropping tree subscribers for a reason that has nothing to do with them.
- **`subscribeTree` belongs on every host's `requests`, because the orgTree is the PLANE's, not the host's** (D14). A host does not have an orgTree; its plane builds one, every time. The Galaxy's `onDagChanged` no-op reads like an absent capability and is not one — it is an unwired notification, which D15 deletes by moving the fan-out into the plane that already holds every input it needs: the merged registry has the watchers, `OrgTree` has `getState()`, and the bridge does the broadcast. ⚠️ **This is the error the `Dag` naming produced**, an hour before D16 was written: *"no org-tree subscribe channel on Galaxy"* plus a hook called `onDagChanged` read as *the Galaxy has no tree*, and a per-host surface was nearly designed on it. `Dag` names the data structure, so the question sounds structural; `OrgTree` names the concept, and "does this plane have an orgTree" answers itself.

### The reload channel is deleted

**The plane is not the mechanism for `.dev` Star reloads, and will not become one.** That is decided elsewhere, not inferred here: [nebula-scope-moves-to-subdomain.md](nebula-scope-moves-to-subdomain.md) § *Decisions* weighed **a subscription to the built digest** — naming this plane, and that it *"already carries enrollment and reaping"* — against a point-to-point nudge, and took the nudge, because a plane subscription *"reloads every teammate watching the galaxy the moment any build lands"* when the signal is wanted for whoever asked and nobody else. Its criteria pin both halves of the successor: only the client that asked for a build has its preview reloaded, and a change Studio makes to the dev Star reaches the dev tab **as a push, with no reload at all**.

**So the channel has no future role, and it has no present one either.** Its original trigger was the ontology-install push to Stars, and the pull replaced it — `nebula-client.ts` says so at the site: on a stale version `Star.read` throws `OntologyStaleError` and the reject path fires `onShouldRefreshUI`, *"relocated from the old push handler"*. `broadcastReload` has no production caller, so a row is written, never fired, and never reaped. ⚠️ Its JSDoc has meanwhile begun inventing a justification — it claims two hosts hold one and that `Galaxy.broadcastReload` fires on build completion, and the Galaxy has neither.

⚠️ **The sweep reaches the scaffold, which is why it is written out rather than left to the phase.** The subscriber is not test-only: `container/app/src/nebula.ts` wires `onReload: () => window.location.reload()` for every dev preview, and that file is frozen verbatim in `scaffold-seed.ts`, so **every generated app ships it**.

- **Source:** `reload-subscriptions.ts`; the Star's field, `subscribeReload`, `broadcastReload` and `onReloadBroadcastResult`; and `resetDevData`'s capture→wipe→restore **with its five-line JSDoc**, which exists only to preserve these rows.
- **Client:** the public `onReload` option, the subscribe-on-connect branch it gates, and `handleReload`.
- **Scaffold:** the `isDevPreview` block in `container/app/src/nebula.ts` **and its comment**, plus the frozen copy in `scaffold-seed.ts`. ⚠️ That comment is wrong today whatever happens here — it tells a user-developer that `onReload` re-syncs their dev preview, and `onShouldRefreshUI` is the only mechanism that fires.
- **Tests:** `reload-version-contract.test.ts`, plus the baseline helpers `inspectReloadSubscribers`, `callStarInspectReloadSubscribers`, `callStarSubscribeReload` and `broadcastReloadForTest`.

⇒ **`resetDevData` does not survive this as "carried over unchanged"**, and `reload-version-contract.test.ts` joins § *What this task must also rewrite* as a named freeze whose coverage loss is argued rather than dropped ([[skip-cycles-are-real]]).

## Design intent, constraints, and future state

**The contract, in three tiers.** A Resources operation passes exactly three checks, each in one place. **The host's `onBeforeCall` requires passage and decides nothing else.** **The facade a gate returns supplies what the caller may not state** — the client's own id on the request leg, a dead client's id on the response leg. **The plane method below holds permission and ownership**, and is reachable from no wire at all. A forgeable parameter therefore survives on the plane harmlessly, because nothing outside can name the method that takes it.

**Two accessors, one door.**

```ts
#resources = new Resources(...)   // the plane — the host calls it directly, never RETURNS it

@mesh() resources()  { return this.#resources.requests }   // request leg
resourcesResults()   { return this.#resources.results }    // response leg — no @mesh()
```

**The plane owns its two surfaces, so a host does one `new` and nothing else.** `requests` and `results` are named for the LEG, which is the one distinguisher true of every member. A host that assembled them itself could wire them wrong, forget one, or hand the wrong plane to one of them; this way the capability carries its own shape, which is ADR-007's idea applied a level down. ⚠️ **The host still calls the plane directly** for the members on neither surface — `ensureResource`, `findCurrentByField`, the three `clear*`, `targetsForQuery` and `streamProgress`. What it never does is *return* it.

**`resourcesResults()` is not a second door.** `ocan/execute.ts` checks the allowlist on a chain's FIRST apply and latches, so a bare accessor is refused as an entry op exactly as a bare method is. `__handleResponse` runs the same path at `requireMeshDecorator: false`, so a fire-back consults no allowlist while `onBeforeCall` still requires passage. A gate with no decorator is therefore response-open and request-closed, and confers that on everything behind it.

**"Response leg" means a FIRE-BACK, not any result-shaped call.** `svc.broadcast`'s flat branch — the one pinned ON — fires back 4-arg into `__handleResponse`. Its tier branch forwards 3-arg through `__forwardBroadcastResult`, which is an ordinary request and meets the allowlist, so a bare accessor is refused there by design.

**The two accessors are told apart by the LEG.** Not by where the caller id comes from: `onInviteResult` takes an `InviteSummary` rather than a dead client's id, and `read` needs no caller id at all. The leg holds for every member; identity holds for most.

⚠️ **Two absences carry the design, and both are invisible at the site that depends on them.** Adding `@mesh()` to `resourcesResults()` — the tidying reflex, since every other accessor has one — opens every continuation target to any logged-in browser in one edit. Spelling a surface's plane reference `private` rather than `#` puts every plane method back on the wire behind the door (D13). **Each owes an assertion that can see an absence**, and `getMeshGuard` cannot: `mesh-decorator.ts` sets `MESH_GUARD` only when a guard argument is passed, so it reads falsy for a bare `@mesh()` and an undecorated method alike. Use `expect(isMeshCallable(Galaxy.prototype.resourcesResults)).toBe(false)`, which depends on no frozen list, and assert each surface exposes no member beyond its row in the table.

**Seen from the client, this collapses many ways into one.** Today `nebula-client.ts` reaches the host through a distinct continuation per entry — `ctn<Star>().transaction(...)`, `.subscribe(...)`, `.subscribeQuery(...)`, and the rest — plus the chained `orgTree()` gate. Afterwards every one is `ctn<Host>().resources().<op>(...)`: one way to reach the plane from every caller, and a new client method cannot invent a second. `OrgTree` is reached as `resources().orgTree.setPermission(...)` — **no parens on `orgTree`, it is a getter** — and a chained `apply` on it throws `TypeError` from `ocan/execute.ts`. The sweep is the client's ten `orgTree` mutators plus the baseline test-app's twenty tree wrappers and two test files; no harness scenario reaches the tree directly. Larry: *"If we have two, why not three? why not 10?"* A second door is not a smaller version of the mess; it is the first instance of it.

**The surface, allocated.** A gate is what makes `public` on the plane mean CALLABLE FROM THE WIRE. Nothing returns the plane today — the only gate returns the tree — so `public` there costs nothing and no method was ever chosen for a wire audience. Neither accessor returns the plane; each returns a surface whose member list is the whole of what it exposes.

| Facade / plane | Member → the plane op it wraps |
|---|---|
| **`requests`** — returned by `@mesh() resources()` | `transaction` → `doTransaction` · `read` → `doRead` · `subscribe` → `doSubscribe` · `unsubscribe` → `removeSubscriber` · `subscribeQuery` → `doSubscribeQuery` · `unsubscribeQuery` → `removeQuerySubscriber` · `subscribeQuerySubscribers` → `doSubscribeQuerySubscribers` · `unsubscribeQuerySubscribers` → `removeQuerySubscriberListWatcher` · `invite` · `orgTree` (getter) · `subscribeTree` |
| **`results`** — returned by `resourcesResults()`, no `@mesh()` | `onInviteResult` · `onBroadcastResult` → `removeSubscriber` · `onQueryBroadcastResult` → `removeQuerySubscriber` · `onQuerySubscriberListBroadcastResult` → `removeQuerySubscriberListWatcher` · `onTreeBroadcastResult` |
| **Plane, reached by BOTH surfaces** | `removeSubscriber` · `removeQuerySubscriber` · `removeQuerySubscriberListWatcher` · the tree remover — exposed by neither surface, so nothing on the wire supplies the id |
| **Plane, host-only** | `ensureResource` · `findCurrentByField` · `clearSubscribers` · `clearQuerySubscribers` · `clearWatchers` · `targetsForQuery` · `streamProgress` |

**The `do*` prefix goes with the PARAMETERS, not with the method.** The plane's ops keep their `clientId` argument; the door derives it and passes it down. That is the whole reason the plane can hold a forgeable-looking parameter safely — and it is the tension the two surfaces dissolve rather than pay. `removeSubscriber(resourceId, clientId)` has two callers of opposite provenance: `unsubscribe`, where a client drops its OWN row and the id is derived, and the reaper, where the id belongs to a client already gone. Self-guarding serves neither — derive, and the reaper cannot name the dead; accept, and the parameter is forgeable. Keeping the raw op on an unreachable plane serves both.

**Why not the two cheaper shapes.** Self-guarding the host-only members fails on most of them: `ensureResource`'s server-composed `actor` is the point of the method, `findCurrentByField` takes no node argument to check, and the three `clear*` take no arguments at all, so there is nothing to derive and a wire caller would drop every subscriber on the host. Pulling their callers inside fails on the layer map — the Galaxy's agent-message commit is host business logic, not a resource capability.

**`streamProgress` is a request-leg PUSH and belongs with the host-only members.** It is `protected`, takes no `result`, and its `progress` and `replyTo` are caller-supplied, so behind a response-open accessor any passage-holder could inject arbitrary text into a shared chat attributed to someone else's live turn — and `replyTo`'s JSDoc makes that attribution load-bearing against a hang. Because the plane has no `this.lmz`, the transient push owes a `ResourceHostBridge` member exactly as `fireInvite` does. ⚠️ `targetsForQuery` stays public on the plane rather than going `#private`: `streamProgress` is its only production route, but a test subclass reaches it through the `queryTargets` seam and `child3-targets.test.ts` has no other one.

⚠️ **Two `invite`s on the client is deliberate — do not "fix" it.** `client.invite(targetScope, …)` reaches the Registry facade and can confer `scopeAdmin`; `client.resources.invite(nodeId, …)` reaches the plane and never can. Both follow `auth.md` § *Grants in both planes*, and they are told apart by the first argument's kind and the `resources.` prefix. The subdomain sibling's standing instruction to name `Galaxy.invite` explicitly means the plane one.

**`subscribeTree`'s own authorization reasoning needs a home.** Its JSDoc argues there is *intentionally no node-level read check* — a member may watch the tree because ADR-008 makes it visible to every member — and today that sits beside `@mesh() orgTree()`, which D1 deletes. It belongs at the merged registry's tree kind, next to the nullable `sub` recording the same fact.

**Load-bearing claims, stated so review can falsify them:**

- **The reapers carry no host-specific work.** All three bodies are the same nine lines with one word changed: test the result for `ClientDisconnectedError`, take `clientInstanceName` off it, and call the matching remover. No host state, no ontology, no config. They move onto `results` with their names intact, so a continuation becomes `this.ctn<Galaxy>().resourcesResults().onQueryBroadcastResult(queryHash)`.
- **Shedding their `@mesh()` closes a hole.** A reaper takes its victim's id off the delivered Error, and `mesh.md` § *Errors across mesh calls* states that a structured error keeps `name` plus custom props across a hop — so a bare `@mesh()` reaper is a door through which a caller-supplied `ClientDisconnectedError` drops somebody else's subscription row. ⚠️ Whether that forge is reachable from a BROWSER is unverified and worth driving before the phases: it decides whether this is a fix to schedule or one to hurry.
- **The decorator is not dead weight, and shedding it has a stated cost.** The tier branch forwards 3-arg, so a reaper reached that way arrives as a request and needs the allowlist. It guards a path both pinned off and, when on, already broken — `NebulaDO.broadcast`'s JSDoc records that tree-path cleanup forwards a per-target failure to the chain's origin, in production the mutating CLIENT, so the reaper never runs and the dead row is never dropped. Lifting that `directThreshold: Infinity` pin already needed a tier binding and the routing fix together; this makes the reapers' arrival a third item, which [backlog.md](backlog.md) § *Lumenize Mesh* must gain.
- **One thing to verify when it is built:** whether a facade-chained continuation survives `__forwardBroadcastResult`'s `replaceNestedOperationMarkers`, which resolves the chain before re-firing it. The leg behaviour itself is settled above and answerable from disk today.
- **No data-plane entry carries `requireDominionHere`**, so deleting the entries weakens nothing.
- **The plane can nearly see what it needs.** `getCallContext` is a constructor argument it threads into four of the five sub-capabilities; `QuerySubscriberListSubs` takes only `ctx`, so the merged self-guarding registry has to add the thread. That is real work, not a fact already true.
- **The client's public surface keeps its name.** `client.resources.transaction(...)` is the app author's vocabulary and its audience is Studio's codegen model; every rename here is internal.

### The orgTree rename (D16)

**`OrgTree` becomes `OrgTree`, and this REMOVES a word rather than adding one.** `orgTree` is already the published name — `client.orgTree.*` and `handleOrgTreeUpdate` are what a user-developer reads — while the internals say `OrgTree` in about 196 places. Two words for one concept is what `naming-judgment` says to spend down, and `Dag` is the wrong one to keep: it names the underlying data structure, where `orgTree` names what the structure is FOR, which is organizing a permission model.

**The set, in its CURRENT spelling** (this list is the one place the old names must survive, so do not sweep it): `DagTree` → `OrgTree` · `dagTree` → `orgTree` · `DagTreeState` · `DagTreeView` · `DagTreeNodeData` · `callStarDagTreeGetState` · `buildDagTreeView`, each taking `Dag` → `Org`; plus the files `dag-tree.ts`, `dag-ops.ts`, `dag-tree.test.ts` and `confine-dag-plane.test.ts`. `onDagChanged` is deleted by D15 rather than renamed.

⚠️ **A substring replace CORRUPTS this repo.** `Bandage` (17), `Pedagogical` (8), `LucideBandage` and `BandageIcon` all contain `dag`. The sweep goes bare-identifier by bare-identifier and verifies with `\bDagTree\b` and friends, never `dag` (`workflow.md` § *Symbol renames*).

**It rides this task as its own FIRST phase, mechanical and alone.** Not a separate file: this task already touches every file the rename touches, so splitting means two sweeps over the same files and whichever lands second re-resolves the other's conflicts. And landing it first is what keeps this task's own output — the Decisions table, the allocation table, the rewritten test freezes, the `mesh.md` paragraph — from being written in a vocabulary it then has to re-sweep.

### The two names (D18)

**The storage engine becomes `Snapshots`.** It manages the `Snapshots` table, its only SQL surface, and ADR-004 calls its rows snapshot sequences. The word appears nowhere as a class or type today, so `class Snapshots` beside the existing `Snapshot` type is the obvious pairing and adds no vocabulary.

**The merged registry becomes `Subscriptions`, class and table alike.** It answers *what is this?* on sight: one class holding subscription rows of several kinds. Today's four tables are `Subscribers`, `QuerySubscribers`, `QuerySubscriberListSubs` and `TreeSubscribers`, so `Subscriptions` reuses no surviving TABLE name — which is where a survivor reading would actually mislead. It reuses a deleted class name, and that reads as the union keeping the general word rather than one sibling outliving three.

**Rejected, and why, so nobody re-proposes them:**

- **`Subscribers`** for the registry — it is one of the four table names, so the survivor problem is real rather than notional.
- **`Enrollments`** — tempting, because [nebula-scope-moves-to-subdomain.md](nebula-scope-moves-to-subdomain.md) says the plane *"already carries enrollment and reaping"*. But `enroll` is this repo's MEMBERSHIP word — *"enrolled through the REAL invite"*, *"enrolled in the `.dev` workspace Star"*, *"rendering the offer enrolled the invitee"* — and reusing it for subscriptions is the collision goal 3 exists to prevent.
- **`SubscriberRegistry`** — `Registry` is `NebulaAuthRegistry`'s word and a top-level concept here.
- **`Watchers`** — already means the query-subscriber-list kind specifically (`clearWatchers`, `removeQuerySubscriberListWatcher`).
- **`TemporalStore` / `SnapshotStore`** for the engine — new vocabulary for a table that has a name. **`History`** — ADR-004 calls history the substrate, not the engine.

⇒ **The subsystem lands at four names, every one already in the reader's head:** `Resources` the plane, `Snapshots` the storage engine, `OrgTree` the permission tree (D16), `Subscriptions` the merged registry.

### The audit of what an untrusted descendant may INVOKE

**Every non-admin `@mesh()` on the Galaxy and on `universe.ts` must be deliberately reachable by a star-scoped stranger, with the reason written in the code** (moved from the Galaxy collapse's Phase 8, 2026-08-23). Today's allocation is incidental rather than decided: those methods were marked `@mesh()` when "non-admin" meant another member of the same org, not somebody who self-signed-up five minutes ago. The ontology reads carry no risk — a tenant gets the app's ontology by signing up anyway. `getGalaxyConfig` and `getUniverseConfig` both need a **content** audit: confirm nothing app-developer-private or universe-owner-private lives there and that nothing is expected to later, and if something is, move the field or split the method. ⚠️ **Never narrow `requirePassage`'s tenant branch**, which `tasks/archive/nebula-confine-admin-bypass.md` warns against by name.

**The `requests` member list is what makes the audit mechanical.** The obvious alternative — a justification comment on each decorator line, checked by a grep — cannot survive a gate, because one `@mesh() resources()` line stands in front of a whole surface and says nothing about what is behind it. Putting a method on `requests` rather than on the host **is** its classification, and a test reads that off the surface a host actually exposes. The grep's domain shrinks to every `@mesh()` entry that is not the door and keeps working there. ⚠️ **`universe.ts` keeps the grep outright** until [nebula-upgrade-universe-with-a-data-plane.md](nebula-upgrade-universe-with-a-data-plane.md) gives it a plane.

**What the audit must discriminate, since a weaker form of it once could not.** Assert the **pair**: a star-tier caller with **no** Galaxy grant is REJECTED by `transaction` and `subscribeQuery`, and the collaborator holding a `write` grant at the chat node is ADMITTED **through the same method**. An earlier version compared two states that could not both exist — with no grants anywhere in the Galaxy's tree, every non-dominion caller lacked one, so "holds no grant" distinguished nothing. ⚠️ **Match the refusal MESSAGE, not the boolean**: a passage refusal and a per-op DAG refusal are indistinguishable as booleans, which is exactly the collapse such a check exists to catch (`live.md`). Done honestly this discharges [ADR-015](../docs/adr/015-passage-and-dominion.md) clause (3) — passage is not dominion.

### What this task must also rewrite, because its deliverables falsify them

- **`galaxy-resource-surface.test.ts`'s three-tier freeze.** It pins a literal bare list, a one-name dominion list and a `requireChatWrite` list off `Galaxy.prototype`. **The dominion and `requireChatWrite` tiers survive unchanged** — `setGalaxyConfig` keeps its decorator (D11) and the four source entries keep theirs. What moves is the bare tier: thirteen of its sixteen leave the host, ten to `requests` and three reapers to `results`. ⇒ The bare-surface freeze re-points to the `requests` surface; the host's own bare set becomes `resources`, the collapsed `getOntology`, and `getGalaxyConfig`. ⚠️ **Add a criterion that no bare `@mesh()` entry falls out of BOTH freezes** — that gap is where a wire-reachable method goes unwatched.
- **`scope-isolation.test.ts`'s Galaxy/Universe widening invariant.** It freezes the same set under the rule that the widening is sound only if every non-admin `@mesh()` method on those hosts holds data any descendant Star may read — the live implementation of the coarse-barrier criterion the subdomain sibling carries. ⚠️ **Shrinking its literal mechanically leaves it GREEN AND VACUOUS about a security invariant**, the failure class `calibration.md` §9 says to spend prevention on. ⇒ It owes a positive control: a door member's DAG refusal still reachable THROUGH the door, matched by message, in the shape § *What the audit must discriminate* describes.
- **`reload-version-contract.test.ts`** (D17). It asserts wipe-preservation and connect-gate routing, and records the channel as deliberately parked — so the coverage it holds is argued, not dropped silently ([[skip-cycles-are-real]]).
- **The subdomain sibling's coarse-barrier criterion.** It enumerates every guard-less `@mesh()` a Star-`aud` token passes the barrier into. That build lands first (§ *Transition*), so it is written against today's per-method surface and **this task rewrites it** to a door plus per-op auth inside the plane.
- **`mesh.md` § *Object-capability access: gate once, then chain*.** It teaches one shape, `@mesh(guard) onlyAdmins()`, whose stated property — reachable only by first passing the gate, so holding the instance IS the authorization — is false for a decorator-less accessor, where nothing is passed and passage alone stands. ⇒ That section gains a paragraph for the response-leg gate with `resourcesResults()` as the canonical instance, and § *`lmz.call` 4-arg* stops citing `Star.onBroadcastResult` as a decorated example. **That is where "do not tidy this" belongs** — a JSDoc MUST on the accessor is not read by someone deciding how gating works. ⚠️ `calibration.md` §11 prescribes the gate-and-chain shape this task applies and shows it decorated; it needs the same paragraph, because it is always-loaded and a session reads it before `mesh.md`.

### Backlog rows this task trips

- **The `requireClientOrigin` guard row** ([backlog.md](backlog.md) § *Other Nebula backlog*) — **absorbed, mechanism declined.** It proposes replacing the copies of the client-origin preamble with a GUARD, the opposite of D4. The door delivers what the row wanted — the preamble written once — by deriving once and passing down, with no guard. ⚠️ Say that plainly in the row: this task moves *opposite* its central mechanism while satisfying its purpose, and a reader who sees only "absorbed" will re-propose the guard.
- **The compose-site sweep row** (same section) — **closed as absorbed; this file is that sweep.** ⚠️ Correct its ⚠️ on the way out: it asserts that a method on a nested plain object is not in the mesh surface, which the door refutes — that is exactly what a gate makes reachable, and believing it is how `streamProgress` nearly landed on a response-open accessor. It also still names `DevStudio`, a class the collapse deleted.
- **The `ensureResource` create-if-absent row** ([backlog.md](backlog.md) § *Testing & Quality*) — **its trigger MOVED rather than retiring.** It fired on the Profile's two new callers, which D10 cut; the guard is still untested and now sits behind two new public accessors that make a later reader assume otherwise. Restate the trigger against what remains.
- ⚠️ **Sweep the rest before the phases.** These three were found by reading; the enumeration is not claimed to be complete, and the rows naming `subscribeReload`, the config pairs or the Profile are the likely remainder.

## The push path — what it stores, and what it rechecks

**Reading a permission costs no storage.** `OrgTree` keeps the tree as a lazily-built state cache with an adjacency index over it, invalidated on every mutation, and `resolvePermission` walks that index in memory. One `evaluatePermissions` call is a map walk, not a read. So the cost objection against checking at push time is weaker than it looks, and the design question is which shape stores the least, not which runs least often.

**The stored `dominionOverHostAtSubscribe` bit exists because the push path has no token.** A Galaxy or Universe admin holds no grant in the host's tree — they reach it by dominion, which is a fact about their claims. Nobody is calling when a push goes out, so a bare grant evaluation would drop exactly those people. The bit is that remembered claim, and something like it is unavoidable while pushes are authorized at all.

⚠️ **What is wrong is not that the bit is stored, it is what the recheck's own comment implies.** `evaluatePermissions` takes the bit as a short-circuit, so for a dominion-holding subscriber the tree is never consulted and the verdict is the one frozen at subscribe. The recheck is live for grant holders and a replay for everyone else, while the comment presents it as uniformly live. By ADR-010's replication rule the bit is a copy of a mutable fact and owes a stated convergence mechanism; it has one — the client re-subscribes on reconnect, re-deriving the bit from the fresh token — but that is written only in `NebulaClient`'s resubscribe walk, on the other side of the wire from the column it converges. **State both at the column.**

**Keep the per-push recheck. It stores the least of the available shapes.** For grants it evaluates live and remembers nothing, so there is no second verdict to go stale. For queries it is not a freshness check at all — it computes the allowed and denied sets per subscriber, which is what lets a denied subscriber be told *which* nodes they lost rather than silently dropped ([ADR-008](../docs/adr/008-full-org-tree-visibility.md)) — so it cannot move regardless. And the obvious alternative is worse: skipping the per-commit evaluation means materializing an allowed flag per row for the commit path to read, a second stored verdict beside the bit, and [ADR-013](../docs/adr/013-identity-profileid-resolution.md) calls a stored authorization verdict a security bug.

**There is no revocation window on the single-resource path.** A revoked grant is caught by the very next push, because `#broadcast` runs from the commit and evaluates live. Nothing is delivered in between, so nothing escapes. Queries differ, and not because they are safer: a permission change also fires an immediate rerun of every live query, and that exists to *disclose* the loss promptly under ADR-008, not to close a hole. ⚠️ Do not "fix" the single-resource path by adding it to that rerun — it would re-deliver on every DAG change to gain nothing, and wanting the two paths to look alike is what this note exists to stop.

**`registerQuerySubscriber` keeps no register-time check, and the merged registry keeps none either.** Refusing at registration is what ADR-008 forbids: a subscriber must be able to learn the denied set, and a refused registration discloses nothing. So it is not an exception to *every method guards itself* — its guard is authentication plus the delivery-time evaluation, and **that is what the method must say.** Recording it as *"always succeeds — no permission check"* is what made it read as a hole for two reviews running.

## Transition

**This task runs after the Galaxy collapse, which has shipped** — `DevStudio` and `DevContainer` are folded into `Galaxy`, and `ctn<DevStudio>()` appears nowhere in `apps/nebula/src` — so it starts from two hosts that are not about to move again and ends at the contract above, with no half-moved state along the way.

**The subdomain build lands first.** [nebula-pre-alpha.md](nebula-pre-alpha.md) § *What remains* orders it ahead of this task (④), so its coarse-barrier criterion is written against today's per-method surface and rewriting it is a deliverable here, listed with the test freezes above.

**What the collapse carries in the meantime — one interim, five artifacts, one removal event.** `invite`'s body and `onInviteResult` live in the plane already, so the security logic is written once and no host copies it. Each host reaches them through a thin forward, and only the `invite` pair is `@mesh()`: `onInviteResult` is `public` and deliberately undecorated at both hosts and in the plane, because the fire-back needs no allowlist and an `@mesh()` there would let any in-scope caller forge an invite outcome and write themselves grants. **This task deletes the two `invite()` doors**, whose work the door takes over, and `resourcesResults()` replaces the two `onInviteResult` forwards.

**The fifth artifact carries no label, which is why it survived three reviews.** `fireInvite` — the closure `invite()` takes as its third parameter — is the only piece of host mesh I/O arriving per-call rather than at construction; its own JSDoc argues for `ResourceHostBridge` and then does not use it. Nothing depends on the closure form: both callers are production hosts and no test substitutes it. **It becomes a `ResourceHostBridge` method** (D8), which is what lets `invite`'s forward be deleted at all, since the plane has no `this.lmz` to reach the facade with. ⇒ **Three source labels to write or repoint:** `fireInvite` gains `TEMP → target=ResourceHostBridge`; both `onInviteResult` labels read `TEMP → target=resources() gate` and must say `resourcesResults()`, because following them as written lands a transcriber on the forge hole the JSDoc two lines above argues against. While in those files, replace the restated "all four host forwards" count in the two `invite` JSDocs with a pointer — a count in source has no derivation, so nothing updates it.

**What this task inherits and confirms rather than builds:** a single `Galaxy` composing the plane, an installed ontology version on it so the staleness check and evict-on-version-change are the same code on both hosts, `invite` and `onInviteResult` already in the plane, and `orgTree()` standing as the gate exemplar on both hosts.

## Future state

A node type that composes the plane inherits a complete access-control boundary, so standing up the next host is a `new` and two accessors. ⚠️ **Design consideration:** once every guard is local, a per-op guard table becomes something *generated from* the code rather than a second copy to keep in sync — worth revisiting after this lands, never before.

**Constraints.** [ADR-007](../docs/adr/007-shared-node-security-core.md) — a node's comms and guards core is composed rather than inherited; this applies the same idea one level down. [ADR-008](../docs/adr/008-full-org-tree-visibility.md) — enforcement at the point of action, never secrecy, which is what makes a delivery-time filter legitimate rather than a gap. `mesh.md` § *Object-capability access: gate once, then chain* is the gate mechanism. `security.md` governs the fail-closed behaviour that must survive the move unchanged.
