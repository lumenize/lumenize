# Studio/dev-loop consolidation — PLAN (2026-06-20, from disposition fan-out: 24 agents)

> ✅ **EXECUTED 2026-06-20.** Survivors built (`nebula-studio.md` rewritten; `nebula-agentic-development-engine.md` created — the codegen/eval split landed in a NEW file, not folded into nebula-studio.md), `nebula.md` realigned, 11 archived + 5 deleted via `git mv`/`git rm`, refs repointed (broken-link scan clean). **Not committed** (left for review). This file is the historical record of the plan.

**Final count:** From 24 Studio/dev-loop-cluster files to 4 survivors (nebula-dev-flows.md [canonical, untouched], nebula-dev-flows-realignment.md [revert tracker, untouched], nebula.md [master, realigned in place], nebula-studio.md [THE Studio build file, absorbs ~9 sources]) + 4 untouched on-hold/parking survivors outside the cluster (nebula-scratchpad.md, on-hold/nebula-skills.md, on-hold/nebula-studio-eval-suite.md, on-hold/nebula-5.5-schema-evolution.md, on-hold/nebula-branches.md), with 11 files archived to tasks/archive/ (frozen) and 5 files deleted (after salvage). Net of the active cluster: ~14 files collapse into nebula-studio.md or vanish; nebula-studio.md is the one new consolidated build home.

## Survivors
- **tasks/nebula-dev-flows.md** — CANONICAL architecture authority — the dev/publish participant model (Cast, Decisions 1-11) + sequence diagrams (Flow 1/1b/1c/2/2b, Topology, DevContainer internals, Source-abstraction seam). LEAVE ALONE. All survivors point here for architecture; none duplicate it.
  - absorbs: Nothing — it is the source. (Source-of-truth for the model the others reference.)
- **tasks/nebula-dev-flows-realignment.md** — Execution tracker for the one-way conformance/realignment + the source-of-truth REVERT checklist (P0 Phase-3.5 re-supersede; P1/P3/P4 term-swaps and rewrites; cross-cutting purge sweeps). It is the live authority for in-place edits to nebula.md / nebula-studio.md and the DONE-but-being-REVERTED checklist. LEAVE in place — this consolidation does NOT duplicate its findings.
  - absorbs: Nothing new — already enumerates the revert (26 findings vs container-dev-loop, 26 vs nebula-studio, P3 schema cluster). Survives until the realignment pass is executed; it is the home of the revert work the consolidation must NOT re-track.
- **tasks/nebula.md** — MASTER task file — current truth + pointers, no historical hedging. Stays the index/phase-table. Realigned IN-PLACE (8 findings, owned by realignment tracker §nebula.md), NOT folded into anything.
  - absorbs: Nothing migrates IN beyond a one-line DONE status for scope-isolation. Realign-in-place only: name the canonical cast (DevStudio/DevContainer/Studio UI/Preview app), point to nebula-dev-flows.md, drop the ~3s-reload preview model + Galaxy-exposes-.d.ts + breaking-edit-auto-reset prose, drop the removed ResourceHistory fixture, reconcile BSL-1.1 vs UNLICENSED.
- **tasks/nebula-studio.md** — THE Studio build task file — the actionable, forward-looking build plan for Studio/the dev loop. References nebula-dev-flows.md for ALL architecture/flows; never restates the model. Add a top-of-file pointer to that effect.
  - absorbs: Keeps its own valid build content (generation engine, generated-artifact spec, bootstrap files + snippet, debounce rule table, .d.ts-as-LLM-context, chat-first authoring UX + STT, iteration/agentic surface, conversation flow, out-of-scope, success criteria — realigned per realignment §nebula-studio). ABSORBS surviving build content from: nebula-container-dev-loop.md (container/fetch/scope-injection/requireAdmin mechanics, 2 entrypoint edits, Q1 bake-deps + bench, Q3 vite-owns-compile, Q4 addressing, Phase-4 salvage-grep teardown, pool-workers test strategy); dev-star.md (resetDevData wipe mechanism re-homed onto Star@.dev); nebula-app-versioning.md (publish-bundle-to-Galaxy + prod lazy-pull as Flow 2/2b pointers, plus AppBundle PK-migration gotcha, Galaxy-admin @mesh(requireAdmin)+B5 security/test patterns, publish-atomicity criterion, deferred binary-assets/cold-start notes); nebula-studio-llm-strategy.md (Kimi 2.7 WHY + no-Think/no-codemode, DevStudio orchestration, two AI contexts, LM strategy arc, eval = live use); nebula-resource-metadata.md (the @title/@description/@inverse convention set, PascalCase-slug rule, source-as-spec, extractTypeMetadata extension — source read re-homed to DevStudio not Galaxy registry); kimi-ui-gen-viability.md (the VIABLE verdict + model slug + loop lessons); nebula-self-hosted-assets.md (same-origin script-src 'self' verification checkpoint + DaisyUI/Lucide-ISC code-gen prereq); preview-iframe-spike.md (separate-window launch/focus fallback note); vibesdk-llm-patterns.md (the pre-build reading task: read list, 9-section skeleton, caveats, stop-condition); plus brief future-capability pointers to on-hold/nebula-skills.md, on-hold/nebula-studio-eval-suite.md, on-hold/nebula-file-storage-backend.md (enterprise BYO-agent + deferred bundle-store), on-hold/nebula-lazy-schema-migrations.md (copy-on-read principle + parser-owns-default-fill boundary).
- **tasks/nebula-scratchpad.md** — General deferred-items / idea parking lot (referenced from nebula.md). NOT part of the Studio sprawl. LEAVE AS-IS (optional one-line trim of the wasm-git bullet to a pointer to nebula-dev-flows.md Decision 6/10).
  - absorbs: Nothing.
- **tasks/on-hold/nebula-skills.md** — Parked future-feature design (DevStudio Skills). LEAVE on-hold as-is; only a DevStar->Star@.dev terminology pass (owned by realignment tracker, line 350), not this consolidation.
  - absorbs: Nothing (gains an inbound one-line pointer FROM nebula-studio.md).
- **tasks/on-hold/nebula-studio-eval-suite.md** — Parked future regression-eval-suite design. LEAVE on-hold as-is; one stale-pointer fix (SFC-in-DO -> DevStudio/DevContainer/vite preview gate) deferred to un-park time.
  - absorbs: Nothing (gains an inbound one-line pointer FROM nebula-studio.md).
- **tasks/on-hold/nebula-5.5-schema-evolution.md** — Parked post-funding production in-place schema-evolution design. LEAVE on-hold as-is; superseding-banner + registry-vocabulary purge owned by realignment tracker (P3, line 451), not this consolidation.
  - absorbs: Nothing.
- **tasks/on-hold/nebula-branches.md** — Parked general URL-level 4-tuple branching spec (no current consumer). LEAVE on-hold as-is; if dev-star.md is removed, repoint its two dev-star.md references to nebula-dev-flows.md Decision 2 (trivial link fixup).
  - absorbs: Nothing.

## Delete whole (after salvage)
- **tasks/nebula-app-versioning.md** — NEVER-BUILT #1b spec; all three pillars (separate ontology-version registry, Galaxy/DevContainer source durability, DevStar-as-sole-compiler) reversed by nebula-dev-flows.md Decisions 9/5/7. Self-superseded twice already. No done-fact to freeze => not an archive candidate. Delete AFTER folding its thin surviving build slice (publish path as Flow 2/2b pointers + AppBundle PK-migration gotcha + Galaxy-admin security/test patterns + publish-atomicity + deferred binary-asset/cold-start notes) into nebula-studio.md.
- **tasks/nebula-studio-llm-strategy.md** — Forward-looking strategy, never built; no done-fact/spike record (its source records — think-vs-cma-bakeoff, kimi-ui-gen-viability — are preserved elsewhere). Delete AFTER folding the Kimi WHY / no-Think-no-codemode / DevStudio-orchestration / two-AI-contexts / LM-strategy-arc / eval content into nebula-studio.md.
- **tasks/nebula-resource-metadata.md** — Active but never-built convention spec. Core (the three JSDoc annotations, PascalCase-slug rule, source-as-spec, additive extractTypeMetadata extension) is still-valid Studio build content; only the Galaxy ontology-version-registry source-retrieval plumbing (getOntologySource/OntologyVersionRow) is OBE per Decision 9. Delete AFTER folding the conventions into nebula-studio.md with source-retrieval re-homed to DevStudio's source tree.
- **tasks/preview-iframe-spike.md** — Planned-but-NEVER-RUN spike; all three probe questions overtaken by nebula-dev-flows.md (same-origin iframe preview, JWT-over-/gateway auth, vite HMR not 3s reload) and its compile-in-DevStar basis is reversed. No run-result to archive. Delete AFTER folding the lone live nugget (separate-window launch/focus fallback) into nebula-studio.md.
- **tasks/vibesdk-llm-patterns.md** — Never-started, still-valid PRE-IMPLEMENTATION reading task (output doc never written; its gate shipped). Orthogonal to dev-flows; nothing reversed. Delete AFTER folding the reading task (read list, 9-section skeleton, caveats, stop-condition) into nebula-studio.md as a pre-build reading entry, then repoint the 4 inbound links (nebula-studio-llm-strategy.md:19, nebula.md:57+92, icebox/think-nebula-integration.md:5).

## Archive (frozen, fact preserved)
- **tasks/nebula-container-dev-loop.md** — Studio container dev-loop spike record (build-seq #1a). Phases 0-3 RAN + mechanism-validated on the torn-down throwaway experiment experiments/container-node-phase0 (NebulaContainer/LumenizeContainer constructs + coexists with Container lifecycle on real infra; @mesh bundles via plain wrangler deploy; command channel ~4-40ms warm; cf-container-target-port trust boundary holds (M1); typed retryable ContainerUnavailableError fixes the cold-boot race; server-derived scope injection ignores request decoys; real-browser mount + HMR js-update e2e ~131ms; enableInternet=false blocks non-allowlisted egress; standard-1 half-vCPU clears the vite-build starvation bar; container needs Docker Desktop not Colima; wrangler containers delete needed). Q1 dep-install bench: curated node_modules 84MB/62pkgs, baked image 139MB, npm install cold 20s — CF has no persistent volume so baking is the only durable store. Phases 3.5 + 4 REVIEWED but NEVER BUILT; Phase 3.5's DevContainer-DO-source-of-truth/DevSource-table conclusion REVERSED by nebula-dev-flows.md (DevStudio = source-of-truth). Build plan moved to nebula-studio.md; live revert checklist is nebula-dev-flows-realignment.md. ARCHIVE AFTER folding surviving build mechanics into nebula-studio.md.
- **tasks/dev-star.md** — DevStar build record: specified + BUILT a DevStar-extends-Star subclass on a DEV_STAR binding with slug-derived selection; Phases P1-P4 built + adversarially verified 2026-06-16 on feat/nebula-studio (uncommitted) — dev-star.test.ts / dev-star-client-binding.test.ts / dev-star-data-lifecycle.test.ts (9 pass + 1 skip) / dev-star-coexistence.test.ts (3 pass). SUPERSEDED 2026-06-19 by nebula-dev-flows.md Decision 2 (no DevStar class — one Star on STAR; dev Star = {u}.{g}.dev instance) + Decision 9 (no Galaxy ontology-version registry; eager deployToDev/applyFetchedState eliminated). DevStar class/DEV_STAR binding/#starBinding() being reverted; only resetDevData survives, re-homed onto Star hard-guarded to .dev. ARCHIVE AFTER folding the resetDevData wipe mechanism (deleteAll -> complete onStart re-init incl. #row/#facet null, root-admin reseed window, alarms caveat, bounded in-flight-write edge, additive-preserved/breaking-reset bargain, dev-slug reservation, scope exclusions) into nebula-studio.md.
- **tasks/nebula-devcontainer-node-type.md** — ARCHIVE WHOLE — no fold. The 4th Lumenize node type, designed + reviewed + fully BUILT across Phases 1-3 (all DONE 2026-06-17): packages/mesh/src/lumenize-container.ts (LumenizeContainer extends @cloudflare/containers Container@0.3.7, composes comms+guards core, enableInternet=false default, fetch() strips cf-container-target-port + pins defaultPort), exported via @lumenize/mesh/container; apps/nebula/src/nebula-container.ts (NebulaContainer onBeforeCall scope-isolation). Tests mesh 14 pass/1 skip + nebula 9 pass, mutation-validated. extends Container does NOT construct under vitest-pool-workers (precheck canary). @cloudflare/containers@0.3.7 reserved-name audit clean (kv __CF_CONTAINER_STATE/OUTBOUND_CONFIGURATION, SQL container_schedules, alarm slot — no __lmz_do_* collision). Satisfied ADR-007's acceptance gate; ADR-007 flipped Proposed->Accepted (+2026-06-18 identity-on-every-entry-path amendment). Deployability gap carried forward: prod wrangler.jsonc has no new_sqlite_classes/containers block (owned by #1a). Architecture invariant already homed in ADR-007 (Accepted) + nebula-dev-flows.md; the live forward-pointer (fetch() must __init identity from routed headers) is ADR-007's amendment. Nothing to fold.
- **tasks/nebula-do-scope-isolation.md** — ARCHIVE WHOLE — orthogonal to the dev-loop cluster (tenant DO isolation, not the publish model). Fully completed + committed + test-verified: NebulaDO.onBeforeCall made structural (Fix 1, commit 7c83407, 2026-06-16) — accepts a mesh call only if its aud is covered by the scope encoded in the DO instance name (buildAuthScopePattern->matchAccess), replacing the TOFU aud-lock; closes Star pre-claim + shared-Galaxy/Universe multi-star rejection (the demo blocker). Phases 0-3 green; type-check clean; __nebula_universeGalaxyStarId TOFU store now inert. Fix 2 deferred -> on-hold/nebula-scoped-helper-naming.md; ResourceHistory fixture removed; M8 raw-RPC public-method surface enumerated for on-hold/mesh-active-callcontext-guard.md. NOTE: the Galaxy @mesh ontology-version methods it references were later eliminated by nebula-dev-flows.md Decision 9 (accurate as of build date only). Add a one-line DONE status to nebula.md's master table; nothing else folds.
- **tasks/nebula-self-hosted-assets.md** — ARCHIVE AFTER folding two enduring requirements into nebula-studio.md. BUILT + green 2026-06-17 on feat/nebula-studio (uncommitted): self-hosted Vue/DaisyUI/Lucide served same-origin from code via Star.onRequest + getPlatformAsset (vendor/platform-assets.generated.mjs), compile-time bare-specifier rewrite at the DevStar write boundary, script-src tightened to 'self'. 444 pass/2 skip; real-chromium SFC mount passed under strict script-src. Findings: a 5.5MB self-contained per-icon Lucide module crashed the workerd isolate at load -> shared _core.js + ~300B/icon kept it ~1.8MB; DaisyUI v5 CSS is plain browser CSS; Lucide is ISC (not MIT), home->house. Two latent #1a bugs fixed here (also memory-homed): nodejs_compat_v2->nodejs_compat + threading compileScript().bindings into compileTemplate bindingMetadata. SUPERSEDED 2026-06-19+ by the dev-only Container vite/vite-build->static-assets model (nebula-dev-flows.md): vite owns compile (compileSFC deleted), no DevStar, in-DO compile/vendored-asset substrate dropped; security goal (same-origin script-src 'self') now met natively by Workers-Assets + static serving. Folds: same-origin script-src 'self' verification checkpoint + DaisyUI/Lucide-ISC code-gen prereq (correct ATTRIBUTIONS to ISC).
- **tasks/nebula-studio-compile-pipeline.md** — ARCHIVE WHOLE — no fold (surviving ideas already captured elsewhere). The in-DO compile/serve/reload pipeline BUILT + green on feat/nebula-studio 2026-06-17 (Phases 1-4): DevStar.compileSFC compiled Vue SFCs (incl lang=ts) inside the dev Star via a vendored esbuild-bundled tsc (3.44MB committed); Star.onRequest served the resident bundle ungated (strict CSP, SPA fallback, base /{dev-star}/{u}.{g}.dev/) with appVersion/authScope/activeScope server-injected; in-DO reload channel (subscribeReload/broadcastReload modeled on subscribeTree). Spike finding (frozen): bare import { transpileModule } from 'typescript' crashes the workerd isolate at module-load — typescript MUST be esbuild-bundled with Node stubs; @vue/compiler-sfc runs unbundled under nodejs_compat. Compile round-trip sub-2ms p50 warm local / ~36ms p50 deployed (Pittsburgh->IAD). Full code preserved in annotated git tag in-do-compile-baseline (commit 0953f6f). PERMANENTLY superseded by nebula-dev-flows.md Decision 2 (no DevStar) + Decision 9 (Star never compiles; vite owns SFC compile, DevStudio/Galaxy own ontology-validator compile) — NOT a warm fallback. Deferred human step never run: wrangler delete --name spike-sfc-galaxy-loop. (Server-injected scope choke point + tsc-must-be-bundled finding already in nebula-container-dev-loop.md / [[tsc-in-workerd-must-bundle]] / workflow.md; teardown tracked as the container-dev-loop Phase-4 residual grep.)
- **tasks/kimi-ui-gen-viability.md** — ARCHIVE AFTER folding the verdict into nebula-studio.md. Kimi UI-gen viability spike — RAN + verified 2026-06-16, all 3 stages green (explicitly not-for-hand-review; named in nebula-dev-flows.md:356 Do-NOT-touch frozen list). Stage A: SFC->runnable-ESM compile works (4 mutation-checked tests; raw import ts from 'typescript' crashes workerd, must bundle). Stage B: Kimi 2.7 (@cf/moonshotai/kimi-k2.7-code, OpenAI-style result.choices[0].message.content) produced a compilable App.vue + ontology.d.ts first-shot. Stage C: thin iterate-on-errors loop converged in 2 rounds (self-corrected invented op:'set' -> real create/put). Substrate findings: native tool-calling works; codemode tool-bridge is JSON-only (ADR-002 violation, dropped); DO facets independent of dynamic workers. Code/probes lived under apps/nebula/spike/sfc-devstar-loop/. Forward plan (DevStar.compileSFC, files-as-resources, in-DO tsc, ontology-version registry) SUPERSEDED by nebula-dev-flows.md. Fold = the VIABLE verdict + model slug + OpenAI-style response shape + loop lessons (native tool-calling not regex-on-prose; feed real Nebula API .d.ts; wire an error-tail) + single-run-directional caveat (statistical suite = on-hold/nebula-studio-eval-suite.md).
- **tasks/container-vite-spike.md** — ARCHIVE WHOLE — no fold (everything still-true is in nebula-dev-flows.md or the experiments' RESULTS/FINDINGS; named in nebula-dev-flows.md:356 Do-NOT-touch list). Spike PROPOSAL (Status: Proposed, never built, drafted 2026-06-17) for the Studio dev-loop pivot from in-DO compile + vendored assets to a Cloudflare Container running real vite in dev + static assets in prod. Framed the go/no-go + six kill-fast questions (Q1 latency, Q2 DO-proxied HMR-WS, Q3 dev->prod static handoff, Q4 cost at N tenants, Q5 agent control surface, Q6 source durability) + the in-DO baseline to beat (~sub-2ms warm local / ~36ms deployed). Outcome: pivot GOed + now canonical; questions answered by experiments interim-dev-loop + container-egress-catrust (not this spike's own experiment). Q6's Galaxy-dual-write source-durability bet was REVERSED to DevStudio shell Workspace + local git source-of-truth, DevContainer a disposable checkout.
- **tasks/spike-container-agent-channel.md** — ARCHIVE WHOLE — no fold (every load-bearing fact already in nebula-dev-flows.md DevContainer-internals model + memory [[agent-channel-container-exec]]; implicitly part of the frozen spike set). Spike RAN + GOed 2026-06-17 (experiments/container-agent-channel, commits cd60945/e6fb249/8a97fe5). Proved the agent->DevContainer command channel local + deployed: command-server on :9000 baked into the image, reached by the DO via containerFetch, agent->DO over mesh (lmz.call); no kill across Q1-Q6. Numbers: warm small-command round-trip ~30ms edge-local, cold first command ~1.36s. Streaming/HMR-parity/server-side-only trust boundary verified. Caveat: a vite build starves the command channel on the default 1/4-vCPU instance — size the dev instance for build bursts. This was Q5 of the container-vite pivot (riskiest unproven piece). Deployed Worker + container app deleted 2026-06-17 after capture; full results in experiments/container-agent-channel/FINDINGS.md. The GO unblocked the DevContainer node type build (4th node type, since landed).
- **tasks/on-hold/nebula-file-storage-backend.md** — ARCHIVE AFTER folding two live threads into nebula-studio.md, then delete the on-hold file. File-storage-backend investigation (split out of nebula-studio.md 2026-06-16, on-hold). Central question — DO SQL vs Artifacts for file resources, reframed 2026-06-04 to 'adopt @cloudflare/shell's Workspace?' — RESOLVED 2026-06-19 in nebula-dev-flows.md: ship on @cloudflare/shell (proven end-to-end in experiments/interim-dev-loop); Artifacts is an optional future swap behind the same seam. The @cloudflare/shell@0.3.8 (2026-06-04) findings are the original point-in-time research snapshot: Workspace = filesystem on DO sync SQL + R2 (single table cf_workspace_<ns>, path PK, inline content or r2_key over threshold), runtime-neutral StateBackend (InMemoryFs + WorkspaceFileSystem), @cloudflare/shell/git = isomorphic-git over the FS (real clone/commit/push, MIT). Its stated demo decision (source = file resources on the dev Star; durable draft = Galaxy SQLite) is two of the three source-durability designs Decision 7 purges. Folds: enterprise BYO-agent git+FS surface (post-demo direction, GitHub-Enterprise+Claude-Code worked example) + the deferred built-artifact bundle-store backend question (keep Galaxy's version API backend-agnostic). Delete the WASM-git re-litigation + Revive-when sections.
- **tasks/on-hold/nebula-lazy-schema-migrations.md** — ARCHIVE AFTER folding two surviving principles into nebula-studio.md, then delete the on-hold file. Lazy schema-migration runner (Star-local, in-place, copy-on-read), deferred 2026-06-15, cut from the demo, NEVER BUILT. Its entire mechanism (OntologyVersionRow.migrationBundle, compileOntologyVersion, Galaxy.ontology:_index chain-walk, per-version Galaxy.getOntologyVersion facet load) rendered obsolete by nebula-dev-flows.md Decision 9 (ontology-version registry eliminated) before any code landed. Preserve the per-Star copy-on-read design specced + the rename trail (branch-migrations.md -> nebula-5.5-dev-mode-migrations.md -> branch-migrations.md -> nebula-lazy-schema-migrations.md). Folds (forward-capability notes only): the Star-agnostic copy-on-read PRINCIPLE (rerouted onto the per-resource ontology-version stamp + OntologyStaleError, NOT the registry — Decision 11) + the parser-owns-default-fill BOUNDARY (additive @default fills on next write; migrations only for renames/type-changes/required-adds/computed). Current dev-loop behavior points to nebula-dev-flows.md Decision 11 / Flow 1b (the .dev-guarded resetDevData wipe-prompt).

## Fold map
- tasks/nebula-container-dev-loop.md ⇒ tasks/nebula-studio.md: Surviving build mechanics (expressed against nebula-dev-flows.md as architecture authority, NOT duplicating flows): pool-workers test reality ([[container-no-construct-pool-workers]]; dev = local wrangler dev on Docker Desktop; deployed e2e via wrangler deploy + WARP fallback); DevContainer design mechanics (extends NebulaContainer, DEV_CONTAINER binding + smart-match guard, two ports 5173-public/9000-host-only, three-way fetch branch, server.allowedHosts pin, server-derived scope-injection choke point, @mesh(requireAdmin) on command methods, writeFile path validation); the TWO entrypoint edits (M3 serving-target switch += env.DEV_CONTAINER; M2 HMR-WS allow via onBeforeConnect); Q1 RESOLVED (bake curated UI-lib set, zero npm-install cold boot, bench numbers); Q3 RESOLVED (vite fully owns SFC compile, delete in-DO machinery, salvage scaffold bootstrap shape, vite diagnostics as method-return data); Q4 RESOLVED (keep {u}.{g}.dev addressing, generateBindingVariations disjointness); Phase 4 teardown (salvage-first residual-grep deletion of getPlatformAsset/compileSFC/rewriteServedSpecifiers/platform-assets/bundle-tsc; KEEP reload channel as #1b publish-refresh; KEEP nodejs_compat); pool-workers test strategy (entrypoint gate fan-out via inert stub-DO bindings, HMR-WS gate, scope-injection pure-function, writeFile guard, deploy-gated it.skip). DO NOT carry the reversed DevContainer-source-of-truth/DevSource/dual-write/DevStar-class/deployToDev/compileSFC-live/Phase-3.5-mermaid — those are the realignment tracker's revert checklist.
- tasks/dev-star.md ⇒ tasks/nebula-studio.md: The resetDevData wipe mechanism, re-homed onto a plain Star@.dev (NO DevStar class, NO DEV_STAR binding): dev sandbox = plain Star at {u}.{g}.dev (own SQLite, prod-isolated via inherited onBeforeCall); resetDevData() hard-guarded to .dev (if !instanceName.endsWith('.dev') throw), @mesh(requireAdmin), body = blockConcurrencyWhile(deleteAll -> onStart()); onStart() must be a COMPLETE re-init (reconstruct DagTree/Resources/Subscriptions, recreate schema+ROOT, null Star.#row/#facet so no stale validator-facet cache); founder root-admin reseed rides next admin call's onBeforeCall first-touch; latent alarms caveat (also evict cached Alarms service if svc.alarms is ever used); bounded concurrency edge (generation-counter hardening deferred; single-admin demo contract); additive-preserved/breaking-reset data bargain; the reset TRIGGER/prompt points to nebula-dev-flows.md Flow 1b/Decision 11 (don't restate); the 'dev' slug reservation don't-forget; scope exclusions (no URL-branching, no prod-fork-seed, no concurrent dev-<name>); the @mesh-surface-freeze guard (every added @mesh method admin-gated) as a standing test.
- tasks/nebula-app-versioning.md ⇒ tasks/nebula-studio.md: Thin surviving build slice into the Studio publish section: publish path (DevContainer vite build -> push built bundle+assets to Galaxy R2 asset set -> prod Star lazy-pulls on version mismatch) as POINTERS to Flow 2/2b (don't restate registry shape); Galaxy = published-only immutable app-version registry; AppBundle PK-migration GOTCHA (adding a version column = PK path -> (version,path) = a schema migration, needs in-DO rebuild create-new/copy/drop/rename under a schema-version latch — only if the bundle store keeps AppBundle); SECURITY pattern (Galaxy publish/admin methods carry @mesh(requireAdmin) because <id>.* widening admits descendant non-admins; re-validate every browser-supplied field server-side); TEST patterns (maintain B5 frozen non-admin allow-list; a genuinely-minted in-scope non-admin must be rejected + write nothing; publish atomicity via debug-sink markers); prod-serve open seam (sync Star.onRequest can't await a cold lazy-pull -> fixed loading-shell + async ensure-version + reload; clean-URL/custom-domain routing) as a post-demo note; deferred binary-asset (file resources / ArrayBuffer) + prod-cold-start (unused compile machinery behind a Worker Loader facet) notes.
- tasks/nebula-studio-llm-strategy.md ⇒ tasks/nebula-studio.md: Generation-engine WHY + strategy: Kimi 2.7 (@cf/moonshotai/kimi-k2.7-code) via Workers AI binding + the WHY (No Cloudflare Think — bundles a rollable loop/codemode/chat-memory/foreign multi-tenant DO surface; No codemode — JSON-only bridge = ADR-002 violation; Claude = eval baseline only); orchestration = thin self-rolled native tool-calling loop on Workers+DO (native tool_calls verified; no LLM-authored server code so no sandbox), HOME = DevStudio (REPLACE the old 'Nebula-owned DO/facet=child-DO' framing — facets deferred, Decision 8); two AI contexts (Studio authoring loop = DevStudio per-call tool-calling, script-per-step facet-sandbox a pocketed optimization; in-app AI chat = post-Studio RAG, likely its own future task file); LM-strategy arc (prompt-engineering -> fine-tuned small model; training data); context-window (ontology pinned, refresh on change; turn-archival deferred); eval = start with Kimi (live use IS the eval), Claude baseline, real generation jobs that must pass validation + access-control + reactivity (full suite -> on-hold/nebula-studio-eval-suite.md); mine vibesdk patterns before wiring. Also update nebula-studio.md's own L45-46 'Agent home...facet' wording during the fold.
- tasks/nebula-resource-metadata.md ⇒ tasks/nebula-studio.md: The annotation-convention build content: @title (type+field, required), @description (type+field, optional), @inverse <fieldName> (relationship, required, names the inverse); body-comments-are-NOT-descriptions; deliberate exclusions (no @id/@unique/@index, @onDelete deferred, no cardinality/FK-location annotations) + why; the WorkoutSession 1:1/1:M/M:N canonical example; PascalCase-interface-name-IS-the-URL-slug rule + URI template + rationale; source-as-spec principle (AI consumes raw .d.ts, not a JSON bundle) — ALIGNS with Decision 9; BUILD steps (extend extractTypeMetadata() additively in @lumenize/ts-runtime-parser-validator; user docs at website/docs/nebula/resource-types.md); runtime use (@title/@description in validation errors, @inverse reserved for post-demo query engine); open Qs (@title-required verbosity, all-interfaces-resources vs embedded value types, cascade-is-UI's-job). RE-HOME (do NOT carry verbatim): source-retrieval from Galaxy via versioned method -> Studio reads the CURRENT ontology .d.ts from DevStudio's source tree (no getOntologySource(version?)/OntologyVersionRow.source/per-version retrieval — Decision 9 killed the registry).
- tasks/kimi-ui-gen-viability.md ⇒ tasks/nebula-studio.md: The durable verdict only: Kimi 2.7 UI-gen is VIABLE (proven 2026-06-16 — given coding-your-ui.md + ontology + Nebula API .d.ts, produces compilable Vue SFC + ontology .d.ts first-shot, self-corrects in ~2 rounds; 'Studio is a wrapper around a proven loop'); model fact (slug @cf/moonshotai/kimi-k2.7-code, OpenAI-style result.choices[0].message.content); loop design lessons (use native tool-calling not regex-on-prose; feed the real Nebula API .d.ts; wire a real error-tail/get_recent_errors); single-run-directional caveat (rigorous regression suite = on-hold/nebula-studio-eval-suite.md). Do NOT carry the OBE implementation plan (SFC-compile-in-DO, tsc-bundle, files-as-resources, register_ontology_version, DevStar.compileSFC).
- tasks/nebula-self-hosted-assets.md ⇒ tasks/nebula-studio.md: Two enduring requirements (NOT the in-DO mechanism): (1) product requirement — generated apps + Studio UI must serve all scripts SAME-ORIGIN under strict script-src 'self' (no CDN/unsafe-eval/inline import map); under the canonical model this is satisfied for free by Workers-Assets (Studio UI, Decision 3) + vite static serving (Preview app), so it becomes a VERIFICATION CHECKPOINT not a build task. (2) code-gen prerequisite — generated apps need styling (DaisyUI, already pinned) + icons (Lucide, ISC not MIT — correct ATTRIBUTIONS to ISC) available same-origin; sourcing is now ordinary baked container vite/npm deps (Flow 1c), NOT in-DO vendored ESM. The two bug fixes (nodejs_compat, bindingMetadata) are already source-applied + memory-homed — don't lose, no fold. Carry one salvageable line: prod = vite build -> Workers Assets (no running container, ~28x CSS win); container image deploy via GHA .github/workflows/deploy-container.yml.
- tasks/preview-iframe-spike.md ⇒ tasks/nebula-studio.md: Two Studio-UI build notes: (1) the Preview app embeds in an in-window iframe inside the Studio UI (same-origin per Decision 3 / Topology — auth via JWT over /gateway WS, preview shell via /dev-container; no separate auth-propagation work needed); (2) the one genuinely-live deliverable — a fallback to seamlessly LAUNCH (or FOCUS, if already open) a separate browser window on the preview URL when iframe embedding fights us (track as a small Studio-UI build item with window-handle reuse). Everything else in the spike is OBE.
- tasks/vibesdk-llm-patterns.md ⇒ tasks/nebula-studio.md: A 'vibesdk LLM-patterns extraction' pre-implementation reading task entry (NOT architecture): status unblocked/never-started; mine cloudflare/vibesdk (MIT) for the LLM-orchestration layer (prompts, model routing, tool defs, agent-state machine, streaming/abort/reconnect, AI Gateway) DevStudio's codegen loop + the in-app chat-feature block need; NOT mining back-end/dev-loop patterns (settled in nebula-dev-flows.md). Carry: the priority read-list (worker/agents/inferutils/config.ts AGENT_CONFIG, prompts.ts, operations/UserConversationProcessor.ts, PhaseGeneration.ts+PhaseImplementation.ts, assistants/codeDebugger.ts, tools/customTools.ts+toolkit/, schemas.ts, AI-Gateway code, samplePrompts.md+docs/, their CLAUDE.md); the 9-section output-doc skeleton; caveats (filter for talking-to-LLMs not what-code-to-generate; MIT — excerpts fine, copying follows ATTRIBUTIONS + <1000 SLOC); stop-condition (all 9 sections + >=1 code ref + a 'what to consider for Nebula' note). Output lands at tasks/reference/vibesdk-llm-patterns.md. Then repoint the 4 inbound links to the new section.
- tasks/on-hold/nebula-file-storage-backend.md ⇒ tasks/nebula-studio.md: Two still-open forward threads as brief post-demo/backlog notes: (1) enterprise/power-user BYO-agent direction — users on their own coding agents (Claude Code/Cursor) expect a real git+filesystem surface not Studio's checkpoint metaphor; worked example GitHub-Enterprise + Claude-Code -> webhook/GH-Action on push-to-main -> Nebula git.clone into a build DO's Workspace -> build via state.* FS -> push built bundle to Galaxy (runs on WorkspaceFileSystem today, no Artifacts dep); strongest argument for a post-demo desktop authoring shell; (2) the built-artifact (compiled UI bundle) store behind Galaxy's version API — candidates Galaxy SQLite/KV/R2/shell Workspace/Artifacts-backed StateBackend; keep Galaxy's version-management API backend-agnostic so the choice stays a StateBackend swap, not an API change (Flow 2 lands the bundle as an R2 asset set; this is the deeper store-product question, low priority). The founding question (which backend) is already RESOLVED in nebula-dev-flows.md Decision 6 — point there.
- tasks/on-hold/nebula-lazy-schema-migrations.md ⇒ tasks/nebula-studio.md: Lazy schema migration as a tracked FUTURE Studio capability (small forward-looking note, NOT a build plan — the mechanism here is dead): per-Star in-place copy-on-read PRINCIPLE — runs against the routed Star's own SQLite with NO Star-identity awareness (works identically on .dev and prod; aligns with Decision 11 lockstep); the hard BOUNDARY — default-fill is the PARSER's job not migration's (additive @default fills on next write via __fillDefaults; a plain read returns stored value verbatim; additive edits need NO migration entry; migrations are ONLY for renames/type-changes/required-adds/computed). Current behavior POINTS to nebula-dev-flows.md Decision 11 + Flow 1b (.dev-guarded resetDevData wipe-prompt). Future-design correction to record: when the runner is built it must ride the per-resource ontology-version stamp + OntologyStaleError, NOT a Galaxy ontology-version registry (eliminated by Decision 9); the OntologyVersionRow.migrationBundle/appendOntologyVersion/compileOntologyVersion design is obsolete, do not carry.

## Migration order
1. 0. PREREQUISITE (separate effort, not this consolidation): the nebula-dev-flows-realignment.md pass owns all IN-PLACE term-swaps/rewrites for nebula.md and nebula-studio.md and the source-of-truth REVERT checklist. This consolidation only MOVES content so whole files can die. Where a fold target needs realigned wording (e.g. DevStudio cast, Star@.dev), apply the realignment edit as part of the fold so nebula-studio.md lands as clean current-truth.
2. 1. BUILD/EXTEND the Studio survivor first: add the top-of-file pointer to nebula-dev-flows.md ('architecture + flows are canonical there; this file is the Studio build plan, do not restate the model'); rewrite nebula-studio.md per its realignment §findings (agent->DevStudio, DevStar->Star@.dev, Studio-UI-from-Workers-Assets, DevStudio-source-of-truth replacing the Durable-draft-ownership section, drop the container-vite-pivot narrative + Files-as-resources + Dev-mode-Star-SFC-compile sections, fix deploy_to_dev/build-sequencing). This makes the destination ready to absorb.
3. 2. FOLD never-built still-valid build content into nebula-studio.md, in dependency order: (a) nebula-resource-metadata.md conventions; (b) nebula-studio-llm-strategy.md engine WHY + strategy; (c) vibesdk-llm-patterns.md reading task; (d) nebula-app-versioning.md publish slice + gotchas; (e) dev-star.md resetDevData mechanism; (f) nebula-container-dev-loop.md build mechanics + teardown + test strategy; (g) the spike verdict (kimi-ui-gen-viability.md) + self-hosted-assets.md requirements + preview-iframe-spike.md fallback note; (h) the future-capability pointers (file-storage-backend, lazy-schema-migrations, plus inbound one-liners to on-hold/nebula-skills.md and on-hold/nebula-studio-eval-suite.md).
4. 3. REALIGN nebula.md IN-PLACE per the realignment tracker (8 findings): cast naming + pointer to nebula-dev-flows.md, drop the ~3s-reload/Galaxy-.d.ts/breaking-edit-auto-reset prose, the removed ResourceHistory fixture, BSL-vs-UNLICENSED; add the scope-isolation DONE one-liner. (No content folds OUT of nebula.md.)
5. 4. ARCHIVE (git mv to tasks/archive/, frozen on entry, AFTER any fold above completes): nebula-container-dev-loop.md, dev-star.md, nebula-devcontainer-node-type.md, nebula-do-scope-isolation.md, nebula-self-hosted-assets.md, nebula-studio-compile-pipeline.md, kimi-ui-gen-viability.md, container-vite-spike.md, spike-container-agent-channel.md, on-hold/nebula-file-storage-backend.md, on-hold/nebula-lazy-schema-migrations.md.
6. 5. DELETE (git rm, AFTER salvage folded in step 2): nebula-app-versioning.md, nebula-studio-llm-strategy.md, nebula-resource-metadata.md, preview-iframe-spike.md, vibesdk-llm-patterns.md.
7. 6. FIX REFERENCES LAST (after files are moved/removed): repoint vibesdk inbound links (nebula-studio-llm-strategy.md is itself deleted; nebula.md:57+92 -> the new nebula-studio.md section; icebox/think-nebula-integration.md:5 -> same); nebula.md phase-table rows for the now-archived/deleted files -> archive paths or the Studio survivor; on-hold/nebula-branches.md two dev-star.md refs -> nebula-dev-flows.md Decision 2; any other inbound links to the 16 removed files. The on-hold terminology passes (nebula-skills.md, nebula-studio-eval-suite.md, nebula-5.5-schema-evolution.md) are owned by the realignment tracker, NOT this consolidation.

## Risks / needs-user
- DOUBLE-HANDLING with the realignment tracker: nebula-dev-flows-realignment.md already enumerates 26 findings against nebula-container-dev-loop.md and 26 against nebula-studio.md (term-swaps + the source-of-truth revert). This consolidation ARCHIVES container-dev-loop and folds its build content into nebula-studio. If the realignment pass runs first against container-dev-loop, that work is wasted (file gets archived). RECOMMEND: run consolidation's fold/archive BEFORE the realignment pass touches the to-be-archived/deleted files; apply realignment edits only to the survivors (nebula.md, nebula-studio.md). The user should confirm the sequencing (consolidate-then-realign vs realign-then-consolidate).
- nebula-studio.md becomes a large file absorbing ~6 sources' build content. Risk it grows into the same sprawl. The user may prefer a 2nd survivor (e.g. keep a separate 'studio dev-loop build' file vs 'studio codegen/UX build' file). The vision says 'maybe 1-2 more survivors at most' — confirm whether one big nebula-studio.md is acceptable or it should split.
- Whether nebula-dev-flows-realignment.md should itself eventually be archived once the realignment is executed. It is the live revert checklist now (DONE-but-being-REVERTED tracking) so it must survive through the realignment, but it is a tracker not a task file — flag for the user whether it stays in tasks/ or moves to archive once items are checked off.
- DevStar build record (dev-star.md, P1-P4) is BUILT but UNCOMMITTED on feat/nebula-studio and is being REVERTED. The actual code revert (delete DevStar class / DEV_STAR binding / #starBinding, re-home resetDevData onto Star) is tracked by the realignment + the Phase-3.5 build, NOT by this doc consolidation. Confirm the user understands archiving dev-star.md does not perform the code revert.
- nebula-self-hosted-assets.md, nebula-studio-compile-pipeline.md, and dev-star.md describe code that is BUILT + green but UNCOMMITTED and slated for teardown (the Phase-4 residual-grep deletion folded into nebula-studio.md). Archiving the docs is safe, but the user should confirm the in-DO-compile teardown grep is the canonical deletion authority and the in-do-compile-baseline git tag (commit 0953f6f) + spike-sfc-galaxy-loop wrangler-delete are tracked.
- nebula-resource-metadata.md is on the ACTIVE demo critical path (nebula.md phase table). Deleting it (after fold) removes a tracked phase row — the fold MUST land its conventions in nebula-studio.md and the nebula.md phase row must be repointed, or the demo plan loses a tracked deliverable. Confirm the conventions are demo-required and belong inside the Studio build plan rather than a standalone file.

## Per-file disposition
- nebula.md → **survive**
- nebula-studio.md → **survive** ⇒ nebula-studio.md
- nebula-container-dev-loop.md → **fold-then-archive** ⇒ nebula-studio.md
- dev-star.md → **split-archive-delete** ⇒ nebula-studio.md
- nebula-app-versioning.md → **fold-then-delete** ⇒ nebula-studio.md
- nebula-devcontainer-node-type.md → **archive-whole**
- nebula-do-scope-isolation.md → **archive-whole** ⇒ nebula.md
- nebula-self-hosted-assets.md → **fold-then-archive** ⇒ nebula-studio.md
- nebula-studio-compile-pipeline.md → **archive-whole**
- nebula-studio-llm-strategy.md → **fold-then-delete** ⇒ nebula-studio.md
- nebula-resource-metadata.md → **fold-then-delete** ⇒ nebula-studio.md
- nebula-scratchpad.md → **keep-onhold-asis**
- kimi-ui-gen-viability.md → **fold-then-archive** ⇒ nebula-studio.md
- container-vite-spike.md → **archive-whole**
- preview-iframe-spike.md → **fold-then-delete** ⇒ nebula-studio.md
- spike-container-agent-channel.md → **archive-whole**
- vibesdk-llm-patterns.md → **fold-then-delete** ⇒ nebula-studio.md
- on-hold/nebula-skills.md → **keep-onhold-asis**
- on-hold/nebula-studio-eval-suite.md → **keep-onhold-asis**
- on-hold/nebula-file-storage-backend.md → **fold-then-archive** ⇒ nebula-studio.md
- on-hold/nebula-5.5-schema-evolution.md → **keep-onhold-asis**
- on-hold/nebula-lazy-schema-migrations.md → **fold-then-archive** ⇒ nebula-studio.md
- on-hold/nebula-branches.md → **keep-onhold-asis**
