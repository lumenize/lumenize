// GENERATED by scripts/gen-validator-seeds.ts — do not hand-edit; edit the INPUTS
// (src/chat-constants.ts, src/tool-args-constants.ts, or the compiler they feed) and
// re-run `npx tsx scripts/gen-validator-seeds.ts`. The package `test` script runs
// `--check` ahead of vitest, which rebuilds both literals and reds on drift.
//
// ⚠️ A changed literal REQUIRES a bundle-id bump beside it — the Worker Loader caches
// by id, so a changed module under a reused id serves the stale validator. See the
// generator's header for which id pairs with which literal.
import type { OntologyVersionRow } from './ontology-compile';

/**
 * The platform chat ontology's compiled seed row — the FIRST install's content for a
 * Galaxy's chat registry ({@link chatOntologySeedRow} hands it over). Precompiled and
 * committed so no Worker ever runs the ontology compiler for a constant input.
 */
export const CHAT_ONTOLOGY_SEED_ROW: OntologyVersionRow = {
  version: "chat-message-v1",
  types: "interface Chat { title: string }\ninterface Message {\n    chat: Chat;\n    content: string;\n    status?: string;\n    thought?: string;\n    replyTo?: Message;\n    codegen?: {\n      model: string;\n      scaffold?: string;\n      sourceCommit?: string;\n      rounds: number;\n      stop: string;\n      appliedPaths: string[];\n      build?: { checked: string[]; findings: string[] };\n      toolCalls: Array<{ name: string; args?: unknown; result?: unknown; error?: string }>;\n    };\n  }",
  validatorBundle: "\nimport { DurableObject } from \"cloudflare:workers\";\n\nconst __typia_transform__validateReport = (() => {\n  const _validateReport = (array) => {\n    const reportable = (path) => {\n      if (array.length === 0) return true;\n      const last = array[array.length - 1].path;\n      return path.length > last.length || last.substring(0, path.length) !== path;\n    };\n    return (exceptable, error) => {\n      if (exceptable && reportable(error.path)) {\n        if (error.value === undefined)\n          error.description ??= [\n            \"The value at this path is `undefined`.\",\n            \"\",\n            `Please fill the \\`${error.expected}\\` typed value next time.`,\n          ].join(\"\\n\");\n        array.push(error);\n      }\n      return false;\n    };\n  };\n  return { _validateReport };\n})();\nconst __typia_transform__createStandardSchema = (() => {\n  var PathParserState;\n  (function (PathParserState) {\n    PathParserState[PathParserState[\"Start\"] = 0] = \"Start\";\n    PathParserState[PathParserState[\"Property\"] = 1] = \"Property\";\n    PathParserState[PathParserState[\"StringKey\"] = 2] = \"StringKey\";\n    PathParserState[PathParserState[\"NumberKey\"] = 3] = \"NumberKey\";\n  })(PathParserState || (PathParserState = {}));\n\n  const typiaPathToStandardSchemaPath = (path) => {\n    if (!path.startsWith(\"$input\")) {\n      throw new Error(`Invalid path: ${JSON.stringify(path)}`);\n    }\n    const segments = [];\n    let currentSegment = \"\";\n    let state = PathParserState.Start;\n    let index = \"$input\".length - 1;\n    while (index < path.length - 1) {\n      index++;\n      const char = path[index];\n      if (state === PathParserState.Property) {\n        if (char === \".\" || char === \"[\") {\n          segments.push({ key: currentSegment });\n          state = PathParserState.Start;\n        } else if (index === path.length - 1) {\n          currentSegment += char;\n          segments.push({ key: currentSegment });\n          index++;\n          state = PathParserState.Start;\n        } else {\n          currentSegment += char;\n        }\n      } else if (state === PathParserState.StringKey) {\n        if (char === '\"') {\n          segments.push({ key: JSON.parse(currentSegment + char) });\n          index += 2;\n          state = PathParserState.Start;\n        } else if (char === \"\\\\\") {\n          currentSegment += path[index];\n          index++;\n          currentSegment += path[index];\n        } else {\n          currentSegment += char;\n        }\n      } else if (state === PathParserState.NumberKey) {\n        if (char === \"]\") {\n          segments.push({ key: Number.parseInt(currentSegment) });\n          index++;\n          state = PathParserState.Start;\n        } else {\n          currentSegment += char;\n        }\n      }\n      if (state === PathParserState.Start && index < path.length - 1) {\n        const newChar = path[index];\n        currentSegment = \"\";\n        if (newChar === \"[\") {\n          if (path[index + 1] === '\"') {\n            state = PathParserState.StringKey;\n            index++;\n            currentSegment = '\"';\n          } else {\n            state = PathParserState.NumberKey;\n          }\n        } else if (newChar === \".\") {\n          state = PathParserState.Property;\n        } else {\n          throw new Error(\"Unreachable: pointer points invalid character\");\n        }\n      }\n    }\n    if (state !== PathParserState.Start) {\n      throw new Error(`Failed to parse path: ${JSON.stringify(path)}`);\n    }\n    return segments;\n  };\n\n  const _createStandardSchema = (fn) => Object.assign(fn, {\n    \"~standard\": {\n      version: 1,\n      vendor: \"typia\",\n      validate: (input) => {\n        const result = fn(input);\n        if (result.success) {\n          return { value: result.data };\n        } else {\n          return {\n            issues: result.errors.map((error) => ({\n              message: `expected ${error.expected}, got ${error.value}`,\n              path: typiaPathToStandardSchemaPath(error.path),\n            })),\n          };\n        }\n      },\n    },\n  });\n\n  return { _createStandardSchema };\n})();\n\nexport const validators = {\n    _OrgNode: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return undefined === input.platformReserved || \"string\" === typeof input.platformReserved;\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [undefined === input.platformReserved || \"string\" === typeof input.platformReserved || _report(_exceptionable, {\n                path: _path + \".platformReserved\",\n                expected: \"(string | undefined)\",\n                value: input.platformReserved\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && false === Array.isArray(input) && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input && false === Array.isArray(input) || _report(true, {\n                path: _path + \"\",\n                expected: \"_OrgNode\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"_OrgNode\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })(),\n    _InviteStatus: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return \"string\" === typeof input.node && \"string\" === typeof input.email && \"string\" === typeof input.tier && \"string\" === typeof input.state && (undefined === input.error || \"string\" === typeof input.error);\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [\"string\" === typeof input.node || _report(_exceptionable, {\n                path: _path + \".node\",\n                expected: \"string\",\n                value: input.node\n            }), \"string\" === typeof input.email || _report(_exceptionable, {\n                path: _path + \".email\",\n                expected: \"string\",\n                value: input.email\n            }), \"string\" === typeof input.tier || _report(_exceptionable, {\n                path: _path + \".tier\",\n                expected: \"string\",\n                value: input.tier\n            }), \"string\" === typeof input.state || _report(_exceptionable, {\n                path: _path + \".state\",\n                expected: \"string\",\n                value: input.state\n            }), undefined === input.error || \"string\" === typeof input.error || _report(_exceptionable, {\n                path: _path + \".error\",\n                expected: \"(string | undefined)\",\n                value: input.error\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input || _report(true, {\n                path: _path + \"\",\n                expected: \"_InviteStatus\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"_InviteStatus\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })(),\n    Chat: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return \"string\" === typeof input.title;\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [\"string\" === typeof input.title || _report(_exceptionable, {\n                path: _path + \".title\",\n                expected: \"string\",\n                value: input.title\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input || _report(true, {\n                path: _path + \"\",\n                expected: \"Chat\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"Chat\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })(),\n    Message: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return \"string\" === typeof input.chat && \"string\" === typeof input.content && (undefined === input.status || \"string\" === typeof input.status) && (undefined === input.thought || \"string\" === typeof input.thought) && (undefined === input.replyTo || \"string\" === typeof input.replyTo) && (undefined === input.codegen || \"object\" === typeof input.codegen && null !== input.codegen && _io1(input.codegen));\n    }; const _io1 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io1\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io1\");\n        }\n        return \"string\" === typeof input.model && (undefined === input.scaffold || \"string\" === typeof input.scaffold) && (undefined === input.sourceCommit || \"string\" === typeof input.sourceCommit) && \"number\" === typeof input.rounds && \"string\" === typeof input.stop && (Array.isArray(input.appliedPaths) && input.appliedPaths.every(elem => \"string\" === typeof elem)) && (undefined === input.build || \"object\" === typeof input.build && null !== input.build && _io2(input.build)) && (Array.isArray(input.toolCalls) && input.toolCalls.every(elem => \"object\" === typeof elem && null !== elem && _io3(elem)));\n    }; const _io2 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io2\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io2\");\n        }\n        return Array.isArray(input.checked) && input.checked.every(elem => \"string\" === typeof elem) && (Array.isArray(input.findings) && input.findings.every(elem => \"string\" === typeof elem));\n    }; const _io3 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io3\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io3\");\n        }\n        return \"string\" === typeof input.name && true && true && (undefined === input.error || \"string\" === typeof input.error);\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [\"string\" === typeof input.chat || _report(_exceptionable, {\n                path: _path + \".chat\",\n                expected: \"string\",\n                value: input.chat\n            }), \"string\" === typeof input.content || _report(_exceptionable, {\n                path: _path + \".content\",\n                expected: \"string\",\n                value: input.content\n            }), undefined === input.status || \"string\" === typeof input.status || _report(_exceptionable, {\n                path: _path + \".status\",\n                expected: \"(string | undefined)\",\n                value: input.status\n            }), undefined === input.thought || \"string\" === typeof input.thought || _report(_exceptionable, {\n                path: _path + \".thought\",\n                expected: \"(string | undefined)\",\n                value: input.thought\n            }), undefined === input.replyTo || \"string\" === typeof input.replyTo || _report(_exceptionable, {\n                path: _path + \".replyTo\",\n                expected: \"(string | undefined)\",\n                value: input.replyTo\n            }), undefined === input.codegen || (\"object\" === typeof input.codegen && null !== input.codegen || _report(_exceptionable, {\n                path: _path + \".codegen\",\n                expected: \"(__type | undefined)\",\n                value: input.codegen\n            })) && _vo1(input.codegen, _path + \".codegen\", true && _exceptionable) || _report(_exceptionable, {\n                path: _path + \".codegen\",\n                expected: \"(__type | undefined)\",\n                value: input.codegen\n            })].every(flag => flag);\n    }; const _vo1 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo1\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo1\");\n        }\n        return [\"string\" === typeof input.model || _report(_exceptionable, {\n                path: _path + \".model\",\n                expected: \"string\",\n                value: input.model\n            }), undefined === input.scaffold || \"string\" === typeof input.scaffold || _report(_exceptionable, {\n                path: _path + \".scaffold\",\n                expected: \"(string | undefined)\",\n                value: input.scaffold\n            }), undefined === input.sourceCommit || \"string\" === typeof input.sourceCommit || _report(_exceptionable, {\n                path: _path + \".sourceCommit\",\n                expected: \"(string | undefined)\",\n                value: input.sourceCommit\n            }), \"number\" === typeof input.rounds || _report(_exceptionable, {\n                path: _path + \".rounds\",\n                expected: \"number\",\n                value: input.rounds\n            }), \"string\" === typeof input.stop || _report(_exceptionable, {\n                path: _path + \".stop\",\n                expected: \"string\",\n                value: input.stop\n            }), (Array.isArray(input.appliedPaths) || _report(_exceptionable, {\n                path: _path + \".appliedPaths\",\n                expected: \"Array<string>\",\n                value: input.appliedPaths\n            })) && input.appliedPaths.map((elem, _index5) => \"string\" === typeof elem || _report(_exceptionable, {\n                path: _path + \".appliedPaths[\" + _index5 + \"]\",\n                expected: \"string\",\n                value: elem\n            })).every(flag => flag) || _report(_exceptionable, {\n                path: _path + \".appliedPaths\",\n                expected: \"Array<string>\",\n                value: input.appliedPaths\n            }), undefined === input.build || (\"object\" === typeof input.build && null !== input.build || _report(_exceptionable, {\n                path: _path + \".build\",\n                expected: \"(__type.o1 | undefined)\",\n                value: input.build\n            })) && _vo2(input.build, _path + \".build\", true && _exceptionable) || _report(_exceptionable, {\n                path: _path + \".build\",\n                expected: \"(__type.o1 | undefined)\",\n                value: input.build\n            }), (Array.isArray(input.toolCalls) || _report(_exceptionable, {\n                path: _path + \".toolCalls\",\n                expected: \"Array<__type>\",\n                value: input.toolCalls\n            })) && input.toolCalls.map((elem, _index6) => (\"object\" === typeof elem && null !== elem || _report(_exceptionable, {\n                path: _path + \".toolCalls[\" + _index6 + \"]\",\n                expected: \"__type.o2\",\n                value: elem\n            })) && _vo3(elem, _path + \".toolCalls[\" + _index6 + \"]\", true && _exceptionable) || _report(_exceptionable, {\n                path: _path + \".toolCalls[\" + _index6 + \"]\",\n                expected: \"__type.o2\",\n                value: elem\n            })).every(flag => flag) || _report(_exceptionable, {\n                path: _path + \".toolCalls\",\n                expected: \"Array<__type>\",\n                value: input.toolCalls\n            })].every(flag => flag);\n    }; const _vo2 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo2\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo2\");\n        }\n        return [(Array.isArray(input.checked) || _report(_exceptionable, {\n                path: _path + \".checked\",\n                expected: \"Array<string>\",\n                value: input.checked\n            })) && input.checked.map((elem, _index7) => \"string\" === typeof elem || _report(_exceptionable, {\n                path: _path + \".checked[\" + _index7 + \"]\",\n                expected: \"string\",\n                value: elem\n            })).every(flag => flag) || _report(_exceptionable, {\n                path: _path + \".checked\",\n                expected: \"Array<string>\",\n                value: input.checked\n            }), (Array.isArray(input.findings) || _report(_exceptionable, {\n                path: _path + \".findings\",\n                expected: \"Array<string>\",\n                value: input.findings\n            })) && input.findings.map((elem, _index8) => \"string\" === typeof elem || _report(_exceptionable, {\n                path: _path + \".findings[\" + _index8 + \"]\",\n                expected: \"string\",\n                value: elem\n            })).every(flag => flag) || _report(_exceptionable, {\n                path: _path + \".findings\",\n                expected: \"Array<string>\",\n                value: input.findings\n            })].every(flag => flag);\n    }; const _vo3 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo3\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo3\");\n        }\n        return [\"string\" === typeof input.name || _report(_exceptionable, {\n                path: _path + \".name\",\n                expected: \"string\",\n                value: input.name\n            }), true, true, undefined === input.error || \"string\" === typeof input.error || _report(_exceptionable, {\n                path: _path + \".error\",\n                expected: \"(string | undefined)\",\n                value: input.error\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input || _report(true, {\n                path: _path + \"\",\n                expected: \"Message\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"Message\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })()\n};\n\n\nconst __typeMetadata = {\n  defaults: {},\n  typeGraph: {},\n  inlineSubtypes: {\"Message\":{\"codegen\":{\"subTypeName\":\"Message/codegen\"}},\"Message/codegen\":{\"build\":{\"subTypeName\":\"Message/codegen/build\"},\"toolCalls\":{\"subTypeName\":\"Message/codegen/toolCalls\",\"containers\":[\"array\"],\"mapKeyTypes\":[null]}}},\n  relationshipHints: {\"_InviteStatus\":{\"node\":{\"target\":\"_OrgNode\",\"cardinality\":\"one\",\"optional\":false}},\"Message\":{\"chat\":{\"target\":\"Chat\",\"cardinality\":\"one\",\"optional\":false},\"replyTo\":{\"target\":\"Message\",\"cardinality\":\"one\",\"optional\":true}}},\n};\n\n/**\n * Deep-clone helper used by the filler. We only touch plain objects and\n * arrays; other types (Date, Map, Set, etc.) are passed through by reference.\n * A WeakMap tracks visited nodes so cyclic structures don't blow the stack.\n */\nfunction __clonePlain(v, seen) {\n  if (v === null || typeof v !== 'object') return v;\n  if (seen.has(v)) return seen.get(v);\n  if (Array.isArray(v)) {\n    const out = [];\n    seen.set(v, out);\n    for (let i = 0; i < v.length; i++) out[i] = __clonePlain(v[i], seen);\n    return out;\n  }\n  if (Object.getPrototypeOf(v) === Object.prototype || Object.getPrototypeOf(v) === null) {\n    const out = {};\n    seen.set(v, out);\n    for (const k of Object.keys(v)) out[k] = __clonePlain(v[k], seen);\n    return out;\n  }\n  // Date, Map, Set, RegExp, TypedArrays, class instances — passed through\n  // by reference. Safe because the filler never mutates them.\n  return v;\n}\n\n/**\n * Apply @default values to `value` against type `typeName`, **mutating in\n * place**. Returns the same `value` reference for caller convenience.\n *\n * In-place is safe because the only entry points are `parse()` / `parseBatch()`\n * on the facet, which receive structured-clones across the Workers RPC boundary\n * — the caller's original object is never touched. Cycles back to the root\n * survive (no clone breaks them), and identity is preserved for callers that\n * pass through a value untouched.\n *\n * Default-literal values themselves are still cloned when applied (the literal\n * is shared across all parses for that type, so we can't hand the same object\n * reference to every caller).\n *\n * Cycle termination: `seen` is a WeakSet tracking objects currently being\n * filled. A re-entry returns immediately, so a self-referential field doesn't\n * recurse forever — the inner traversal is skipped because the outer call is\n * already handling that object.\n */\nfunction __fillDefaults(value, typeName, seen) {\n  if (value === null || typeof value !== 'object' || Array.isArray(value)) return value;\n\n  const typeDefaults = __typeMetadata.defaults[typeName];\n  const sub = __typeMetadata.typeGraph[typeName];\n  const inlineForType = __typeMetadata.inlineSubtypes[typeName];\n  // Fast-path: nothing to fill for this type. Skip the seen-bookkeeping entirely.\n  if (!typeDefaults && !sub && !inlineForType) return value;\n\n  if (!seen) seen = new WeakSet();\n  if (seen.has(value)) return value;\n  seen.add(value);\n\n  if (typeDefaults) {\n    for (const field of Object.keys(typeDefaults)) {\n      if (value[field] === undefined) {\n        value[field] = __clonePlain(typeDefaults[field], new WeakMap());\n      }\n    }\n  }\n\n  if (sub) {\n    for (const field of Object.keys(sub)) {\n      const edge = sub[field];\n      const fieldVal = value[field];\n      if (fieldVal === undefined || fieldVal === null) continue;\n      const containers = edge.container ? [edge.container] : [];\n      __recurseContainer(fieldVal, containers, 0, edge.target, seen);\n    }\n  }\n  if (inlineForType) {\n    for (const field of Object.keys(inlineForType)) {\n      const entry = inlineForType[field];\n      const fieldVal = value[field];\n      if (fieldVal === undefined || fieldVal === null) continue;\n      if (entry.discriminator) {\n        // Discriminated union — read the discriminator at runtime to pick a\n        // variant. If the runtime value doesn't match any variant, skip\n        // recursion; typia's validator will flag the bad discriminator.\n        if (typeof fieldVal !== 'object' || Array.isArray(fieldVal)) continue;\n        const discValue = fieldVal[entry.discriminator.field];\n        const variantSubTypeName = entry.discriminator.variants[String(discValue)];\n        if (variantSubTypeName) {\n          __fillDefaults(fieldVal, variantSubTypeName, seen);\n        }\n      } else {\n        const containers = entry.containers || [];\n        __recurseContainer(fieldVal, containers, 0, entry.subTypeName, seen);\n      }\n    }\n  }\n\n  return value;\n}\n\n/**\n * Walk `val` through an outer-to-inner container chain, calling\n * `__fillDefaults` (which mutates in place) at the innermost level. Containers\n * keep their identity — we iterate, we don't rebuild. Empty chain means the\n * field is a direct reference — recurse straight in.\n */\nfunction __recurseContainer(val, containers, depth, subTypeName, seen) {\n  if (depth >= containers.length) {\n    if (val === null || typeof val !== 'object' || Array.isArray(val)) return;\n    if (val instanceof Set || val instanceof Map) return;\n    __fillDefaults(val, subTypeName, seen);\n    return;\n  }\n  const container = containers[depth];\n  if (container === 'array') {\n    if (!Array.isArray(val)) return;\n    for (const e of val) __recurseContainer(e, containers, depth + 1, subTypeName, seen);\n    return;\n  }\n  if (container === 'set' || container === 'readonlyset') {\n    if (!(val instanceof Set)) return;\n    for (const e of val) __recurseContainer(e, containers, depth + 1, subTypeName, seen);\n    return;\n  }\n  if (container === 'map' || container === 'readonlymap') {\n    if (!(val instanceof Map)) return;\n    for (const [, v] of val) __recurseContainer(v, containers, depth + 1, subTypeName, seen);\n    return;\n  }\n}\n\n/**\n * Loud-warning enrichment (Nebula \"no foot-guns\" principle). A relationship\n * field is a by-id reference: in the write shape it is typed string / string[],\n * so embedding an object/array instead of an id yields a bare typia error\n * (\"expected (string | undefined)\") that gives no hint about WHY. When the\n * offending field is a known relationship and the value is an object, rewrite\n * the error description to say so explicitly — reference related resources by\n * id, do not embed them. No-op when no relationshipHints were supplied.\n */\nfunction __enrichRelationshipErrors(errors, typeName) {\n  const rels = __typeMetadata.relationshipHints[typeName];\n  if (!rels || !errors) return errors;\n  for (const err of errors) {\n    const p = err.path || \"\";\n    if (p.slice(0, 7) !== \"$input.\") continue;\n    // First path segment after \"$input.\" — handles \"$input.self\" and the\n    // array-element form \"$input.children[0]\" alike.\n    const field = p.slice(7).split(\".\")[0].split(\"[\")[0];\n    const rel = rels[field];\n    if (!rel) continue;\n    const v = err.value;\n    if (v === null || typeof v !== \"object\") continue;\n    const want = rel.container === \"array\"\n      ? \"an array of '\" + rel.target + \"' resource id strings\"\n      : \"a '\" + rel.target + \"' resource id string\";\n    err.description =\n      \"Field '\" + field + \"' is a relationship to '\" + rel.target +\n      \"' resources, so it expects \" + want + \" (reference by id) — not an \" +\n      \"embedded object. Create the related resource separately and reference \" +\n      \"it by its id.\";\n  }\n  return errors;\n}\n\nexport class ParserValidator extends DurableObject {\n  parse(value, typeName) {\n    const v = validators[typeName];\n    if (!v) {\n      return {\n        valid: false,\n        errors: [{ path: \"$\", expected: typeName, value, description: \"unknown type\" }],\n      };\n    }\n    const filled = __fillDefaults(value, typeName);\n    const result = v(filled);\n    if (result.success) return { valid: true, data: result.data };\n    return { valid: false, errors: __enrichRelationshipErrors(result.errors, typeName) };\n  }\n\n  parseBatch(items) {\n    const out = new Map();\n    for (const [key, item] of items) {\n      out.set(key, this.parse(item.value, item.typeName));\n    }\n    return out;\n  }\n}\n",
  relationships: {"_InviteStatus":{"node":{"target":"_OrgNode","cardinality":"one","optional":false}},"Message":{"chat":{"target":"Chat","cardinality":"one","optional":false},"replyTo":{"target":"Message","cardinality":"one","optional":true}}},
};

/**
 * The codegen tool-args validator module (typia, from `TOOL_ARGS_TYPES`) — what
 * `Galaxy.#ensureToolArgsFacet` hands the Worker Loader under `TOOL_ARGS_BUNDLE_ID`.
 */
export const TOOL_ARGS_VALIDATOR_MODULE: string = "\nimport { DurableObject } from \"cloudflare:workers\";\n\nconst __typia_transform__validateReport = (() => {\n  const _validateReport = (array) => {\n    const reportable = (path) => {\n      if (array.length === 0) return true;\n      const last = array[array.length - 1].path;\n      return path.length > last.length || last.substring(0, path.length) !== path;\n    };\n    return (exceptable, error) => {\n      if (exceptable && reportable(error.path)) {\n        if (error.value === undefined)\n          error.description ??= [\n            \"The value at this path is `undefined`.\",\n            \"\",\n            `Please fill the \\`${error.expected}\\` typed value next time.`,\n          ].join(\"\\n\");\n        array.push(error);\n      }\n      return false;\n    };\n  };\n  return { _validateReport };\n})();\nconst __typia_transform__createStandardSchema = (() => {\n  var PathParserState;\n  (function (PathParserState) {\n    PathParserState[PathParserState[\"Start\"] = 0] = \"Start\";\n    PathParserState[PathParserState[\"Property\"] = 1] = \"Property\";\n    PathParserState[PathParserState[\"StringKey\"] = 2] = \"StringKey\";\n    PathParserState[PathParserState[\"NumberKey\"] = 3] = \"NumberKey\";\n  })(PathParserState || (PathParserState = {}));\n\n  const typiaPathToStandardSchemaPath = (path) => {\n    if (!path.startsWith(\"$input\")) {\n      throw new Error(`Invalid path: ${JSON.stringify(path)}`);\n    }\n    const segments = [];\n    let currentSegment = \"\";\n    let state = PathParserState.Start;\n    let index = \"$input\".length - 1;\n    while (index < path.length - 1) {\n      index++;\n      const char = path[index];\n      if (state === PathParserState.Property) {\n        if (char === \".\" || char === \"[\") {\n          segments.push({ key: currentSegment });\n          state = PathParserState.Start;\n        } else if (index === path.length - 1) {\n          currentSegment += char;\n          segments.push({ key: currentSegment });\n          index++;\n          state = PathParserState.Start;\n        } else {\n          currentSegment += char;\n        }\n      } else if (state === PathParserState.StringKey) {\n        if (char === '\"') {\n          segments.push({ key: JSON.parse(currentSegment + char) });\n          index += 2;\n          state = PathParserState.Start;\n        } else if (char === \"\\\\\") {\n          currentSegment += path[index];\n          index++;\n          currentSegment += path[index];\n        } else {\n          currentSegment += char;\n        }\n      } else if (state === PathParserState.NumberKey) {\n        if (char === \"]\") {\n          segments.push({ key: Number.parseInt(currentSegment) });\n          index++;\n          state = PathParserState.Start;\n        } else {\n          currentSegment += char;\n        }\n      }\n      if (state === PathParserState.Start && index < path.length - 1) {\n        const newChar = path[index];\n        currentSegment = \"\";\n        if (newChar === \"[\") {\n          if (path[index + 1] === '\"') {\n            state = PathParserState.StringKey;\n            index++;\n            currentSegment = '\"';\n          } else {\n            state = PathParserState.NumberKey;\n          }\n        } else if (newChar === \".\") {\n          state = PathParserState.Property;\n        } else {\n          throw new Error(\"Unreachable: pointer points invalid character\");\n        }\n      }\n    }\n    if (state !== PathParserState.Start) {\n      throw new Error(`Failed to parse path: ${JSON.stringify(path)}`);\n    }\n    return segments;\n  };\n\n  const _createStandardSchema = (fn) => Object.assign(fn, {\n    \"~standard\": {\n      version: 1,\n      vendor: \"typia\",\n      validate: (input) => {\n        const result = fn(input);\n        if (result.success) {\n          return { value: result.data };\n        } else {\n          return {\n            issues: result.errors.map((error) => ({\n              message: `expected ${error.expected}, got ${error.value}`,\n              path: typiaPathToStandardSchemaPath(error.path),\n            })),\n          };\n        }\n      },\n    },\n  });\n\n  return { _createStandardSchema };\n})();\n\nexport const validators = {\n    WriteFileArgs: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return \"string\" === typeof input.path && \"string\" === typeof input.content;\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [\"string\" === typeof input.path || _report(_exceptionable, {\n                path: _path + \".path\",\n                expected: \"string\",\n                value: input.path\n            }), \"string\" === typeof input.content || _report(_exceptionable, {\n                path: _path + \".content\",\n                expected: \"string\",\n                value: input.content\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input || _report(true, {\n                path: _path + \"\",\n                expected: \"WriteFileArgs\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"WriteFileArgs\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })(),\n    ReadFileArgs: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return \"string\" === typeof input.path;\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [\"string\" === typeof input.path || _report(_exceptionable, {\n                path: _path + \".path\",\n                expected: \"string\",\n                value: input.path\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input || _report(true, {\n                path: _path + \"\",\n                expected: \"ReadFileArgs\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"ReadFileArgs\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })(),\n    EditFileArgs: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return \"string\" === typeof input.path && \"string\" === typeof input.anchor && \"string\" === typeof input.replacement;\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [\"string\" === typeof input.path || _report(_exceptionable, {\n                path: _path + \".path\",\n                expected: \"string\",\n                value: input.path\n            }), \"string\" === typeof input.anchor || _report(_exceptionable, {\n                path: _path + \".anchor\",\n                expected: \"string\",\n                value: input.anchor\n            }), \"string\" === typeof input.replacement || _report(_exceptionable, {\n                path: _path + \".replacement\",\n                expected: \"string\",\n                value: input.replacement\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input || _report(true, {\n                path: _path + \"\",\n                expected: \"EditFileArgs\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"EditFileArgs\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })(),\n    BuildArgs: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return undefined === input.preview || \"boolean\" === typeof input.preview;\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return [undefined === input.preview || \"boolean\" === typeof input.preview || _report(_exceptionable, {\n                path: _path + \".preview\",\n                expected: \"(boolean | undefined)\",\n                value: input.preview\n            })].every(flag => flag);\n    }; const __is = input => \"object\" === typeof input && null !== input && false === Array.isArray(input) && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input && false === Array.isArray(input) || _report(true, {\n                path: _path + \"\",\n                expected: \"BuildArgs\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"BuildArgs\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })(),\n    MarkCompleteArgs: (() => { let $visited = new WeakMap(); const _io0 = input => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_io0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_io0\");\n        }\n        return true;\n    }; const _vo0 = (input, _path, _exceptionable = true) => {\n        if (input && typeof input === \"object\") {\n            let $s = $visited.get(input);\n            if ($s && $s.has(\"_vo0\"))\n                return true;\n            if (!$s) {\n                $s = new Set();\n                $visited.set(input, $s);\n            }\n            $s.add(\"_vo0\");\n        }\n        return true;\n    }; const __is = input => \"object\" === typeof input && null !== input && false === Array.isArray(input) && _io0(input); let errors; let _report; return __typia_transform__createStandardSchema._createStandardSchema(input => {\n        $visited = new WeakMap();\n        if (false === __is(input)) {\n            errors = [];\n            _report = __typia_transform__validateReport._validateReport(errors);\n            ((input, _path, _exceptionable = true) => (\"object\" === typeof input && null !== input && false === Array.isArray(input) || _report(true, {\n                path: _path + \"\",\n                expected: \"MarkCompleteArgs\",\n                value: input\n            })) && _vo0(input, _path + \"\", true) || _report(true, {\n                path: _path + \"\",\n                expected: \"MarkCompleteArgs\",\n                value: input\n            }))(input, \"$input\", true);\n            const success = 0 === errors.length;\n            return success ? {\n                success,\n                data: input\n            } : {\n                success,\n                errors,\n                data: input\n            };\n        }\n        return {\n            success: true,\n            data: input\n        };\n    }); })()\n};\n\n\nconst __typeMetadata = {\n  defaults: {},\n  typeGraph: {},\n  inlineSubtypes: {},\n  relationshipHints: {},\n};\n\n/**\n * Deep-clone helper used by the filler. We only touch plain objects and\n * arrays; other types (Date, Map, Set, etc.) are passed through by reference.\n * A WeakMap tracks visited nodes so cyclic structures don't blow the stack.\n */\nfunction __clonePlain(v, seen) {\n  if (v === null || typeof v !== 'object') return v;\n  if (seen.has(v)) return seen.get(v);\n  if (Array.isArray(v)) {\n    const out = [];\n    seen.set(v, out);\n    for (let i = 0; i < v.length; i++) out[i] = __clonePlain(v[i], seen);\n    return out;\n  }\n  if (Object.getPrototypeOf(v) === Object.prototype || Object.getPrototypeOf(v) === null) {\n    const out = {};\n    seen.set(v, out);\n    for (const k of Object.keys(v)) out[k] = __clonePlain(v[k], seen);\n    return out;\n  }\n  // Date, Map, Set, RegExp, TypedArrays, class instances — passed through\n  // by reference. Safe because the filler never mutates them.\n  return v;\n}\n\n/**\n * Apply @default values to `value` against type `typeName`, **mutating in\n * place**. Returns the same `value` reference for caller convenience.\n *\n * In-place is safe because the only entry points are `parse()` / `parseBatch()`\n * on the facet, which receive structured-clones across the Workers RPC boundary\n * — the caller's original object is never touched. Cycles back to the root\n * survive (no clone breaks them), and identity is preserved for callers that\n * pass through a value untouched.\n *\n * Default-literal values themselves are still cloned when applied (the literal\n * is shared across all parses for that type, so we can't hand the same object\n * reference to every caller).\n *\n * Cycle termination: `seen` is a WeakSet tracking objects currently being\n * filled. A re-entry returns immediately, so a self-referential field doesn't\n * recurse forever — the inner traversal is skipped because the outer call is\n * already handling that object.\n */\nfunction __fillDefaults(value, typeName, seen) {\n  if (value === null || typeof value !== 'object' || Array.isArray(value)) return value;\n\n  const typeDefaults = __typeMetadata.defaults[typeName];\n  const sub = __typeMetadata.typeGraph[typeName];\n  const inlineForType = __typeMetadata.inlineSubtypes[typeName];\n  // Fast-path: nothing to fill for this type. Skip the seen-bookkeeping entirely.\n  if (!typeDefaults && !sub && !inlineForType) return value;\n\n  if (!seen) seen = new WeakSet();\n  if (seen.has(value)) return value;\n  seen.add(value);\n\n  if (typeDefaults) {\n    for (const field of Object.keys(typeDefaults)) {\n      if (value[field] === undefined) {\n        value[field] = __clonePlain(typeDefaults[field], new WeakMap());\n      }\n    }\n  }\n\n  if (sub) {\n    for (const field of Object.keys(sub)) {\n      const edge = sub[field];\n      const fieldVal = value[field];\n      if (fieldVal === undefined || fieldVal === null) continue;\n      const containers = edge.container ? [edge.container] : [];\n      __recurseContainer(fieldVal, containers, 0, edge.target, seen);\n    }\n  }\n  if (inlineForType) {\n    for (const field of Object.keys(inlineForType)) {\n      const entry = inlineForType[field];\n      const fieldVal = value[field];\n      if (fieldVal === undefined || fieldVal === null) continue;\n      if (entry.discriminator) {\n        // Discriminated union — read the discriminator at runtime to pick a\n        // variant. If the runtime value doesn't match any variant, skip\n        // recursion; typia's validator will flag the bad discriminator.\n        if (typeof fieldVal !== 'object' || Array.isArray(fieldVal)) continue;\n        const discValue = fieldVal[entry.discriminator.field];\n        const variantSubTypeName = entry.discriminator.variants[String(discValue)];\n        if (variantSubTypeName) {\n          __fillDefaults(fieldVal, variantSubTypeName, seen);\n        }\n      } else {\n        const containers = entry.containers || [];\n        __recurseContainer(fieldVal, containers, 0, entry.subTypeName, seen);\n      }\n    }\n  }\n\n  return value;\n}\n\n/**\n * Walk `val` through an outer-to-inner container chain, calling\n * `__fillDefaults` (which mutates in place) at the innermost level. Containers\n * keep their identity — we iterate, we don't rebuild. Empty chain means the\n * field is a direct reference — recurse straight in.\n */\nfunction __recurseContainer(val, containers, depth, subTypeName, seen) {\n  if (depth >= containers.length) {\n    if (val === null || typeof val !== 'object' || Array.isArray(val)) return;\n    if (val instanceof Set || val instanceof Map) return;\n    __fillDefaults(val, subTypeName, seen);\n    return;\n  }\n  const container = containers[depth];\n  if (container === 'array') {\n    if (!Array.isArray(val)) return;\n    for (const e of val) __recurseContainer(e, containers, depth + 1, subTypeName, seen);\n    return;\n  }\n  if (container === 'set' || container === 'readonlyset') {\n    if (!(val instanceof Set)) return;\n    for (const e of val) __recurseContainer(e, containers, depth + 1, subTypeName, seen);\n    return;\n  }\n  if (container === 'map' || container === 'readonlymap') {\n    if (!(val instanceof Map)) return;\n    for (const [, v] of val) __recurseContainer(v, containers, depth + 1, subTypeName, seen);\n    return;\n  }\n}\n\n/**\n * Loud-warning enrichment (Nebula \"no foot-guns\" principle). A relationship\n * field is a by-id reference: in the write shape it is typed string / string[],\n * so embedding an object/array instead of an id yields a bare typia error\n * (\"expected (string | undefined)\") that gives no hint about WHY. When the\n * offending field is a known relationship and the value is an object, rewrite\n * the error description to say so explicitly — reference related resources by\n * id, do not embed them. No-op when no relationshipHints were supplied.\n */\nfunction __enrichRelationshipErrors(errors, typeName) {\n  const rels = __typeMetadata.relationshipHints[typeName];\n  if (!rels || !errors) return errors;\n  for (const err of errors) {\n    const p = err.path || \"\";\n    if (p.slice(0, 7) !== \"$input.\") continue;\n    // First path segment after \"$input.\" — handles \"$input.self\" and the\n    // array-element form \"$input.children[0]\" alike.\n    const field = p.slice(7).split(\".\")[0].split(\"[\")[0];\n    const rel = rels[field];\n    if (!rel) continue;\n    const v = err.value;\n    if (v === null || typeof v !== \"object\") continue;\n    const want = rel.container === \"array\"\n      ? \"an array of '\" + rel.target + \"' resource id strings\"\n      : \"a '\" + rel.target + \"' resource id string\";\n    err.description =\n      \"Field '\" + field + \"' is a relationship to '\" + rel.target +\n      \"' resources, so it expects \" + want + \" (reference by id) — not an \" +\n      \"embedded object. Create the related resource separately and reference \" +\n      \"it by its id.\";\n  }\n  return errors;\n}\n\nexport class ParserValidator extends DurableObject {\n  parse(value, typeName) {\n    const v = validators[typeName];\n    if (!v) {\n      return {\n        valid: false,\n        errors: [{ path: \"$\", expected: typeName, value, description: \"unknown type\" }],\n      };\n    }\n    const filled = __fillDefaults(value, typeName);\n    const result = v(filled);\n    if (result.success) return { valid: true, data: result.data };\n    return { valid: false, errors: __enrichRelationshipErrors(result.errors, typeName) };\n  }\n\n  parseBatch(items) {\n    const out = new Map();\n    for (const [key, item] of items) {\n      out.set(key, this.parse(item.value, item.typeName));\n    }\n    return out;\n  }\n}\n";
