# tsc-transpile bundle intermediate (regenerated by scripts/bundle-tsc.mjs).
# The bundle output itself — vendor/tsc-transpile.bundle.mjs — IS committed
# (fresh-clone CI + deploy need it without a build step); only the barrel
# entrypoint is throwaway.
vendor/_tsc-barrel.mjs
