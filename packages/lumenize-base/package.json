{
  "name": "@lumenize/lumenize-base",
  "version": "0.1.0",
  "description": "Base class for Cloudflare Durable Objects with NADIS auto-injection",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src/**/*"
  ],
  "scripts": {
    "test": "vitest --run",
    "test:coverage": "vitest --coverage --run",
    "coverage": "COVERAGE=true vitest --run --coverage",
    "types": "wrangler types"
  },
  "keywords": [
    "cloudflare",
    "durable-objects",
    "workers",
    "dependency-injection",
    "nadis",
    "lumenize"
  ],
  "license": "MIT",
  "dependencies": {
    "@lumenize/utils": "*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.3",
    "@lumenize/alarms": "*",
    "@lumenize/core": "*",
    "@vitest/coverage-istanbul": "3.2.4",
    "vitest": "3.2.4",
    "wrangler": "^4.38.0"
  },
  "peerDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.3",
    "vitest": "3.2.4"
  }
}

