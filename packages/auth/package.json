{
  "name": "@lumenize/auth",
  "version": "0.21.0",
  "private": true,
  "description": "De✨light✨ful authentication for Cloudflare Durable Objects",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "files": [
    "src/**/*"
  ],
  "scripts": {
    "test": "vitest --run",
    "test:coverage": "vitest --coverage --run",
    "coverage": "COVERAGE=true vitest --run --coverage",
    "types": "wrangler types"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "durable-objects",
    "authentication",
    "jwt",
    "magic-link"
  ],
  "author": "Lumenize",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.3",
    "@lumenize/testing": "^0.21.0",
    "@vitest/coverage-istanbul": "3.2.4",
    "hono": "^4.11.9",
    "typescript": "^5.9.2",
    "vitest": "3.2.4",
    "wrangler": "^4.38.0"
  },
  "dependencies": {
    "@lumenize/debug": "^0.21.0",
    "@lumenize/routing": "^0.21.0"
  },
  "peerDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.3",
    "vitest": "3.2.4"
  }
}
