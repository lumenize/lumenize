{
  "name": "@lumenize/proxy-fetch",
  "version": "0.17.0",
  "description": "De✨light✨ful proxy fetch for cost-effective external API calls from Durable Objects",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "files": [
    "src/**/*"
  ],
  "scripts": {
    "test": "vitest --run",
    "test:worker": "vitest --run --project worker",
    "test:live": "vitest --run --project live-integration",
    "coverage": "vitest --run --coverage",
    "types": "cd test/worker && wrangler types"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "durable-objects",
    "fetch",
    "queue",
    "cost-optimization",
    "dx"
  ],
  "author": "Lumenize",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.3",
    "@vitest/coverage-istanbul": "3.2.4",
    "typescript": "^5.9.2",
    "vitest": "3.2.4",
    "wrangler": "^4.38.0"
  },
  "dependencies": {
    "@lumenize/core": "*",
    "@lumenize/lumenize-base": "*",
    "@lumenize/structured-clone": "^0.17.0",
    "@lumenize/test-endpoints": "^0.17.0",
    "@lumenize/utils": "^0.17.0"
  },
  "peerDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.3",
    "vitest": "3.2.4"
  }
}
