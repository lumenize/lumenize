/* GENERATED by gen-arms.mjs — DO NOT EDIT. arm=mesh (baseline — @lumenize/mesh only) */
import { env } from 'cloudflare:workers';
import { EchoDO } from './echo-do.js';
// baseline arm — no extra import

export { EchoDO };
(globalThis as any).__arm = { id: 'mesh' };

export default {
  async fetch(request: Request): Promise<Response> {
    const url = new URL(request.url);
    if (url.pathname === '/echo') {
      // A previously-unused name => a brand-new DO. A repeated name => already exists.
      const name = url.searchParams.get('name') ?? 'default';
      const value = await (env as any).ECHO.getByName(name).echo('x');
      return Response.json({ arm: 'mesh', name, value });
    }
    return new Response('Not Found', { status: 404 });
  },
};
