{
  "name": "dw-bundler-spike",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "node scripts/bundle-tsc.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "setup": "npm install && npm run bundle && npm run types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.86.0"
  },
  "dependencies": {
    "@cloudflare/worker-bundler": "^0.0.4"
  }
}
